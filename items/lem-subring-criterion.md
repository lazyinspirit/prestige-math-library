---
id: lem-subring-criterion
kind: lemma
title: "Subring criterion: $S \\subseteq R$ is a subring if and only if $1_R \\in S$ and $a - b \\in S$ and $ab \\in S$ for all $a, b \\in S$; and an intersection of subrings is a subring"
status: draft
origin: session
deps: [def-subring, def-ring, def-subgroup, lem-subgroup-criterion, lem-intersection-of-subgroups, def-group]
justified_by: []
aliases: []
landmark: false
short: "subring test"
proof_strategy: direct
verification:
  precheck: pass
sources:
  scraped: []
  references:
    - title: "Subring (Wikipedia)"
      url: "https://en.wikipedia.org/wiki/Subring"
    - title: "Subgroup test (Wikipedia)"
      url: "https://en.wikipedia.org/wiki/Subgroup_test"
pipeline_run: null
---

## Statement

Let $R$ be a ring ([[def-ring]]) with zero $0_R$ and identity $1_R$, and let
$S \subseteq R$. Then:

1. $S$ is a subring of $R$ ([[def-subring]]) **if and only if**
   $1_R \in S$, and $a - b \in S$ and $ab \in S$ for all $a, b \in S$;
2. if $\mathcal{S}$ is a nonempty set of subrings of $R$, then
   $K = \bigcap_{S \in \mathcal{S}} S$ is a subring of $R$. In particular the
   intersection of two subrings is a subring.

## Facts & Assumptions

**Given:** A ring $R$ with zero $0_R$ and identity $1_R$, and a subset $S \subseteq R$; $a - b$ abbreviates $a + (-b)$ ([[def-ring]]).

[L1] A subring is a subset containing $1_R$ and closed under addition, additive inverses and multiplication; it is then a ring with the same zero, identity and additive inverses as $R$ ([[def-subring]]).

[L2] $(R,+,0_R)$ is an abelian group ([[def-ring]], [[def-group]]).

[L3] One-step subgroup test, written additively: a nonempty $T \subseteq R$ with $a - b \in T$ for all $a, b \in T$ is a subgroup of $(R,+,0_R)$; and a subgroup contains $0_R$ and is closed under addition and under additive inverses ([[lem-subgroup-criterion]], [[def-subgroup]]).

[L4] The intersection of a nonempty set of subgroups of a group is a subgroup ([[lem-intersection-of-subgroups]]).

## Proof

**Proof technique:** direct.

1.1 Suppose $S$ is a subring. Then $1_R \in S$ by (T1); for $a, b \in S$ we have $-b \in S$ by (T3) and hence $a - b = a + (-b) \in S$ by (T2); and $ab \in S$ by (T4). [L1]

1.2 Conversely, suppose $1_R \in S$ and that $a - b \in S$ and $ab \in S$ for all $a, b \in S$. Then $S$ is nonempty, so by the one-step test it is a subgroup of $(R,+,0_R)$; hence $0_R \in S$, $S$ is closed under addition and $-x \in S$ for every $x \in S$. Together with $1_R \in S$ and closure under multiplication, that is exactly (T1) to (T4), so $S$ is a subring. [L1, L2, L3]

2.1 Steps 1.1 and 1.2 prove claim 1. [step 1.1, step 1.2, L1]

2.2 Claim 2. Each $S \in \mathcal{S}$ is a subgroup of $(R,+,0_R)$ by [L1] and [L3], so $K$ is a subgroup of $(R,+,0_R)$ by [L4]; in particular $K$ is closed under addition and under additive inverses. Also $1_R \in S$ for every $S \in \mathcal{S}$, so $1_R \in K$; and if $a, b \in K$ then $ab \in S$ for every $S \in \mathcal{S}$, so $ab \in K$. Hence $K$ satisfies (T1) to (T4) and is a subring. [step 1.1, L1, L3, L4]

3.1 Claims 1 and 2 are established in steps 2.1 and 2.2. [step 2.1, step 2.2] ∎

## Remarks

- **Why the identity clause survives the economy.** The one-step test compresses
  the three *additive* conditions into one, and nothing compresses (T1): a
  subset can satisfy $a - b \in S$ and $ab \in S$ and still miss $1_R$
  altogether. The companion page's even integers are exactly that, which is why
  $1_R \in S$ is stated separately in claim 1 rather than folded in.

- **Nonemptiness comes for free here.** [[lem-subgroup-criterion]] requires the
  subset to be nonempty; in claim 1 that is supplied by $1_R \in S$, so the
  criterion has no separate nonemptiness hypothesis.

- **Claim 2 is what would make "the subring generated by a set" meaningful.**
  Intersections of subrings being subrings, the intersection of all subrings
  containing a given subset is the smallest one containing it. No such
  construction is used on this page; the claim is recorded because it costs one
  step and because [[lem-intersection-of-subgroups]] already supplies the
  additive half.
