---
id: lem-algebra-generated-by-finitely-many-integral-elements-is-module-finite
kind: lemma
title: "A subalgebra generated by finitely many integral elements is module-finite"
status: draft
origin: session
provenance:
  statement: literature-derived
  proof: ai-altered
deps: [def-integral-element-and-algebraic-integer, thm-integrality-and-finite-module-equivalences, lem-transitivity-of-module-finiteness, def-finite-type-and-module-finite-algebras, def-subring]
justified_by: []
aliases: []
landmark: false
proof_strategy: induction
verification:
  precheck: pass
  judge:
    model: "deepseek-v4-pro + gpt-5.6-terra"
    verdict: pass
    date: 2026-08-25
sources:
  scraped: []
  references:
    - title: "M. Hochster, Introduction to Commutative Algebra, Math 614, Ch. 5"
      url: "http://web.archive.org/web/20230308152844if_/https://dept.math.lsa.umich.edu/~hochster/615W22/614Lx.pdf"
    - title: "A. Altman and S. Kleiman, A Term of Commutative Algebra, 13th ed., §10 and (16.21)"
      url: "https://web.mit.edu/18.705/www/13Ed.pdf"
pipeline_run: null
---

## Statement

Let $A\subseteq B$ be commutative rings, $A$ a subring of $B$
([[def-subring]]), let $n\in\mathbb N$ and let $b_1,\ldots,b_n\in B$ be integral
over $A$ ([[def-integral-element-and-algebraic-integer]]). Then the
$A$-subalgebra $A[b_1,\ldots,b_n]$ of $B$ is module-finite over $A$
([[def-finite-type-and-module-finite-algebras]]).

The zero ring is not excluded: if $A=0$ then $B=0$ and the conclusion holds with
the empty generating list.

## Facts & Assumptions

**Given:** Commutative rings $A\subseteq B$ with $A$ a subring of $B$, a natural number $n$, and elements $b_1,\ldots,b_n\in B$ integral over $A$.

[L1] A subring of $B$ contains $1_B$ and has the same zero and identity as $B$ ([[def-subring]]).

[L2] $R[a_1,\ldots,a_n]$ is the image of the unital ring homomorphism $R[x_1,\ldots,x_n]\to A$ agreeing with the structure map on constants and sending $x_i$ to $a_i$; it is the smallest subring of $A$ containing the image of $R$ and $a_1,\ldots,a_n$. An algebra is **module-finite** over $R$ when it is finitely generated as an $R$-module ([[def-finite-type-and-module-finite-algebras]]).

[L3] For a homomorphism of commutative rings $A\to B$, an element $b\in B$ is **integral over $A$** when it is a root of a monic polynomial in $A[X]$ ([[def-integral-element-and-algebraic-integer]]).

[L4] Let $A\subseteq B$ be commutative rings with $A\ne0$ and let $b\in B$. Then $b$ is integral over $A$ if and only if $A[b]$ is finitely generated as an $A$-module ([[thm-integrality-and-finite-module-equivalences]]).

[L5] If $B$ is generated as an $A$-module by finitely many elements and a $B$-module $M$ is generated as a $B$-module by finitely many elements, then the products of the two lists generate $M$ as an $A$-module; module finiteness is transitive along a tower of commutative algebras ([[lem-transitivity-of-module-finiteness]]).

## Proof

**Proof technique:** induction.

1.1 Dispose of the zero base ring first. If $A=0$ then $1_A=0_A$, and a subring shares the identity and zero of the ambient ring, so $1_B=0_B$ and $B=0$; every subalgebra of $B$ is then the zero module over $A$, generated by the empty list. For the rest of the argument assume $A\ne0$, so that $1_B\ne0_B$ and every subring of $B$ containing $A$ is a nonzero commutative ring. [L1, L2, given]

1.2 The case $n=0$: the subalgebra $A[\,]$ generated by the empty list is the smallest subring of $B$ containing $A$, which is $A$ itself, and $A$ is generated as an $A$-module by $1_A$. [base, L2, given]

1.3 Let $k\in\mathbb N$ and suppose $A_k:=A[b_1,\ldots,b_k]$ is module-finite over $A$. [ih]

2.1 Write $A_{k+1}:=A[b_1,\ldots,b_{k+1}]$. Both $A_{k+1}$ and $A_k[b_{k+1}]$ are the smallest subring of $B$ containing $A$ and $b_1,\ldots,b_{k+1}$, so they are equal. The element $b_{k+1}$ is a root of some monic $f\in A[X]$; the coefficients of $f$ lie in $A\subseteq A_k$, so $f$ is a monic polynomial in $A_k[X]$ with the same coefficients, and evaluating it at $b_{k+1}$ gives the same element of $B$, namely $0$. Hence $b_{k+1}$ is integral over $A_k$. Since $A_k$ is a nonzero commutative subring of $B$ by step 1.1, the integrality criterion gives that $A_k[b_{k+1}]=A_{k+1}$ is a finitely generated $A_k$-module; with the assumption of step 1.3 that $A_k$ is a finitely generated $A$-module, transitivity makes $A_{k+1}$ a finitely generated $A$-module. [L2, L3, L4, L5, step 1.1, step 1.3]

3.1 The base case of step 1.2 and the passage of step 2.1 give, by induction on $n$, that $A[b_1,\ldots,b_n]$ is module-finite over $A$ for every $n\in\mathbb N$. [step 1.2, step 2.1, discharge-induction] ∎

## Remarks

- **The nonzero hypothesis of the cited integrality theorem is why the zero ring is disposed of first.** [[thm-integrality-and-finite-module-equivalences]] assumes $A\ne0$; step 1.1 removes that case by hand rather than leaving the citation standing over a ring the source excludes.

- **Integrality over the enlarged ring is inherited, not re-proved.** The same monic polynomial serves at every stage, which is what keeps the induction from needing a new integrality hypothesis at each step.

- **Finitely many elements is essential.** The subalgebra generated by an infinite set of integral elements is integral over $A$ but need not be module-finite; each finite subfamily is, and the union of an increasing chain of finite modules need not be finite.
