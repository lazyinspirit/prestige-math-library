---
id: thm-normal-basis-theorem
kind: theorem
title: "Every finite Galois extension has a normal basis"
status: published
origin: session
provenance:
  statement: literature-derived
  proof: ai-altered
deps: [def-normal-basis, thm-normal-basis-theorem-over-an-infinite-base-field, thm-normal-basis-theorem-for-a-cyclic-extension, thm-extensions-of-finite-fields-are-galois-with-cyclic-frobenius-group, def-finite-field-and-its-order, def-extension-degree-and-finite-extension, def-linear-basis, thm-unique-coordinates-with-respect-to-an-ordered-basis, thm-finite-galois-extension-characterizations]
justified_by: []
aliases: []
landmark: true
proof_strategy: cases
verification:
  audited: 2026-08-26
  precheck: pass
  judge:
    model: "deepseek-v4-pro + gpt-5.6-terra"
    verdict: pass
    date: 2026-08-25
sources:
  scraped: []
  references:
    - title: "J. S. Milne, Fields and Galois Theory, v5.10, Theorem 5.18"
      url: "https://www.jmilne.org/math/CourseNotes/FT.pdf"
    - title: "P. L. Clark, Field Theory (course notes/monograph), Theorem 8.25"
      url: "https://web.archive.org/web/2023/http://alpha.math.uga.edu/~pete/FieldTheory.pdf"
pipeline_run: null
---

## Statement

Every finite Galois extension $K/F$ has a normal basis ([[def-normal-basis]]):
there is $\alpha\in K$ whose family of conjugates
$(\sigma_1\alpha,\dots,\sigma_n\alpha)$, indexed by
$\operatorname{Gal}(K/F)=\{\sigma_1,\dots,\sigma_n\}$, is an ordered $F$-basis of
$K$.

## Facts & Assumptions

**Given:** A finite Galois extension $K/F$ of degree $n$, so that $K$ is an $F$-vector space of dimension $n$ ([[def-extension-degree-and-finite-extension]], [[thm-finite-galois-extension-characterizations]]).

[L1] Every finite Galois extension of an infinite field has a normal basis ([[thm-normal-basis-theorem-over-an-infinite-base-field]]).

[L2] Every finite Galois extension whose Galois group is cyclic has a normal basis ([[thm-normal-basis-theorem-for-a-cyclic-extension]]).

[L3] An extension $E/\mathbb F_q$ of finite fields of degree $m$ is Galois with $\operatorname{Gal}(E/\mathbb F_q)$ cyclic of order $m$, generated by $x\mapsto x^{q}$ ([[thm-extensions-of-finite-fields-are-galois-with-cyclic-frobenius-group]]).

[L4] A finite list $v$ of length $n$ is an ordered basis of $V$ if and only if every $x\in V$ has exactly one coordinate list $\lambda:n\to F$ with $x=\sum_{i<n}\lambda_iv_i$ ([[thm-unique-coordinates-with-respect-to-an-ordered-basis]], [[def-linear-basis]]).

[L5] A finite field is a field whose underlying set is finite, and its order is that cardinality ([[def-finite-field-and-its-order]]).

## Proof

**Proof technique:** cases.

1.1 In the case that $F$ is infinite, [L1] applies directly and $K/F$ has a normal basis. [assume-case inf, L1]

1.2 In the case that $F$ is finite, fix an ordered $F$-basis $v$ of $K$ of length $n$; by [L4] the map sending a coordinate list $\lambda:n\to F$ to $\sum_{i<n}\lambda_iv_i$ is a bijection onto $K$, so $|K|=|F|^{n}$ is finite and $K$ is a finite field. [assume-case fin, L4, L5, given]

2.1 In that same finite case, $K/\!F$ is therefore an extension of finite fields of degree $n$, so $\operatorname{Gal}(K/F)$ is cyclic by [L3], and [L2] gives a normal basis. [step 1.2, L2, L3]

3.1 The two cases are exhaustive, a field being finite or infinite and not both, so a normal basis exists in either case. [step 1.1, step 2.1, cases-exhaustive] ∎

## Remarks

- **Two genuinely different proofs, not one proof with a case split.** The infinite case runs on a determinant that is a nonzero polynomial ([[thm-normal-basis-theorem-over-an-infinite-base-field]]); the finite case runs on a cyclic vector for the Frobenius acting linearly ([[thm-normal-basis-theorem-for-a-cyclic-extension]]). Neither argument covers the other case: the first fails because a polynomial can vanish on all of a finite field, the second because a Galois group need not be cyclic.

- **The finite case is not a hypothesis on the group.** It is a hypothesis on the base field, which forces the group to be cyclic through [L3]. That is the whole reason the split is by the base field rather than by the group.
