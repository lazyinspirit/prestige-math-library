# Stage-1 scaffold-policy repair

The dispatch variables identify run `<run>` and batch `<i>`. Re-run the
whole-run manifest-only content-policy check and repair only the reported
subjects owned by `research/<run>-batch-<i>.pages.json`.

Treat dependency failures as mathematics, not syntax. Add a genuinely needed
supporting definition or result inside this batch, or replace/remove an invalid
dependency only when the planned statement and proof strategy do not need it.
Keep the scaffold dependency-closed and source-backed. Update this batch's
coverage and notes whenever the repair changes its harvested content; do not
touch the plan, item files, library pages, another batch, or workflow state.

Run the batch coverage checklist, whole-run `manifest-deps`, whole-run
manifest-only `content-policy`, and `validate-plan` before returning.
