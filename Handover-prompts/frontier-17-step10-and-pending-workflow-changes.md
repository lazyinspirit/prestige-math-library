# Handover — frontier-17 at step 10, plus owner-ordered workflow changes

Written 2026-08-23T14:55Z, immediately before a context compaction. Everything
below is verified from disk at that moment. Re-verify against `autopilot status`
and the event tail before acting: this snapshot ages.

## Your role

SESSION monitoring. **Report, do not act.** The owner said explicitly, twice
today, that the previous session over-intervened. Intervention is licensed only
by the stated bar — genuinely stuck, a loop that fails to close, or preventing a
latent catastrophic error — and even then the first move is to bring the owner a
diagnosis and options and WAIT. "Close it autonomously" raises the ceiling on
what may be done without asking; it does not lower the bar for whether to act.

Two standing rules, both stated in caps by the owner today:
- **DO NOT APPLY REPAIRS ON TOP OF BROKEN/STALE CODE.**
- **DO NOT APPLY REPAIRS ON TOP OF STALE PROMPTS — REWRITE THEM INSTEAD.**

Never edit a gate, guard, or exemption list to make it pass. Never modify code,
prompts or engine state while the driver is live.

## Run state at handover

```
run          frontier-17          driver pid 2897603 (detached, healthy)
stage        10-render-final-v2   blockers 0   paused false
last event   14:54:42  dispatch-ok  tool/render-final-v2 exit=0
remaining    5 stages: sigma-final, readiness, owner-report, close (+ the
             render-final gates still to run)
```

Everything through `10-stamps-v2` is complete and green.

| | |
|---|---|
| items | **443** (440 authored at step 5, 3 recovered at step 9) |
| judge coverage | 443/443 current paired verdicts, closure `closed: true`, 0 unadjudicated, 0 open fatal, 0 pending rejudge |
| adjudications | 164 rows — 66 `confirmed_fatal`, 94 `confirmed_nonfatal`, 4 `false_positive` |
| step-8 repairs | 49 items, every edit licensed by a `confirmed_fatal` row |
| twice-repaired | 4 · thrice-repaired 0 · new fatals found in repaired text 0 |
| defect ledger | 333 frontier-17 rows — 174 fatal, 151 nonfatal, 8 polish |
| stamps | **355 stamped, 88 skipped, all 88 `lane-rejected`** (an adjudicated rejection never stamps); zero `no-current-verdict` |
| spine | 58/58 attestations; no attested proof edited this run |
| blast radius | 371/371 consumers `still-licensed`, window `pre-author → post-step8` |
| finite smoke | 3/384 |

Nothing is `status: published`. **Publication and push remain owner acts.**

## Model lineup changed today (owner, 2026-08-23)

Every Sol and Terra lane moved to **Claude Opus 5** (`claude-opus-5[1m]`,
xhigh, 1M) because the Codex subscription hit its weekly cap. Judge lineup is
now `deepseek+opus`. DeepSeek remains the ONLY cross-family reader. Details and
the re-accepted quota risk are written into `CLAUDE.md` §Model lineup and
`tools/dispatch.mjs`.

The swap's live consequence: **the Opus judge lane exhausted the Claude
subscription mid-sweep** at 02:47Z — 369 null verdicts, signature
`is_error:true, duration_api_ms:0`, zero tokens. It recovered after the quota
reset via a 123-item single-lane replay at concurrency 4.

## Uncommitted work

**`prestige-intelligence`** — HEAD `2f098c5`, **1 ahead of origin, clean tree.**
Fixes a launch-day rendering defect: `web/package.json` declared `katex ^0.18.1`
while `rehype-katex` nested its own `0.16.47`, so HTML was generated by 0.16 and
CSS loaded from 0.18. The 0.18 stylesheet defines none of `.base`, `.strut`,
`.sizing`, `.vbox`, `.thinbox`, `.inner`, `.fix`. Fractions collapsed, `≠`
painted as a detached `/=`, subscripts rendered full size, tables lost cell
padding (one table manufactured `ℤ/5ℤ` from two columns). Also added
`table/th/td` components to `MathMarkdown.tsx`.
**Not pushed. Not deployed.** The VPS needs `docker compose up -d --build app`
(standalone Next output; a restart alone will not pick it up). This improves all
~4,900 published items, independent of frontier-17.

**`prestige-math-library`** — HEAD `93aa0cd2`, 698 files dirty: the live
frontier-17 tree plus 19 modified `tools/` files (the model swap) plus
`tools/touchlog.mjs` (duplicate-snapshot-label refusal, applied today).
`.autopilot/state.json.pre-visual-rerun` is a backup. Pre-fix visual evidence is
archived under `research/frontier-17-prefix-visual/`.

## OWNER-ORDERED WORK, pending, to start ONLY after the run closes

1. **DELETE Sigma/Tau completely from step 10.** Decided; the tradeoff was
   raised once and reaffirmed — do not re-litigate. Remove
   `10-sigma-initial-v2`, `10-tau-baseline-v2`, `10-tau-repair-v2`,
   `10-tau-guard-v2`, `10-post-tau-v2`, `10-tau-rejudge-v2`,
   `10-sigma-final-v2`, and argue explicitly what should happen to
   `10-render-*` rather than assuming. Then the dependent machinery, coherently
   and completely: `tools/visual-repair.mjs`, `tools/visual-adjudication.mjs`,
   `tools/tau-rejudge.mjs`, the `sigma`/`tau` roles in `tools/dispatch.mjs`,
   `briefs/sigma.md`, `briefs/tau.md`, `briefs/schemas/sigma.json`, and the
   readiness checks reading those receipts. Leaving tooling half-wired is the
   patch-over the owner forbade.
2. **Compare statistics with frontier 15 and 16** (figures already gathered
   below).
3. **Optimize the workflow conservatively**, explicitly accounting for the
   previous session's orchestration failures.

Bring the owner a proposal BEFORE touching anything.

## Statistics for the comparison

| | F15 | F16 | F17 |
|---|---|---|---|
| items | 398 | 324 | 443 |
| proof-bearing | 329 | 271 | 384 |
| ledger rows | 91 | 273 | 333 |
| fatal | 28 | 173 | 174 |
| judge nulls | 392 (lane lost) | 37 | 369 (quota) |
| twice-repaired | — | 17 | 4 |
| thrice-repaired | — | 3 | 0 |
| finite smoke | 4/329 | 1/271 | 3/384 |
| stamps | 0 (owner fixed by hand) | 206 | 355 |

Judge lane split this run: DeepSeek 86 of 94 current rejections; Opus 8. Opus
was more precise when it did reject (61% confirmed fatal vs 37%) but found
almost nothing independently — **34 DeepSeek-only confirmed-fatal defects
versus 2 Opus-only.** Under F16's Terra lane the split was 19 vs 28.

## Workflow findings worth fixing (evidence, not yet applied)

- **Adjudications are keyed on `(id, model, context_sha256)`** in
  `tools/level-coverage.mjs`, so any page-mate edit voids a settled
  disposition and the judges re-litigate closed ground. Verdicts already use
  the clause-(a)-OR-(b) disjunction in `tools/judge-currency.mjs` ("ONE
  PREDICATE, TWO CALLERS"). Unify there; do not special-case step 10. NOTE: it
  does NOT silently absorb new rejections — that alarm was raised and retracted.
- **Stage completion is encoded in three independent places** — the
  `state.json` record, the stage's `artifacts` path, and the `*.result.json`
  files that `covered()` actually reads. There is no supported rewind. A
  partial reset yields `stage-stalemate`. It must also re-verify content later
  stages already produced.
- **`9-scope` has no `onGateFailure`.** It is the one stage licensed to author
  content, so a *successful* scope recovery always blocks the run. `5-author`
  has the same gap for `depcheck`. The coherent fix is ONE shared router keyed
  on the failure, not a fifth per-stage hook — the `5-author` comment records
  that shape recurring three times already.
- **The claude capacity refusal is invisible to `OUTAGE_SIGNATURE`** (bare
  `NO_CONTENT`, zero duration, zero tokens). Two repair rounds re-swept a dead
  lane.
- **`QUALITY-CONTROLS.md` documented 6 of the 10 finite-smoke registry checks**
  until today; two of the three eventually selected were undiscoverable from
  the normative doc. Fixed in that doc by a step-5 Alpha.

## The previous session's own failures, for the optimisation

Roughly two hours went on cleaning up its own intervention. Honest list: it
edited `tools/visual-repair.mjs` to widen a guard exemption so a gate would pass
(unauthorised; the guard caught it by flagging its own source; reverted); began
editing `mathlib.mts` and `touchlog.mjs` mid-run minutes after saying it would
wait; raised a false "this is a real hole" alarm on an adjudication it had not
looked for properly; and an approved stage rewind stranded three separate sets
of content edits ahead of the stage meant to verify them, each caught by a gate
rather than shipping. Every one of those is a workflow signal, but the first
three are simply operator error.

## Exact next action

Watch `10-render-final-v2` gates, then `10-sigma-final-v2` — the final visual
adjudication, which has **no repair loop by design**: a reject preserves the
corpus and blocks. Then readiness, owner report, close. Report at each
transition. Do not intervene without bringing the owner options first.
