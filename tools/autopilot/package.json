{
  "name": "autopilot",
  "version": "0.2.0",
  "type": "module",
  "description": "A deterministic TypeScript control plane for multi-stage agent pipelines. Runs a build end to end with no human and no orchestrator in the loop.",
  "bin": { "autopilot": "./bin/autopilot.mts" },
  "scripts": {
    "test": "node --import tsx --test \"test/*.test.mts\"",
    "typecheck": "tsc -p tsconfig.json",
    "check": "npm run typecheck && npm test"
  },
  "devDependencies": { "tsx": "^4.19.2", "typescript": "^5.6.3", "@types/node": "^22.9.0" },
  "license": "UNLICENSED"
}
