Complete only published-audit step A4 for the owning batch
`research/audit/wave8-topology.pages.json`.

Read `CLAUDE.md`, `AUDIT-WORKFLOW.md`, `research/audit/RESUME.md`, the Audit-
Beta brief, the batch findings/ledger/contracts, and the authoritative
orchestrator decision `research/audit/wave8-A3.md` in full. Apply the two
approved citation/dependency repairs and all 32 component provenance/source
updates. Correct the CSUN attribution. Preserve every `origin` field: the
findings sentence instructing deletion of `origin` is explicitly rejected by
A3. Remove only legacy `authorship` if present (none currently exists). Do not
apply any unlisted change.

Update the namespaced ledger if needed to match the A3 correction and final
item tags. Regenerate affected contract entries from final text. The strict
contract command must remain 24/24, zero errors, zero warnings. Run reflow and
precheck on each changed proof-bearing item, remove stale verification state
only where the workflow requires for material repairs, run relevant dependency/
content/render/citation checks, and `git diff --check`. Write only scoped
`items/` and this batch's namespaced audit artifacts. Do not touch RESUME,
another batch, shared/global receipts, touch ledgers, pages, or judge ledgers.
Do not ask for shell or edit permission. Do not spawn subagents. End with exact
changed ids, pure-retag count, material-repair count, contract result, and
commands/results.
