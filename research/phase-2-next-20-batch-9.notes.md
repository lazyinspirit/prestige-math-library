# Batch 9 construction record — phase-2-next-20

This is Step 1 construction and an owner handoff, not mathematical approval. The two assigned pairs are preserved at orders 473/474 and 479/480. The manifest contains 101 items: DG-16 A 31 and B 7; DG-19 A 51 and B 12. Every item was appended in prerequisite order and given its own `step1-decisions.mjs record` outcome before the next item was appended. All outcomes are **escalated**, with no owner flag and no overwritten decision. No unchanged ready record existed. The source barrier affects both complete inventories; the additional mathematical holds below are not waived by future source retrieval.

Read CLAUDE.md, README.md, SCHEMA.md, WORKFLOW.md, briefs/beta-scaffold.md, the consumer ledger instructions, generated batch task, current batch manifests, planning notes and drift review. Verified the active state directory exists and checked Git history (`f9857f71e` at entry). Historical RESUME files were not used. Other batches were writing concurrently, so whole-run checks below are snapshots, not final engine verdicts. No shared plan, published content, engine state or verdict was edited. Only the required dependency-ledger refresh tool merged our consumer input.

## Design authority and inventory

`research/plan-spec.json` controls the run. DG-16 starts at design line 4108; line 4268 is its sources section, not a replacement design. DG-19 starts at line 4983; line 5138 likewise belongs to that section's sources. Read both complete DG sections through their proof-trap/choice/forward-reference paragraphs. The named DG/AT/HA requirements agree with the current canonical requires arrays and page orders; no page-level design/spec conflict was found. The design item inventory is retained in full, including all false statements and B items. No new pair or page split is proposed: both A pages remain below 60 items.

Necessary local additions, all initially unused identities, placed before their consumers:

- DG-16 before original item 4: `lem-real-singular-cochains-identify-with-functions-on-the-supplied-simplex-basis`. Makes the Hom_Z(integer chains,R) versus Hom_R(real chains,R) specialization explicit. These are not Hom_Z(real chains,R).
- DG-16 before original item 17: `lem-compatible-smooth-simplex-faces-have-a-neighbourhood-extension`. The current relative Whitney supplier requires smoothness on an open neighbourhood, not merely separately on faces. Local coordinate-hyperplane correction and the proposed global retraction bridge are recorded; the latter still needs a complete audit.
- DG-19 before original item 4: `lem-a-bundle-connection-is-local-and-restricts-to-open-sets`. Bump proof precedes local-frame use. At a point where a section vanishes, the X(chi)s term vanishes; this argument needs only chi(p)=1. Extension of local sections uses the stronger neighbourhood plateau actually constructed in the published chart-bump proof.

The `statement` fields are short scaffold summaries; full contracts and qualifications are in `proof_plan`. They are not authored item bodies. Several summaries deliberately state an unresolved obligation rather than pretend to be a publication-ready theorem; owner reconciliation and Step 3 must supply final statements. No generic numbered proof or proof-verification stamp was generated.

## Source evidence and limits — B9-SOURCE

Ran `node tools/source-fetch-check.mjs --coverage research/phase-2-next-20-batch-9.coverage.json --stamp --timeout-sec 30`. It failed for all four sources with EAI_AGAIN. The tool internally performed the initial attempt and five retries per source in the single invocation; coverage preserves all 24 actual URL/time/outcome records. There are **no fetch stamps**. Do not reset the allowance. The tool's immediate retries did not allow alternate-location searches to be interleaved; later browser searches are recorded separately, not relabelled as those retries.

Original URLs retained:

- Hatcher: https://pi.math.cornell.edu/~hatcher/AT/AT.pdf
- Park: https://people.math.harvard.edu/~pspark/derham.pdf
- Datar: https://math.iisc.ac.in/~vvdatar/Lecture_Notes/RG_Lectures_typesett_published.pdf
- Merry: https://www2.math.ethz.ch/will-merry/files/Merry%20-%20Differential%20Geometry%20(2021).pdf

Authoritative web search found Park's author-hosted text, Datar's official teaching page, and the ETH Merry location. Browser extraction recovered Park, Hatcher and Datar text; the parenthesis-encoded alternate ETH URL also returned Internal Error. Datar PDF screenshots failed with cache-miss errors. This is evidence of a retrieval/tool limitation, not permanent unavailability. Browser text is not a fabricated checker stamp. Every source has `source_resolution.status: owner-escalation`; none is dropped, and no certain alternative-proof waiver is claimed.

Actual reading scope is intentionally narrower than the design's desired source programme:

- Park §§1–2, pp.1–5 through Theorem 2.4, was read in the browser, including the face-extension correction, radial homotopy extension, boundary time-delay continuity argument and prism identities. Its Theorem 2.2 cites Whitney rather than proving it; the corresponding published relative Whitney proof was read separately. Park selects global simplex smoothings; our design instead uses finite support. It is not independent of Lee, as Park's introduction explicitly says. Hatcher is the independent algebraic treatment. Park alone does not supply the countable cohomology globalization theorem.
- Hatcher's absolute cohomology MV paragraph and displayed short exact sequence, pp.203–204, were inspected; the full intended pp.108–132 and 197–204 ranges were not read here. Proposition 2.21's statement was inspected and the current repository's full explicit least-depth proof was read. Do not equate this with reading all of Hatcher's proof.
- Datar's extracted locality proof (Lemma 5.1.1/Corollary 5.1.2), induced connection examples, along-curve operator/local ODE portion, Koszul uniqueness discussion, tensor-coordinate derivative proof and Hessian discussion were inspected at the exact locators in coverage. The complete global parallel-transport argument and full chapters were not inspected. Some source arguments are left to the reader; Proposition 5.1.8's extracted proof even ends at “We simply take”. Such omissions are not accepted as complete local proofs. The manifest spells out independent local algebra/ODE strategies, which still need Step 3 review.
- Merry was not read; its empty harvest is an intentional unresolved check, not invented source evidence. The design's Lee, Nair, Merry DG-16 and van der Vorst suggestions remain original design history and pending source work, not falsely harvested replacements. No earlier genuine failure record for those additional unattempted suggestions was invented.

Coverage gives explicit dispositions for every result actually encountered in the scoped source reading and preserves all design obligations as canonical entries. Additional source-count/full-reading shortfalls remain held. Deferrals point to current plan pages, not informal DG labels.

## Item-level mathematical holds

1. **AT-6 unpublished and signs.** At entry batch 8 had an empty manifest. By final interface inspection it contained scaffold contracts for `def-singular-cochain-complex-with-coefficients` and `thm-mayer-vietoris-sequence-in-singular-cohomology`; neither is a published proof. The cochain definition has the required integer-chain and R-module specialization. Its MV convention is U restriction minus V restriction; DG uses V minus U. The comparison of the two short exact sequences has identity maps on the first two terms and minus identity on the overlap. Hence delta_DG=-delta_AT with otherwise identical cohomology identifications. The open consumer rows name exactly these two supplier IDs and the page requirement. Do not declare the connectors identical without this sign adjustment. No cross-batch edit or new pair is requested.

2. **Smooth face extension / boundary target.** `lem-relative-smoothing-of-a-continuous-simplex-along-its-faces` needs compatible prescribed homotopies from the original faces, not arbitrary compatible new face maps. A boundary map might otherwise be non-nullhomotopic and not extend. The original smooth simplex must be fixed in the finite induction used for injectivity. The local face-extension correction is recorded in its new preceding lemma. The published `thm-relative-whitney-approximation-for-manifold-valued-maps` assumes smoothness on an open neighbourhood and uses an embedded boundaryless target with tubular retraction. Its full proof was inspected, including its explicit AC_omega assumption and straight-line homotopy kept in one ball. The required neighbourhood bridge and the interpretation for manifolds with boundary remain unresolved; a half-space target does not have an ordinary smooth open-neighbourhood retraction at its boundary. Do not silently limit the useful comparison claim to boundaryless targets without owner reconciliation. A strengthened local half-space smoothing construction, not an unproved general corners theorem, is the candidate repair.

3. **Countable globalization.** `thm-countable-mayer-vietoris-open-set-principle` and `thm-smooth-and-continuous-real-singular-cohomology-agree` are held for the full exhaustion/odd-even band argument. The scaffold records finite MV induction and the required two-stage Euclidean-open/manifold proof, but does not have a fully verified construction of the countable union cover and all its intersections. The required local countable-union lemma should be placed immediately before the open-set theorem on DG-16; no B additions or new pair are required. Exact prospective ID: `lem-countable-increasing-open-unions-preserve-mayer-vietoris-comparisons` (not minted here without the missing proof). Required chain: countable disjoint products plus two-open MV and five lemma -> band/exhaustion lemma -> Euclidean rational-box globalization -> manifold globalization -> smooth cohomology comparison. Source still to verify: a full lecture-note/textbook treatment of the countable MV argument; Park §§1–2 do not prove it. In particular do not claim cohomology commutes with arbitrary increasing unions or that arbitrary intersections of coordinate balls are convex.

4. **Axiom-sensitive false statements.** `fs-dualizing-every-short-exact-sequence-is-automatically-exact-in-zf` and `fs-a-homology-isomorphism-of-chain-complexes-automatically-gives-a-cohomology-isomorphism-after-dualizing-without-further-machinery` need exact domains/axioms. Over a field under AC dualization is exact and a quasi-isomorphism does induce a dual quasi-isomorphism. Over Z the acyclic sequence Z --2--> Z -> Z/2 provides a counterexample after Hom_Z(-,Z), but that is not a real-vector-space ZF counterexample. The manifest retains both claims as unresolved rather than weakens them or claims an independence proof. `cex-hom-of-homology-is-not-used-as-the-definition-of-singular-cohomology` is designed as a reminder; its cex identity requires a genuine refuted statement/witness or owner-approved kind/identity reconciliation. No such change was made.

5. **Nowhere differentiable example.** The initial outcome of `cex-a-continuous-nowhere-differentiable-singular-one-simplex` records an unverified supplier. A later search found and read published `thm-takagi-function-is-continuous-and-nowhere-differentiable` and `def-tent-function-and-takagi-series`. The former proves uniform convergence with the majorant 2^(-n-1) and excludes finite derivatives by nested dyadic secants whose slopes have consecutive increments ±1. This is a plausible exact supplier: sigma=T:[0,1]->R gives the intended nowhere-differentiable simplex, including one-sided endpoint conventions. Its M-test, uniform-limit and derivative dependency proofs and plan closure were not fully checked here. Record the candidate rather than overwrite the existing escalation. No new pair is indicated. A cusp such as abs(t-1/2) proves only nonsmoothness and must not replace this example.

6. **Connection contracts.** `thm-covariant-derivative-along-a-curve-is-independent-of-frame-and-extension` means agreement when an ambient extension exists. A nonconstant section along a constant curve generally has no ambient extension. All sections must be handled by the pullback definition. The local ODE supplier was read in full: it extends a continuous coefficient matrix beyond a compact time interval, uses Gronwall and compact-region continuation, and proves global existence on that interval. The compact-subdivision and arbitrary-interval patching still require the relevant topology interfaces in the final proof closure. Tensor product and exterior constructions require the arbitrary-bundle tensor interface, not merely tangent tensor notation; the Hom bundle interpretation is available but needs complete declaration reconciliation in the first definitions.

7. **Curvature terminology.** `fs-torsion-free-means-curvature-free` precedes the planned curvature definition on DG-21 (`riemann-curvature-and-riemannian-submanifolds`). The proposed direct witness g=exp(2x^2)(dx^2+dy^2) has nonzero covariant-derivative commutator at x=0 despite symmetric Christoffels. Its meaning as curvature needs a local definition/invariance check before the false statement, or owner reconciliation of the intended local scope. Do not cite DG-21 forward. The independent path-dependence false statement uses the line connection d+x dy around a square, and the sphere B counterexample computes quarter great circles directly; neither uses Gauss–Bonnet or a later holonomy theorem.

## Published actual-prerequisite debt for canonical-ledger reconciliation

**Confirmed existing choice-contract finding, not a new duplicate defect.** The affected actual consumer is new, unpublished and escalated `thm-every-smooth-vector-bundle-admits-a-connection`. Its route reaches published `thm-smooth-partitions-of-unity-exist-on-manifolds` -> published `lem-every-open-cover-of-a-manifold-has-a-countable-cover-by-relatively-compact-coordinate-balls-subordinate-to-it` and published `lem-a-countable-coordinate-ball-cover-has-a-countable-locally-finite-shrinking`. Read all three complete proofs, plus the full `thm-second-countable-implies-lindelof` and coordinate-ball supplier.

Exact evidence:

- Cover lemma 1.1 selects U_p and a coordinate ball for every p, without declaring AC. Its 2.1 invokes `thm-second-countable-implies-lindelof`, whose statement explicitly assumes AC_omega, also absent from the cover lemma's contract.
- Shrinking lemma 3.1 selects local data for all points of a compact annulus; 4.1 collects finite selected families across countably many annuli. No choice hypothesis/dependency is declared. Its exhaustion integer can be the least eligible integer, so that part need not consume choice.
- Partition theorem 2.1 selects countably many bumps with no stated choice hypothesis/dependency. The actual output is indexed by the locally finite refinement, with assignment to original cover members; the connection sum must respect that indexing and support containment.

This matches the existing canonical ledger's UC34 agent 1 entry and the later partition rows (including the row near line 29999 at inspection). It is **actual prerequisite debt** for the general connection-existence proof, so that theorem is held. It does not block the canonical Koszul construction from an already supplied metric, whose proof does not need arbitrary connection existence. Unrelated metric-existence or published de Rham consumer debt on shared pages was not used as a blanket block.

Required repair strategy: either state/depend on full AC in the published suppliers and identify all their actual selections, preserving separate choice-free local clauses; or rewrite the constructions through the family of all eligible balls/pairs and countable selections to prove the claimed AC_omega bound. The latter cannot be inferred from the currently written point-indexed selection. Then state the exact inherited assumption in `thm-every-smooth-vector-bundle-admits-a-connection`; sum weighted local derivatives after extension by zero and use sum rho_i=1. Existing published `def-axiom-of-choice` and `def-countable-choice` are axiom suppliers. No new Phase-2 repair supplier/pair is needed; the new connection theorem is a consumer, not a replacement proof supplier for partitions. All these published supplier files remain unchanged. Source evidence is the exact local proofs plus their existing published source references; this dispatch did not fully refetch their books.

No other published defect is certified by this partial audit. The whole transitive proof closure was not read or independently approved. Each item decision lists only dependency statements/proofs or earlier local strategies actually examined by that point, and does not mistake the automatic transitive hash for mathematical validation. No recorded-result replacement or Foundations page is constructed in this batch.

## Checks

Results are appended below after execution. Owner/operator reconciliation and the full engine gate still follow; these results cannot close any escalation.

| Check | Actual result |
|---|---|
| `coverage-checklist.mjs research/phase-2-next-20-batch-9.coverage.json --require-destination` | Exit 1: 2 A pages, 126 canonical/harvest rows, 25 errors, 0 warnings. Errors retain owner-escalated sources and Merry's unread/empty harvest. Invalid informal deferral destinations encountered in the first check were corrected to existing plan page IDs; no such errors remain. |
| `manifest-deps.mjs research/phase-2-next-20-batch-*.pages.json` | Exit 0: 435 in-flight items at the check snapshot, 0 missing deps arrays, 0 errors. |
| `content-policy.mjs --manifest-only research/phase-2-next-20-batch-*.pages.json` | Exit 1: 435 items, 22 errors, 0 warnings. All 22 were missing external dependency IDs in batches 5 and 10; no batch-9 item was named. Their owners must reconcile these; this dispatch did not edit them. |
| `validate-plan.mjs research/plan-spec.json` | Exit 0. Canonical plan validation passes; it explicitly warns that 641 planned pages have empty item arrays. This pass alone does not validate new manifest proof dependencies. |
| `validate-plan.mjs /tmp/b9-run-overlay-plan.json` | Exit 1: 25 errors on a temporary merge of the canonical plan and all current run manifests. Missing supplier IDs and undeclared page prerequisites occur outside batch 9 (batches 5, 10 and 14). No batch-9 hard error. Shared canonical plan was not changed. |
| `extcheck.mjs --quiet` | Exit 0; existing published consequences of recorded material were reported as warnings. No new external_refs, recorded-result dependencies or Foundations items were introduced by batch 9. This is a structural result, not a transitive proof audit. |
| `source-fetch-check.mjs --coverage research/phase-2-next-20-batch-9.coverage.json` | Exit 1: 0/4 fetch-verified and 0/4 resolved; 4 source failures. Check mode made no new network attempts. No drop or confidence was fabricated to satisfy its drop-oriented diagnostics. |
| `frontier-dependency-ledger.mjs refresh --run phase-2-next-20` | Exit 0, refreshed and deduplicated the three owned open consumer rows. A subsequent attempted `check` subcommand returned usage/exit 1 because that subcommand does not exist; it made no validation claim or content change. |
| Owned census, order and plan-metadata inspection | Exactly 101 unique manifest IDs and 101 escalation records, all owner=false; all four page metadata/requirements arrays match the current plan. No same-page forward dependency. Whole-run policy found no owned ID or alias collision. |

Construction is complete as an **escalated handoff**, not as ready content. The next authorized owner/operator action is to reconcile the source transport and complete-reading obligations, actual partition choice debt, AT-6 sign interface and the exact local mathematical holds above. Do not automatically re-dispatch a scaffold repair wave, overwrite an escalation or treat worker completion as Step 3 review. The later Takagi candidate and newly read published debt are handoff evidence; existing per-item decisions were preserved.
