# frontier-19 — group Alpha `c` step-3 recheck

- `B7-1` — `confirmed`: `research/frontier-19-batch-7.pages.json` still gives `cor-there-is-a-lebesgue-measurable-subset-of-r-that-is-not-borel` the repaired subspace-trace route, with `thm-borel-sigma-algebra-of-a-subspace-is-the-trace` in both the strategy and `deps`, and `research/frontier-19-batch-7.proof-contracts.json` still cites that theorem for the Borel-trace step; the published item `items/thm-borel-sigma-algebra-of-a-subspace-is-the-trace.md` still states exactly `\mathcal B(Y)=\mathcal B(X)|_Y`, so the route now closes with no remaining block.
- `B7-2` — `confirmed`: `research/frontier-19-batch-7.pages.json` still gives `fs-a-dense-set-of-outer-measure-zero-and-a-dense-set-of-full-inner-measure-cannot-coexist` the repaired dependency set `cex-dense-set-of-measure-zero`, `thm-bounded-subsets-of-r-are-lebesgue-measurable-exactly-when-inner-and-outer-measures-agree`, `def-lebesgue-inner-measure-on-r`, and `thm-lebesgue-measure-of-a-box-of-every-kind`, while `research/frontier-19-batch-7.proof-contracts.json` cites the same published witness and interval-measure theorem; the rejected published example `items/ex-a-dense-null-g-delta-set-containing-the-rationals.md` still has `provenance.statement: ai-generated`, and the replacement witness `items/cex-dense-set-of-measure-zero.md` still has `provenance.statement: literature-derived`, so the provenance defect remains fixed with no remaining block.
- `B8-1` — `confirmed`: `research/frontier-19-batch-8.coverage.json` still routes `Residue at infinity` to `the-residue-theorem` and `The argument principle and logarithmic derivatives of meromorphic functions` to `the-argument-principle-and-rouche`; `research/plan-spec.json` and `research/plan-complex-analysis-track.md` still carry those exact CA-9 and CA-10 page ids, and the source pages still separate those headings the same way, so the stale-destination finding remains repaired with no remaining block.
- `B8-2` — `confirmed`: after the August 25, 2026 refresh, `research/frontier-19-alpha-c-scope-decisions.json` still records `owner-decision` for Brosnan `Proposition 3.53`, Helgason `Theorem 20`, Viaclovsky `Derivative estimates for harmonic functions`, and `Harnack's first theorem`, while keeping the two Green rows as `stands` to `subharmonic-functions-and-the-dirichlet-problem`; `research/plan-spec.json` and `research/plan-complex-analysis-track.md` still provide no exact current page id for those four owner-decision results, so the pushback does not reopen the finding and the remaining block is owner scope choice, not a hidden scaffold miss.

Scope receipts were refreshed on Tuesday, August 25, 2026 with `node tools/scope-decisions.mjs refresh --run frontier-19 --group c`; the refresh reported `8 decline(s), 0 pending`, and `node tools/scope-decisions.mjs check --run frontier-19 --group c` still passes with `8 current decline(s), 0 error(s)`. The verified machine verdicts in `research/frontier-19-alpha-c-step3-verdicts.json` remain correct as three `sufficient` entries, so no verdict downgrade is owed.
