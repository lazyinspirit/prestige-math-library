# Wave 11 Audit-Alpha task — A6 only

Act as the independent Wave 11 Audit-Alpha under `briefs/audit-alpha.md` and the complete `AUDIT-WORKFLOW.md`. You are GPT 5.6 Sol through Codex at `xhigh` with the 1,000,000-token context window. Recover the durable run from disk before acting; never trust a summary in place of the current artifacts. The user requires autonomous operation: never ask for shell-command or edit permission. Use the already-approved host routes for in-scope dispatches, or record a genuine blocker.

Do not start until the orchestrator has marked Wave 11 A4 complete and supplied final `post-A4` artifacts. Then:

1. Read all 61/61 current scoped items and all four current page files in full, including every Remark, not merely the eight A4 material items. Read the complete current proof contracts, A0/A1/A2/A3/A4 record, provenance ledger, final manifest, split, URL liveness, impact template, and generated-risk report.
2. Audit every final Wave 11 manifest edge, every exact out-of-scope target needed to license it, every changed-interface consumer in the impact template, every same-batch/cross-pair edge, and every proof-bearing top-100 spine item routed to this wave. No sampling is permitted. Apply the binding 30-second threshold and record nonfatal debt without editing it.
3. Independently adjudicate all A4 changes: eight material item repairs, the one page sentence, the two established-knowledge concurrences, and the Pugh Chapter 3 Exercise 35 source override. Treat `fs-substitution-holds-for-every-integrable-integrand` as a source-correction candidate for the eventual A7 repair-target set even though its body is byte-identical.
4. Dispatch GPT 5.6 Terra `xhigh`/1M read-only exact-final certifiers for every material repair, the page repair, and the Pugh source disposition. Dispatch DeepSeek V4 Pro `max` tool-less proof-refuters for every risk-report high/critical item and every `ai-generated` seed or required large-cone consumer. Each refuter packet must contain the complete current target, contract, and exact current text of every cited/declared dependency. Preserve all attempts append-only and retry transport-only failures without overwriting them.
5. Resolve every generated-statement risk disposition, especially the load-bearing oscillator and primitive/nonintegrable counterexample seeds. Try retag/restate/unfold/narrow in order; use `verified-generated` only after the required adversarial evidence. Resolve all failed URL rows as live/search-resolvable, replaceable, redundant, or blocking with exact evidence.
6. You may make only Alpha-authorized A6 fatal/major repairs justified by your whole-scope read. Every Alpha item/page repair requires a fresh exact-final independent Terra certification and consumer-impact refresh. Remove stale stamps on edited items, update exact contracts, and report every edited id; pure retags never enter the rejudge set.
7. Write the durable A6 record and all required receipts: `wave11-alpha.md`, `wave11-published-repairs.md`, `wave11-rejudge-targets.json` with `mode: published-audit-repair-targets`, final impact/spine/genrisk/risk receipts, certifier/refuter archives, and exact current contracts/manifests as required. Take no touch snapshots; the orchestrator owns stage snapshots.
8. Run the isolated and authoritative A6 gates available to you and leave the tree quiescent. Do not run A7, judges, A8, A9, A10, commit, or push. Handoff the exact A7 ids and any blocker.

Read repaired Remarks with a numbered step's suspicion. Verification stamps are written last, only after exact-final independent certification.
