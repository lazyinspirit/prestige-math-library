# Wave 10 Audit-Alpha report

Role: sole Audit-Alpha for A6, GPT 5.6 Sol via the Codex subscription, `xhigh`, 1,000,000-token context.

## Durable recovery and full-reading checkpoint — 2026-08-09

- Baseline commit recovered and verified: `6fb3b6c9d08752db2239a2e0794752f0f69107fc` (`chore(audit): conclude wave 7`). The shared worktree is intentionally dirty with concurrent audit artifacts and the authorized Wave 10 A4 mutations. This role will not revert, overwrite, commit, or push unrelated work.
- Normative instructions read in full before action: `CLAUDE.md`, `AUDIT-WORKFLOW.md`, `research/audit/RESUME.md`, and `briefs/audit-alpha.md`. The complete durable Wave 10 A0, A3, Beta findings, page manifest, provenance ledger, proof contracts, audit manifest, split, touch log, URL liveness receipt, and the finalized Wave 8/Wave 9 Alpha records were recovered and reconciled from disk rather than trusted from summaries.
- Disk state verified against the durable A4 receipt. The scope is exactly 57 items and four page files. Both proof-contract copies contain the same 49 proof-bearing contracts and differ only in namespace metadata; strict validation is 49/49 with zero errors and zero warnings. The touch log contains the expected baseline, pre-A4, and post-A4 snapshots.
- Read all 57 exact-current scoped item files and all four exact-current page files in full, including every Fact, numbered proof step, Remark, source, provenance field, and page relationship. Read all 99 distinct exact-current out-of-scope dependency targets supporting the final manifest. Read all 59 proof-bearing items in the exact-current top-100 dependency spine. Read all 338 exact-current affected consumers in full; the 282 not already covered by another required surface were transported in 39 bounded batches but not sampled.
- Reconciled all 888 final manifest relationships against exact current source and target text: 698 published-backward, 183 same-batch, and 7 forward relationships. The mandatory frozen-text barriers are exactly 86 Wave10-to-Wave8 relationships from 36 sources into 20 targets and 29 Wave10-to-Wave9 relationships from 22 sources into eight targets. Every one of the 115 edges remains licensed by the exact A9-frozen earlier-wave text. No Wave 8 or Wave 9 item or page text was altered.
- Independently adjudicated the three A4 item repairs and the delegated derivative A-page repair. The corrected sharpness Remark in `lem-integral-elementary-bounds`, the difference-quotient Fact in `thm-monotonicity-from-the-derivative`, the honest `L1` use in `ex-integral-of-the-floor-function`, and the page sentence describing differentiability as a hypothesis are all necessary and correct. No additional fatal scoped or consumer defect has been found, so no A6 mathematical mutation is currently licensed under R1.
- Source recovery reproduces 58 distinct scoped URLs: 56 ordinary live resolutions and two exact caveats. The UNLP PDF endpoint fails local TLS-chain validation (`status: 0`, local issuer), while the McGill PDF returns HTTP 200 but exceeds the sweep's complete-transfer deadline because of its size. Both caveats have been sent to exact-context Terra source-disposition readers and remain open until those results are reconciled.
- Provenance recovery reproduces all 57 rows: 31 literature-derived/exact-source statements, 25 ai-altered/semantic-source statements, one ai-generated/no-source repository-specific statement, 35 ai-altered proofs, 14 ai-generated proofs, and eight not-applicable proof rows. All 54 pure-retag applications preserve the mathematical body; the three remaining rows are the material A4 item repairs above.
- Independent external lanes were assembled from exact-current hash-bound packets. Forty-nine critical-risk proof routes were dispatched to DeepSeek V4 Pro at its maximum reasoning setting through the approved 16-call scheduler. Six GPT 5.6 Terra `xhigh`/1M packets were dispatched through the Codex subscription: the three materially repaired items, the repaired derivative page, and the two caveated source dispositions. Every attempt and transport artifact is append-only. Final reconciliation, exact certification stamps, receipts, and A6 gates remain in progress at this checkpoint.

## A6 final adjudication and repairs

- The three A4 item repairs and the one delegated A-page repair remain independently necessary and correct. Alpha made no A6 mathematical-body edit under R1. Alpha made one source-only item repair: `cex-differentiable-with-unbounded-derivative-is-not-lipschitz` replaced the reader-unusable UNLP practice-sheet URL with the official live MIT 18.785 Lecture 19 PDF, printed page 2, which explicitly supplies the square-root non-Lipschitz quotient witness. Normalized hash changed from `0766c3134a980d2dbde5883e1303b0fc510f87b1395a56af7557fee8c9b8c8f8` to `6cb1e75a9f8c3ecc6ccaabefdd20d8b895a343effae13f6390730ba6ffb3f8ca`. Both exact consumers, `cor-bounded-derivative-implies-lipschitz` and `ex-mean-value-theorem-bounds-the-square-root-increment`, were read and remain unchanged and licensed.
- Alpha corrected exactly three provenance rationales: the repaired square-root source row and the two `x^3` strict-increase rows. The McGill PDF is reader-usable and supports derivative material, but not strict increase; both strict-increase rationales now attribute that claim only to Hunter. The final URL sweep is 57/58 live. The one remaining caveat is McGill's HTTP-200 partial-transfer timeout after 178552/290259 bytes; its exact content limitation and both original 56/58 failures are preserved in `wave10-url-dispositions.json`.
- Final classification is three A4 material item repairs, one A6 source repair, and 53 pure retags. Mechanical metadata added current owner-delegated Terra verification stamps to exactly the four repaired item targets. The A4 page repair is hash-bound and certified but unstamped. No Wave 8 or Wave 9 item/page text was changed.
- The one scoped generated-risk seed, `rem-riemann-integral-choice-ledger`, is dispositioned `verified-generated`: Alpha read the complete ledger, every named prerequisite, and all ten direct-citation consumers. It has zero logical consumers, its repository-local accounting matches current proof expenditures, and future citation containment remains in force.

## Independent-reader results

- The append-only dispatch archive contains 59 prompts, 59 logs, and 59 result packets: 51 DeepSeek V4 Pro attempts and eight GPT 5.6 Terra attempts. Fifty-eight transports completed successfully; the sole failed transport is the preserved original `thm-lebesgue-criterion` attempt.
- The exact-final DeepSeek scope is complete for 49/49 critical routes: 48 final `CLEAN` verdicts and one `DEFECTS` finding adjudicated `confirmed-nonfatal`. The finding on `lem-derivative-of-a-power` step 4.2 asks that the truncating sequence be named separately, but the surrounding sentence already defines that sequence explicitly, so the inference closes immediately under the 30-second rule and no R1 edit is licensed. The original Lebesgue transport failure was retried append-only and returned `CLEAN`; the source-repaired square-root item was rerun on its new exact hash and returned `CLEAN`.
- Terra certified exact-final text for four item targets and one page: the three A4 material items, the A6 source-repaired item, and the derivative page. The two initial source-disposition routes returned `REFUSED` and are preserved with their closures: replace UNLP, and restrict McGill attribution. Two append-only exact-current Terra source-repair attempts independently returned `CERTIFIED`; `a6r3` is the canonical receipt evidence.

## Receipts and gates

- Proof contracts: 49/49 strict, zero errors and zero warnings; both copies are identical except namespace metadata. Risk routes: 49 critical, 48 clean, one confirmed nonfatal, zero fatal. Finite smoke: zero errors. Content policy: 57/57 with zero errors and the expected one generated-kind warning. Generated risk: receipt passes. Spine: 59/59 exact-current proof-bearing top-100 attestations, zero errors/warnings. Edges: 888/888 exact dispositions, including all 115 frozen barriers. Provenance: 57/57. URLs: final 57/58 with the one exact retained transport caveat. Isolated impact: 57 changed Wave 10 interfaces and 338 consumers, zero errors and zero warnings.
- The correct authoritative `post-A6` touch snapshot was recorded. Because the owner ran Waves 8–10 concurrently, the authoritative `pre-A4` to latest interval contains the combined 239-interface/729-consumer surface. Consequently the A6 gate of record is 13/14 pass: its only failure is that the deliberately Wave10-isolated 57/338 receipt cannot disposition the other waves' 182 interfaces and 391 additional consumers. `wave10-concurrent-impact-mismatch.json` durably preserves the complete combined and isolated id sets plus exact receipt/touch hashes. The isolated durable `wave10-impact-touches.json` and `wave10-impact-audit.json` pass exactly. Root/orchestrator owns the combined concurrent closure and instructed Alpha not to rewrite shared earlier-wave artifacts.
- Touchlog mechanically counts the source-repaired square-root item twice: the A4 provenance/source retag and the A6 URL replacement. This is reported to the orchestrator for the repeated-repair personal-audit threshold. No other Wave 10 item received a distinct A6 mathematical/source mutation.

## Exact edit and A7 target handoff

- A4 repair surface independently certified: three item repairs and one page repair.
- A6 Alpha public-content edits: zero mathematical bodies, one item source list, and three provenance-ledger rationales. Mechanical stamps: four items. Generated-risk dispositions: one. Proof-contract risk-review records: 49 in each synchronized contract copy.
- `wave10-rejudge-targets.json` contains exactly four ids: `cex-differentiable-with-unbounded-derivative-is-not-lipschitz`, `ex-integral-of-the-floor-function`, `lem-integral-elementary-bounds`, and `thm-monotonicity-from-the-derivative`. It excludes all 53 pure retags, the page edit, consumers, siblings, and reader-only routes.
- A6 blocker: only the owner-authorized combined-impact receipt mismatch described above. Mathematical, source, certification, provenance, edge, spine, risk, and isolated consumer-impact audit work is complete. Alpha did not run A7, A8, A9, commit, or push.

## A8 recovery checkpoint — 2026-08-09

- Recovered the complete durable A6 record, repair/certification/risk/edge/spine/impact/touch receipts, current four-id A7 target receipt, all eight current A7 verdict rows, attempt/cost ledgers, and the exact current target/dependency text from disk. A7 is exactly four ids with paired coverage: DeepSeek passed all four; Terra passed `thm-monotonicity-from-the-derivative` and rejected the other three at matching exact item/context hashes.
- The worktree still contains the owner-authorized concurrent Waves 8–10 artifacts. The current four target texts reproduce both the A7 judge `item_sha256`/context hashes and the separate verification-stripped R1 hashes. No A8 content edit had occurred at recovery. Because the orchestrator-provided touch log lacked the mandatory stage boundary, Alpha recorded the single correct `pre-a8` snapshot before adjudication or mutation.
- Exact next action: read each rejection's cited item and contract quote verbatim, apply the 30-second rule, append one exact-hash adjudication per rejected Terra row, and edit only a `confirmed_fatal` target. Existing combined and isolated impact receipts will not be rewritten.

## A8 closure

- Adjudicated all three exact-current Terra rejections append-only: two `false_positive`, one `confirmed_nonfatal`, zero `confirmed_fatal`. `cex-differentiable-with-unbounded-derivative-is-not-lipschitz` is licensed verbatim by `lem-of-abs-value` Fact L1; `lem-integral-elementary-bounds` derives its maximum absolute-value bound directly from the cited maximum definition and absolute-value inequalities; `ex-integral-of-the-floor-function` has a genuine but non-load-bearing continuity-set citation compression that closes in under thirty seconds from integer-part uniqueness and the epsilon-delta definition.
- R1 therefore licensed no mutation. Exact A8 edited ids: none. Public-interface changes: zero. Required consumer union: zero. Contract/stamp/certifier changes: zero. Exact A8 rejudge targets: none. The Step-8 guard passes with zero changed/created/deleted items and zero errors/warnings.
- Durable A8 details and receipts: `wave10-A8.md`, `wave10-A8-adjudication-receipt.json`, `wave10-A8-impact-review.json`, `wave10-A8-step8-guard.json`, `wave10-A8-gate-receipt.json`, `wave10-coverage.json`, and `wave10-A8-rejudge-targets.json`. The A8 gate of record passes 3/3, including current 4/4 A7 paired coverage with the three exact adjudications. Alpha did not run paired judges, A9, commit, or push.
