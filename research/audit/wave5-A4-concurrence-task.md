# Wave 5, A4 gate repair — one D2 evidence-class concurrence

You are Alpha for **wave 5**. The wave is halted at A4's gates on a single
`content-policy` error, and it is one only you can clear.

`lem-lindelof-degree-is-well-defined` (batch `wave5-topology-countability`) is
recorded with `evidence: established-knowledge`. Under D2 that class is the sole
URL waiver and requires your recorded concurrence (`alpha_concurred: true`);
without it the statement falls back to `ai-generated`, which the gate refuses.

**The Beta did the right thing.** Its rationale says, verbatim, "ESCALATED TO
ALPHA for the evidence class", and it supplied
`https://www1.essex.ac.uk/maths/people/fremlin/chap5a.pdf`, noting that Fremlin
5A4A asserts the minimum defining L(X) by writing it down, but that no located
source states its well-definedness as a separate proposition. That is exactly the
situation D2 reserves for you.

Decide from disk and from the source:

1. Open `items/lem-lindelof-degree-is-well-defined.md` and read what it claims.
2. Check the cited source yourself. A `certifier` (read-only `claude-sonnet-5`)
   has web access if you want an independent read; an `audit-refuter`
   (DeepSeek, tool-less) needs its context assembled into a `--task` file.
3. Then either record `alpha_concurred: true` on that ledger row in
   `research/audit/wave5-topology-countability.provenance.jsonl` with your
   rationale, **or** decline it and set an evidence class the source actually
   supports.

Owner rule, binding here: `ai-generated` requires a positive determination of
genuine novelty. Never assign it merely because a source failed to surface — a
recoverable restatement of established mathematics is `ai-altered`. And
uncertainty never falls toward a sourced label.

Change only that ledger row unless your decision requires the item's provenance
tags to change with it. Record the decision in `research/audit/wave5-alpha.md`.
