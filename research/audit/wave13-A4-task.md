# Wave 13 Audit-Beta task — A4 only

Resume the exact Wave 13 Audit-Beta record you created at A1/A2. Read the complete independent A3 decision in `research/audit/wave13-A3.md` before acting. Apply **only** its approved A4 changes to the exact scope in `research/audit/wave13-real-analysis.pages.json`.

Authorized work:

- The orchestrator owns the single stage-level touch snapshots. Do not take per-item or additional touch snapshots.
- Apply all 76 approved component provenance/source determinations while preserving every legacy `origin` field and removing only legacy `authorship` if present.
- Apply exactly the three amended material repairs in A3: the safe enlarged-rectangle relative-indicator proof for `thm-jordan-boundary-criterion`; the isolated reciprocal `[L6]` Fact/use for `thm-lebesgue-criterion-in-rn`; and the qualified qualitative `[L2]` attribution plus direct sharp-interval calculation for `ex-geometric-power-series-and-an-interior-reexpansion`.
- Apply A3's contract details exactly, including `choose`/L3 use and checked iff directions for the Jordan boundary theorem, exact L6 use only at Lebesgue step 6.1, and both geometric L2 mappings using step 2.1.
- Correct the two false iff boundary dispositions in the contract for `thm-jordan-content-and-indicator-integrability`; this is contract-only and must not change that item or add it to material/rejudge targets.
- Treat exactly the three A3-listed ids as material: remove their stale judge/audited state, regenerate affected contracts from exact final text, and leave pure-retag verification state intact.
- Update only Wave 13 namespaced findings/provenance/contracts and ordinary A4 receipts. Do not touch pages or Waves 11, 12, or 14; do not run A6 or later.

Verify exact 76/62 scope/cardinality, strict proof contracts with zero errors/warnings, prechecks, provenance/source application, URL fields, content policy, risk report, reflow, relationships, `git diff --check`, and preservation of all 24 earlier-wave barrier edges (1 to Wave11, 23 to Wave12). Record the exact pure-retag/material split and all contract-only amendments. Never ask for shell-command or edit permission; use already-authorized in-scope workspace operations or record a genuine blocker.
