# certifier/a6cert-009-rem-function-space-conventions 2026-08-05T12:55:30.033Z

## stdout
### VERDICT — REFUSE

### WHAT I READ
- the item, in full: `items/rem-function-space-conventions.md`
- dependencies opened on disk: `def-compact-open-topology`, `def-metric-compactness`, `def-compact-space`, `thm-compactness-agrees-with-metric-compactness`, `def-locally-compact-metric-space`, `def-evaluation-map`, `thm-the-exponential-law`, `def-metrizable-space`
- also read: `library/topology/compactness.md` (page manifest), `library/topology/function-space-topologies.md` (page manifest), `research/plan-topology-track.md` (global page order: `compactness` = spec order 255, `function-space-topologies` = spec order 283)
- URLs fetched: none (the repair diff added no `sources.references` entry; the existing three were untouched by this repair, so the brief's URL-fetch instruction doesn't apply)

### FINDING 1 — FATAL — location: point 6, second bullet ("Two objects on this page are deliberately new rather than reused")
CLAIM AS WRITTEN: "[[def-locally-compact-metric-space]] is a metric special case of a notion this library does not yet define in general. Its own dictionary remark records the agreement and why the agreement is immediate."

WHY IT FAILS: This is false, and it is contradicted by the very item cited in the same sentence. `def-locally-compact-metric-space`'s own Remarks section (unaltered by this repair) says: "**Dictionary: this was the metric special case of a notion this library did not yet define in general, and now does.** ... General topological compactness is now available at this point in the reading order, alongside the metric notion ([[def-metric-compactness]]) this page uses..." — i.e. `def-locally-compact-space` (on the `compactness` page, spec order 255) already exists and precedes `function-space-topologies` (spec order 283) in the library's single global reading order (`research/plan-topology-track.md`: "255 < 283"; SCHEMA.md §7: "Pages are read in a single global order.").

This is confirmed by a second, independent sibling item on the same page: `thm-the-exponential-law`'s own remark states "compactness for an arbitrary topological space ([[def-compact-space]]) ... [is] developed earlier in the reading order." Point 1 of this very item (the part the wave-5 repair correctly fixed) makes the identical correction for `def-compact-open-topology`. The claim in point 6 is the one instance of this exact stale-reading-order defect that the repair missed — the same defect class SCHEMA.md §"Remarks JUSTIFY" flags by name ("Darboux's theorem is developed on a later page" — its page is at a *lower* order, and unbuilt) and that this dispatch brief specifically warns is where nearly every fatal defect in this corpus has been found.

WHAT WOULD FIX IT: Replace the sentence to match the now-corrected framing used in point 1 and in `def-locally-compact-metric-space`'s own remark, e.g.: "`def-locally-compact-metric-space` is the metric special case of local compactness; the general topological notion (`def-compact-space`, `def-locally-compact-space`) is now available earlier in the reading order, and its own dictionary remark records the agreement with the general notion via `thm-compactness-agrees-with-metric-compactness`."

### ASSESSMENT
I independently verified the repaired claim in point 1 (that `def-compact-space` and `thm-compactness-agrees-with-metric-compactness` are available earlier in the reading order than this page) against the global page-order spec and against two other published items on the same page (`def-locally-compact-metric-space`, `thm-the-exponential-law`) that already carry the corrected framing — the repair is correct and consistent with the rest of the corpus. I also checked the domain/target-metric split (points 1–2) against `def-compact-open-topology`, `def-evaluation-map`, and `thm-the-exponential-law`, the Hausdorff claim (point 5) against `def-metrizable-space`, and the notation/sphere disambiguation (point 5) against `def-compact-open-topology` — all accurate. However, point 6 retains an assertion the repair did not touch, and it is now false: it asserts the library "does not yet define" general local compactness, when `def-locally-compact-space` already exists at an earlier page in the same global reading order, a fact the item's own cited dependency and a sibling landmark theorem on the same page both already state correctly. This is a false claim about the library's current state, not a 30-second gap.

1 fatal, 0 nonfatal


## stderr

