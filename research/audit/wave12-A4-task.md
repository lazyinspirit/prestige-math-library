# Wave 12 Audit-Beta task — A4 only

Resume the exact Wave 12 Audit-Beta record you created at A1/A2. Read the complete independent A3 decision in `research/audit/wave12-A3.md` before acting. Apply **only** its approved A4 changes to the exact scope in `research/audit/wave12-real-analysis.pages.json`.

Authorized work:

- The orchestrator owns the single stage-level touch snapshots. Do not take per-item or additional touch snapshots.
- Apply all 61 approved component provenance/source determinations while preserving every legacy `origin` field and removing only legacy `authorship` if present.
- Apply CP-1 to the eight exact material ids in A3: delete the eight unused Fact paragraphs and their 13 Fact links, but implement A3's mandatory Bolzano–Weierstrass amendment by relocating the two square-root/absolute-value citations into the actually using Remark and keeping those two dependencies. Apply exactly the four CP-1 frontmatter dependency removals enumerated by A3; do not remove the nine preserved dependencies.
- Apply CP-2 exactly: repair L7 in `lem-p-norms-are-norms-and-induce-the-published-metrics`, remove the direct `def-complete-ordered-field` link, and add/retain `def-ordered-field` in `deps` and L7.
- Treat exactly the eight A3-listed ids as material. Remove their stale audit/judge/verified state as directed by A3, regenerate their proof-contract entries from exact final text, and leave pure-retag verification state intact.
- Update only Wave 12 namespaced findings/provenance/contracts and ordinary A4 receipts. Do not edit the long page summary or the generated scope Remark debt; do not touch Waves 11, 13, or 14; do not run A6 or later.

Required final accounting: 53 pure retags / 8 material; 48 contracts; 398 numbered steps; 667 Fact-citation entries; 384 boundary dispositions; strict 48/48 with zero errors/warnings; four declared-dependency removals and one addition; all 29 Wave12→Wave11 barrier edges preserved. Verify scope/cardinality, prechecks, provenance/source application, URL fields, content policy, risk report, reflow, relationships, and `git diff --check`. Never ask for shell-command or edit permission; use already-authorized in-scope workspace operations or record a genuine blocker.
