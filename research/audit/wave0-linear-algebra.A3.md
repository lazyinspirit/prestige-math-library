# Wave 0 · linear-algebra — A3 orchestrator adjudication (2026-08-02)

## Decisions

- **Provenance retags: APPROVED for 23 of 25.** The two
  `established-knowledge` rows (E1
  `cex-pairwise-trivial-intersection-is-not-a-direct-sum`, E2
  `ex-sum-and-intersection-in-f-three`) are HELD until Alpha's concurrence at
  A6 — do not write their provenance blocks at A4. If Alpha declines, the row
  becomes `ai-generated`/`trivial` with a `generation.role`, per the ledger
  note — and the witness then gets the truth-risk verification recorded.
- **Step-1.5 mis-attribution in `lem-vector-space-elementary-consequences`:
  APPROVED, minimal form** — repair the step so it claims only what its cited
  fact licenses (existence), or properly derive/cite uniqueness if the lemma's
  Statement needs it. Beta drafts the smallest correction; Alpha checks. The
  judge-stamp concern is moot: A7 rejudges every wave-0 item regardless (R2).
- **[L6] understatement in `ex-sequence-space-and-eventually-zero-subspace`:
  DECLINED** — understating a target is the safe direction; triage.
- **Page count claims (both pages): → A10 owner queue** (accurate, legacy,
  §6-banned going forward).
- **The two unverified reader-visible URLs (`Ordered_field`,
  `Union_(set_theory)`): verify at A4**; replace only if dead.
- All 18 contract items route to Alpha `risk_review` at A6 (15 CRITICAL /
  2 HIGH / 1 MODERATE, volume-driven).
