# Wave 0 · topology — A3 orchestrator adjudication (2026-08-02)

Verified from disk: F1 (`rem-metric-axiom-conventions` line 84 asserts
"$\overline{\mathbb{R}}$ has deliberately not been built" while
`def-extended-reals` is `status: published` — published-claim decay, a false
claim about the library in a published Remark) and F2 (the 404
`Distance_from_a_point_to_a_set` URL appears in 4 items).

## Decisions

- **Provenance retags: APPROVED, all 40** (5 literature-derived/exact-source,
  35 ai-altered/semantic-source; zero waivers, zero classification
  escalations; D5 no-op).
- **F1 APPROVED (class a, decay correction)** — repair the false "has
  deliberately not been built" claim in `rem-metric-axiom-conventions` and
  apply the softer wording to the three related scope-denial sentences
  (`def-metric-bounded-diameter`, `lem-real-line-is-a-metric-space`,
  `lem-sup-metric-is-a-metric`). Whether the corrected text should wikilink
  `def-extended-reals` is DEFERRED to Alpha (cross-category reference
  choice); write the correction so it is honest without the link, and Alpha
  may add it.
- **F2 APPROVED** — replace the 404 URL with the verified
  `Hausdorff_distance` URL in the 4 items.
- **F4 (unstated symmetry use, `thm-metric-sequential-closure` step 1.1)
  APPROVED** — minimal fix: state/tag M2 where used (consistent with the
  foundations D4 class).
- **F5 (`fs-closed-ball…` Statement advertising a "two-point subspace" while
  the witness is {0} ∪ [1,2]) APPROVED** — minimal restatement aligning the
  Statement with the actual witness; Alpha checks the final wording.
- **F6 (`ex-post-office-metric` Remark using undefined vector addition)
  APPROVED** — minimal wording fix.
- **F3/F7 (36 about-the-book URLs; naming quirk) → recorded; the URL pattern
  joins the corpus-wide A10 owner-queue item** (same pattern flagged by
  dedekind and combinatorics).
- **F8/F9** — the identified in-batch overtaken scope-denial claims are
  APPROVED for correction at A4 (same decay class as F1); the page-prose
  counts and the unbacked Hausdorff-distance forward promise go to the A10
  owner queue; A9 still re-greps corpus-wide.
- `ex-p-adic-ultrametric` (no judge block since 2026-07-27): A7 covers it —
  R2 judges every wave-0 item on current text.
