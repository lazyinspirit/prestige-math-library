# Frontier 29 contract audit

Date: Tuesday, September 1, 2026.
Dispatch: `contract-audit-4`
Run: `frontier-29`

## Reproduced detector state before repair

- `boundary-audit` on `research/frontier-29-proof-contracts.json` reported `109` template clusters / `2069` clustered rows and `89` contradicted candidates.
- `citation-fidelity` on `research/frontier-29-proof-contracts.json` reported no missing quotes and no widening candidates.
- `gate-liveness` reported `VACUOUS finite-smoke 0 checks`.

## Template-cluster repairs

- Batch `2`: rewrote the clustered boundary reasons/evidence to item-specific rows across `8` cluster(s) / `432` clustered rows over `56` item(s). Post-fix: none.
- Batch `6`: rewrote the clustered boundary reasons/evidence to item-specific rows across `33` cluster(s) / `200` clustered rows over `33` item(s). Post-fix: none.
- Batch `8`: rewrote the clustered boundary reasons/evidence to item-specific rows across `8` cluster(s) / `348` clustered rows over `45` item(s). Post-fix: none.
- Batch `9`: rewrote the clustered boundary reasons/evidence to item-specific rows across `10` cluster(s) / `214` clustered rows over `27` item(s). Post-fix: none.
- Batch `10`: rewrote the clustered boundary reasons/evidence to item-specific rows across `30` cluster(s) / `208` clustered rows over `30` item(s). Post-fix: none.
- Batch `12`: rewrote the clustered boundary reasons/evidence to item-specific rows across `4` cluster(s) / `24` clustered rows over `12` item(s). Post-fix: none.
- Batch `13`: rewrote the clustered boundary reasons/evidence to item-specific rows across `2` cluster(s) / `16` clustered rows over `13` item(s). Post-fix: none.
- Batch `15`: rewrote the clustered boundary reasons/evidence to item-specific rows across `1` cluster(s) / `4` clustered rows over `2` item(s). Post-fix: none.
- Batch `16`: rewrote the clustered boundary reasons/evidence to item-specific rows across `8` cluster(s) / `370` clustered rows over `48` item(s). Post-fix: none.
- Batch `17`: rewrote the clustered boundary reasons/evidence to item-specific rows across `3` cluster(s) / `26` clustered rows over `16` item(s). Post-fix: none.
- Batch `19`: rewrote the clustered boundary reasons/evidence to item-specific rows across `2` cluster(s) / `227` clustered rows over `30` item(s). Post-fix: none.

Changed records for this part:

- `research/frontier-29-batch-1.proof-contracts.json`
- `research/frontier-29-batch-2.proof-contracts.json`
- `research/frontier-29-batch-4.proof-contracts.json`
- `research/frontier-29-batch-5.proof-contracts.json`
- `research/frontier-29-batch-6.proof-contracts.json`
- `research/frontier-29-batch-8.proof-contracts.json`
- `research/frontier-29-batch-9.proof-contracts.json`
- `research/frontier-29-batch-10.proof-contracts.json`
- `research/frontier-29-batch-12.proof-contracts.json`
- `research/frontier-29-batch-13.proof-contracts.json`
- `research/frontier-29-batch-15.proof-contracts.json`
- `research/frontier-29-batch-16.proof-contracts.json`
- `research/frontier-29-batch-17.proof-contracts.json`
- `research/frontier-29-batch-19.proof-contracts.json`

## Contradicted-candidate adjudications

| Candidate | Disposition | Evidence | Changed record |
| --- | --- | --- | --- |
| `fs-isomorphic-middle-groups-force-equivalent-group-extensions [iff-forward]` | upheld | In FALSE: isomorphic middle groups force equivalent extensions with fixed kernel and quotient, “equivalent extensions” names the categorical predicate being refuted, not a theorem-level biconditional connective. | Batch 1 iff-forward row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `fs-isomorphic-middle-groups-force-equivalent-group-extensions [iff-reverse]` | upheld | In FALSE: isomorphic middle groups force equivalent extensions with fixed kernel and quotient, “equivalent extensions” names the categorical predicate being refuted, not a theorem-level biconditional connective. | Batch 1 iff-reverse row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `cex-the-hardy-littlewood-maximal-operator-is-not-strong-type-one-one [zero]` | upheld | In The Hardy-Littlewood maximal operator is not strong type $(1,1)$, step 2.1 only divides by $lambda(B(x,tfrac32\|x\|_2))$ and $\|x\|_2^n$ after fixing $\|x\|_2ge2R$ and using positive Euclidean-ball measure, so the detector's zero-denominator regex is inapplicable. | Batch 4 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `cex-the-unit-interval-indicator-has-nonintegrable-maximal-function [zero]` | upheld | In The maximal function of $\mathbf 1_{[0,1]}$ is not integrable, step 1.1 works only for $xge1$, so the denominators $2x$ and $x$ stay positive throughout the witness computation. | Batch 4 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `ex-a-locally-integrable-function-can-fail-to-differentiate-on-a-null-set [zero]` | upheld | In A locally integrable function can fail to differentiate on a null set, steps 1.2 and 2.1 use the explicit radii $r_m=2^{-2m}$ and $s_m=2^{-2m-1}$, both positive, so the averages never divide by zero. | Batch 4 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `ex-a-unit-mass-spike-has-a-large-maximal-superlevel-set [zero]` | upheld | In A unit-mass spike has a large maximal superlevel set, the statement assumes $varepsilon>0$ and $0<tle(2varepsilon)^{-1}$, and step 1.2 works only for $xgevarepsilon$, so the denominators $2x$ and $2t$ are already positive. | Batch 4 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `ex-an-interval-endpoint-is-not-a-density-point [zero]` | upheld | In An endpoint of an interval has density one half, not one, step 1.1 forms $r/(2r)$ only for $0<rle1$, so the denominator is excluded from zero by the written radius hypothesis. | Batch 4 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `ex-the-centered-maximal-function-of-the-unit-interval-indicator-on-r [zero]` | upheld | In The centered maximal function of $\mathbf{1}_{[0,1]}$ on $\mathbb R$, step 1.1 defines $I_r=[x-r,x+r]$ only for $r>0$, so the flagged factor $2r$ is derivative notation for a genuine interval radius, not a missed zero branch. | Batch 4 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `ex-the-lebesgue-set-of-the-rationals-indicator-is-all-of-r [zero]` | upheld | In The $L^1_{loc}$ class of $\mathbf 1_{\mathbb Q}$ has every point as a Lebesgue point, step 2.1 averages over $B(x,r)$ with $r>0$, and the cited ball-measure fact keeps $lambda(B(x,r))>0$. | Batch 4 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `fs-there-is-a-measurable-set-with-density-one-half-in-every-interval [zero]` | upheld | In FALSE: some measurable set has density one half in every interval, step 2.1 applies the hypothesis only to intervals $(x-r,x+r)$ with $r>0$, so the denominator $2r$ is already excluded from vanishing. | Batch 4 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `lem-continuous-compactly-supported-functions-differentiate-by-ball-averages [zero]` | upheld | In Continuous compactly supported functions are recovered by small ball averages, step 2.1 works only for $0<r<delta$, and the cited Euclidean-ball lemma gives $lambda(B(x,r))>0$ on that range. | Batch 4 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `prop-centered-and-uncentered-maximal-functions-are-comparable [zero]` | upheld | In The centered and uncentered maximal functions are pointwise comparable, step 2.1 divides by $lambda(B(y,r))$ and $lambda(B(x,2r))$ only for genuine balls with $r>0$, so both measures are positive by the cited ball-measure facts. | Batch 4 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-almost-every-point-is-a-lebesgue-point [zero]` | upheld | In Almost every point is a Lebesgue point of a locally integrable function, step 2.1 averages over $B(x,r)$ with $r>0$, and the cited Euclidean-ball measure lemma keeps $lambda(B(x,r))>0$. | Batch 4 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-differentiation-along-families-shrinking-nicely [empty]` | upheld | In Differentiation holds along families shrinking nicely, the hypothesis already gives a family $(E_r(x))_{r>0}$ indexed by all positive radii with constant $alpha_x>0$, so the detector's family regex does not expose an optional empty-family branch. | Batch 4 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-differentiation-along-families-shrinking-nicely [zero]` | upheld | In Differentiation holds along families shrinking nicely, step 1.1 uses $lambda(E_r(x))$ and $alpha_xlambda(B(x,r))$ only under the shrink-nicely hypothesis $lambda(E_r(x))gealpha_xlambda(B(x,r))$ with $alpha_x>0$ and $r>0$, so both denominators are already positive. | Batch 4 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-differentiation-of-borel-measures-finite-on-compact-sets-on-r-n [empty]` | upheld | In Differentiation of sigma-finite Borel measures finite on compact sets, the “family shrinking nicely” clause is indexed by all positive radii and is not an optional empty family parameter. | Batch 4 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-differentiation-of-borel-measures-finite-on-compact-sets-on-r-n [zero]` | upheld | In Differentiation of sigma-finite Borel measures finite on compact sets, steps 1.1 through 3.1 divide by $lambda(E_r)$ and $lambda(B(x,r))$ only for shrinking-nicely families with $alpha>0$ and balls of positive radius, so the detector's zero-denominator regex is inapplicable. | Batch 4 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-first-fundamental-theorem-of-calculus-for-l-one [zero]` | upheld | In The indefinite integral of an $L^1$ function is differentiable almost everywhere, step 2.1 uses $h$ only in difference quotients for $h>0$ and $h<0$; the derivative limit never evaluates at $h=0$, so there is no separate zero-denominator branch to certify. | Batch 4 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-hardy-littlewood-maximal-inequality-for-balls [zero]` | upheld | In The centered Hardy-Littlewood maximal operator is weak type $(1,1)$, the statement already fixes $t>0$, and steps 1.1 through 2.1 divide by $t$ only under that written threshold hypothesis. | Batch 4 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-lebesgue-density-theorem [zero]` | upheld | In Lebesgue density theorem, steps 2.1 and 2.2 average over $B(x,r)$ with $r>0$, and the cited Euclidean-ball fact keeps $lambda(B(x,r))>0$ throughout. | Batch 4 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-marcinkiewicz-interpolation-for-weak-one-one-and-strong-infinity [zero]` | upheld | In Marcinkiewicz interpolation from weak $(1,1)$ and strong $(\infty,\infty)$, the statement has $1<p<infty$, step 1.1 fixes $t>0$ and $eta>0$, and step 4.1 uses $p-1$ only under that strict inequality, so the flagged denominators are already excluded from zero. | Batch 4 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-vitali-covering-lemma-for-balls-with-fivefold-dilates [empty]` | upheld | In Vitali covering lemma for Euclidean balls with fivefold dilates, the two clauses are written for a listed family ${B_1,dots,B_m}$ and a sequence $(B_j)_{jge1}$; the detector is reacting to the union notation rather than to a missing optional-empty parameter. | Batch 4 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `lem-riesz-lemma [zero]` | repaired | Step 1.1 proves $dgeho>0$, and step 2.1 then divides by $\|y-m\|$ only after choosing $m$ with $\|y-m\|ge d>0$. | Batch 5 zero row changed to checked with step-linked evidence. |
| `thm-all-norms-on-a-finite-dimensional-complex-space-are-equivalent [iff-forward]` | upheld | In All norms on a finite-dimensional complex normed space are equivalent, “equivalent” names the previously defined two-sided norm-comparison predicate; this theorem asserts that predicate under finite-dimensionality rather than a new biconditional whose directions need separate certification. | Batch 5 iff-forward row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-all-norms-on-a-finite-dimensional-complex-space-are-equivalent [iff-reverse]` | upheld | In All norms on a finite-dimensional complex normed space are equivalent, “equivalent” names the previously defined two-sided norm-comparison predicate; this theorem asserts that predicate under finite-dimensionality rather than a new biconditional whose directions need separate certification. | Batch 5 iff-reverse row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-coordinate-map-for-a-finite-dimensional-normed-space [empty]` | repaired | Steps 1.3 and 2.2 explicitly isolate the $n=0$ case, where the coordinate sums are empty and both $T$ and $T^{-1}$ are bounded trivially. | Batch 5 empty row changed to checked with step-linked evidence. |
| `cex-a-random-variable-need-not-have-a-finite-expectation [zero]` | upheld | In A random variable need not have a finite expectation, the denominator $n(n+1)$ is indexed by $nge1$ in steps 1.1 and 1.2, so the detector's zero-denominator regex is inapplicable. | Batch 6 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `cor-cauchy-schwarz-for-random-variables [iff-forward]` | repaired | Step 2.1 cites [L2] for the forward equality implication from the almost-sure zero-or-proportionality alternatives to equality in Cauchy-Schwarz. | Batch 6 iff-forward row changed to checked with step-linked evidence. |
| `cor-cauchy-schwarz-for-random-variables [iff-reverse]` | repaired | Step 2.1 cites [L2] for the converse implication: equality in Cauchy-Schwarz forces one of the almost-sure zero-or-proportionality alternatives. | Batch 6 iff-reverse row changed to checked with step-linked evidence. |
| `cor-chebyshev-inequality-for-random-variables [zero]` | upheld | In Chebyshev's inequality for random variables, the statement already assumes $a>0$, so the denominator $a^2$ is strictly positive before step 2.1 divides by it. | Batch 6 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `cor-expectation-agrees-with-the-finite-weighted-sum [empty]` | upheld | In Expectation agrees with the published finite weighted sum, a finite probability space already comes with a fixed finite underlying set $Omega$, and the sums in step 2.1 are over that written data rather than an optional empty family. | Batch 6 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `cor-markov-inequality-for-random-variables [zero]` | upheld | In Markov's inequality for random variables, the statement assumes $a>0$, and step 1.1 applies the inequality only at that positive threshold. | Batch 6 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `ex-markov-and-chebyshev-sharpness [zero]` | upheld | In Markov and Chebyshev sharpness, both witness families take $a>0$ in the Statement, so the denominators $a$ and $a^2$ are fixed positive numbers inside the explicit equalities. | Batch 6 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-finite-probability-spaces-are-exactly-finite-full-power-set-probability-spaces [empty]` | upheld | In Finite probability spaces are exactly finite full-power-set probability spaces, the item is about a fixed finite set $Omega$ carrying total mass $1$; the displayed sums are over that data, not over an optional empty family. | Batch 6 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-normal-equations-for-best-affine-l2-prediction [empty]` | upheld | In Normal equations for best affine $L^2$ prediction, the predictor list $Y_1,dots,Y_n$ is fixed data; when $n=0$ the displayed sums and covariance equations are simply vacuous, so the detector's family regex does not expose a missing branch. | Batch 6 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `cex-an-abscissa-need-not-be-a-line-of-convergence [zero]` | upheld | In A boundary line need not have uniform convergence behavior, step 2.1 uses $s_*=pi i/log 2$ with $log 2>0$, and step 1.1 sums only over $kge1$, so there is no zero-denominator branch. | Batch 8 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `cor-completely-multiplicative-dirichlet-series-euler-product [empty]` | upheld | In Completely multiplicative Dirichlet series have geometric Euler factors, the Euler product ranges over all primes and each local factor over $kge0$, so the detector is reacting to the series notation rather than to an omitted empty-family case. | Batch 8 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `cor-divisor-dirichlet-series-zeta-square [empty]` | upheld | In The divisor-counting Dirichlet series is the square of the zeta Dirichlet series on Re s greater than 1, both Dirichlet series are indexed by $nge1$, not by an optional empty family. | Batch 8 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `cor-euler-totient-dirichlet-series [empty]` | upheld | In The Dirichlet series of Euler's totient is zeta of s minus 1 divided by zeta of s on Re s greater than 2, the series are indexed by positive integers and do not introduce an optional empty family branch. | Batch 8 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `cor-mobius-dirichlet-series-reciprocal-zeta [empty]` | upheld | In The Dirichlet series of the Möbius function is the reciprocal of the zeta Dirichlet series on Re s greater than 1, the series are indexed by $nge1$, so the detector's empty-family regex is inapplicable. | Batch 8 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `ex-liouville-dirichlet-series [zero]` | upheld | In The Dirichlet series of the Liouville function is zeta of 2s divided by zeta of s, the stated domain is $Re s>1$, where the cited Euler product makes $zeta(s)$ a nonzero product of nonzero factors, so no separate zero-denominator branch remains. | Batch 8 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-dirichlet-series-absolute-half-plane-holomorphy [empty]` | upheld | In Absolute convergence at one point forces absolute and locally uniform convergence on closed half-planes to the right, both the original series and its derivative series are indexed by $nge1$, not by an optional empty family. | Batch 8 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-dirichlet-series-half-plane-convergence [empty]` | upheld | In Convergence at one point of a Dirichlet series forces local uniform convergence on the open half-plane to its right, the Dirichlet series is indexed by $nge1$, so the detector is reacting to series notation rather than to a missing empty branch. | Batch 8 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-dirichlet-series-multiplication-convolution [empty]` | upheld | In Multiplying absolutely convergent Dirichlet series gives Dirichlet convolution, the double series and divisor regrouping run over positive integers, so there is no optional empty-family branch to certify. | Batch 8 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-dirichlet-series-summatory-integral [empty]` | upheld | In Dirichlet series from arithmetic functions admit the Abel-summation integral formula, the arithmetic sequence $(a_n)$ and partial-sum function $A(x)$ are fixed data, not an optional empty family. | Batch 8 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-equivalent-field-absolute-values-characterisation [zero]` | repaired | Step 2.1 chooses $a$ with $\|a\|_1>1$ and then uses step 1.2 to deduce $\|a\|_2>1$, so both logarithmic denominators are strictly positive before $c$ is defined. | Batch 8 zero row changed to checked with step-linked evidence. |
| `thm-landau-dirichlet-series [empty]` | upheld | In Landau's theorem for Dirichlet series with nonnegative coefficients, the Dirichlet series is indexed by $nge1$ with finite abscissa $sigma_c$, so the detector is reacting to series notation rather than a missing empty branch. | Batch 8 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-landau-dirichlet-series [zero]` | upheld | In Landau's theorem for Dirichlet series with nonnegative coefficients, step 1.1 divides only by $m!$ with $mge0$, so the factorial denominator is never zero and the detector's regex is inapplicable. | Batch 8 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-multiplicative-dirichlet-series-euler-product [empty]` | upheld | In A multiplicative Dirichlet series factors as an Euler product on its absolute half-plane, the Euler product runs over primes and the local factors over $kge0$, so there is no optional empty-family branch. | Batch 8 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-p-adic-completion-agrees-with-the-fraction-field-of-zp [zero]` | upheld | In The p-adic completion agrees with the fraction field of Z_p, step 2.1 writes $x=(p^N x)/p^N$ only with $p^N$ a fixed positive prime power, so the detector's zero-denominator regex is inapplicable. | Batch 8 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-p-adic-completion-is-a-field [zero]` | repaired | Step 3.1 first proves $\|[x]\|_p>0$ and chooses $c>0$ with $\|x_n\|_p,\|x_m\|_pge c$, so the denominator $\|x_n\|_p\|x_m\|_p$ is bounded away from zero before inversion. | Batch 8 zero row changed to checked with step-linked evidence. |
| `thm-p-adic-digit-expansion [empty]` | upheld | In Every p-adic number has a unique digit expansion, the expansion is indexed by all integers $nge N$, and the zero element already has the explicit all-zero expansion, so there is no optional empty-family branch. | Batch 8 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-p-adic-newton-criterion [zero]` | repaired | The hypothesis $\|f(a_0)\|_p<\|f'(a_0)\|_p^2$ forces $\|f'(a_0)\|_p>0$, and step 2.1 keeps every $f'(a_n)$ at that same nonzero size before any division occurs. | Batch 8 zero row changed to checked with step-linked evidence. |
| `thm-product-formula-for-the-rationals [empty]` | upheld | In The product formula for the rationals, the product always includes the archimedean place and only finitely many nontrivial prime places for $x e0$, so the detector's empty-product regex is inapplicable. | Batch 8 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-uniqueness-of-dirichlet-series-coefficients [empty]` | upheld | In A Dirichlet series with absolute convergence on a right half-plane is determined there by its coefficients, the two series are indexed by $nge1$ and the least index $m$ with $h(m) e0$ is a positive integer, so there is no separate empty-family branch. | Batch 8 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-uniqueness-of-dirichlet-series-coefficients [zero]` | upheld | In A Dirichlet series with absolute convergence on a right half-plane is determined there by its coefficients, step 2.1 only divides by $n$ for terms with $n>mge1$, so the denominator is already a positive integer. | Batch 8 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-von-mangoldt-logarithmic-derivative-zeta [empty]` | upheld | In The logarithmic derivative of the zeta Dirichlet series is the Dirichlet series of the von Mangoldt function on Re s greater than 1, the prime-power expansion and Dirichlet series are indexed by positive integers rather than an optional empty family. | Batch 8 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-von-mangoldt-logarithmic-derivative-zeta [zero]` | upheld | In The logarithmic derivative of the zeta Dirichlet series is the Dirichlet series of the von Mangoldt function on Re s greater than 1, step 1.1 writes $zeta(s)$ on $Re s>1$ as an Euler product of nonzero factors $(1-p^{-s})^{-1}$, so the logarithmic derivative does not introduce a zero denominator on the stated domain. | Batch 8 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `ex-splitting-the-theta-mellin-integral-isolates-the-two-polar-terms [zero]` | repaired | Step 2.1 explicitly isolates the meromorphic factor $1/(s(s-1))$, so the $s=0$ and $s=1$ poles are the recorded boundary rather than an omitted zero case. | Batch 9 zero row changed to checked with step-linked evidence. |
| `ex-zeta-four-equals-pi-to-the-four-over-ninety [zero]` | upheld | In The special-value formula gives $\zeta(4)=\pi^4/90$, the flagged denominators are $e^t-1$ inside the Bernoulli-series expansion and the fixed number $2cdot4!$ in step 2.1; no separate zero-parameter branch is left unhandled. | Batch 9 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `fs-riemann-zeta-is-given-by-its-dirichlet-series-for-all-s-not-one [empty]` | upheld | In FALSE: zeta is given by the same Dirichlet series for every complex $s$ other than $1$, the refutation uses one fixed Dirichlet series and the explicit witness $s=1/2$, so the detector's empty-family regex is inapplicable. | Batch 9 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `lem-riemann-zeta-dirichlet-series-converges-locally-uniformly [empty]` | upheld | In The Dirichlet series for zeta converges absolutely and locally uniformly on the half-plane $\operatorname{Re}s>1$, the series is indexed by $nge1$ and the compact set $K$ is fixed data, not an optional empty family. | Batch 9 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-dirichlet-eta-representation [empty]` | upheld | In The Dirichlet eta series is holomorphic on $\operatorname{Re}s>0$ and equals the prefactor times zeta there, the paired alternating series and the compact set $K$ are fixed data; the detector is reacting to series notation rather than a missing empty branch. | Batch 9 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-riemann-zeta-continuation-to-the-right-half-plane [zero]` | repaired | Step 3.1 treats the $s=1$ boundary explicitly by identifying the right-hand side as meromorphic with a single simple residue-one pole there. | Batch 9 zero row changed to checked with step-linked evidence. |
| `thm-riemann-zeta-functional-equation [zero]` | upheld | In The Riemann zeta function satisfies the classical sine-gamma functional equation, step 1.2 manipulates identities of meromorphic factors and the theorem's conclusion is itself an identity of meromorphic functions, so the zeros of $Gamma(s/2)$ or $sin(pi s/2)$ are not separate pointwise denominator cases the boundary row must split off. | Batch 9 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `lem-barycentric-coordinates-are-unique [empty]` | upheld | In Barycentric coordinates are unique, the statement starts with the listed vertices $v_0,dots,v_n$, so the displayed sums are over fixed simplex data rather than an optional empty family. | Batch 12 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `lem-simplicial-boundary-is-independent-of-oriented-representative [empty]` | upheld | In The simplicial boundary is independent of the chosen oriented representative, the formula is attached to one ordered simplex $[v_0,dots,v_n]$; the detector is reacting to the face sum, not to a missing empty-family branch. | Batch 12 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-euler-poincare-formula-for-a-finite-simplicial-complex [empty]` | upheld | In The Euler-Poincare formula for a finite simplicial complex with free homology, the alternating sum is over the finitely many chain and homology ranks of one finite complex $K$, not over an optional empty family. | Batch 12 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `lem-a-large-cy-restricted-subgraph-in-the-three-outcome-theorem-forces-a-smaller-scale-restricted-subgraph [empty]` | upheld | In A large cy-restricted subgraph in the three-outcome theorem forces a smaller-scale restricted subgraph, the hypothesis already requires $\|F\|ge(cy)^{4c_3/c_4}\|G\|$, so the detector's family regex is not pointing to an optional empty witness family. | Batch 15 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `lem-a-wonderful-anticonnected-complete-or-sparse-blockade-yields-a-restricted-subgraph-or-a-large-anticomplete-pair [empty]` | upheld | In A wonderful anticonnected complete-or-sparse blockade yields a restricted subgraph or a large anticomplete pair, the lemma starts from a blockade of length $ellge y^{-a}$ with equal-sized blocks, so the family data are already nonempty before the dichotomy begins. | Batch 15 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `lem-constant-scale-restricted-generalized-niceness-yields-an-x-scale-restricted-subgraph-a-polynomial-clique-or-stable-set-or-a-blockade [empty]` | upheld | In Constant-scale restricted generalized niceness yields an x-scale restricted subgraph, a polynomial clique or stable set, or a blockade, the claim starts from one $c^2$-restricted graph $G$ and its induced subgraphs rather than from an optional empty family parameter. | Batch 15 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `lem-cy-restricted-generalized-niceness-yields-three-outcomes [empty]` | upheld | In cy-restricted generalized niceness yields three outcomes, the theorem starts from one $cy$-restricted graph $G$ with outcomes measured against $\|G\|$, so the detector's family regex is not exposing a missing empty branch. | Batch 15 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `lem-generalized-niceness-yields-four-reduction-outcomes [empty]` | upheld | In Generalized niceness yields four reduction outcomes, the item starts from one $y$-restricted graph $G$ and a fixed forbidden family $mathcal F$, so the family notation is not an optional empty witness family. | Batch 15 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `ex-a-time-dependent-translation-field-and-its-evolution-operator [zero]` | upheld | In A time-dependent translation field and its evolution operator, the symbol $d/dt$ is derivative notation, not division by a variable denominator, so the detector's zero-denominator regex is inapplicable here. | Batch 16 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `ex-the-rotation-vector-field-and-circle-flow [zero]` | upheld | In The planar rotation field has the circle rotation flow, the symbol $d/dt$ is derivative notation, not a denominator that can vanish, so the detector is inapplicable. | Batch 16 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `fs-every-smooth-vector-field-is-complete [zero]` | repaired | Step 2.1 explicitly uses the vanishing of $1-x_0t$ at $t=1/x_0$ to produce the finite-time blow-up witness against completeness. | Batch 16 zero row changed to checked with step-linked evidence. |
| `prop-a-vector-field-tangent-to-an-embedded-submanifold-restricts-to-a-vector-field [zero]` | upheld | In A vector field tangent to an embedded submanifold restricts to a vector field on it, the detector has matched the coordinate-basis symbol $partial/partial x^i$; the proof never divides by a quantity that could be zero. | Batch 16 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `prop-coordinate-formula-for-the-lie-bracket [empty]` | upheld | In Coordinate formula for the Lie bracket, the chart data and coordinate sums are fixed local-frame data; the detector is reacting to the summation notation rather than to a missing empty-family branch. | Batch 16 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `prop-related-complete-vector-fields-have-intertwined-flows [zero]` | upheld | In Related complete vector fields have intertwined flows, the symbol $d/dt$ in step 2.1 is derivative notation, not a denominator that can vanish. | Batch 16 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `prop-smoothness-of-a-vector-field-is-equivalent-to-smooth-coordinate-components [empty]` | upheld | In Smoothness of a vector field is equivalent to smooth coordinate components, the coordinate functions $X^1,dots,X^n$ are the fixed chart coefficients from the theorem itself; the detector is reacting to the local-frame sum rather than an omitted empty family. | Batch 16 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-compactly-supported-time-dependent-vector-fields-have-global-evolution-on-a-compact-time-interval [empty]` | upheld | In Compactly supported time-dependent vector fields have global evolution on a compact time interval, the union over $tin J$ records the support condition on a fixed compact interval $J$; it is not an optional empty family branch. | Batch 16 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-derivations-of-smooth-functions-are-smooth-vector-fields [zero]` | upheld | In Derivations of smooth functions are exactly smooth vector fields, the detector has matched the basis notation $partial/partial x^i$ in step 2.1; the proof never divides by a quantity that could be zero. | Batch 16 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-lie-derivative-of-a-vector-field-equals-the-lie-bracket [zero]` | upheld | In The Lie derivative of a vector field equals the Lie bracket, the symbols $d/dt\|_{t=0}$ in steps 1.1 and 2.1 are derivative notation at a base time, not denominator terms. | Batch 16 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `cor-local-level-set-cone-at-a-morse-critical-point [empty]` | repaired | Step 2.1 explicitly handles $lambda=0$ and $lambda=n$ by reading the corresponding side of the quadratic-cone equation as an empty sum equal to $0$. | Batch 17 empty row changed to checked with step-linked evidence. |
| `thm-morse-lemma [empty]` | repaired | Step 1.1 explicitly handles $n=0$, where both displayed sums are empty and the normal form reduces to $f=f(p)$. | Batch 17 empty row changed to checked with step-linked evidence. |
| `lem-affine-finite-field-hashes-are-pairwise-independent [empty]` | upheld | In Affine maps over a finite field form a pairwise independent hash family, the family $mathcal H$ is parameterized by all pairs $(a,b)in F^2$, and step 1.1 already fixes distinct inputs $x e x'$, so the detector's empty-family regex is inapplicable. | Batch 19 empty row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `lem-chernoff-bound-for-bernoulli-trials [zero]` | upheld | In A Chernoff bound for sums of independent Bernoulli trials, step 2.1 chooses $lambda$ by $e^{-lambda}=1-alpha$, and the hypothesis $0<alpha<1$ keeps $1-alpha$ strictly positive before that step. | Batch 19 zero row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-freivalds-matrix-product-verification [iff-forward]` | upheld | In Freivalds' one-trial matrix-product test has one-sided error, “accept exactly when $A(Br)=Cr$” defines the acceptance event of the randomized test; the theorem's mathematical conclusions are the one-sided probability bounds, not a biconditional between claims. | Batch 19 iff-forward row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |
| `thm-freivalds-matrix-product-verification [iff-reverse]` | upheld | In Freivalds' one-trial matrix-product test has one-sided error, “accept exactly when $A(Br)=Cr$” defines the acceptance event of the randomized test; the theorem's mathematical conclusions are the one-sided probability bounds, not a biconditional between claims. | Batch 19 iff-reverse row kept not_applicable and now carries reviewed.upheld by contract-audit-4. |

No item-text repair was licensed or needed in this dispatch, so no new `research/defect-ledger.jsonl` row was written.

## Finite-smoke repair

Candidate: `gate-liveness` reported `VACUOUS finite-smoke 0 checks`.

Disposition: repaired by adding one applicable `matrix-ring-laws-mod-n` obligation to `thm-freivalds-matrix-product-verification` in the batch-19 contract.

Evidence:

- `thm-freivalds-matrix-product-verification` explicitly states `A(Br)=Cr` and step `1.1` records the exact excerpt `If $C=AB$, then the equality $Cr=(AB)r=A(Br)$ follows directly from the finite-sum definition in [L1].`
- `matrix-ring-laws-mod-n` is the in-repo finite-smoke probe that checks the matrix associativity convention behind `(AB)r=A(Br)` and passed on this assertion excerpt.

Changed record:

- `research/frontier-29-batch-19.proof-contracts.json`

Post-fix `finite-smoke` result:

- `1` check over `1/575` item(s) carrying obligations, passing.

## Post-merge reruns

- `node tools/merge-proof-contracts.mjs --level frontier-29 research/frontier-29-proof-contracts.json research/frontier-29-batch-*.proof-contracts.json`
  - pass; wrote the merged run contract with `575` scoped items.
- `node tools/proof-contract.mjs research/frontier-29-proof-contracts.json --strict`
  - pass with warnings; `0 error(s), 2 warning(s), 575/575 item(s) checked`.
- `node tools/finite-smoke.mjs research/frontier-29-proof-contracts.json`
  - pass; `0 error(s), 1 check(s) over 1/575 item(s) carrying obligations`.
- `node tools/boundary-audit.mjs research/frontier-29-proof-contracts.json --fail-on-contradicted --fail-on-template`
  - pass; `0` template clusters, `0` contradicted dispositions, `77` upheld rows reported.
- `node tools/citation-fidelity.mjs research/frontier-29-proof-contracts.json --fail-on-missing-quote`
  - pass; `1420` citations checked, no missing quote, no widening candidate.
- `node tools/gate-liveness.mjs --run frontier-29 --contracts research/frontier-29-proof-contracts.json --checklists research/frontier-29-batch-1.coverage.json,...,research/frontier-29-batch-19.coverage.json --min-checks 1`
  - pass; finite-smoke 1 checks, proof-contract 575 items checked, coverage-checklist 985 harvested results, precheck 9737 items checked.
- Diagnostic only: `node tools/risk-report.mjs research/frontier-29-proof-contracts.json`
  - pass; `0 error(s), 575 item(s) routed`.
- Diagnostic only: `node tools/risk-report.mjs research/frontier-29-proof-contracts.json --require-reviewed`
  - pass; `0 error(s), 575 item(s) routed`.

## Remaining blocker

- None for the dispatched contract-detector failures. Strict proof-contract still emits two non-blocking `shotgun-bracket` warnings on `thm-acyclic-resolution-theorem-for-right-derived-functors` and `thm-acyclic-resolution-theorem-for-left-derived-functors`.
