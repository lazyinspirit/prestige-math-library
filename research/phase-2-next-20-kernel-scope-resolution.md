# Kernel joint-integrand scope resolution

The existing completed theorem proves finite kernels and kernels with a specified common measurable finite-mass exhaustion, including every probability-kernel consumer in PT11. Its full four-step proof was read: the finite complement calculation, product pi-lambda argument, prescribed simple approximation, common exhaustion and signed finite-integrability locus are valid. The unrestricted reading of the old design sentence is false, not an absent theorem to import.

Here is a complete counterexample under the already authorized AC. Let both measurable spaces be the real line with its Lebesgue sigma-algebra. Take a non-Lebesgue-measurable set V from the fully read published `thm-vitali-sets-exist-under-choice-on-r-over-q` and `thm-a-vitali-set-is-not-lebesgue-measurable`. Define a measure K(x,.) as counting measure on R with the point x omitted when x belongs to V, and as full counting measure otherwise. This is a set-defined family: membership in V is allowed in its definition without assuming that indicator measurable.

For each fixed x this is a measure, even on the full power set. Counting measure means the finite cardinality on finite sets and infinity on infinite sets. For disjoint sets, finite union cardinalities add. If their union is infinite, either one set is infinite or their finite cardinalities have unbounded partial sums; in either case countable additivity gives infinity. Restricting this measure to R minus one specified point preserves countable additivity.

For each fixed measurable A, the evaluation x -> K(x,A) is measurable. If A is infinite, removing at most one point leaves it infinite, so this function is constantly infinity. If A is finite, it equals |A|-1 on the finite set A intersect V and |A| elsewhere. Every finite subset of R is Lebesgue measurable. This proves all kernel evaluation conditions, despite V itself being nonmeasurable.

The diagonal D={(x,y):x=y} belongs to the product sigma-algebra. Explicitly, its complement is the countable union over rational q of the measurable rectangles (-infinity,q) times (q,infinity), and (q,infinity) times (-infinity,q): a strict inequality between two reals admits an intermediate rational. Hence f=1_D is a nonnegative product-measurable function. Its section at x is the singleton {x}, so the section integral equals zero on V and one off V. Its inverse image of {0} is V, which is not measurable. Thus the unrestricted joint-integrand assertion fails even for a bounded nonnegative jointly measurable f, and even though every fixed-set kernel evaluation is measurable.

Operator disposition: make the intended finite/common-exhaustion hypotheses explicit in PT11's design sentence, retaining the general measure-kernel definition and the complete valid theorem. No probability, composition, conditional-law or disintegration claim is removed. No new pair or unproved supplier is added. This counterexample is an operator scope argument, not an independently reviewed publication item. The existing theorem remains choice-free; AC is spent only to produce this counterexample's V through the two named published results.

Source honesty: root searched for authoritative general-kernel treatments; an attempted BNU lecture-PDF opening failed, so no reading of that PDF is claimed. This counterexample was derived and checked locally from the stated measures and the two fully read published Vitali proofs. It is not attributed to a search snippet or to the failed PDF.

The separate Gaussian proof-route obligation is also resolved: the complete real integration-by-parts/ODE proof preserves the exact N(m,sigma^2) transform including sigma=0. Replace the design's procedural instruction to complete the square with this actual proof route; no contour integral or changed conclusion is accepted.
