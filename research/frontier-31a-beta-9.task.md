# Batch 9 of run `frontier-31a` — Tor Flatness and Global Dimension · Universal Coefficients and Kunneth Theorems

2 A/B pairs, homological-algebra. You own every page here and you author them all at step 5.

## `tor-flatness-and-global-dimension`

| | |
|---|---|
| A page | `tor-flatness-and-global-dimension` · order **365.055** · `homological-algebra` |
| B page | `tor-flatness-and-global-dimension-examples` |
| title | Tor Flatness and Global Dimension |
| requires | `yoneda-extensions-and-homological-dimension-examples`, `flatness-and-faithful-flatness` |
| design | `research/plan-homological-algebra-track.md` L2126 |

**Open the design section and hold yourself to what it states.** The traps, the decided conventions and the chosen route for this page are written there, by whoever designed it, with the sources to hand. They are not repeated here: this file is generated and has read no mathematics. A scaffold that contradicts a decision the design records is wrong however rich it looks.

Check the design's stated `order` and prerequisites against `research/plan-spec.json` above. **If they disagree, do not pick one** — record it in your notes and carry on with the spec. Stage 1's `drift` unit adjudicates design-vs-spec conflicts for the whole run, and settling one inside a batch is how two batches end up built against two different plans.

## `universal-coefficients-and-kunneth-theorems`

| | |
|---|---|
| A page | `universal-coefficients-and-kunneth-theorems` · order **365.057** · `homological-algebra` |
| B page | `universal-coefficients-and-kunneth-theorems-examples` |
| title | Universal Coefficients and Kunneth Theorems |
| requires | `tor-flatness-and-global-dimension-examples` |
| design | `research/plan-homological-algebra-track.md` L2284 |

**Open the design section and hold yourself to what it states.** The traps, the decided conventions and the chosen route for this page are written there, by whoever designed it, with the sources to hand. They are not repeated here: this file is generated and has read no mathematics. A scaffold that contradicts a decision the design records is wrong however rich it looks.

Check the design's stated `order` and prerequisites against `research/plan-spec.json` above. **If they disagree, do not pick one** — record it in your notes and carry on with the spec. Stage 1's `drift` unit adjudicates design-vs-spec conflicts for the whole run, and settling one inside a batch is how two batches end up built against two different plans.

## Standing, for every pair here

- **Two independent treatments**, at least one a textbook, monograph or full lecture-note set with a harvestable table of contents. Wikipedia is a convention tiebreaker, never a pair's backing.
- **Enumerate each source's own section and named-result headings** over the exact range you read, and give every harvested heading a disposition: `included` (naming the item id), `inline`, `already-published`, `deferred` or `out-of-scope` — the last two with a written reason about that specific result.
- **Self-contained scope.** No item may rest on a result the library has not established. If a needed result is missing, build the prerequisite definitions and theorems; dropping it for want of a lemma you could have written is not a permitted disposition.
- **An A page over 60 items is SPLIT**, decided now. `validate-plan.mjs` enforces it, and after authoring a split is a rewrite.
- **Never pad.** Decompose long proofs into focused lemmas and make a pass for cheaply proved corollaries; never mint a statement to enrich a page.

**No permission prompts of any kind**, including inside an `&&` chain.

## Owner-authorized recovery directive

The first author and first recovery both returned an empty contract instead of
performing this task. That is not a valid completion. All 29 direct
outside-batch dependencies now exist as current `items/*.md` files and may be
read normally. Author all 88 manifest items and all four pages in this batch,
in manifest order, then replace the empty proof-contract report with contracts
for every proof-bearing item. Do not stop merely because the batch is large,
because files are initially absent, or because an earlier note records the now
resolved dependency blocker. Preserve the full manifest scope; an empty or
partial proof-contract report is an incomplete artifact.

### Substantive recovery pass: Tor and flatness pair

All 88 carriers now exist, but the 75 proof-bearing items use only three generic
two-step bodies and therefore establish none of their claims. In this pass,
substantively reauthor every proof-bearing item on
`tor-flatness-and-global-dimension` and
`tor-flatness-and-global-dimension-examples`. Each theorem, example, false
statement, and counterexample must name its actual modules, complexes, maps,
kernel/image calculation, or witness; no shared "unpack the definitions" body
is acceptable. Preserve the UCT/Künneth carriers for the next scoped pass and
state that remaining work honestly. Regenerate contract entries made stale by
the Tor repairs; do not claim the whole batch mathematically complete while the
UCT/Künneth templates remain.

### Substantive recovery pass: UCT and Künneth pair

The Tor/flatness pair is now substantively authored and must be preserved. The
remaining 35 generic proof bodies are exactly the 28 proof-bearing items on
`universal-coefficients-and-kunneth-theorems` and the seven on its examples
page. Replace every one with an item-specific proof, refutation, computation,
or counterexample that names the actual complexes, coefficient modules, maps,
short exact sequences, Tor/Ext terms, splittings, signs, or witnesses. Rebuild
the proof-contract report to include all 75 proof-bearing items across the
whole batch, and do not claim completion if any of the three former generic
body phrases remains.
