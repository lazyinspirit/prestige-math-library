# Explicit averaged Carleson model reconstruction

The existing reconstruction lemma now precisely transfers uniform finite-model strong or weak(p,p) estimates,1<p<infinity, to the real-line maximal operator on Schwartz input. It includes the originally intended weak-L2 and all-strong-Lp interfaces and proves that original-grid estimates imply all translated/reciprocally dilated-grid estimates by coordinate conjugation. Root was notified before this statement clarification.

Read Lacey section2 in full, including the assumed distribution multiplier characterization and its averaging argument. The local repair avoids those assumptions. Fully read the direct packet/cutoff definitions, Schwartz convolution/inversion, sigma-finite Tonelli/Fubini, dominated/monotone convergence, complex Holder and layer-cake suppliers.

At scales l=r2^k, -K<=k<=K, average spatial shifts over[0,r2^K] and frequency shifts over[0,2^K/r]. Each is a common period for every retained scale. Spatial averaging produces the autocorrelation convolution multiplier psi(l(xi−nu))². Frequency averaging with the actual value N(x) gives H(l(N(x)−xi)), where H(t)=integral_[1/4,3/4] psi(u−t)²du. Averaging r in[1,2] with dr/(r log2) yields m_K(d)=(log2)^−1 integral_[2^-K,2^(K+1)]H(td)dt/t. It increases to kappa*1_(d>0), with kappa positive and finite: H is supported[1/8,7/8] and is at least2/9 on[3/8,5/8]. Both explicit lower and upper bounds for kappa are proved.

At each fixed finite scale range, spatial/frequency index truncations have a pointwise majorant C_K||f||2, uniformly over all parameters and x. This justifies limits and testing averages without assuming an infinite model is already bounded. The later multiplier limit is dominated by kappa|hat f|. All absolute-integrability/Fubini and bounded-compact-test limits are recorded. The final testing-to-norm or testing-to-weak conversions are explicit, followed by finite rational selectors and monotone maxima. No distribution theory, unproved operator classification or pointwise domination by one grid is used.

Manifest and exact contracts synchronized; no new items, source attempts, source waivers or owner receipts. Strong-L2 extension and normalized torus transference remain to be authored.

Checks: explicit-path canonical precheck, strict selected-item contract, renderer and batch5 content-policy27/27 pass. Local authorship and checks are not independent mathematical review.
