# FA terminal evidence — queue e, position 3

Decision: repaired. Read the full current item, all three cited dependency statements/proofs, the already inspected batch-18 A/B pages and conventions, manifest/coverage context, proof contract/boundaries, both Terra rejections, and Alpha's adjudication and repair note. The old risk material consisted of generic boundary rows and routine proof steps; neither justified the tilt nor checked the index-two case.

Terra correctly rejects the assertion that every connecting pair has a finite slice. The maximum-to-minimum index drop is two, so its unparametrized slice is one-dimensional. More fundamentally, the previous proof assumed its principal transversality conclusion in the Given clause. I replaced that with an explicit tilt and direct barrier calculation.

The embedding is the upright radius-(2,1) torus followed by a rigid rotation. The new height is (A sin u+epsilon sin v)/S, A=2+cos v, S=sqrt(1+epsilon²), 0<epsilon<1/2. Rotation preserves the induced metric diag(A²,1). Differentiation yields the stated ODE. Solving its zeros gives exactly four points; the diagonal Hessian has nonzero entries, with vv entry ±1. The upper saddle lies in the interior of pi<=v<=2pi, and its unstable branches cannot leave this forward-invariant strip because the field points strictly inward on both boundaries. The lower saddle lies outside the strip. This excludes the only possible saddle connection; strict height descent excludes its reverse. All remaining surface intersections are either empty, involve an open extremal basin, or are complementary eigenspaces at a critical point. Therefore the pair is Morse--Smale for every stated epsilon, without a genericity assumption.

The final step retains only the licensed index-drop-minus-one slice dimensions. The two open extremal basins have a nonempty open intersection: their complements consist of the finitely many one-dimensional saddle branches and critical points (compact gradient descent has critical endpoints), which cannot cover a surface. Hence the maximum-to-minimum slice is nonempty and one-dimensional, not finite. No blanket compactness/finiteness conclusion remains.

Source verification: opened the underlying PDF https://math.berkeley.edu/~nm.eagles/notes/Morse_Homology_Notes-NME.pdf, §1.1, printed p. 3 and beginning p. 4. Its torus example supports the tilted induced-metric pair, and the following paragraph states the index-drop-minus-one dimension. The explicit ODE, invariant strip and Hessian verification are my independent calculation. Audin–Damian §2.2.d describes a field perturbation, so that citation alone was insufficient for a rigid-tilt argument; the directly relevant source was added.

Validation: focused precheck and strict batch proof-contract checks passed with zero failures/errors/warnings. Both contract copies now contain the actual derivations, exact current citation statements, and case-specific risk evidence. No additional dependency edit, judge call, or pass stamp.

Obligations: none for position 3. Next: record these bytes, then begin position 4.

Context refresh at position 4: the repaired Baire sibling explicitly fixes closed finite-dimensional M and smooth Morse f. It is consistent with the tilted torus and is not used to assume the example's transversality. Current item and dependency bytes were rechecked unchanged.

Final sibling-context review: queue position 8 now states the relative field theorem for sufficiently small fixed critical balls inside larger Morse charts, with distinct critical values, and proves it by descending stable destinations. Its coverage no longer imports the stronger arbitrary prescribed-zero-region metric theorem. This is consistent with the batch-18 page conventions. The present item does not use that relative theorem in its proof; its mathematical decision and item bytes remain unchanged. Refreshing this receipt binds it to the final sibling statements after the authorized sequential repairs.
