# frontier-33 — Step-5/6 gate repair audit

Dispatch: `gate-batch-1-all`. Scope: the 20 frontier-33 batches listed in the generated task. This report records Alpha repair evidence only; it is not a judge verdict, publication stamp, or stage transition.

## Gate-failure dispositions

- `depcheck`: confirmed mechanical YAML defects on five B pages. The literal `\u2014` sequences in double-quoted titles were replaced by the actual em dash on the weak-laws, classical-zero-free-region, decomposition/inertia, co-Bird, and category-O example pages. The matching manifest titles already denoted the em dash and needed no semantic change.
- `extcheck`: confirmed structural citation defects on `rem-carleson-hunt-almost-everywhere-convergence`, `rem-kolmogorov-block-polynomials-have-large-partial-sum-maxima`, and `rem-kolmogorov-lone-fourier-series-diverges-almost-everywhere`. Their existing Laugesen/Grafakos reference data were changed from inline maps to the block-list form required by `extcheck`; titles, URLs, locators, claims, and external-dependency records are unchanged.
- `proof-contract`: confirmed the malformed exponent `\widehat{\mathfrak m}^{,2}` in `thm-completion-preserves-regular-local-rings` F1 and the three matching stale batch-7 citation quotations. The item and quotations now reproduce `lem-completion-preserves-embedding-dimension` exactly as `\widehat{\mathfrak m}^2`.
- `citation-fidelity`: the sole widening candidate, `ex-fourier-partial-sum-operators-and-uniform-boundedness` F1, is upheld after item-specific review. The cited statement quantifies the norm identity over `N>=0`; its separate `N>=1` lower bound implies unboundedness of that same family, so the restatement does not widen the source. The contract records this reason.
- `finite-smoke` liveness: `ex-decomposition-groups-in-a-tower` now carries `cyclic-subgroup-lagrange` with `max_modulus: 4` and the exact assertion “Thus restriction has kernel the order-two subgroup generated by exponent 4.” The check independently verifies the finite cyclic-subgroup arithmetic through order four; it is falsification evidence, not proof.

## Boundary audit

All 96 repeated-text clusters (1,897 rows across 566 items) were reconciled with their current carriers. Each retained disposition now identifies the carrier and preserves its original axis-specific evidence or reason; no row was converted merely to silence the detector.

Two contradicted `empty` rows were genuinely stale and are now `checked`:

- `lem-cantor-cylinder-masses`: steps 1.1 and 2.1 explicitly treat `m=0`, whose digit sums are empty, cylinder is `[0,1]`, and mass is one.
- `lem-skolem-witness-closure-on-a-cardinal`: steps 1.1 and 2.2 show that the Skolem-function family contains the constant-zero function and require nonzero `alpha` before elementarity.

The other 23 detector candidates were upheld individually in their owning contracts: `lem-finite-truncations-are-dense-in-c0-and-ell-one`, `thm-dual-of-c0-is-ell-one`, `thm-complex-dual-of-ell-one-is-ell-infinity`, `cor-iid-finite-variance-weak-law`, `thm-khinchin-weak-law-for-iid-integrable-variables`, `thm-kolmogorov-maximal-inequality`, `thm-kolmogorov-convergence-criterion`, `cor-kolmogorov-two-series-sufficiency`, `lem-kronecker-summation-lemma`, `thm-kolmogorov-strong-law-under-summable-normalized-variances`, `cor-independent-nonidentical-finite-variance-strong-law`, `lem-one-sided-maximal-inequality-for-symmetric-independent-sums`, `lem-largest-summand-bound-for-symmetric-independent-variables`, `thm-truncated-centering-criterion-for-an-iid-weak-law`, `lem-levy-maximal-inequality-for-independent-tail-sums`, `thm-independent-series-probability-and-almost-sure-convergence-agree`, `lem-logarithmic-integral-asymptotic-expansion`, `thm-clifford-homogeneous-restriction-formula`, `lem-induction-from-the-inertia-group-recovers-the-module`, `fs-nonconstructive-expanders-suffice-for-uniform-reductions`, `thm-sum-check-soundness`, `lem-sum-check-verifier-is-polynomial-time-given-point-evaluation`, and `prop-arithmetized-formula-counting-sum`. Each contract records the exact nonempty fixed index, positive endpoint, identity coset, existing constituent, or positive-round hypothesis that makes the detector inapplicable.

Current boundary result: 6,064 rows scanned across 20 contracts; 0 template clusters, 0 contradicted candidates, 23 upheld reviews, and 0 unauthored items.

## Frozen reader/refuter evidence recheck

- `lem-cauchy-in-probability-sequences-have-a-measurable-limit`: the refuter's zero-index finding was live. Step 1.1 now sets `n_0=1`, constructs `n_k` for `k>=1`, and asserts successive-difference bounds only for `k>=1`; no undefined `Y_0` remains. Batch-3 contract derivation and author receipt were refreshed.
- `ex-from-psi-to-the-logarithmic-integral`: the reader's unlicensed-inference finding was live. The circular citation to the final `pi-Li` theorem was replaced by the exact quantitative psi error, psi/theta prime-power comparison, Chebyshev partial-summation identity, and logarithmic-integral definition. The item, batch-5 manifest, citation/derivation contract, notes, and author receipt agree.
- `prop-simultaneous-attachment-at-a-morse-critical-value`: the refuter's one-critical-point mismatch was live. The inapplicable one-critical-point transport citation was removed. The proof now cites `lem-local-critical-value-lowering-preserves-the-upper-sublevel` for the simultaneous disjoint modifications, and steps 2.1 and 3.1 use `thm-regular-interval-diffeomorphism` only on the explicitly critical-point-free complementary and lower bands; the item, batch-16 manifest, contract, notes, and author receipt agree.
- `category-o-finiteness-duality-and-blocks-examples` and `thm-completion-preserves-regular-local-rings`: the frozen findings are closed by the title and exponent repairs above.
- `def-tensor-product-total-complex-of-chain-complexes`: confirmed but unowned. The published dependency starts with arbitrary chain complexes while calling `\bigoplus_{p+q=n}` a finite-diagonal direct sum; without boundedness the diagonal can be infinite. The assigned batch-6 consumer is bounded above and does not require that false parenthetical, but repairing the published dependency requires owner/published-content authority to delete “finite-diagonal” or add a boundedness hypothesis. No content workaround or out-of-scope edit was made.

## Current validation

- `depcheck --quiet`: exit 0; no cycles, unresolved references, draft-on-published-page errors, or YAML errors.
- `extcheck --quiet`: exit 0; all recorded-not-proved statements have recognized citations and valid external-reference structure.
- strict `proof-contract`: all 20 batch contracts pass; two pre-existing advisory shotgun-bracket warnings remain nonfatal and were not among the assigned failures.
- `citation-fidelity --fail-on-missing-quote`: 2,242 citations over 758 authored items; no missing quotation and no widening candidate; one item-specific upheld review.
- `boundary-audit --fail-on-contradicted --fail-on-template`: exit 0 with the counts recorded above.
- scoped `finite-smoke`: one check executed and passed for `ex-decomposition-groups-in-a-tower`.
- Reflow reported all seven materially edited items unchanged. Focused precheck passed the four proof-bearing repairs; focused render checks passed all edited items/pages. Scoped content-policy and manifest-dependency checks passed, and the current plan remains acyclic with no item cycle, forward reference, B-page dependency, or unresolved id.
- All 20 `frontier-33-author-check-*.json` receipts were refreshed from current files and report `ok: true`. Frozen reader/refuter reports were not rerun or rewritten.

No gate ledger or supplemental 6b decision was written because this dispatch is at stage `5-author`; the append-only decision/ledger interface in `briefs/tasks/alpha-step6-gate.md` applies at 6b. The unowned published dependency above remains the only authority-bound issue.
