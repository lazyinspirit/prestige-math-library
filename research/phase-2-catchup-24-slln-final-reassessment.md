# SLLN pair — independent final reassessment

Run `phase-2-catchup-24`, 2026-09-09. Scope: only
`strong-laws-of-large-numbers` and its companion (16 A / 7 B), with their
actual prerequisite clauses. No owner override, Gaussian review, publication,
or all-published-ancestor certification. The final decision below concerns
adequacy of these scaffold proof contracts, not completed authored proofs.

## Prior finding and actual additional repairs

Read the complete Alpha-a final recheck and current pair, not just its old
decision. The inverse-tangent page prerequisite is now present in the live
manifest, canonical plan and PT6 prose. The 12:43 escalation's reason is
therefore stale even though it signed the then-current bytes.

Independent examination found two further exact interface omissions, repaired
under root's explicit bounded authorization before considering acceptance:

- `lem-summability-of-truncated-normalized-variances` invoked integral MCT
  but declared the monotone-real-sequence theorem. It now declares
  `thm-monotone-convergence-for-the-integral` and specifies the increasing
  finite sums of nonnegative measurable functions used in the interchange.
- The six AC-qualified independent-copy consumers now declare countable
  choice, dependent choice and the earlier ordinary recursion theorem. Their
  local proofs restrict AC for CC, select a successor function on a serial
  relation using AC, and iterate it from the prescribed initial point to
  prove DC. This exactly matches the published independent-copy supplier;
  no later A665 implication is imported. The six are the IID-shift corollary,
  heads frequency, integrable-mean estimator, nonidentical-variance example,
  weak-not-strong-law example and infinite-absolute-mean Cauchy example.

Only these seven item objects and their coverage proof plans changed in batch3.
PT6 prose records the repaired interfaces and replaces its stale cubic Etemadi
recipe by the actual geometric-subsequence argument. No other batch3 pair
changed. Historical strategy comments about the previously defective L2/rate
clauses are superseded by the published repairs described next; those clauses
are not premises of the current pair's arguments.

## Mathematical reassessment

The current published convergence criterion proves its almost-sure result
from maximal tails and one measurable Cauchy event, and now proves L2 convergence
to that very limit by Fatou without representative choice. The normalized-
variance theorem proves the general assertion by measurable centering/scaling
and pathwise Kronecker; its logarithmic rate also has a complete earlier-only
dyadic proof. Read the actual repaired files and Kronecker's proof.

Checked all 23 current contracts. Tail-sum bounds retain strict tails and
infinite values. For truncation, t² sum_{n>=ceil(t)}n^-2 <=2t (with the
separate t<=1 branch) gives the claimed constant2; the integral MCT repair
makes its use precise. BC1, dominated convergence and Cesaro averaging then
give the mutual-independence L1 law. Necessity permits an initially random
finite limit: X_n/n tends to0, BC2 forces tail summability, and the proved
law identifies that limit.

Etemadi uses positive/negative parts, geometric floor subsequences, variance
addition requiring only pairwise independence, and the geometric-tail bound
C_alpha/m². Nonnegative partial sums interpolate; the countable family
alpha=1+1/r gives the mean limit. It never invokes mutual-independence random
series for a pairwise-independent family. The logarithmic rate separately
uses positive monotone normalizers and the summable dyadic p-series bound.

The maximal ergodic proof establishes its inequality on the whole space,
including the complement of its positivity set, so integration cancels by
invariance. The local ergodic theorem uses exact invariance of the extended
limsup, the maximal lemma on (h-epsilon)1_D, ergodicity, and both signs.
Bounded truncation plus integral contraction proves L1 convergence. The shift
is measurable and preserving on cylinders, then on the generated sigma-algebra;
strictly invariant events are tail events, giving ergodicity by zero-one.
This route is independent of the IID strong law. Read the actual earlier
systems-pair preservation, integral-invariance and ergodicity contracts.

The examples preserve their advertised distinctions. The Pareto example has
mean3 and infinite second moment. The rare-event example uses telescoping
summands, so it is genuinely a sample-mean counterexample. The Cauchy example
constructs its law and divergent absolute mean before applying necessity.
Its displayed partial fractions satisfy the polynomial identity and give
the scale-addition density; the exceptional x=0,a=b point is null. Logarithms
cancel on finite truncations before taking limits. Compact FTC calculations,
Riemann/Lebesgue agreement under CC, monotone exhaustion and interval
pi-system uniqueness supply the analytic interfaces, including sum-density
and scaling. No characteristic-function or future normal-law theorem is used.

## Source evidence and checks

Read complete Durrett fifth-edition Jan.11,2019 PDF proofs: Theorem2.4.1 and
Lemmas2.4.2–4, printed76–78; Theorems2.5.5–6,2.5.9,2.5.11, printed84–87;
Lemma6.2.2 and Theorem6.2.1, printed335–337. Full local file:
`/tmp/uc34-prob-durrett.pdf`, recovered from
https://sites.math.duke.edu/~rtd/PTE/PTE5_011119.pdf.
Recovered and read Roch Note5 pp5–6 from
https://people.math.wisc.edu/~roch/grad-prob/gradprob-notes5.pdf.
Roch's pairwise random-series invocation and its erroneous printed primitive
factor are not used: the pair uses Durrett's geometric Etemadi proof and its
own dyadic rate estimate. Read the full published integral-MCT, recursion,
independent-copy, inverse-tangent and Riemann/Lebesgue agreement proofs.

Scoped direct-order and page-closure check: **157 edges, zero errors**, using
the actual published natural-number page header for the recursion theorem
where canonical inventory is empty. Content-policy with the required batch1
systems supplier context: **89 items, zero errors/warnings**. Checking batch3
alone initially reported six missing cross-batch supplier IDs; supplying their
real batch1 context resolves them, not deleting the dependencies. Coverage
check: **44 rows, zero errors/warnings**. Source-fetch: **5/5 existing verified
receipts**, no fabricated stamp or source drop. Scoped whitespace check passes.
Those broader tool input files supply context only; mathematical assessment
and edits remain limited to this SLLN pair.

## Final disposition

**Repaired, non-owner confidence1.** Root synchronized the seven exact item
objects. A fresh comparison confirms both canonical A/B inventories and
requires arrays match the live pair exactly; counts remain16/7. Source-backing
also passes (30 authored results in the contextual batch3 input).
The fresh current scaffold hash is
`878f5dae33f2a0ef47fd9cde6fee6bc192d67542e2f9d2dbd62db1aaf7dcb506`.
Record the final non-owner repaired decision with `scaffold-resolution.mjs`
against this hash. No remaining substantive gap was found in the current
statements and complete local proof contracts. This supersedes the stale
inverse-tangent escalation; no owner override is used.
