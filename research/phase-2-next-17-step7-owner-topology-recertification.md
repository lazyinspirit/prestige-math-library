# Step 7 owner repair: topology B-leaf interfaces

The Step 7 preflight `depcheck` found load-bearing citations from nine run items to two published B/examples leaves. Each item had an exact `confirmed_fatal` Step-6 adjudication; this owner repair remains within those licensed items. The two B examples remain published and unchanged. I read the affected arguments, the pair-sequence and sphere-homology suppliers, and the relevant local proof steps before replacing the citations.

- `lem-integral-surface-cup-pairing-from-the-oriented-polygon` and `ex-integral-cohomology-ring-of-an-orientable-surface`: the polygon boundary's all-ones edge chain is primitive by the cellular boundary calculation; the pair connector is an isomorphism because the polygon contracts. Its unique preimage is the positive relative generator, so the signed fan normalization does not need the disk example.
- `ex-integral-cohomology-ring-of-a-torus` and `prop-the-first-hurewicz-map-in-degree-one-is-abelianization`: the oriented triangle-boundary cycle is primitive in the simplicial circle and maps to the singular generator through the comparison used in `cor-homology-of-spheres`.
- `cex-a-homology-equivalence-need-not-be-a-homotopy-equivalence-without-simple-connectivity`: disk contractibility and the pair exact sequence make the connector to $H_1(S^1)$ an isomorphism. Its unique positive preimage gives the attaching vector $2\alpha-3\beta$ by naturality and degree-one Hurewicz additivity.
- `thm-top-homology-characterizes-compact-orientable-manifolds`: the pair connector identifies a relative disk generator with the positive sphere boundary cycle; for $n=1$ the target is the augmentation kernel generated by $(1,-1)$. Change of coefficients commutes with the connector, so coefficient one maps to the $R$-module generator.
- `thm-alexander-duality-for-compact-locally-contractible-subsets-of-a-sphere` and `ex-fundamental-classes-and-duality-for-spheres-and-tori`: radially project the alternating boundary of an oriented simplex to the sphere. The sphere-homology proof makes it a primitive singular generator; its outward local coefficient fixes the boundary-orientation sign.
- `thm-jordan-brouwer-separation`: the stereographic chart and its inverse are now displayed and checked directly, including the limit that identifies the unbounded Euclidean complementary component.

The nine item frontmatters, batch-5/6 manifests and proof contracts were synchronized. Focused precheck passed 9/9; the strict proof contracts for all nine passed. Repository `depcheck` and `fwdcheck` pass. The plan was refreshed and `splice-plan --verify` reports agreement on all 34 pages. The full merged proof contract reports 557/557 checked with zero errors; risk-report, boundary-audit and citation-fidelity pass. The remaining rejudge is still pending and will test these owner repairs independently.

This receipt recertifies the changed carriers of `lem-integral-surface-cup-pairing-from-the-oriented-polygon`, an item originally authored by a Step 3 auditor and previously promoted by a Step 7 auditor. It does not claim that structural checks establish mathematical validity or that a new judge verdict already exists.
