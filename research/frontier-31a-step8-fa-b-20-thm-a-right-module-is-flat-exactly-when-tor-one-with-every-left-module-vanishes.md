# FA terminal evidence — queue position 20

Disposition: repaired. Source status: familiar.

Reviewed the current item, all three direct dependencies, balanced Tor, the Tor-zero proposition, published arbitrary-ring tensor universal property, Sol adjudication, initial wrong-handed LES rejection and final Tor-zero rejection, batch-9 Tor A/B context, conventions, coverage and proof/risk contract. Sol has correctly changed the LES to the left-module variable and uses a full projective resolution. Terra correctly asks for the Tor0 identification in the actual argument. The full LES proof mentions its tensor tail, but the standalone proof benefits from an explicit natural identification.

Added a balanced-map cokernel proof of arbitrary-ring right exactness and applied it to P1→P0→X→0. This gives H0(N tensor P)=N tensor X, commuting with comparison maps via augmentations. The balanced Tor definition identifies it with Tor0 and has been added explicitly. The left-variable LES then supplies image(Tor1(N,P/K))=kernel(N tensor K→N tensor P) as claimed. Universal vanishing makes every inclusion injective; the cokernel argument supplies the remaining exactness. The forward implication follows from preservation of the entire augmented left resolution by a flat N. These arguments cover zero objects and the zero ring, keep the right/left module order, and do not confuse an arbitrary ambient module P with a projective one.

The published Tor-zero proof cites a commutative-only right-exactness theorem; it was inspected but is not added or relied on. The local universal-property calculation avoids any prerequisite edit. This elementary flatness test is familiar and fully checked against the local algebra, so no external source verification was needed. Updated only this item, its manifest deps, and batch/merged contracts including stale presentation-based boundary records. Focused precheck precedes recording. Next action is position 21 only after the receipt; no unresolved obligation remains here.

Context refresh after position 26: The weak-dimension peer now explicitly assumes DC and supplied resolutions on both hands and takes suprema in N union infinity with sup empty=0. Earlier tensor/Tor calculations, flatness criteria and balance do not depend on this dimension corollary; their formulas and hypotheses remain unchanged. Own item bytes are unchanged. Reseal the same decision.
