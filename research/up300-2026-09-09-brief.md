# U-P scan — 300 items, 2026-09-09

Read CLAUDE.md and README.md fully before repository work. Scan only your
100 assigned items in `research/up300-2026-09-09-assignments.json`, in order.
The selection is the first 300 current U-P rows, distributed round-robin.
Old scan files are historical; do not edit or run their synchronization script.

Be unbiased: the U-P label, historical concerns and old judgment stamps are
leads, not verdicts. Read ONE complete current item at a time, including its
proof/refutation and remarks. Check the actual used supplier interfaces and
relevant supplier proofs, implicit prerequisites, hypotheses, quantifiers,
boundary cases and choice scope. Finish and record its result before opening
the next assigned target. Reading suppliers is allowed during that target.

For unfamiliar or uncertain mathematics, search the web and read the full
relevant passage in authoritative sources (original papers, author/university
texts, Stacks, etc.). Record actual URLs and locators. Snippets and inaccessible
links are not source reading. Investigate uncertainty; neither source failure
nor lack of confidence alone proves a defect. Report an unresolved issue to
root rather than invent a binary verdict or silently skip the item.

Completed classifications:

- `clear`: free of defects within the complete item and used-interface scan.
  Implicit but correct use of adequate already-published dependencies is not
  a defect under the owner's scan policy; identify those suppliers. Do not
  call an accepted item repaired or claim its whole transitive closure audited.
- `U-C`: confirmed defective, with repair audit pending. Give exact location,
  mathematical evidence and inadequate/unmet premise. A necessary unpublished
  Phase-2 supplier does not close the defect. Include other mathematical defects.
  Do not classify as A-P or repair the item during this scan.

Assume AC wherever necessary as authorized. A false choice-free claim or use
of an insufficient weaker principle remains a defect. Preserve explicit choice
scope and never use Recorded, Not Proved Here as proof supply. False-statement
items need valid refutations, not true headlines. Do not infer defects solely
from unrelated content on a prerequisite page or harmless orientation links.

For U-C, record exact published/planned supplier IDs and availability, Phase-2
pair/prose location where known, and a concise recommended repair strategy.
Tell root if necessary machinery is absent from Phase 2; root reconciles scope.

Write ONLY `research/up300-2026-09-09-sol-N.json` for your assigned N, using
apply_patch. Initialize {"agent":N,"results":[]}. After EACH item append:

    {"id":"...","disposition":"clear or U-C","sha256":"current file hash",
     "scope":"full item and interfaces actually checked",
     "findings":["specific evidence, with proof locations"],
     "published_dependencies_checked":["IDs and used clauses"],
     "implicit_dependencies":["IDs, use and verified publication"],
     "phase2_suppliers":["IDs, homes, state, adequacy"],
     "repair_strategy":"empty for clear; concrete strategy for U-C",
     "source_reads":[{"url":"...","locator":"passage actually read"}],
     "limitations":"bounded scope, not whole-library certification"}

Validate your JSON after each append. Notify root after each result with ID,
classification and brief reason; root maintains the shared canonical ledger.
Recheck file bytes before recording; reread changes if another worker edited
the target. Do not prefill results or classify from string matching. No item,
page, plan, shared ledger, workflow, verification or historical receipt edits.
Do not spawn agents. Checkpoint in your own result file before compaction.
Continue through all 100; report exact completion and classification totals.
