# FA terminal evidence — queue e, position 7

Decision: repaired. Read the current item, all four direct dependencies, the consecutive-relative-homology and pair-exact-sequence results needed for the repair, batch-10 A/B and coverage/manifest context, both contract copies and their generic risk rows, Alpha's naturality adjudication/repair, and both Terra rejections. Alpha correctly restricts naturality to cellular maps. The final rejection correctly identifies the missing algebraic heart of the comparison theorem.

The replacement explicitly proves H_k(X^m;G)=0 for k>m by finite induction using concentration of consecutive relative groups and pair exactness. Thus i_n:H_n(X^n)->C_n is injective with image ker(partial_n), and i_(n-1) is injective too. Since d_n=i_(n-1) partial_n, this identifies ker(d_n) with i_n H_n(X^n). For n=0 the same identity follows directly from i_0 being the absolute-to-empty-relative isomorphism and d_0=0.

The next pair sequence ends C_(n+1) -> H_n(X^n) -> H_n(X^(n+1)) -> 0. The image of its connecting map corresponds under i_n exactly to im(d_(n+1)). Quotienting now proves H_n^cell = H_n(X^(n+1)), rather than merely asserting unidentified stable cycles/boundaries. Subsequent inclusions are isomorphisms on H_n, and the given finite-support colimit lemma identifies the stable group with H_n(X). This covers infinite-dimensional CW complexes and arbitrary abelian G. Naturality follows from commuting chain maps of skeletal pairs; the proof explicitly checks connecting maps by applying the chain boundary to a relative cycle representative. No cellular approximation of arbitrary continuous maps is assumed.

Added direct dependencies on the two exact results actually used and synchronized the item's manifest entry and both proof-contract copies. Neither dependency's bytes were edited, so no prerequisite-repair licence is needed. The statement now explicitly restricts n>=0, consistent with the reviewed cellular-homology definition.

This mathematics is familiar: the kernel/image diagram chase, relative homology concentration, and skeletal stabilization are standard algebraic topology arguments. I verified the maps and low-degree cases directly from the current local statements; no external verification was needed or claimed.

Validation: focused precheck and strict batch proof-contract checks passed with zero errors/warnings. The risk record now names the actual injections, lowest degree, infinite colimit and exact naturality scope. No judge call, verdict or pass stamp.

Obligations: none for position 7. Next: refresh earlier cellular receipts for the explicit degree scope, record position 7, then begin the final queued relative perturbation theorem.
