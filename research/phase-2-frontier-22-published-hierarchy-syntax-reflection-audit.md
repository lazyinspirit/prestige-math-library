# Frontier-22 published hierarchy, syntax and reflection audit

Date: 2026-09-11

## Scope and deduplication

This bounded audit covers sixteen previously unclassified published interfaces
used directly or immediately upstream of active group-h set-theory arguments.
Every complete item was read. Before adding rows, the canonical index and whole
ledger were searched by exact ID, title, aliases and the relevant mechanisms:
finite reflection/witness bounds, hierarchy/rank/Foundation, transitive closure,
and set-coded syntax/structure recursion. All sixteen IDs and titles were absent;
none declares an alias. Existing rows for the satisfaction recursion and
satisfaction-coincidence interfaces were retained unchanged.

This is not a complete audit of every transitive dependency and does not certify
the active draft consumers. No published content or workflow state was edited.

## Exact bounded dispositions

| Item | SHA-256 | Evidence |
|---|---|---|
| `def-set-signature-and-finite-syntax-strings` | `f50039977eae3a8f3e27325369db890d9bd362e56a5a4398ad525557162f51c6` | Tagged disjoint symbol sets and positive finite arities give a set alphabet. Finite words and concatenation are set-coded uniformly; empty and uncountable signatures are covered. |
| `def-set-coded-terms-and-formulas` | `e2f62b7e12a494b6f2a90f480263962e44d828d158a271c880ec2528ee9caedc` | Fully delimited constructor codes, natural-stage closure and a union over `omega` define the least term/formula sets. Every finite constructor list appears at a common stage, including the pure-membership boundary. |
| `lem-unique-parsing-of-set-coded-syntax` | `099cc1de7fda0f6d63fe584979056f3ea661bfeccf672878d73aa0ae792aeafa` | Parenthesis balance and balance-one separators recover the unique outer tag, fields and shorter child expressions. The delimiter scheme prevents symbol/child ambiguity. |
| `thm-structural-recursion-on-set-coded-syntax` | `c4b65b0d4cf7a6e6816e695427aa2017dc8e96c660856724605d6d96cb0641c5` | Strong induction gives structural induction. Natural recursion on set-valued partial graphs extends all expressions of the next length uniquely; the increasing union is the required set function and structural induction proves uniqueness. |
| `def-set-structures-and-variable-assignments` | `724fbd5e06e103b3a02cad4cc9c0e0b19af7e1a0679c1d129fa963d940d19818` | Nonempty set carriers, set-indexed interpretations, literal equality, assignments, updates and strong/embedding/isomorphism conventions are coherent. Positive function/relation arities agree with the signature interface; constants handle nullary operations separately. |
| `def-cumulative-hierarchy-stages` | `ebb0f687a280237c25e844ec442aa3b5643fa276fdc6a5f4b94c18a7c73c1461` | Set transfinite recursion on each ordinal interval defines the stages in ZF without Foundation; uniqueness gives overlap agreement, and the text correctly treats the global hierarchy as a definable class function. |
| `prop-cumulative-hierarchy-transitivity-and-growth` | `f32b70ebf310804b088aaa48afc8036620e002a5907e0b524158040b113ab369` | Simultaneous ordinal induction proves transitivity/nesting and `V_alpha cap Ord=alpha`. The successor and limit calculations are sound, and the final bounded-stage argument excludes both `alpha` and `V_alpha` from `V_alpha`. |
| `def-transitive-closure-of-a-set` | `a13224d7de328895dd39866c7f1ab521b906104f050055c5e394ac3d0493fc61` | Natural iteration of `x -> x union union(x)` plus Replacement and Union defines the least transitive superset without Foundation. The distinction between containing `a` as a subset and containing `a` as an element is explicit. |
| `prop-transitive-closure-minimality` | `9e540e09df02068a16a3c6baeca51ba094ab96f5351db516af272bd431805546` | Stage induction proves containment in every transitive superset; this immediately yields monotonicity, idempotence and the root-inclusive `a in TC({a})` clause used by HOD. |
| `thm-foundation-equivalent-to-hierarchy-exhaustion` | `8d7cb596e501c5ca657e8930c6ea7b8999206af3a5afa9acb29121faaca3e9be` | The transitive closure localizes Foundation-based class induction; membership induction supplies unique least stage indices and Replacement bounds them; conversely a least hierarchy height in a nonempty set yields a member disjoint from that set. No circular rank use occurs in the last direction. |
| `def-membership-rank-of-a-set` | `b41d3db53b11dd1fe9173c3f000e73105fe54bf2df497840943b204e7ba9cbf0` | The item explicitly assumes Foundation before specializing well-founded-relation rank to membership, retains setlikeness, and states the empty and strict-predecessor clauses. |
| `thm-membership-rank-and-hierarchy-levels` | `5d42c5e7ec6112a9fba423ce2778931519ccea01fd00de5dfb098b4b005fd626` | Successor and limit induction proves `x in V_alpha` exactly when `rank(x)<alpha`; the supremum rank equation then gives the subset, least-stage and successor-shell clauses, including empty sets and limit ordinals. |
| `prop-minimum-rank-selection-and-collection` | `79a29f60825a71d10ae0deb4600e7f4e1ca706a0036622e77b6c4f1c67473ddf` | A single class witness bounds a set of attained ranks, whose least value yields a set of minimum-rank members. Replacement collects the unique least ranks, so one hierarchy stage contains witnesses for every parameter without selecting a witness family. The converse Collection-to-Replacement argument uses functionality correctly. |
| `lem-finite-formula-witness-criterion` | `37622cfa54b881e9afad862e01ee5062988987855b3be42dce2d3c4aeb9d156c` | Atomic restricted membership agrees, Boolean constructors preserve agreement, and the pointwise witness condition supplies precisely the reverse existential direction. Subformula closure licenses every induction step; definable-class usage is schema-level. |
| `lem-finite-formula-witness-rank-bounds` | `fb7417272539b5cd5122f23f36d9215e78ba285c04caded9f7617bc73edf7019` | For the fixed finite formulas, unique least witness ranks or least witness-containing stages are definable functions of parameter tuples. Replacement collects only those ordinals and a successor supremum bounds witnesses; no witness family is selected and no Choice principle is used. |
| `thm-montague-levy-finite-reflection` | `b7a5cdb2a0f3ba0b54fbfc3898f06e53e5d9a3b471025610843935b515cd88c1` | Iterating the definable witness-bound function over `omega` and taking its supremum gives a limit stage closed under all witness requirements. Continuity handles an arbitrary increasing exhaustive hierarchy, while Foundation supplies exhaustion for `V`; the empty-class schema convention is explicit. This is the exact finite-reflection clause used by current constructibility/PMEA arguments. |

## Frontier disposition

No new published defect, Phase-2 supplier or live frontier blocker was found in
these exact clauses. The active group-h review also reported these interfaces as
adequate, but this receipt rests on the complete local item reads above. No fresh
external retrieval was necessary because the local proofs expose the relevant
choice, Foundation and schema boundaries and cite the corresponding textbook
sources. Broader formula-semantics and active-consumer correctness remain outside
this bounded receipt.
