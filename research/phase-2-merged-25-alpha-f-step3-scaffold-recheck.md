# Group f — final scaffold adjudication

Run: `phase-2-catchup-24`. Dispatch: `scaffold-final-f-5c7640ac32cf2386`. Date: 2026-09-09.

## Decisions

| Batch | A page | Decision | Ordinary verdict |
|---|---|---|---|
| 12 | `one-tape-simulation-normal-forms` | accept | sufficient |
| 13 | `boone-machine-simulation-and-fixed-presentation-undecidability` | accept | sufficient |

These decisions cover each A page and its B companion. No mathematical repair was required or applied. No owner-decision file existed when checked. The current source-status fix changes no claim or proof route. Mathematical acceptance does **not** waive the failed fresh URL-liveness/source-backing checks below or authorize publication. The driver retains those gates.

Read CLAUDE.md, README.md, SCHEMA.md, WORKFLOW.md, frontier-dependency-ledger instructions, the initial group-f review and ordinary verdicts, the two current manifests, coverage and fix notes, the current plan, the one-tape design at plan-computability-theory-track.md:3094 and the binding direct-Boone design at plan-group-theory-track.md:4621. Runtime status identifies the active run at Step 3, consistent with .autopilot/phase-2-catchup-24; HEAD observed 76afd32f7. Historical RESUME files were not used.

## Batch 12

All five owned contracts are sound. The extended definition reconstructs the configuration set and successor for the three-direction tuple; it does not silently instantiate a two-direction definition outside its domain. Separation inside the tape-function/product sets supplies each update. Finite adjunction plus the finite-subset theorem preserves support. Recursion applies to the total identity extension at halting states; only the genuine pre-halting prefix is called a computation. A least halting index is obtained by the stated bounded induction, without AC.

The macro has all entries required on nonhalting states of Q×{0,1}; only the two tag-zero designated outcomes halt. At an S instruction it writes the intended symbol, moves right into tag one, rewrites the neighbor unchanged, and moves left. Successor injectivity gives predecessor(h+1)=h including h=0. The tag-one copy of a halting target is still nonhalting. Conversely the only continuation from an interior state is the stipulated return, so no extra macro behavior occurs.

The theorem uses this exact lemma. Induction gives k_i=i+s_i, 0≤s_i≤i, and equality of the whole tape, head and state at boundaries. Each finite prefix is partitioned into lengths one and two; k_i≥i makes this cover every time of an infinite run. The three possible maximal outcomes therefore agree in both directions. An already-halting starting configuration gives t=s_t=0. The B-page accepting trace and empty-input trace are correct. Deleting entries violates totality; the separate complete S-to-R replacement witness changes acceptance to rejection, and L/R in that order fails at the origin.

The actual published supplier route was checked through the machine/configuration and initial-tape definitions, finite-support closure, natural-number recursion and induction, finite cardinality/pigeonhole, successor and additive order. The explicit natural-number construction uses Infinity and Separation, despite legacy ZFC wording in lem-omega-smallest-inductive; its argument spends no Choice. No old stay-put or multitape simulation theorem supplies this proof. The two page prerequisites are earlier and published. The three A items precede the two B leaves; the A count is 3, below 60.

## Batch 13

The thirteen A contracts retain the commissioned direct construction. The compiler preserves the dot at the original cell zero, separates writing from motion, clamps left movement by a no-op at the dot, and sends rejection to a total loop. The two-step invariant includes accepting targets and empty input v(epsilon)=blank. Its actual input is the published recognizer for HALT_TM, not the later simulation pair in batch 12.

The semigroup congruence is proved by contextual finite symmetric paths before multiplication of equivalence classes is used. The five instruction families and three cleanup families preserve the augmented configuration domain in both orientations. At the stopping state an empty right tape is allowed. Every nonterminal augmented configuration has a unique forward successor; in a shortest undirected path to q, a backward edge must eventually be followed by a forward edge and their valley can be deleted. Thus the halting converse does not assume reversible machine transitions. The finite semigroup and then the finite Boone presentation are fixed before the input varies.

The group definition includes the full conjugated-t commutator and same-order sharp operation. The base-group lemma uses the infinite cyclic reduced-word group, successive tape-letter HNN extensions, and the free product H*F(Qbar). Its retraction and the automorphism fixing H and sending q_a to F_i# q_a G_i prove the displayed associated sets are free bases. This is sufficient for the stated basis isomorphisms. The finite multiple-letter Britton argument is supplied internally by induction on extensions; no missing separate theorem is assumed. The normal-form comparison preserves the sequence of rule letters in reduced words.

The tower proves C=<x,r_i> free using A_i∩<x>=B_i∩<x>={1}. It centralizes the actual subgroup D=<C,q^-1 t q> at the last extension, which needs no assertion that those generators freely generate D. The local published stable-letter convention t c t^-1=phi(c) is translated by taking inverse stable letters where appropriate. The used HNN normal-form proof constructs the action on normal words before deriving uniqueness; Britton and base injectivity then supply the needed nontriviality clauses. Explicit AC supplies the transversal choices and propagates to the group-stage consumers.

For pushing, the exponents d=2^|V|-1 satisfy the doubling-plus-one recurrence. Both signs follow from the rule relation; inversion of the reversed positive left context gives the two sharp identities. This yields both orientations of contextual replacement, and the final centralizer calculation gives W=1. Extraction applies Britton first to k, then to an expression with minimal conjugated-t length; an internal t pinch of either sign removes two occurrences, while the first t pinch yields the auxiliary equation in the embedded base.

Reconstruction retains the stronger signed-word induction. The free-product comparison at zero rule length forces the terminal state and empty tapes. A central rule pinch at positive length identifies the unique twisted state generator. In the tape HNN group a wrong first surviving sign at the context interface would require x in <x^2>; infinite order excludes that. The left equation gives the mirrored endpoint condition. Empty residual words cause no cancellation. The x-inversion automorphism handles the reverse orientation, and the substituted tape words remain freely reduced before the shorter induction is applied. This proves the exported implication on positive special words. The special-word equivalence and the fixed-presentation theorem compose these exact directions; the last reduction is a finite copying/sign-change word procedure. All three B examples have the claimed calculations or specialization argument.

Published machine suppliers were checked through the fixed code, prefix parser, machine-description test, one-step interpreter, self-application lemma, acceptance diagonalization and halting reduction. The raw input alphabet can be binary with a distinct blank; numeric machine codes use the separately specified recoding into {1,2}, as in the self-application lemma. The concrete parser/finite-sweep proofs supply computability, not merely the weak abstract phrase “effective decoder.” The group route was checked through reduced free words, factor actions/free-product normal forms, HNN associated-subgroup conversion, coset multiplication/quotient well-definedness and normal closure. Identity/inverse preservation is the used clause of the group-homomorphism interface; its integer monoid example is not an additional machine-simulation premise. Only the finite clause of def-recursive-and-finite-group-presentations is used, as the scaffold explicitly states. No recursive-enumeration equivalence, recorded theorem, or co-resident example is imported to prove the replacement.

Every declared owned edge is earlier and adequate. Conservative edges to the tower and extraction/pushing stages resolve to sound preceding local contracts, with explicit inherited AC; they are not excused merely because the calculation might omit them. No edge was removed. All eight page prerequisites are earlier and published; A/B inventories are 13/3. There is no missing prerequisite pair, forward proof dependency, circular use of the replacement, or path to the deferred set-theory catalogue in the owned closure.

## Sources and scope

The following complete relevant text was independently read in this adjudication:

- Checkoway, [Lecture 16](https://checkoway.net/teaching/cs301/2018-spring/slides/Lecture-16-tm-variants.pdf), printed slides 2–10, PDF pages 2–19: the extended transition, tagged-state construction and three-outcome simulation argument.
- Savage, [Models of Computation, Chapter 5](https://cs.brown.edu/people/jsavage/book/pdfs/ModelsOfComputation_Chapter5.pdf), §5.1 pp.210–211 through the discussion preceding §5.1.1: independent textbook finite-control/tape and output conventions. Its abnormal-left-termination convention is not substituted for the local clamp.
- Rotman, [Chapter 12](https://math.uchicago.edu/~shmuel/lg-readings/Joseph%20J.%20Rotman%2C%20The%20Word%20Problem%20.pdf), pp.418–433 through sufficiency QED and pp.438–447 through Lemma 12.15 QED. The browser rejected the 13,656,201-byte body for size; the recovered complete 53-page local PDF/text in /tmp/uc19-rotman-EAIf3e supplied the reading. Page images 431 and 439 were also inspected. The false cleanup invariant, spurious state/x commutation and incomplete displayed corollary are corrected by the owned arguments, not accepted from print.
- Simpson, [A Slick Proof](https://sgslogic.net/t20/logic/seminar/050517.pdf), all five pages through references, especially Definition 1 and Theorem 8: independent HNN and fixed-presentation endpoint comparison, not a replacement for the required Boone reconstruction.

The UNSW drop retains real initial/five-retry HTTP403 evidence, subsequent recovery searches, historical dispositions and three complete alternative arguments. Those arguments exactly match the present interface, macro and simulation dependencies and were independently checked above. No renewed drop or fictitious reading/fetch receipt was created.

All 53 harvested rows retain dispositions. All 17 declines stand: no tape folding, general recursive-set closure, modular-machine replacement, or prescribed-degree result is needed by an owned proof. Refreshed the group scope decisions and corrected five stale item-ID spellings in three evidence rows to the actual manifest IDs. No decline, claim, dependency, coverage source, plan entry or manifest changed.

## Frontier and published-consumer bookkeeping

Read both owned cross-batch inputs; both correctly remain []. Batch 13 compiles the published recognizer directly rather than consuming batch 12. Refreshed the derived shared ledger with --require-reviewed. No same-frontier declaration or implicit proof use is missing from group f.

The canonical published-consumer ledger's U-C19 entries remain A-P: fs-every-finitely-presented-group-has-solvable-word-problem, fs-unsolvable-word-problem-means-no-word-can-be-decided, and fs-the-novikov-boone-theorem-proves-the-uniform-problem-only await thm-novikov-boone-undecidability-of-the-word-problem and its 13-item scaffolded closure. Their strategies remain respectively the fixed counterexample, an empty-word instance, and specialization of a uniform decider. None supplies the new theorem. The old stay-put theorem is not used by batch 12. No published-consumer defect was closed by this scaffold acceptance; published content and the canonical consumer ledger were left read-only.

## Checks actually run

- manifest-deps over all run manifests: exit 0; 691 items, 0 normalizations, 0 errors.
- content-policy --manifest-only on batches 12/13: exit 0; 21 items, 0 errors/warnings.
- coverage-checklist on both owned files: exit 0; 53 results, 0 errors, 1 low-yield warning for the Boone page. The explicit decline adjudication above addresses it.
- source-fetch-check on both owned files --stamp: exit 0; 4/5 verified, 0 newly stamped; 5/5 resolved, including one documented drop.
- validate-plan research/plan-spec.json: exit 0; 1,624 pages; existing redundancy warnings and unitemized pages remain outside this acceptance.
- validate-plan /tmp/group-f-plan-overlay.json --max-items 60: exit 0. The temporary plan overlays every current run manifest, so owned inventories were checked even where the canonical plan awaits splice. 978 itemized pages, no cycles, forward references, B-page dependencies or unresolved IDs; 641 pages remain page-only. Log: /tmp/group-f-plan-check.log.
- extcheck --quiet: exit 0; 57 existing published/unproved warnings. None is an actual supplier of these proofs.
- scope-decisions refresh and check --run phase-2-catchup-24 --group f: 17 declines, 0 pending; check exit 0, 0 errors after the ID corrections.
- frontier-dependency-ledger refresh --run phase-2-catchup-24 --require-reviewed: exit 0, refreshed/deduplicated.
- url-sweep on owned coverage --out /tmp/group-f-url-liveness.json --fail-on-dead: exit 1; 0/4 live, all four curl code 6 (host-resolution failures). Browser source reading and existing genuine fetch stamps are separate evidence, not fabricated URL-sweep success.
- source-backing with that fresh liveness file --require-verified: exit 1; 15 backing-lost diagnostics caused by those failed host resolutions (3 batch-12 results, 12 batch-13 results). A successful live-transport gate remains outstanding; no result/source is dropped because of this temporary environment failure.

A preliminary Python command failed because only python3 is installed; subsequent scripts used python3. pdftoppm was unavailable; mutool successfully rendered the page for actual inspection. Neither failure is represented as a passed check.

## Handoff

The two mathematical scaffolds are accepted with confidence 1 and sufficient ordinary verdicts. The only edited pre-existing review artifact is the group scope-decision evidence; batch notes record this handoff. The fresh source-liveness/backing gate remains failed as explicitly recorded. Do not interpret terminal acceptance as an all-gates-pass or publication receipt. No pair will be modified after its terminal record is written.
