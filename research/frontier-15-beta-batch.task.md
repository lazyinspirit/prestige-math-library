> **Generic task.** No batch-specific file was found, so the engine fell back
> to this one. Your batch number is in the "This dispatch" section appended
> below; substitute it wherever `<i>` appears. A batch-specific file
> `research/frontier-15-beta-<i>.task.md` SHOULD exist for every batch of
> this run — if you are reading this, say so in your notes, then proceed.

## Batch `<i>` of run `frontier-15` — scaffold

Read `research/frontier-15-batch-<i>.pages.json` for your pages, then locate
each pair's design section: the run's pairs and their design docs are tabled
in `research/frontier-15-step0-notes.md`. Scaffold per `briefs/beta-scaffold.md`:
source-grounded, dependency-closed, harvest recorded in
`research/frontier-15-batch-<i>.coverage.json`, notes and recommendations in
`research/frontier-15-batch-<i>.notes.md`.

**No permission prompts of any kind**, including inside an `&&` chain.
