# FA terminal evidence — queue g/8

Item: thm-cover-small-inclusion-is-a-chain-homotopy-equivalence. Decision: repaired.

Read the current theorem and all three full direct dependencies (thm-cover-small-singular-chains-compute-singular-homology, lem-every-finite-singular-chain-becomes-cover-small-after-enough-subdivision, thm-barycentric-subdivision-is-chain-homotopic-to-the-identity); the subdivision and prism definitions and chain-map theorem needed to verify their support and identity; batch-12 A/B pages, manifest/coverage, both contracts, group-g conventions, reader/refute-12; Alpha's repair/adjudication and both Terra rejection rows (2026-09-05T18:51:05.119Z and 2026-09-05T21:39:46.193Z).

The original independent-depth operator was not a chain map. Alpha's prism-corrected formula addresses that problem, but the final rejudge correctly identifies an unjustified face-depth inequality. Membership of S^q sigma in the subgroup of small chains can involve cancellation. One cannot infer the same depth suffices for each individual face. I do not assume that implication or an unproved absence of cancellations.

Independent repair: work over Z, use the finite-chain lemma to define the least admissible a(sigma), and recursively set m(sigma) to the maximum of a(sigma) and the m-values of every codimension-one face. Vertices have m=0. This is finite dimension recursion with finitely many faces and least natural numbers, so no global choice of subdivision depths is needed. S preserves the small subgroup; thus all depths at least a(sigma) work. The face inequality now holds by definition, even with repeated faces and cancellation. For a small simplex all face depths and its own least depth are zero by induction.

The support property is checked from the current definitions: S and T are finite sums of sigma composed with affine maps into its domain, hence preserve every containing cover member. The current prism recursion P_n=b_n(iota_n-P(boundary iota_n)) yields the claimed 1-S identity via the affine boundary formula in positive degree, with P_0=0 handled separately. No dependency repair is required.

With D_q=sum(T S^i), telescoping gives 1-S^q=boundary D_q+D_q boundary. Set D sigma=D_m(sigma) sigma and R=1-boundary D-D boundary. Then boundary R=R boundary directly from boundary squared zero. Its explicit correction on each face tau is a sum over i from m(tau) to m(sigma)-1; every such S^i tau is small and T preserves small chains. Thus R lands in the small subcomplex. D vanishes there, so r inclusion=1 and 1-inclusion r=boundary D+D boundary. Finally the integral small subgroup is degreewise a direct summand on a subset of the singular basis. Tensoring the actual maps and homotopy identities with any G gives the result, without invoking flatness or arbitrary splitting of G-modules. Empty X, G=0, vertices, zero correction sums, and repeated faces are covered explicitly.

Verified full authoritative source: Hatcher, Chapter 2, Proposition 2.21 stages (2)–(4), printed pp.121–124/PDF pp.24–27, including the complete end of the proof:
https://pi.math.cornell.edu/~hatcher/AT/ATch2.pdf
This supports the support-preserving S,T construction, telescoping homotopy, and corrected retraction formula. Its compressed least-depth presentation is not treated as a proof of the questionable inequality: the recursive maximum above independently supplies the missing compatibility.

Added existing definition dependencies def-barycentric-subdivision-chain-operator and def-barycentric-subdivision-prism-homotopy to expose support data; synchronized this entry in batch-12 manifest and batch/merged contracts. No dependency file changed, so no prerequisite licence. Initial precheck found line-wrapped proof rows; after joining those rows, focused precheck and rendercheck pass, and strict batch/merged contracts each pass 1/1 with zero errors/warnings. Statement/interface unchanged; no earlier resolution needs resealing for this proof-only change. No unresolved obligation, new judge call, or pass stamp. Next action after successful recording: position 9.
