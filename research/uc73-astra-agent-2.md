# UC-73 Astra agent 2 audit receipts

## 12. thm-principal-subvariety-codimension-one — A-R

Read full consumer and all five original dependency contracts, including published PIT/height-formula proofs. The height argument is sound; old affine correspondence is draft, while coordinate-domain/Noetherian hypotheses were only asserted. Local closure now uses repaired item 8's explicit prime/closed-set dictionary proof, published strong Nullstellensatz for nonempty principal zero locus, and Hilbert basis for Noetherianity. Proved the domain property directly from irreducibility, and spelled out why a component corresponds to a minimal prime over (f). This uses the actual codimension definition's difference clause only; no finite-component assertion or general-variety affine machinery is invoked. No extra Phase-2 supplier needed.

Final deps: `thm-affine-variety-dimension-coordinate-ring`, `def-codimension-irreducible-subvariety`, `thm-krull-principal-ideal-theorem`, `cor-height-plus-quotient-dimension-affine-domain`, `cor-strong-nullstellensatz-two-inclusions`, `thm-hilbert-basis-theorem`, `def-axiom-of-choice`, all published earlier. AC contract retained, dependency added, exact inherited Nullstellensatz uses named. The source strategy is the existing published PIT plus height formula; no unfamiliar theorem imported and no further external source needed. Assigned item/receipt changed, truthful local verification. Targeted precheck/rendercheck pass. Parent mirror final deps and proof strategy. This is not independent certification of the complete PIT or affine-domain dimension closure.

## 11. thm-ideal-projective-closure-saturation — A-R

Read full consumer and all four original dependencies, including agent 1's repaired closure definition. The Nullstellensatz dependency is unused: I=I(A) is a hypothesis, so vanishing is already ideal membership. Removed the draft correspondence. Expanded both directions of homogeneous membership using G=x_0^(d-e)g^h, including g=0, and dehomogenization of an ideal expression. Explained extension from homogeneous elements to the entire homogeneous ideals and included empty A. Only published earlier same-page closure, saturation and homogenization suppliers remain (A home order 366.045); no Phase-2 or AC prerequisite. The proof is fully local; no unfamiliar mathematics or external assertion imported. Assigned item/receipt changed, truthful local verification. Targeted precheck/rendercheck pass. Parent remove the old correspondence edge from canonical plan and mirror expanded strategy. No claim that arbitrary input generator homogenizations suffice: I^h explicitly uses all members of I as before.

## 10. thm-global-regular-functions-projective-variety — A-R

Read full consumer and all six original suppliers, including agent 1's repaired regular-function definition. The original graded finite-dimensional argument is valid once coordinate powers clear denominators, but its old affine-global-functions supplier is draft, and Cayley–Hamilton was undeclared. A complete local replacement avoids that Phase-2 prerequisite: form the homogeneous ideal of ALL admissible denominators. Its lifted affine zero locus has no nonzero point, so the published strong Nullstellensatz puts a power of each coordinate in the ideal. Homogeneous-degree extraction gives exactly the required clearing powers. This then feeds the finite-dimensional multiplication/Cayley–Hamilton argument. No choice of a representative for every point is made; AC is declared and spent precisely on the published strong Nullstellensatz.

Final deps: `def-regular-function-projective-variety`, `def-homogeneous-coordinate-ring`, `lem-projective-irreducibility-homogeneous-prime`, `cor-strong-nullstellensatz-two-inclusions`, `thm-cayley-hamilton`, `def-axiom-of-choice`, all published and earlier than this theorem (the projective suppliers are earlier on its A page). Read full published Cayley–Hamilton proof. Full-text source check: Milne AG v6.10, https://www.jmilne.org/math/CourseNotes/AG.pdf, §6g Proposition 6.16 and Remark 6.17, lines 8008–8039; this verifies the denominator-domain interface and advertised global constancy. The denominator-ideal closure is supplied fully locally, not claimed to be the source's proof. Assigned item and receipt changed; truthful local verification replaces old stamps. Targeted precheck/rendercheck pass. Parent should mirror final deps and denominator-ideal proof strategy; no new Phase-2 scope.

## 9. thm-affine-variety-product-coordinate-ring — A-P

Read full consumer and all seven supplier contracts (old duality/morphism proofs and tensor coproduct proof), plus the two replacement antiequivalence items. The tensor-domain evaluation argument is correct for irreducible classical affine varieties over algebraically closed k. It uses finite linear independence, not a choice of an infinite vector-space basis. The categorical construction then genuinely consumes unpublished affine geometry interfaces. Exact old→new AG-A cutover: `def-affine-variety-classical`→`def-classical-affine-variety-interface`; both `cor-affine-algebraic-set-coordinate-duality-complete` and `thm-affine-algebraic-sets-coordinate-duality`→`thm-classical-affine-algebraic-sets-reduced-algebras-antiequivalence`; `thm-affine-morphisms-coordinate-ring-anti-equivalence`→`thm-classical-affine-morphisms-coordinate-ring-antiequivalence`; `thm-affine-variety-prime-coordinate-ring`→`thm-classical-affine-variety-prime-coordinate-ring`. All replacements draft, authored in active 5-author. Retain published tensor coproduct supplier.

A disjoint-coordinate construction can simplify object existence, but a full categorical regular-map/coordinate-ring interface still needs the pending affine global-function and morphism results; replacing every such interface locally here would duplicate the substantive Phase-2 construction. Defer and preserve the evaluation proof, then apply the exact antiequivalence replacements. Contract must explicitly assume AC, add `def-axiom-of-choice`, and identify inheritance at the Nullstellensatz/coordinate dictionary; do not label finite coefficient selection an AC use. Replace backtick supplier names with declared wikilink facts for clarity. No new scope, no theorem counterexample; no unfamiliar assertion imported. Item unchanged, checks n/a. Product definition's own category restriction is respected because the theorem claims only the affine-variety universal property.

## 8. thm-affine-variety-dimension-coordinate-ring — A-R

Read complete consumer, chain/ring dimension contracts, old correspondence proof and published quotient-ideal/strong-Nullstellensatz/Hilbert-basis proofs. A local proof closes: define the affine coordinate ring and vanishing ideal explicitly, prove affine Noetherianity by Hilbert basis, prove irreducibility/primality with separating equations, invoke the published strong Nullstellensatz only to realize every prime, and prove that quotient correspondence preserves primality. Reverse finite chains and take suprema; empty/zero-ring convention retained. This removes both the old draft correspondence and the unused finite-component strength inherited through the general dimension definition. Exact final deps: `def-dimension-noetherian-topological-space` (same A page 366.057, position 0 before consumer position 5), `cor-strong-nullstellensatz-two-inclusions` (111.013), `thm-hilbert-basis-theorem`, `thm-correspondence-theorem-ideals` (48), `def-krull-dimension-of-a-ring`, `def-axiom-of-choice`; all published. AC is explicitly inherited at prime realization. No Phase-2 supplier needed. No unfamiliar theorem imported; proof evidence is the read published suppliers and complete local algebra. Assigned item/receipt changed; old stamps replaced by truthful local verification. Initial precheck requested phase renumbering; adopted it. Final targeted precheck/rendercheck pass. Parent must mirror final deps/proof strategy in canonical plan. Whole closure not certified.

## 7. lem-zero-dimensional-classical-variety-finite — A-P

Read full consumer, dimension definitions, finite-component supplier proof, and Phase-2 finite-component replacement. The proof is a correct T1/finite-components argument; it does not use either listed affine dimension or Nullstellensatz theorem. Those two dependencies should be removed at cutover. However its actual finite-component supplier `lem-classical-variety-noetherian-components` step 1.2 invokes draft `lem-algebraic-set-finite-irreducible-components`. Exact Phase-2 replacement: `lem-classical-affine-algebraic-set-finite-irreducible-components` (AG-A, draft, complete proof present). General-variety definition is another actual interface; see item 13. A local rewrite of the unused edges would not close the actual finite-component prerequisite, so defer the combined repair. After that supplier publishes and the general-component lemma is repaired, retain the two-step T1 proof; add `def-axiom-of-choice` and identify the inherited Noetherian-decomposition AC use (the finite selection of one point per component itself does not need AC). No new scope or uncertain mathematics; no external theorem imported. No item changes/checks. Contract is true including empty X; no counterexample found. Scope limited to this argument and identified interfaces.

## 6. lem-standard-projective-opens-are-affine-spaces — A-R

Read complete item and all three dependency contracts/proofs. Original proof only established a bijection and asserted chart isomorphism; it omitted the topology and regular-function transport. The sole draft dependency merely defined affine space/zero sets and can be supplied completely locally. Removed `def-affine-algebraic-set`; stated the polynomial-zero-set topology and local polynomial-quotient convention, proved the homeomorphism by dehomogenization/homogenization, and proved transition pullbacks preserve local quotients in both directions. Included n=0. Only published projective points/topology suppliers remain, earlier on the same A page order 366.045. No AC use, no Phase-2 prerequisite needed. No uncertain mathematical fact was imported. Targeted precheck and rendercheck pass; item and receipt changed, truthful delegated verification recorded. Parent canonical plan should remove the draft dependency and mirror the expanded chart-interface contract. This is local closure of the chart interface, not construction of every classical variety.

## 5. lem-relative-projective-homogeneous-equations — A-P

Read full consumer and all four supplier contracts, plus the product theorem proof. The homogeneous-equation extension by T_i H is correct: off the distinguished chart the extra factor vanishes, and on it the original equation is recovered up to a unit. The monomial-degree bound and empty/unit-ideal/N=0 cases are correct. Full-text source read: Milne AG v6.10, §6q, Lemma 6.51(a,b), PDF p.158, https://www.jmilne.org/math/CourseNotes/AG.pdf, lines 9396–9455, supports the chartwise and cone arguments (the source's displayed radical equality should only be containment for general empty loci; the consumer correctly uses containment).

Confirmed direct draft `thm-affine-nullstellensatz-correspondence` could locally be replaced by published `cor-strong-nullstellensatz-two-inclusions`, but this alone does not close the product-chart use: `thm-dimension-product-varieties` steps 1.1–2.1 require the reduced-coordinate/morphism/localization interfaces, not its unrelated dimension clause. Exact pending AG-A roots: `thm-classical-affine-algebraic-sets-reduced-algebras-antiequivalence`, `thm-classical-principal-open-coordinate-ring-localization`, `thm-classical-affine-global-regular-functions-coordinate-ring`, and `lem-classical-morphisms-glue-on-open-cover` (draft; active run remains 5-author). Their Phase-3 consumers include the published general prevariety definition and product theorem. No missing Phase-2 scope identified. Strategy: finish these actual chart/product prerequisites, retain the homogeneous extension proof, replace F3 directly by the published strong Nullstellensatz, and add `def-axiom-of-choice` with the precise inherited use at step 1.2 (contract already assumes AC). Standard-chart interface is audited next. No item edit; targeted checks n/a; no theorem counterexample found. This is not an assertion that dimension debt blocks product construction.

## 4. lem-projective-variety-cone-irreducible — A-R

Read complete consumer, cone definition, old draft prime-coordinate theorem and repaired published projective-prime lemma. Local closure: the prime vanishing ideal rules out a union of two proper closed subsets by multiplying separating equations. Replaced the unsupported coordinate-ring shortcut by that full local argument and explicitly proved the cone vanishing-ideal identity. Removed `thm-affine-variety-prime-coordinate-ring`; remaining suppliers are the published cone definition and earlier projective-prime lemma on the same A page, order 366.045. Only the latter's choice-free irreducibility clause is used, not its AC radical correspondence. No Phase-2 supplier or AC dependency is necessary. No unfamiliar mathematical step or external assertion imported. Changed assigned item and receipt; truthful local verification replaces stale stamps. Targeted precheck and rendercheck pass. Parent should remove the old draft dependency from the canonical plan. The audit certifies this local proof repair, not the whole supplier closure.

Assignment complete: all 13 items audited sequentially. Final totals: 8 A-R, 5 A-P, no clean/no-repair receipts. A-R items are 3, 4, 6, 8, 10, 11, 12, 13; A-P items are 1, 2, 5, 7, 9. All eight edited items have successful targeted rendercheck; seven proof items also have successful targeted precheck. The definition precheck exits successfully with 0 proof-bearing items checked, recorded as n/a. No new Phase-2 scope required. Shared canonical ledger/plan updates remain parent-owned.

## 13. def-classical-algebraic-prevariety-regular-maps-and-varieties — A-R

Read complete definition and all five original dependency contracts, then both existing published locally ringed-space definitions. Direct draft affine-set definition and body-only local-ringed/sheaf-model prerequisites were confirmed. Full local closure succeeds without Phase-2 affine global-section machinery: define polynomial zero-set models and quotient-function sheaves; prove sheaf gluing, local stalks with residue field k, affine quasi-compactness by finite generation of the ideal of subordinate principal opens, and agreement of locally ringed maps with local coordinate pullbacks. This supplies exactly the locally affine definition and regular-map interface, not an antiequivalence or coordinate-ring localization theorem. Empty/reducible models and over-k condition retained; no AC needed.

Final deps: `def-algebraically-closed-field`, `def-locally-ringed-space`, `def-morphism-locally-ringed-spaces`, `def-irreducible-topological-space-and-subset`, `thm-hilbert-basis-theorem`. The locally ringed suppliers are published on A `sheaf-operations-exactness-ringed-spaces-and-module-pullback` order 366.051, before consumer home `schemes-subschemes-and-morphisms-locally-of-finite-type` order 366.055. Both definitions read in full. Full-text authoritative check: Stacks §26.2, Definition 26.2.1 and discussion of function-sheaf residue maps, https://stacks.math.columbia.edu/tag/01HA, lines 16–50 read. Milne retrieval for this final check timed out; screenshot recovery failed and local pdftotext was unavailable, so no new Milne full-text receipt is claimed here. The affine model construction and chart-equivalence arguments are complete locally. Assigned item and receipt changed, old stamps replaced by truthful local verification. Targeted precheck: 0 applicable proof items, success (metadata n/a); targeted rendercheck pass. Parent mirror deps/expanded contract. This definition's prior debt is discharged; earlier A-P receipts naming it as an additional interface should now treat that interface as available, while their other exact unmet suppliers remain pending.

## 3. lem-projective-irreducibility-homogeneous-prime — A-R

Read the entire item and its three dependency contracts/proofs, the projective algebraic-set definition, and the published strong Nullstellensatz proof. The old draft affine correspondence was stronger than the single identity actually used. Local closure succeeds using published `cor-strong-nullstellensatz-two-inclusions`, home `noether-normalisation-and-nullstellensatz` order 111.013, earlier than consumer home `projective-algebraic-sets-projective-morphisms-and-cones` order 366.045. Replaced the draft dependency with that exact supplier; replaced the unnecessary variety definition with the algebraic-set definition; defined I_+ and the cone locally; expanded the homogeneous-primality test and radical converse; declared `def-axiom-of-choice` and identified AC only at the strong Nullstellensatz application. The irreducibility equivalence itself remains choice-free. No Phase-2 supplier is needed for this repaired proof.

Changed only `items/lem-projective-irreducibility-homogeneous-prime.md` and this receipt. Removed obsolete judge/owner-audit evidence and recorded delegated local verification honestly. First precheck failed on split trailing tags; corrected them. Final targeted precheck and rendercheck pass. The primary evidence for repair is the fully read already-published strong Nullstellensatz proof; no unfamiliar mathematical claim was imported. Parent should reconcile its canonical plan dependency list and retain downstream AC impact review separately. This is a defect-focused local repair, not independent review of the supplier's whole closure.

Scope: assigned published items, one at a time; prerequisite adequacy and current proofs, not independent judging or whole-closure certification. Date: 2026-09-08. No shared ledger, plan, workflow state or other authors' files edited. `AG-A` below means Phase-2 A page `classical-affine-varieties-coordinate-rings-morphisms-and-rational-maps-interface`, order 366.0403. Its replacement items are unpublished drafts unless explicitly noted. The old affine roots remain draft; a published home never upgrades them. AC is permitted, but no new local AC invocation is invented.

## 1. lem-fibre-as-base-change-to-point-classical — A-P

Read the entire item, both dependency contracts, and the replacement fibre definition. The reduced-fibre argument correctly uses a point test object and kills the radical because function rings are reduced; it does not confuse a scheme fibre with its reduction. Original concern confirmed: direct dependency `def-regular-map-image-and-fibre-classical` is draft. Step 2 additionally needs the affine coordinate-ring/radical dictionary, and step 3 needs the reduced-algebra antiequivalence. The universal-property definition is published but does not itself prove those algebraic interfaces. Local deletion of the draft definition would not close these uses.

Defer: replace the old fibre definition by `def-classical-regular-map-image-and-set-theoretic-fibre` (AG-A, draft); use `thm-classical-affine-nullstellensatz-correspondence`, `thm-classical-affine-algebraic-sets-reduced-algebras-antiequivalence` and `lem-classical-morphism-inverse-image-of-closed-is-closed` (AG-A). Preserve the existing reduced-quotient and uniqueness proof after those interfaces publish. The general locally affine convention must be available from the prevariety definition; this is already existing scope, not a new supplier request. Source evidence: Milne, *Algebraic Geometry* v6.10, https://www.jmilne.org/math/CourseNotes/AG.pdf, §5i, Example 5.31 and Notes 5.32, full-text lines 6050–6097 read, agrees on point base change and the necessity of reduction. No new AC use in this finite/local argument; propagate any supplier AC contract at cutover. No item changes, checks not run (read-only audit). No theorem counterexample found; confirmed defect is unpublished/load-bearing prerequisite use.
Build-state observation: `.autopilot/phase-2-wave-1/state.json` is at `5-author`, not publication. State is observational only and can advance during this audit.

## 2. lem-general-variety-function-field-charts — A-P

Read entire item and all three dependency contracts, including both old draft proofs and both replacement proofs. The rational-function germ argument and dense-image restriction argument are sound. The old `thm-function-field-independent-affine-open` only proves its artificially principal-open convention; the consumer genuinely needs arbitrary affine charts. The Phase-2 replacement now explicitly supplies arbitrary nonempty affine opens, with restriction compatibility, so no extra scope is needed. Both direct old field suppliers remain draft.

Exact cutover: `thm-function-field-independent-affine-open` → `thm-classical-function-field-independent-of-affine-open`; `lem-dominant-map-pullback-function-fields` → `lem-classical-dominant-map-pulls-back-function-fields` (both AG-A drafts, proofs now present). Published `lem-classical-variety-noetherian-components` has its own prerequisite debt, being independently audited by agent 1; this is a real use for existence of affine charts, although finite-component strength is unnecessary here. Keep the present germ/cocycle proof and use the adequate replacements. AC already appears in the contract but `def-axiom-of-choice` is missing from deps and the inherited use is not identified: add that dependency and name the replacement Nullstellensatz/domain-interface inheritance at Phase 3, rather than pretend a new choice of a single chart uses AC. No substantive mathematical uncertainty requiring new external research arose; the replacement proofs were read, not merely their titles. No content edits; local checks n/a. Scope is these proof interfaces, not their complete transitive closure.
