# Carleson single-tree local proof repair

The existing lemma now has a complete local proof, retaining its exact statement and all packet, Fourier, density and top conventions. It proves the stronger sum of absolute complex bilinear tile contributions using unit complex phases, and separately treats the possible top member before splitting strict plus and minus trees.

Read Lacey arXiv math/0307008 section6 pp20–24 in full, and the complete local bodies of the packet and size/density definitions, Plancherel, complex L2 pairing, Euclidean maximal Lp corollary, Schwartz convolution law, Schwartz inversion and monotone integral convergence. The Euclidean Lebesgue maximal specialization is valid under AC/countable choice and sigma-finiteness, irrespective of the separately recorded arbitrary-measure interpolation debt.

Two source shortcuts are replaced explicitly. A witness contained in three times a complementary interval's parent can have length4|J|, so the proof splits small scales<=4|J| and large scales>=8|J|. The density witness is an ancestor of length4|J|. Small terms are summed by dyadic lattice tails and distant spatial shells; support measures are bounded on each actual complementary interval. For plus trees, the exact multiplier Theta(v)=psi((v-1/2)/5) is1 on[0,1] and supported[-1/8,9/8]. Applied to a dyadic frequency ancestor, it selects exactly the larger spatial scales, because excluded packet supports have a gap at least1/(4r). A difference of two such convolution projections gives the selector's exact scale block. The kernels have scale>=|J| and hence are controlled by the infimum of the centered maximal function over J. The two sparse-support factors and the fully proved plus-tree L2 synthesis bound give delta*sigma*|I_top|.

Added only existing direct suppliers for complex pairing, Schwartz convolution, inversion and monotone integral convergence. No new items, statement changes, source attempts, source waivers or owner receipts. Synchronized the batch5 manifest/contracts. The forest now has both earlier missing analytic suppliers authored, but these three items await ordinary review and root decision reconciliation. Other maximal-theorem analytic holds remain open.

Checks: canonical explicit-path precheck and strict one-item contract pass; renderer passes; batch5 policy27/27 passes. These checks are mechanical, not an independent mathematical review.
