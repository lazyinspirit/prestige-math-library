## Batch 3 — step 5 authoring

Splice receipt: `research/frontier-13-splice-3.json`. **Read it first** — its
`summary_corrections` map is binding, and the lead Alpha has already decided each
A page summary.

| page | kind | order | items |
|---|---|---|---|
| `diagonalisation-and-the-minimal-polynomial` | A | 88 | 26 |
| `diagonalisation-and-the-minimal-polynomial-examples` | B | 89 | 12 |
| `inner-product-spaces-and-orthogonality` | A | 94 | 34 |
| `inner-product-spaces-and-orthogonality-examples` | B | 95 | 16 |

**88 items to author.**

### Specific to this batch

Your page-88 sources were re-anchored twice. Author from the CURRENT coverage rows (Conrad, Axler, Knapp, Conrad *Potential Diagonalizability*) and keep the `replacement_audit` intact — it is the artifact that caught a sourcing failure no gate could see. Two live findings: `cor-generalised-eigenspace-decomposition-…` rests on Axler 8.22, which opens *"Suppose $\\mathbf F=\\mathbf C$"*, so do not let it claim the arbitrary-field case; and `def-gram-matrix-and-gram-determinant` plus `thm-gram-determinant-detects-linear-independence` are tagged `literature-derived` while "Gram matrix"/"Gram determinant" occur **0 times** in all 404 pages of Axler and 0 times in Treil Ch. 5 — do **not** write Axler as their reference. Also: your notes contradict themselves on `ex-primary-decomposition-over-q-…` (line 159 says `literature-derived`, line 327 says `ai-altered`); settle it.
