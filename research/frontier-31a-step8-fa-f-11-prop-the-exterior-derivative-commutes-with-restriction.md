# Final adjudication — queue position 11

Disposition: repaired. Source status: familiar.

Read the complete current item and both direct dependencies, the published bump lemma, batch-17 page/manifest/coverage conventions, its proof and boundary contract, Alpha evidence and both Terra rejections. Alpha added the invariant formula but still labelled the cross-manifold conclusion as an application of same-manifold locality. I replace that inference with an explicit comparison.

The repair quantifies M, U, k and omega and distinguishes d_M from d_U. For any p in U, a chart-supported bump realizes arbitrary tangent vectors by smooth fields on M. Restrict those same fields to U. Evaluations of omega, their directional derivatives, and the vector-field brackets restrict identically, so each term in the two invariant formulas is equal at p. Both sides are forms, hence equality on arbitrary tangent tuples gives equality of covectors. The open inclusion has the identity tangent map, so its pullback is precisely restriction. Empty U is vacuous and k=0 has no bracket terms. The argument avoids any claim that locality alone compares two operators on different manifolds.

This familiar local differential-calculus argument is independently checked and requires no external verification. Added the published bump dependency and synchronized the owned manifest and this item's local/aggregate proof contract. No dependency item changed, so no prerequisite licence is required. Earlier DG decisions remain valid when this sibling restriction proof is made explicit; refresh their contexts.

Focused precheck, strict proof-contract, and rendercheck pass. No judge stamp or unresolved obligation. Next: record exact bytes, then position 12.
