# Task — complete the differential-topology traceability

**You are an expert in differential topology.** You own exactly
`research/plan-differential-topology-track.md`, which **you already wrote**. This
is a targeted completion pass, not a rewrite. Read the file first and continue it.

## The gap, measured from disk

Your track proposes **668 items**, and every one of them carries
`[S: literature-derived]`. Your §9 crosswalk names **452** of them. The remaining
**216 claim literature backing while naming no source heading and no page
range** — only 6 of your 668 item rows carry an inline locator.

By kind, the 216 are: 113 `ex-`, 48 `cex-`, 25 `rem-`, 13 `lem-`, 11 `cor-`,
3 `def-`, 2 `prop-`, 1 `thm-`. So this is not only B-page examples: **55 are
A-page mathematical content**.

For comparison, measured the same way: algebraic geometry's crosswalk names
990/990 of its proposals and Fourier analysis 299/299. Algebraic topology had the
same gap as you and closed it by adding an inline locator to all 568 rows.

## What to do

**Make every one of the 216 traceable.** Either route, consistently:

1. **Extend the §9 crosswalk** so every canonical unit names all the item ids it
   produced, including the examples and counterexamples; or
2. **Add an inline locator** to those item rows — treatment plus exact section or
   page range, in the style your §8 per-pair source matrix already uses
   (`Nicolaescu Ch. 2 §§2.1--2.5, pp. 23--66`, `Hirsch Ch. 5 §§1--2, pp. 121--140`).

Constraints:

- **Change no item id, no pair, no statement, no rationale, and no provenance
  label.** This pass adds traceability only.
- Do not add items. Do not re-decompose. Do not touch any other file.
- **If an item cannot be attributed to a range you actually read, say so in an
  explicit "unattributed" list and — where the honest label is not
  `literature-derived` — correct that one provenance value and note it.** A
  constructed counterexample of your own is legitimately `ai-generated` provided
  it stays a B-page item and is never a dependency target; what is not legitimate
  is a `literature-derived` label with no literature behind it. An admitted gap
  or a corrected label both beat a fabricated citation.
- Preserve your 160-heading denominator and the DT-30 source-depth exception.
- No permission prompts of any kind. Record a blocker in the file if truly stuck.

## Report back

How many of 668 are now traceable; which route; the unattributed list; and every
provenance label you corrected, with its reason.
