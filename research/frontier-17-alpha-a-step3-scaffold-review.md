# Frontier 17 — Alpha group a step-3 scaffold review

Scope: batches 2, 4, and 5, the complete real-analysis group. The assignment is
coherent: the trigonometric pair consumes the volume and surface developments,
and the surface development consumes constant-rank geometry. I read the five
live A/B manifests, their notes and archived source/design notes, all five
coverage records, the governing RC-2/RC-5/RC-7/RC-8/RC-13 design sections, the
scope amendment, the current plan spec, and the Definition or Statement of every
published dependency named by the owned manifests.

## Verdicts

| A page | Batch | Verdict | Reason |
|---|---:|---|---|
| `trigonometric-and-oscillatory-examples-in-several-variables` | 2 | insufficient | The owner-added surface prerequisite is not consumed: the sine surface-area result remains deferred even though its theorem is now earlier in this run. The existing volume example also bypasses RC-5's decided route. |
| `constant-rank-submersions-and-regular-level-sets` | 4 | insufficient | The mathematical development is deep, but the Toronto §2.8 harvest omits two named examples inside its claimed locator. |
| `regular-surfaces-and-surface-integrals` | 4 | insufficient | The patch, area, scalar-integral, flux, graph, revolution, horn, and Schwarz-lantern development is present, but the source-backed false implication “finite volume implies finite lateral surface area” promised by the RC-5 amendment is absent. |
| `volumes-of-elementary-solids-and-solids-of-revolution` | 5 | sufficient | The standard slicing, disc, washer, shell, elementary-solid, ball, and boundary-counterexample development is present after the direct route repairs below. |
| `the-real-gamma-and-beta-functions` | 5 | sufficient | The real-variable theory is broad and properly decomposed after the zero-exponent and repeated-differentiation routes were repaired. |

## Severity and disposition

| Finding | Pair | Mathematical severity | State | Disposition |
|---|---|---|---|---|
| B2-1 | trigonometric several variables | breadth-blocking | open | Add the sine surface-area computation and repair the stale coverage deferral. |
| B2-2 | trigonometric several variables | route-blocking | open | Make the sine volume example consume RC-5's disc theorem rather than rebuilding that theorem from lower facts. |
| B4-1 | constant rank | fatal if authored | repaired | Removed two load-bearing edges onto AI-generated counterexamples; the false statements now reproduce their witnesses inline from eligible inputs. |
| B4-2 | regular surfaces | fatal dependency closure | repaired | Added the actual earlier prerequisite pages for Gram determinants, content-zero modification, the integral logarithm, and volume. |
| B4-3 | constant rank | coverage-blocking | open | Add dispositions for Toronto MAT237 §2.8 Examples 3 and 5. |
| B4-4 | regular surfaces | breadth-blocking | open | Add the finite-volume/infinite-lateral-area false implication with Gabriel's horn. |
| B4-5 | regular surfaces | fatal contract drift if authored | repaired | Declared the boundary-exception lemma used by the closed-cylinder contract. |
| B5-1 | volumes | fatal independence/citation route | repaired | Removed the first ball-volume theorem as a logical input to the named Cavalieri proof and supplied its actual disc, additivity, cylinder, cone, and Cavalieri inputs. |
| B5-2 | volumes | fatal missing inference | repaired | Added determinant scaling for the radius-\(\rho\) sectional ball in the \(n\)-ball recursion. |
| B5-3 | Gamma/Beta | fatal endpoint gap | repaired | Added the logarithmic primitive and logarithm range at \(s=0\), \(p=0\), and \(q=0\). |
| B5-4 | Gamma/Beta | fatal quantifier route | repaired | Added induction to the all-orders dominated-differentiation argument for Gamma smoothness. |
| B5-5 | volumes and Gamma/Beta | fatal prerequisite closure | repaired | Added `improper-integrals` to the volume pair and synchronized the Gamma manifest with the plan's volume edge. |

## Batch 2 — trigonometric and oscillatory examples in several variables

### Standard development and design

The scaffold contains the circular counterexample to vector-valued mean-value
equality, \(\sin(xy)\) and its mixed partials, a differentiable
\(xy\sin(1/(x^2+y^2))\) example with unbounded nearby partials, a radial
oscillator with discontinuous gradient, a bounded non-equicontinuous sine
family, the sine-generated solid's volume, and the spherical-coordinate
injectivity failure. The A-page proposition correctly repairs RC-13's false
angular-oscillator claim: \(xy/(x^2+y^2)=\tfrac12\sin(2\theta)\) does not have
all directional derivatives, while the published all-zero-directional witness
is \(r^2\tan\theta\) off its seam.

B2-1. The surface of revolution generated by \(y=\sin x\) is only treated by
volume. This is not a valid deferral now. The owner added
`regular-surfaces-and-surface-integrals` to Frontier 17 precisely because
RC-13 uses this area, and the page now precedes RC-13. APEX Calculus II §7.4,
Example 7.4.14 computes

\[
2\pi\int_0^\pi \sin x\sqrt{1+\cos^2x}\,dx
=2\pi\bigl(\sqrt2+\operatorname{arsinh}1\bigr).
\]

The Beta should add a source-backed example using
`cor-surface-area-of-revolution-formula`; the published inverse-hyperbolic
logarithm formula is already in the page's prerequisite closure. The two
OpenStax §2.4 coverage rows currently marked deferred become included by that
item.

B2-2. RC-13 says the sine solid takes its volume from RC-5. The present
`ex-volume-of-the-sine-solid-of-revolution` reconstructs measurability and
Cavalieri from lower published results and cites no RC-5 item, despite declaring
the page prerequisite. Rebind it to
`thm-volume-of-a-solid-of-revolution-by-discs`; retaining a short independent
Jordan-measurability check is harmless, but the decided cross-page route must be
visible in `deps` and in the batch contract.

### Harvest, B page, decomposition, and size

The Lebl and Toronto derivative headings match their locators, and the OpenStax
disc and surface-of-revolution headings are present. The low-yield warning
(four scaffolded rows among twenty-three harvested rows) is mostly explained by
honest `already-published` dispositions. It does not excuse the now-stale
surface-area deferral. The B page is substantive, with eight worked items rather
than token entries. The A proposition is an honest shared identity lemma; the
remaining computations do not need further decomposition. The A page has one
item, so no split is indicated.

Verdict: **insufficient** until B2-1 and B2-2 are fixed.

## Batch 4 — constant rank, regular level sets, and regular surfaces

### Constant rank and regular level sets

The standard Euclidean development is present: differential rank, rectangular
minors, lower semicontinuity, immersions and submersions, the two coordinate
lemmas behind constant rank, the normal form and its extreme cases, openness of
submersions, constant-rank and regular-level slices, tangent vectors as curve
velocities, and general vector-valued Lagrange multipliers through an explicit
transpose-factorization lemma. This is a faithful decomposition of the Lee and
Tu treatments rather than a monolithic rank theorem. The B page is real: it has
spheres, ellipsoids, graphs, a hyperboloid, cone and cusp rank drops,
nonconstant rank, a smooth critical level, a sphere multiplier computation, and
three false statements.

B4-1. The provenance ledger marks
`cex-cone-level-set-has-a-rank-drop-at-the-apex` and
`cex-critical-value-can-have-a-smooth-level-set` as AI-generated constructions,
but each was a `deps` target of a false statement. That violates the
statement-provenance rule even though both witnesses are correct. I repaired
both false-statement strategies to reproduce the witnesses inline from eligible
definitions, derivative facts, and the source-backed graph theorem, and updated
their batch contract entries.

B4-3. The Toronto MAT237 §2.8 locator runs through the multiple-constraint
discussion, but the harvest skips two named examples:

- Example 3 rewrites the unit-circle constraint as
  \((x^2+y^2-1)^2=0\), making the gradient vanish on the constraint and causing
  the multiplier method to miss the extrema. It should be disposed as
  `already-published` to
  `cex-lagrange-multiplier-rule-needs-a-regular-constraint`.
- Example 5 is the worked two-constraint problem following the general theorem.
  It may be absorbed into the general multiplier item or declined as another
  calculation, but it needs a result-specific row.

Lee's Theorem 7.13 and Theorems 8.8–8.12, and Tu's Theorems 11.1–11.6, are
otherwise harvested faithfully. The manifold converse and orthogonal-group
application are genuine subject-boundary declines. The A page has nineteen
items and needs no split.

Verdict for `constant-rank-submersions-and-regular-level-sets`:
**insufficient** until B4-3 is repaired.

### Regular surfaces and surface integrals

The standard classical Euclidean development is present and well decomposed:
cross-product algebra and its Gram identity; admissible patches; tangent
planes; signed reparametrization; first fundamental form and area density;
scalar surface integrals; unit normals and flux; boundary exceptions and finite
patch presentations; the regular-level-set seam; graph formulas; and scalar and
area formulas for surfaces of revolution. The B page contains sphere,
cylinder, torus, graph-flux, cone, orientation, degeneracy, Gabriel-horn, and
Schwarz-lantern computations and false statements.

The Taylor §3.2, Toronto §5.3, Sjamaar §8.1, APEX Gabriel-horn, Northwestern
cone, and Brewin §3.2 harvests match the named ranges. The Taylor torus moments
and abstract-manifold material are defensible declines, and Brewin supplies the
stated \(4NM\)-triangle lantern construction and divergence regime.

B4-2. The live scaffold used four pages outside its declared closure:
`inner-product-spaces-and-orthogonality` for Gram determinants,
`improper-and-parameter-dependent-multiple-integrals` for content-zero
modification, `the-integral-logarithm-and-its-characterisations` for the horn
lower bound, and the volume pair needed by the retained finite-volume boundary
claim. I added all four backward prerequisites to the batch manifest and
`plan-spec.json`.

B4-5. The closed-cylinder proof contract directly cites
`lem-parameter-boundary-exceptions-do-not-affect-surface-integrals`, but the
example's scaffold omitted it from `deps`. I added the edge.

B4-4. APEX's Gabriel-horn example proves the exact boundary statement that the
RC-5 amendment assigned to this surface page: finite volume does not imply
finite lateral surface area. The scaffold has the divergent-area example but
never states the false implication or brings the finite-volume half into its
refutation. Add a source-backed `fs-` item that uses the volume theorem and
improper \(p\)-test for \(\pi\), and the existing surface-revolution theorem
for divergence. The new volume prerequisite already supplies the legal A-page
route; no B-page dependency is needed.

The A page has twenty-two items and the B page twelve. No split is indicated.

Verdict for `regular-surfaces-and-surface-integrals`: **insufficient** until
B4-4 is added.

## Batch 5 — volumes and the real Gamma/Beta functions

### Volumes of elementary solids and solids of revolution

The standard development is present: product-null and compact-graph-null
lemmas, an explicit boundary decomposition, the general solid-between-graphs
Fubini theorem, volume under a graph, disc and washer methods, cylinder and
cone formulas, two different three-ball proofs, cylindrical shells, and the
\(n\)-ball slicing recursion. The B page contains distinct cylinder/cone,
torus, shell/washer, Gabriel-horn, low-dimensional ball, compact non-Jordan,
Cavalieri-converse, and one-iterated-integral examples.

Taylor Proposition 3.1.7 and Theorem 3.1.9, Angenent Chapter 8 `3–5, Axler
§5C, and APEX's Gabriel-horn example are faithfully represented. The two
translated-axis exercises are genuine computational variants, not missing
theorems. The B page is substantive and the A page has fifteen items.

B5-1. The item named as the Cavalieri proof depended on
`cor-volume-of-a-closed-three-ball` and used it in a numbered cross-check step,
so it was not independent under this library's logical `deps` contract. I
removed that edge and proof step. Its route now cites the disc section area,
finite additivity, cylinder and cone volumes, and Cavalieri; the disc proof is
only a Remark cross-reference.

B5-2. The \(n\)-ball recursion used
\(V_{n-1}(\rho)=\rho^{n-1}V_{n-1}(1)\) without an induction hypothesis or
dependency that established scaling. I added the published determinant-scaling
theorem and bound it in the proof contract.

B5-5. Gabriel's horn uses one-variable improper integration, which was outside
the pair's declared closure. I added the backward `improper-integrals` edge to
the manifest and plan. The plan validator passes.

Verdict: **sufficient** after these direct repairs.

### The real Gamma and Beta functions

The page gives the exact convergence domains, recurrence and factorial values,
smoothness, strict log-convexity, endpoint behavior and the unique minimum,
Gautschi's inequality, a decomposed Bohr–Mollerup proof, Beta symmetry and its
trigonometric form, the Beta–Gamma identity and recurrences, independent
Gaussian and Wallis proofs of \(\Gamma(1/2)\), a decomposed real Stirling proof,
and the unit-ball formula with scaling, limit, and maximizing dimension. The B
page has special values, dimensional values, the converse failure for
log-convexity, a periodic non-log-convex recurrence solution, and the intended
false statements.

The two-page Toronto handout, Chandrasekharan's Lectures 6–7, and Axler §5C are
faithfully harvested. The complex holomorphy, reflection, duplication, and
sectorial Stirling rows are valid destinations under the settled real/complex
Gamma seam. Reversing the impossible “log-convex but not convex” example was
mathematically necessary and is correctly reflected on disk. The A page has
twenty-five items and needs no split.

B5-3. The Gamma and Beta convergence strategies called the \(s=0\), \(p=0\),
and \(q=0\) thresholds “real-power primitive” cases. At exponent \(-1\), that
primitive is logarithmic. I added
`thm-logarithm-derivative-and-integral` and
`thm-natural-logarithm-laws`, separated the threshold branches in both
strategies, and updated both contracts.

B5-4. The smoothness theorem quantifies over every derivative order and its
contract said “induction” without declaring the induction principle. I added
the dependency and bound the successor iteration explicitly in the contract.

The plan already carried the required backward volume edge; I synchronized the
live batch manifest with it. The Wallis results are in the volume closure through
`areas-of-elementary-plane-figures`, so no redundant direct
`pi-the-equivalent-characterizations` edge is needed.

Verdict: **sufficient** after these direct repairs.

## Plan, provenance, and gates

All five design orders agree with `plan-spec.json`. The design's omitted
direct topology/arc-length/mixed-partials suppliers are already in the declared
transitive closures, as the independent drift review recorded. There is no
owner-only forward edge and no proposed split.

I applied only backward prerequisite repins:

- `volumes-of-elementary-solids-and-solids-of-revolution` gained
  `improper-integrals`;
- `regular-surfaces-and-surface-integrals` gained
  `inner-product-spaces-and-orthogonality`,
  `improper-and-parameter-dependent-multiple-integrals`,
  `the-integral-logarithm-and-its-characterisations`, and
  `volumes-of-elementary-solids-and-solids-of-revolution`.

The existing scope-amendment edges from Gamma to volumes and from RC-13 to
volumes and regular surfaces were retained. The Gamma batch manifest was
synchronized with its plan edge.

Every published dependency target has `status: published` and an eligible
`literature-derived` or `ai-altered` Statement. After B4-1, no planned
AI-generated construction is a `deps` target. Every source-backed item in the
planned provenance ledger has a reader-visible URL in its assigned coverage
source, and all twenty-one distinct coverage source records are currently
full-text stamped.

Checks after the repairs:

- JSON parsing: pass for the three manifests, both changed batch contract files,
  and `plan-spec.json`;
- `validate-plan.mjs research/plan-spec.json`: exit 0;
- `content-policy.mjs --manifest-only`: 130 scoped items, 0 errors, 0 warnings;
- `coverage-checklist.mjs`: five pages, 174 harvested rows, 0 errors, one
  explained batch-2 low-yield warning;
- `source-fetch-check.mjs`: 21/21 source records fetch-verified;
- custom requires-closure audit: 0 dependency homes outside the current
  declared closure;
- custom provenance/contract audit: 0 AI-generated dependency targets and 0
  same-batch contract citation sources absent from their item's `deps`.

The three open breadth/harvest findings are deliberately left as
`insufficient` verdicts so the stage-3 fix loop routes them to the owning Betas
and cannot splice past them.
