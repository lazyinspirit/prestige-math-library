# Owner-directed authoring of the 22-pair frontier

The owner has explicitly instructed: “Author all 22 pairs”, “All 22 pairs belong to the same frontier”, and “Author all 22 pairs right now”. The frontier is recorded in `research/phase-2-frontier-22.json`. The existing run names identify two execution partitions of that one frontier; they are not separate selection waves.

Proceed to actual authoring of all original20 pairs while the existing Fourier author continues on the other2. The operator advances the completed scaffold stage under this direction; the 101 ready /442 held item records remain honest, unchanged evidence. Open source, prerequisite and proof findings in each batch's notes/coverage are the authors' explicit repair workload. Preserve source attempt counts and all claims. Resolve gaps locally with fully proved definitions and lemmas on owned existing A pages. Escalate exact uncertainties that remain; do not turn a planned argument into an approval or publish incomplete proofs.

The owner approves proceeding with the current selected scope. Scope approval does not certify mathematical correctness. The normal pre-author baseline, authored-item decisions, source checks and all independent review/judgment gates remain required. Report scope changes and cross-partition dependency changes to the operator for prompt reconciliation. No extra pair, published-content edit, source retry reset, or publication is authorized by this direction.

The original20 proof/source checkpoints are in `research/phase-2-next-20-supervision.md` and the batch notes. Fourier suppliers are owned by `phase-2-fourier-support-2` and are being authored in the same frontier; read their current files and exact contracts without duplicating or editing that worker's items. The Sol auditor owns the canonical published ledger; report exact findings in group handoffs.


## Operator follow-up: clocked simulator —16:32 UTC

A detailed local time construction is available in `research/phase-2-next-20-clocked-simulation-local-proof.md`: integral capacity2^(i+1) buffers, exact ordered redistribution, high-reset-safe charging, lazy input archive, fixed finite-alphabet implementation and clamped left/right-only compilation. This is a proposed argument for actual incorporation and checking, not an item approval. The author-owned theorem and contract are untouched; its Step3 escalation remains open. No source retry counter changed. The Arora–Barak PDF was genuinely retrieved for reading and matches its prior recorded hash.
