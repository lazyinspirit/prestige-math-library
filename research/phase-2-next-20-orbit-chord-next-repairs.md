# Next HG local repairs: orbit chords, centralizer, independent poles

Working proof plan by root, not an accepted item/contract. Continue continuous supervision while completing it. Full prior centralizer and independent-existence drafts, pole proof, boundary-product lemma and positive-stable-length proof have been read. These two remaining holds may close without the exact general Morse theorem. No new pair is proposed.

1. Uniform dyadic scale from orbit quasi-isometry constants lambda,c: set kappa=3delta. Choose J>=4 with 2^J/lambda-c>lambda+c+4kappa J. Existence follows from 2^J>=J^2 for J>=4 and eventual quadratic domination. If every a_(2N)-a_N<=4kappa for N=1,2,...,2^(J-1) along powers of two, telescoping contradicts the lower bound at2^J. Hence some N=2^j has L=a_N>2K+4kappa, K=a_N-a_(2N)/2>=0. L<=lambda 2^J+c, K<=L. This makes all constants depend only on delta/lambda/c, unlike an unspecified Fekete convergence scale.

2. Repeat the already proved finite-chain product induction: consecutive distances L and local turns K imply endpoint turns<=K+kappa, all interior endpoint products<=K+2kappa, and linear escape. Derive rather than citing a proof-only intermediate as a public theorem.

3. In a delta-slim triangle with basepoint v and other vertices a,b, a point of [a,b] lies at distance <(a|b)_v+3delta+1 from v. If radial distance t=(a|b)_v+2delta+2epsilon exceeds d(v,a), use endpoint a. Otherwise take z on[v,a] at t; a delta+epsilon witness on[v,b] would imply (a|b)_v>=t-delta-epsilon, contradiction. The witness lies on[a,b], yielding the bound; take epsilon=1/3. Only finite geodesic selections and approximate witnesses, no properness/AC.

4. For a finite orbit chord [x_i,x_j], every intermediate orbit point has distance<E=K+2kappa+3delta+1 from that chord. Choose finite near-points with parameter values t_m, endpoints exactly chord endpoints. Consecutive t_m differ by at most L+2E. A first crossing of any chord parameter shows that every chord point is within L+3E of some orbit point. This holds for i=0 as well as negative indices.

5. Boundary pole-pair product at o is at most K+4kappa: the signed N-step representatives have mixed products<=K+2kappa and the supremal/representative comparison costs2kappa. For every h preserving the unordered pole pair, exact isometry invariance and symmetry give B_(ho)(p+,p-)=B_o(p+,p-). Therefore choose some i,j>=1 with (x_-i|x_j)_(ho)<K+4kappa+1. Step3 puts ho near the chord within E_h=K+4kappa+3delta+2; step4 puts it within R=L+4K+10kappa+12delta+5 of an orbit point g^(mN)o. Thus g^(-mN)h lies in a fixed finite ball. This proves finite index of <g> in the whole pole-pair stabilizer, hence in C_G(g), without assuming commutation in the geometric argument. Bound R<=5(lambda 2^J+c)+10kappa+12delta+5.

6. Shared-pole commensurability: orient g,h so their positive poles agree. On each large h^j, the h orbit estimates and equivalence to the g orbit imply (h^j|g^m)_o>=a_j-C_h-kappa-epsilon for some sufficiently large m. The identity (o|g^m)_(h^j)=a_j-(h^j|g^m)_o and steps3-4 give distance(h^j, g^i)<=R' for some i>=0, with R' independent of j. Use least such i(j); escape forces i(j) unbounded. The finite ball contains g^-i(j)h^j. Choose two EXACTLY EQUAL such elements with both j and i increasing (finite pigeonhole + escape). Equality gives g^(i2-i1)=h^(j2-j1), not just an equal-distance estimate. Positive powers have the same two poles by the proved orbit definition, so the original pole pairs coincide.

7. If G is non-elementary, use the completed infinite-order existence lemma to obtain g. If all conjugate pole pairs intersect its pair, step6 says all coincide. Then all G stabilizes that pair, and step5 makes G virtually cyclic, contradiction. Hence one conjugate hgh^-1 has disjoint poles from g. No general two-ends theorem required.

Before installation: write the complete local arguments and necessary explicit interfaces on the existing A page, reconcile scope immediately if claims change, and run exact contracts/precheck/render. Do not mark these holds repaired from this plan. General exact Morse and its transport/boundary-extension consumers remain unresolved.
