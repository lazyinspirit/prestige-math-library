# Task — complete the algebraic-topology crosswalk

**You are an expert in algebraic topology.** You own exactly
`research/plan-algebraic-topology-track.md`, which **you already wrote**. This is
a targeted completion pass, not a rewrite. Read the file first and continue it.

## The gap, measured from disk

Your track proposes **568 items**. Your §7 heading-to-item crosswalk has 60
`included` rows naming **231 distinct item ids**. The other **337 items are not
tied to any source heading or range** — and unlike the other tracks in this
commission, your item rows carry no inline source locator either, only a
provenance label. So a reader cannot tell which reading backs 59% of the track.

For comparison, measured the same way: algebraic geometry's crosswalk names
990/990 of its proposals, Fourier analysis 299/299, and the Lie track carries an
explicit locator on 93% of its item rows.

## What to do

**Make every proposed item traceable to a source.** Either route is acceptable —
pick whichever fits the structure you already have, and be consistent:

1. **Extend the §7 crosswalk** so that every `included` canonical unit names *all*
   the item ids it produced, not a representative four. 43 of your 60 rows name
   exactly four ids, which reads as a cap rather than a census. Add rows for any
   canonical unit you read and decomposed but did not list.
2. **Or add an inline locator** to each item row — the treatment plus the exact
   section or page range, in the style your §6 source matrix already uses
   (`Hatcher pp. 102--184`, `May §§6--24`, `Miller 18.905 §§1--38`).

Constraints:

- **Change no item id, no pair, no statement, and no provenance label.** This
  pass adds traceability, nothing else. If completing the crosswalk reveals an
  item you cannot attribute to any range you actually read, say so explicitly in
  a short "unattributed" list rather than inventing a citation — that list is a
  legitimate outcome and far more useful than a fabricated locator.
- Do not add items. Do not re-decompose. Do not touch any other file.
- Keep your normalized-denominator note in §7: counting duplicate headings from
  independent treatments once is a defensible choice and it is stated clearly.
  Just make the *item* side complete.
- No permission prompts of any kind. Record a blocker in the file if truly stuck.

## Report back

The number of items now traceable, out of 568; which route you used; and the
unattributed list if any.
