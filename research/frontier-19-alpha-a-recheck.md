# frontier-19 — group Alpha `a` step-3 recheck

- `B1-1` — `confirmed`: `research/plan-group-theory-track.md` still misstates GT-3 item 6 as `prop-primitive-actions-have-equal-sized-blocks`, but `research/frontier-19-batch-1.pages.json` now correctly uses `prop-blocks-in-a-finite-transitive-action-have-equal-size` and routes the prime-degree corollary through that finite-transitive statement. `research/frontier-19-alpha-a-step3-scaffold-review.md` identified the design claim as false, and the live scaffold carries the needed repair with no remaining block-size defect.
- `B1-2` — `confirmed`: GT-11 in `research/plan-group-theory-track.md` still names `prop-finite-and-virtually-cyclic-growth-types`, but the current scaffold intentionally omits a separate item and still closes the page honestly: `research/frontier-19-batch-1.pages.json` keeps the growth backbone through generating-set independence, quasi-isometry invariance, free-group exponential growth, Bass-Guivarch, nilpotent polynomial growth, and Gromov as a sourced `rem-`. `research/frontier-19-batch-1.notes.md` records the omission and the prior Alpha review accepted that drop with no reopen.
- `B1-3` — `confirmed`: both the canonical and source harvest rows for `Groups of uniform exponential growth` in `research/frontier-19-batch-1.coverage.json` still resolve to `destination: "owner-decision"`, and that classification remains correct after refresh. `research/plan-group-theory-track.md` and `research/plan-spec.json` still contain no later page dedicated to uniform exponential growth or Lehmer-type lower bounds, so this is a real plan-level scope decision rather than a local batch miss.
- `B9-1` — `confirmed`: MA-6 in `research/plan-category-theory-track.md` still phrases item 6 left-only, but `research/frontier-19-batch-9.pages.json` now correctly broadens it to `thm-the-comma-category-limit-and-colimit-formulae-compute-kan-extensions`, and item 7 depends on that two-sided theorem. The same two-sided item is recorded in `research/frontier-19-batch-9.coverage.json`, so the codensity/right-Kan half now has a truthful named dependency.
- `B9-2` — `confirmed`: the current scaffold and coverage still treat the disputed source material as genuine subject-boundary declines. `research/frontier-19-batch-9.coverage.json` records Riehl `6.4 Derived functors as Kan extensions` as `out-of-scope` for this ordinary-category page and separately records Leinster `§4 integration and §§7-8 double duals and ultraproducts` as `out-of-scope`; `research/plan-category-theory-track.md` still describes the page as Kan extensions, density, free cocompletion, and codensity, with those later homotopical, measure-theoretic, and model-theoretic topics outside the page remit.

Scope-decision refresh result: `node tools/scope-decisions.mjs refresh --run frontier-19 --group a` reported `8 decline(s), 0 pending`, and the refreshed `research/frontier-19-alpha-a-scope-decisions.json` still supports the existing `sufficient` verdicts for all three A pages in this group.
