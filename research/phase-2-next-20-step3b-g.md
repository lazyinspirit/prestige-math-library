# Step 3b group g — phase-2-next-20, batch 12

## Handoff disposition

**Incomplete: 21 of 22 items fully authored and locally accepted/repaired; one theorem remains escalated.** Both assigned draft pages are written. Every original/current item ID and promised claim is retained. The current manifest has 19 A items and 3 B examples, including the two Step-1 local coding lemmas. No new pair or item was added. No published content, owner decision, independent review, engine state, canonical plan or published-consumer ledger was edited. No judge/audit stamp was added.

Read CLAUDE.md, README.md, SCHEMA.md, the assigned design section in `research/plan-computability-theory-track.md` lines 3135–3234 and following context, current batch manifest/coverage/notes, Step-3a owner decision, and `research/phase-2-next-20-owner-authoring-direction.md`. That direction authorizes authoring while retaining unresolved obligations; it is not mathematical approval. The pair's refreshed sufficient-scope receipt has the same scope hash as the existing owner proceed decision: `49c5dfa2c48b07cf958a6e2b2e15e02b058fd9899eb7fd2dc0df24b4918b975b`.

The authoritative next action is owner resolution of the open time-simulator proof and source dispositions. Completed mathematical branches do not use the held simulator, so they remain sound independent work. This is a local author audit, not an independent review or an exhaustive audit of the previously reported 420 published transitive nodes.

## Exact open theorem

`thm-clocked-universal-simulation-with-time-and-space-bounds`, on A page `effective-numberings-reductions-resources-and-randomness`, retains both promised interpreter bounds. Its item now explicitly states that the time bound is unproved, and gives only the separate current-configuration space construction. It is not a completed theorem or a supplier. `provenance.proof: not-supplied` records the absence of a full proof. The strict proof-contract scope includes it but intentionally has no completed contract entry.

The missing argument is the full fixed-finite-alphabet geometric-buffer construction with positive integral base capacities, ordered redistribution, empty/half/full invariants, reset charging in the presence of higher-level resets, and lazy access to long inputs so initialization costs O_M(n), not O_M(n log n). It must establish O_M(n+b log(b+2)) for b=0,1 as well as large b. The current row-scan space construction cannot supply this time estimate. The proposed owner remedy is to finish and independently review that complete argument on the existing assigned A page, without dropping the time claim or citing the published unsupported joint simulator.

The escalation receipt was written before final source-metadata normalization and the parity-safe physical-tape clarification. It remains owner-held and was not overwritten. Its current item/dependency hash is `1f86fe4cbffab6ba47923f18676d4af77478807c03dfae4fc8102ef20e32ba1e`; the decision tool correctly reports “changed inputs require a current owner decision.” The owner must refresh the hold or resolve the mathematical obligation at this hash. All other 21 item decisions were refreshed after final checks and are current.

## Source evidence and remaining qualifications

No new external source retrieval or external full-text reading is claimed in this dispatch. The initial failure plus five recovery attempts already recorded for each original source were preserved, not reset. Native DNS failure is not permanent unavailability. Two genuine earlier fetch records (Arora–Barak and Patey) remain reused in coverage; Gallier and Aspnes retain their owner-escalation source dispositions. No drop or invented Step-1 confidence was recorded.

Exact inherited locators remain:

- Arora–Barak, `https://theory.cs.princeton.edu/complexity/book.pdf`: Appendix 1.A, printed pp.35–38 / PDF pp.51–54 (time simulation only); §7.1, PDF pp.132–133; Appendix A Lemma A.25, PDF p.475. Existing partial-reading evidence is not promoted to complete reading.
- Gallier, original `https://www.cis.upenn.edu/~cis5110/notes/tcbook-comp.pdf`; recovery edition `https://www.cis.upenn.edu/~cis2620/notes/tcbook-u-comp.pdf`: §§1.7–1.8, printed pp.33–34; §2.3 Proposition 2.5, pp.57–58; §3.1 Theorem 3.1, pp.68–69 and Theorem 3.2 p.70; §5.1 Theorems 5.1–5.2, pp.128–129. Original and recovery editions are not silently identified.
- Aspnes, `https://cs.yale.edu/homes/aspnes/classes/469/notes.pdf`: §§5.2.1–5.2.4, printed pp.71–75; H.2.2, pp.482–483. Original complete reading remains unresolved.
- Patey, `https://ludovicpatey.com/courses/comp-thy-2023/cr11-en.pdf`: design locator chapter 5, Definitions 4.1 and 5.2, printed pp.87–89; no new independent reading claimed.

The complete local numbering and Chernoff arguments and exact current dependencies have been added as `source_resolution.alternatives` under the existing Gallier/Aspnes holds. This supplies genuine alternative-proof evidence without authorizing a source waiver or pretending that the original source was read. Coverage still has 16 inherited source rows, not an exhaustive new harvest. Owner source reconciliation and the incomplete external section inventories remain open. Structural source checks now fail only on those two held dispositions (four coverage errors), not on missing alternative arguments.

## Dependency and scope reconciliation for Step 4

Fully authored retained local suppliers:

- `lem-fixed-coding-primitive-recursive-arithmetic-and-sequences`: explicit PR arithmetic, bounded search, decreasing-tail list length, reverse/append/concatenation and bounded parsing.
- `lem-fixed-machine-history-predicate-is-primitive-recursive`: actual bounded syntax/configuration/adjacency tests, first-halt recognition and output uniqueness.

New direct dependency declarations, synchronized between manifest and item frontmatter:

- `def-rp-corp-zpp-bpp-and-pp-classes` → `thm-nonnegative-series-bounded-partial-sums`, for survival-sum expectation and bounded restart sums.
- `lem-chernoff-bound-for-independent-bernoulli-trials` → `thm-exponential-addition-formula`, `cor-exponential-reciprocal-and-positivity`, `thm-derivative-of-exponential`, `thm-exponential-is-strictly-increasing`, `thm-logarithm-derivative-and-integral`, `thm-chain-rule`, `thm-algebra-of-derivatives`, `cor-mean-value-theorem`. These supply exactly the explicitly used analytic laws; elementary inequalities are derived in the item.
- Both F_5 examples → `thm-z-mod-p-is-a-field`; primality of 5 and the numerical inverse calculations are written locally.

A current home/plan traversal found every non-owned direct dependency in the existing six-page prerequisite closure. No extra page prerequisite is needed. The batch-owned cross-batch input remains `[]`: no same-group, other-batch or Fourier-partition supplier is consumed. The prescribed frontier-ledger refresh passed after dependency changes.

Canonical `research/plan-spec.json` still has zero items on each owned A/B page; the owned manifest and page files have 19 and 3. This is the expected **pre-splice mismatch**, not a completed canonical-plan reconciliation. Step 4 must splice the inventories and updated dependency declarations and rerun validation. Preserve orders 612.2/612.4, all IDs, companions and titles. Suggested shared prose amendments: name the newly explicit series/calculus/prime-field prerequisites in the development description; clarify that arity notation feeds a raw program the tuple word, not a tuple word misidentified as a unary numeral; retain the time-simulator hold. No shared prose was edited here.

No AC use was found in the authored arguments. Programs, list folds, finite seed solutions and finite probability spaces are explicit. The apparent analytic AC route was checked by reading the current IVT and sequential-continuity proofs: IVT consumes only the choice-free continuity-to-sequences direction (sequential criterion step 1.2), not its countable-choice converse (step 1.3). No blanket inference of AC from graph reachability is made. No Recorded result is consumed.

## Published concerns for the serial reconciler

1. **Confirmed missing proof supplier, already UC-73/A-P:** `def-efficient-universal-simulation-with-clock`, page `time-and-space-hierarchy-theorems`. Its current Definition fixes one U and asserts both O(n+b log(b+2)) time and O(n+S+log(b+2)) space. Read the full current text. Neither a definition nor Appendix 1.A provides the joint guarantee. Required exact supplier is `thm-clocked-universal-simulation-with-time-and-space-bounds`, still an incomplete draft and not published. Repair strategy: after that theorem is genuinely completed/published, split time consumers onto U_time and space consumers onto U_space, preserving constructor time/workspace terms. Confidence: confirmed dependency/proof omission, not a proof that no joint simulator can exist. This published item is not used by our new supplier and does not independently block its construction. Update the existing finding, do not duplicate it.
2. **Confirmed identity/alias collision; mathematical misresolution not established:** `thm-algebra-of-derivatives`, page `the-derivative-and-mean-value-theorems`, has alias `thm-product-rule`; an actual distinct item `thm-product-rule` is published on `finite-counting-and-binomial-coefficients`. Read both full texts. One is differentiation and the other cardinality of finite products. The new Chernoff item uses the unambiguous canonical ID `thm-algebra-of-derivatives`. Required supplier: none from Phase 2. Repair strategy: owner/serial audit determines loader behavior, removes or migrates the colliding alias, and checks citations intended for the differentiation rule. Confidence: certain collision, no claim of a demonstrated incorrect rendered link or mathematical proof defect.
3. **Potential ambiguity, not a confirmed theorem defect:** `def-turing-machine-configuration`, page `turing-machines-configurations-and-computation`, writes a configuration word over Q union Gamma with an untagged state inserted before the scanned symbol. If the alphabets overlap, the word need not encode a unique configuration. With numeric states/symbols containing 0,1, the configuration q=0,h=0,tape (1,1) and q=1,h=1,tape (0,1) both yield the word (0,1,1). Both satisfy the stated finite-support convention. The item does not explicitly assert injectivity, so this is an impact-review candidate only. The published step interpreter already uses separate self-delimiting state/head/tape blocks and is unaffected; our `def-kleene-computation-predicate-and-output-map` likewise uses an explicit triple/list encoding. Required suppliers for a repair: either the published self-delimiting interpreter convention or the new numeric triple definition after publication. Proposed remedy: specify tagged/disjoint symbols if this word is intended as a code, or explicitly say it is not an injective encoding. Do not classify downstream defects without finding an actual use of untagged decoding.

The canonical published-consumer ledger was not edited. These findings are reports to its serial owner, not classification-index updates or independent audits.

## Checks actually run

| Check | Final result | Scope / limitation |
|---|---|---|
| Explicit-path precheck | exit 0; 18 proof-bearing items checked, 0 failures | All 22 item paths supplied. Three pure definitions and the explicitly incomplete theorem contain no numbered proof for the tool to check; this is not proof completion. |
| Explicit-path rendercheck | exit 0; 24 files | All 22 items and both draft pages; real KaTeX and renderer YAML parsing. |
| Authored content-policy | exit 0; 22 items, 0 errors/warnings | Current batch manifest and written items. |
| Strict proof-contract | exit 1; 21/22 checked, exactly one error | Missing completed contract for the held time-simulator theorem. Every completed item passes, with actual numbered derivations, cited excerpts/uses and eight boundary dispositions. |
| validate-plan research/plan-spec.json | exit 0 | Canonical plan is acyclic/consistent; owned inventories have not been spliced. Reported 978 inventoried pages and 641 empty planned inventories at the check. |
| coverage-checklist --require-destination | exit 1; 4 errors | Gallier and Aspnes remain owner-held; full local alternative arguments are now present. |
| source-fetch-check, check mode | exit 1; 2/4 verified/resolved | No fetch/stamp/retry was attempted. The two source holds remain. |
| frontier dependency refresh | exit 0 | Batch 12 input empty, as verified against current exact dependencies. |
| Scoped Step-3 decision inspection | 21 current closed, 1 owner-held; scope closed | No owner flag used; held theorem requires current owner decision after final clarification. |

Initial local checks exposed phase-number canonicalization, an unsupported KaTeX macro, and source/generation YAML formats not recognized by the content-policy reader. These were repaired and the final checks above rerun. The mathematical content of completed items was not regenerated from scaffold strategies. Formatting scripts only wrote the explicitly authored bodies and extracted their actual numbered claims/tags into contracts. Independent review records were preserved.

## Per-item checkpoint (final current state)

Each checkpoint was recorded before advancing to the next manifest item. Current exact statements/proofs and boundary evidence are in `items/ID.md` and `research/phase-2-next-20-batch-12.proof-contracts.json`; the following records give the retained claim, actual decision evidence and examined direct interfaces. Source qualifications above apply throughout.
### def-uniform-asymptotic-time-space-comparison

**ACCEPT — complete local authoring**

Retained claim/conventions: For nonnegative functions f,g on N, f=O(g) means there exist C>0,N with f(n)<=C g(n) for all n>=N; f=o(g) means for each epsilon>0 some N works with f(n)<=epsilon g(n). Write O_M when C,N may depend on the fixed finite description M but not on input or clock. Fix finite-alphabet, finitely many right-infinite clamped tapes, transition-count time, and the sum of visited cells as space. Resource bounds used after reading the whole input obey a stated n+1 lower guard; read-only input space, if used, is separately labelled.

Evidence: Authored the quantified O/o and fixed-description uniformity conventions, with visited versus initialized cells distinguished. Read the natural-number and multitape definitions. No simulator assertion or constructibility equivalence is imported. Final explicit-path precheck/rendering, authored content-policy and this item’s strict contract pass after YAML source-format normalization; dependent receipt refreshed without reauthoring unchanged mathematics.

Examined direct dependency IDs: `def-multitape-and-nondeterministic-machines`, `def-natural-numbers`.

### def-machine-time-and-space-constructibility

**ACCEPT — complete local authoring**

Retained claim/conventions: Fix nondecreasing integer-valued t,s with t(n),s(n)>=n+1 for the complete-input-reading model. Time constructible means a fixed deterministic multitape transducer, on unary 1^n, produces canonical bin(t(n)) and halts in O(t(n)) steps. Space constructible means such a transducer outputs bin(s(n)) using O(s(n)) visited cells. Exact-clock constructibility means a machine halting at exactly t(n) transitions and is a separately stated stronger interface; no equality of these definitions is assumed. Constructor workspace W_t(n) remains explicit.

Evidence: Authored binary-output, space and exact-clock conventions separately. Constructor workspace and external-clock costs remain explicit. This definition uses only the preceding resource interface; no unread constructibility theorem is assumed. Final explicit-path precheck/rendering, authored content-policy and this item’s strict contract pass after YAML source-format normalization; dependent receipt refreshed without reauthoring unchanged mathematics.

Examined direct dependency IDs: `def-uniform-asymptotic-time-space-comparison`.

### thm-clocked-universal-simulation-with-time-and-space-bounds

**ESCALATED — not complete**

Retained claim/conventions: There exist two distinct fixed finite-alphabet multitape interpreters U_time and U_space for coded deterministic finite-tape machines. On valid (M,x,bin(b)) each reproduces acceptance or rejection attained in at most b simulated transitions, and rejects at expiry. Every malformed input halts rejecting. Put n=|<M,x>| (including its self-delimiting syntax) and S=sum of simulated visited cells. For each fixed M, U_time costs O_M(n+b log(b+2)) transitions and U_space uses O_M(n+S+log(b+2)) visited cells, including decoder and clock storage. b=0 and b=1 are included. No fast-time bound is asserted for U_space and no linear-S bound for U_time.

Evidence: Unresolved time conjunct: no complete buffer redistribution/reset charging proof or lazy-input initialization proof. Space construction is explicit but cannot certify the conjunction. Owner remedy: supply/review full Appendix 1.A adaptation on this same A page; preserve both interpreters and b=0,1. Exhausted source attempts retained; no confidence or completion asserted.

Examined direct dependency IDs: `def-effective-encoding-of-turing-machines`, `def-machine-time-and-space-constructibility`, `def-multitape-and-nondeterministic-machines`, `def-one-step-configuration-relation`, `def-turing-machine-initial-and-halting-configuration-interface`, `lem-machine-encoding-is-injective-and-decodable`.

### def-primitive-recursive-functions-by-initial-functions-and-schemes

**ACCEPT — complete local authoring**

Retained claim/conventions: PR is the least arity-indexed class of total functions N^k->N containing zero, successor and projections, closed under composition and primitive recursion f(a,0)=g(a), f(a,y+1)=h(a,y,f(a,y)). Nullary constants and empty parameter tuples are allowed. A predicate is PR exactly when its 0/1 characteristic function is PR.

Evidence: Authored PR schemes including nullary cases and proved parameterized recursion/totality from the published unique-recursion theorem, whose complete proof was read. Source recovery remains open at coverage level; no source reading is invented. Final explicit-path precheck/rendering, authored content-policy and this item’s strict contract pass after YAML source-format normalization; dependent receipt refreshed without reauthoring unchanged mathematics.

Examined direct dependency IDs: `def-natural-numbers`, `thm-recursion`.

### lem-fixed-coding-primitive-recursive-arithmetic-and-sequences

**ACCEPT — complete local authoring**

Retained claim/conventions: Addition, multiplication, truncated subtraction, comparisons, bounded sums/products, bounded search, exponentiation, and the following finite-list operations are PR. Pair(a,b)=2^a(2b+1), empty list=0, cons(a,z)=Pair(a,z); head and tail are zero at zero and otherwise the 2-adic valuation and odd-part decoder. Length, coordinate (zero beyond length), append, concatenation and bounded list predicates are PR.

Evidence: Proved the actual PR arithmetic, bounded search, cons decoding, decreasing-tail length and list folds. The bounded scan argument depends on the explicit PR update operations, not an assertion that computable means PR. Read PR schemes and induction proof. Final explicit-path precheck/rendering, authored content-policy and this item’s strict contract pass after YAML source-format normalization; dependent receipt refreshed without reauthoring unchanged mathematics.

Examined direct dependency IDs: `def-primitive-recursive-functions-by-initial-functions-and-schemes`, `thm-induction-principle`.

### def-kleene-computation-predicate-and-output-map

**ACCEPT — complete local authoring**

Retained claim/conventions: Use the preceding list code for all histories and word data, but retain the published literal machine-code syntax. Index e denotes the e-th binary word in length-lex order (zero-based); malformed words denote a nowhere-defined function. Numeric input is canonical binary, with bits 0,1 represented by machine letters 1,2. Valid programs for numeric functions have s>=2. A terminal tape outputs the maximal initial word over {1,2}; it is accepted as a numerical output only if its bit word is canonical. A run halting with invalid output denotes undefined, implemented by an explicit looping wrapper. For arity k, encode inputs as the published self-delimiting tuple of canonical numerals. T_k(e,a,z) means z is a nonempty list of valid consecutive configurations starting on that tuple and ending at its first halt with canonical numerical output. U(z) extracts that value, and is 0 if z has no valid final output.

Evidence: Fixed the literal program indexing, uniform tuple inputs including unary, canonical configuration trimming, first-halt predicate and total output extractor. Read the published encoding, tuple proof, configuration, step and partial-function texts. The existing configuration-word notation is not used as an unambiguous numeric parser. Final explicit-path precheck/rendering, authored content-policy and this item’s strict contract pass after YAML source-format normalization; dependent receipt refreshed without reauthoring unchanged mathematics.

Examined direct dependency IDs: `def-effective-encoding-of-turing-machines`, `def-one-step-configuration-relation`, `def-partial-function-computed-by-a-machine`, `def-turing-machine-configuration`, `lem-finite-tuples-admit-effective-prefix-free-encodings`, `lem-fixed-coding-primitive-recursive-arithmetic-and-sequences`.

### lem-fixed-machine-history-predicate-is-primitive-recursive

**ACCEPT — complete local authoring**

Retained claim/conventions: For each fixed k, T_k and U just defined are primitive recursive. T_k(e,a,z) holds exactly when z codes a terminating numeric computation of program e on a, and all witnesses for fixed e,a yield the same U(z).

Evidence: Authored the bounded program/configuration parsers and precise adjacent-step test, then proved recognition and uniqueness by determinism. All loops are bounded by explicit decoded lengths; this is a complete local PR proof independent of the held simulator. Final explicit-path precheck/rendering, authored content-policy and this item’s strict contract pass after YAML source-format normalization; dependent receipt refreshed without reauthoring unchanged mathematics.

Examined direct dependency IDs: `def-kleene-computation-predicate-and-output-map`, `lem-fixed-coding-primitive-recursive-arithmetic-and-sequences`, `thm-induction-principle`.

### thm-kleene-normal-form-for-the-fixed-machine-coding

**ACCEPT — complete local authoring**

Retained claim/conventions: For every fixed arity k, the numeric function of index e is phi_e(a)=U(mu z T_k(e,a,z)), with undefined value when no witness exists. The partial recursive functions, generated from initial functions by composition, primitive recursion and unbounded minimization, are exactly the partial functions computed by Turing machines under these numeric encodings. Minimization of a partial g requires g(a,j) defined and nonzero for every j<z and g(a,z)=0.

Evidence: Authored least-witness normal form and a finite tape-routine compiler for initial functions, strict composition, primitive recursion and sequential minimization. Read the complete step-interpreter proof; no fast simulation result is used. Explicit parity-safe stay elimination is included. Final explicit-path precheck/rendering, authored content-policy and this item’s strict contract pass after YAML source-format normalization; dependent receipt refreshed without reauthoring unchanged mathematics.

Examined direct dependency IDs: `def-primitive-recursive-functions-by-initial-functions-and-schemes`, `lem-fixed-machine-history-predicate-is-primitive-recursive`, `lem-step-by-step-interpreter-for-machine-codes`.

### def-computable-many-one-reducibility-interface

**ACCEPT — complete local authoring**

Retained claim/conventions: For A,B subsets of N, A<=_m B means there is a total computable f:N->N such that n in A iff f(n) in B for every n. For oracle X, A<=_m^X B means the same with f total X-computable: a finite machine may ask membership of a canonical numeral in X and receives its bit answer in one query transition. For languages L,K over fixed finite alphabets, L<=_m K instead uses a total string-valued computable map. A numeric reduction is equivalent to a string reduction on canonical numeral inputs whose outputs on those inputs are required to be canonical. No unrestricted image-language equivalence is asserted.

Evidence: Authored numeric/string/oracle many-one definitions and both directions of the restricted encoding bridge, plus identity and composition. No nonmember of the target is assumed. Patey original-source reading remains unclaimed; the local argument is explicit. Final explicit-path precheck/rendering, authored content-policy and this item’s strict contract pass after YAML source-format normalization; dependent receipt refreshed without reauthoring unchanged mathematics.

Examined direct dependency IDs: `def-partial-function-computed-by-a-machine`, `thm-kleene-normal-form-for-the-fixed-machine-coding`.

### def-oracle-turing-reducibility-interface

**ACCEPT — complete local authoring**

Retained claim/conventions: An oracle X subset of N answers a query written as a canonical binary numeral by its membership bit; malformed queries receive 0 by convention. A<=_T B means a deterministic oracle machine with oracle B halts on every n and outputs the characteristic bit of A. Queries may be adaptive and both yes and no answers are usable. Fix relative partial computability and X-c.e. sets by oracle computation and acceptance; membership deciders always halt.

Evidence: Authored adaptive query semantics, relative partial computability and c.e. recognition separately from total characteristic deciders. Proved transitivity by finite-run induction and the single-query many-one implication. Final explicit-path precheck/rendering, authored content-policy and this item’s strict contract pass after YAML source-format normalization; dependent receipt refreshed without reauthoring unchanged mathematics.

Examined direct dependency IDs: `def-computable-many-one-reducibility-interface`, `def-multitape-and-nondeterministic-machines`.

### def-acceptable-numbering-with-universal-evaluation

**ACCEPT — complete local authoring**

Retained claim/conventions: A numbering lists all unary partial computable functions phi_e under the fixed numeral and tuple encodings. It is acceptable when evaluation (e,x)->phi_e(x) is partial computable and there is a total computable composition compiler c with phi_c(e,d)=phi_e composed with phi_d. The fixed machine numbering, assigning malformed programs the empty partial function, has these properties. Arity-k notation means unary evaluation on the fixed tuple encoding.

Evidence: Proved universality and a total literal composition compiler without s-m-n. Clarified arity notation as raw single-word evaluation on tuple encodings, rather than confusing tuple words with canonical natural numerals. All original numbering claims are retained. Final explicit-path precheck/rendering, authored content-policy and this item’s strict contract pass after YAML source-format normalization; dependent receipt refreshed without reauthoring unchanged mathematics.

Examined direct dependency IDs: `def-kleene-computation-predicate-and-output-map`, `lem-step-by-step-interpreter-for-machine-codes`, `thm-kleene-normal-form-for-the-fixed-machine-coding`.

### thm-smn-for-the-fixed-acceptable-numbering

**ACCEPT — complete local authoring**

Retained claim/conventions: For each m,n>=1 there is a total computable s_m_n(e,a_1,...,a_m) such that phi_s_m_n(e,a)(y_1,...,y_n)=phi_e(a_1,...,a_m,y_1,...,y_n), with equality of domains and values. The compiler is also uniform in the finite positive arities and encoded parameter tuple.

Evidence: Authored a uniform finite specialization compiler with actual arity header/word-block construction and domain equality. Constants are expanded into finite writing-state chains; no execution of e occurs during compilation. Final explicit-path precheck/rendering, authored content-policy and this item’s strict contract pass after YAML source-format normalization; dependent receipt refreshed without reauthoring unchanged mathematics.

Examined direct dependency IDs: `def-acceptable-numbering-with-universal-evaluation`, `lem-fixed-coding-primitive-recursive-arithmetic-and-sequences`.

### thm-kleene-fixed-point-theorem-for-program-indices

**ACCEPT — complete local authoring**

Retained claim/conventions: For every total computable F:N->N there is e with phi_e=phi_F(e) as partial functions. An index e can be obtained effectively from an index of F on the promise that F is total; this asserts extensional equality, not e=F(e).

Evidence: Proved the direct s(q,q) fixed-point construction with effective compilation from F’s index and exact divergence agreement. Totality is used only to guarantee the F subcall terminates; no numerical index equality is asserted. Final explicit-path precheck/rendering, authored content-policy and this item’s strict contract pass after YAML source-format normalization; dependent receipt refreshed without reauthoring unchanged mathematics.

Examined direct dependency IDs: `def-acceptable-numbering-with-universal-evaluation`, `thm-smn-for-the-fixed-acceptable-numbering`.

### def-polynomial-time-verifier-and-reduction-interface

**ACCEPT — complete local authoring**

Retained claim/conventions: Polynomial time means worst-case all-input multitape time <=C(n+1)^k for fixed C,k. A polynomially balanced verifier relation R(x,w) is decided in time polynomial in |x|+|w| and has an explicit witness guard |w|<=p(|x|); its language is {x:exists w, |w|<=p(|x|) and R(x,w)}. A polynomial many-one reduction of binary languages is a total transducer f with x in L iff f(x) in K, and polynomial time/output length. Define P and the verifier presentation of NP at this interface without asserting P=NP or a completeness theorem.

Evidence: Authored P/verifier-NP and polynomial reduction interfaces, direct polynomial composition and explicit witness padding. These use fixed programs, not the unresolved fast universal simulator. Final explicit-path precheck/rendering, authored content-policy and this item’s strict contract pass after YAML source-format normalization; dependent receipt refreshed without reauthoring unchanged mathematics.

Examined direct dependency IDs: `def-multitape-and-nondeterministic-machines`, `def-partial-function-computed-by-a-machine`, `def-uniform-asymptotic-time-space-comparison`.

### def-probabilistic-polynomial-time-machine-interface

**ACCEPT — complete local authoring**

Retained claim/conventions: A PTM is a fixed multitape machine with two total transition tables; each transition selects a fresh independent unbiased bit. It is polynomial time if every random branch on input x halts within p(|x|), for one fixed integer polynomial bound. Equivalently supply a uniform random word r in {0,1}^{p(|x|)} to a deterministic simulator; unused suffix bits are ignored. Space/time refer to the computation, not to the finite probability space itself.

Evidence: Authored finite random-word semantics and proved fresh-bit equivalence by prefix counting and path induction, including unused suffixes. The all-branch polynomial condition is explicit. Final explicit-path precheck/rendering, authored content-policy and this item’s strict contract pass after YAML source-format normalization; dependent receipt refreshed without reauthoring unchanged mathematics.

Examined direct dependency IDs: `def-independence-of-finite-random-variables`, `def-polynomial-time-verifier-and-reduction-interface`.

### def-rp-corp-zpp-bpp-and-pp-classes

**REPAIRED — complete local authoring**

Retained claim/conventions: For a polynomial all-branch PTM, RP has acceptance probability zero off L and at least 1/2 on L; coRP has probability one on L and at most 1/2 off L; BPP has at least 2/3 on L and at most 1/3 off L; PP has strictly more than 1/2 on L and at most 1/2 off L. ZPP is zero-error expected polynomial time, equivalently a polynomial-time PTM with outputs yes/no/unknown, never wrong and with unknown probability at most 1/2 on every input. ZPP=RP intersect coRP.

Evidence: Repaired the missing expected-time foundation by declaring and reading thm-nonnegative-series-bounded-partial-sums. Authored finite-prefix truncated-expectation identity, Markov cutoff, bounded restart sum and both directions of ZPP=RP intersection coRP. No infinite probability space or AC is assumed. Final explicit-path precheck/rendering, authored content-policy and this item’s strict contract pass after YAML source-format normalization; dependent receipt refreshed without reauthoring unchanged mathematics.

Examined direct dependency IDs: `def-probabilistic-polynomial-time-machine-interface`, `thm-markov-inequality`, `thm-nonnegative-series-bounded-partial-sums`.

### def-pairwise-independent-hash-family-interface

**ACCEPT — complete local authoring**

Retained claim/conventions: For finite sets D,R with R nonempty and a finite nonempty seed set K, a uniformly seeded family (h_k:D->R) is pairwise independent if each h_k(x) is uniform and for distinct x,y and all a,b in R the probability of (h_k(x),h_k(y))=(a,b) is 1/|R|^2. Seeds may give repeated functions. This is stronger than merely bounding collision probabilities. For a finite field F, h_(a,b)(x)=ax+b with uniform (a,b) in F^2 is such a family, allowing a=0.

Evidence: Authored the general seed-count definition and proved the affine finite-field family on A, including marginal counts, zero slopes, unique solutions and the singleton-domain condition. Read field axioms and inverse uniqueness; no prime-field construction is assumed here. Final explicit-path precheck/rendering, authored content-policy and this item’s strict contract pass after YAML source-format normalization; dependent receipt refreshed without reauthoring unchanged mathematics.

Examined direct dependency IDs: `def-field`, `def-independence-of-finite-random-variables`.

### lem-chernoff-bound-for-independent-bernoulli-trials

**REPAIRED — complete local authoring**

Retained claim/conventions: For mutually independent Bernoulli(p_i) variables X_1,...,X_N on a finite probability space, S=sum X_i and mu=sum p_i, for delta>=0, P(S>=(1+delta)mu)<=exp(-mu((1+delta)log(1+delta)-delta)). For 0<=delta<1, P(S<=(1-delta)mu)<=exp(-mu(delta+(1-delta)log(1-delta))). Consequently for 0<=delta<=1 the upper tail is <=exp(-mu delta^2/3), the lower tail <=exp(-mu delta^2/2), and the two-sided deviation <=2exp(-mu delta^2/3). At delta=1 the lower formula is interpreted as exp(-mu); mu=0 and N=0 are handled directly. Also for N>0, t>=0, P(|S-mu|>=t)<=2exp(-2t^2/N).

Evidence: Repaired the missing calculus dependencies and derived every analytic inequality locally by explicit derivatives and MVT, with no integration or unproved Chernoff supplier. Authored optimized/simplified multiplicative tails, delta=1, mu=0, empty sum and both-sign centered-mgf additive tails. Read every newly declared calculus statement and proof; no full transitive-corpus audit is claimed. Final explicit-path precheck/rendering, authored content-policy and this item’s strict contract pass after YAML source-format normalization; dependent receipt refreshed without reauthoring unchanged mathematics.

Examined direct dependency IDs: `cor-exponential-reciprocal-and-positivity`, `cor-mean-value-theorem`, `def-bernoulli-and-binomial-random-variables`, `def-natural-logarithm`, `def-real-exponential-function-and-e`, `thm-algebra-of-derivatives`, `thm-chain-rule`, `thm-derivative-of-exponential`, `thm-expectation-of-products-of-independent-random-variables`, `thm-exponential-addition-formula`, `thm-exponential-is-strictly-increasing`, `thm-logarithm-derivative-and-integral`, `thm-markov-inequality`.

### thm-schwartz-zippel-over-finite-fields

**ACCEPT — complete local authoring**

Retained claim/conventions: Let F be a finite field, m>=1 and f in F[x_1,...,x_m] a nonzero formal polynomial of total degree d. For a nonempty subset A of F and independent uniform a_i in A, P(f(a_1,...,a_m)=0)<=min(1,d/|A|). In particular A=F gives d/|F|. Nonzero constant polynomials have probability zero; a nonzero formal polynomial may induce the zero function on F^m when d is large.

Evidence: Authored the full induction by zero counts, with field-to-domain cancellation, nonzero leading coefficient specializations, constants, singleton sample sets and large-degree polynomial functions. Read the root-bound proof and exact monomial-degree definition. Final explicit-path precheck/rendering, authored content-policy and this item’s strict contract pass after YAML source-format normalization; dependent receipt refreshed without reauthoring unchanged mathematics.

Examined direct dependency IDs: `def-field`, `def-monomials-multidegree-and-total-degree`, `thm-induction-principle`, `thm-root-bound-for-polynomials-over-a-domain`.

### ex-smn-specializes-a-two-argument-index

**ACCEPT — complete local authoring**

Retained claim/conventions: Let e be the explicitly compiled index for f(a,y)=a+y. Then q=s_1_1(e,3) computes y->3+y, in particular phi_q(0)=3 and phi_q(4)=7.

Evidence: Authored the addition loop invariant and actual stored-parameter tuple/numeral calculations, defining e by its finite compiled code rather than fabricating a decimal index. Final explicit-path precheck/rendering, authored content-policy and this item’s strict contract pass after YAML source-format normalization; dependent receipt refreshed without reauthoring unchanged mathematics.

Examined direct dependency IDs: `lem-fixed-coding-primitive-recursive-arithmetic-and-sequences`, `thm-smn-for-the-fixed-acceptable-numbering`.

### ex-pairwise-independent-affine-hashing-over-a-prime-field

**REPAIRED — complete local authoring**

Retained claim/conventions: Over F_5 the 25 seeds (a,b) in F_5^2 give h(x)=ax+b. At distinct inputs 1,3 and prescribed outputs 2,4 the unique seed is (1,1). Every ordered output pair occurs once, so the two values are independent uniform. The family is not three-wise independent: h(2)=2h(1)-h(0).

Evidence: Added the exact published prime-field supplier and verified primality of 5 and its inverse table locally. Computed the unique seed and the zero-versus-1/125 triple witness; no field construction is hidden. Final explicit-path precheck/rendering, authored content-policy and this item’s strict contract pass after YAML source-format normalization; dependent receipt refreshed without reauthoring unchanged mathematics.

Examined direct dependency IDs: `def-pairwise-independent-hash-family-interface`, `thm-z-mod-p-is-a-field`.

### ex-schwartz-zippel-for-a-bivariate-polynomial

**REPAIRED — complete local authoring**

Retained claim/conventions: For f(X,Y)=XY-1 over F_5, total degree is 2 and exactly four of 25 pairs are roots: (1,1),(2,3),(3,2),(4,4). Thus the actual zero probability is 4/25, below the Schwartz–Zippel bound 2/5.

Evidence: Added the published prime-field supplier and fully calculated all fibers of XY=1 modulo 5. The independent product count and strict comparison with 2/5 are explicit. Final explicit-path precheck/rendering, authored content-policy and this item’s strict contract pass after YAML source-format normalization; dependent receipt refreshed without reauthoring unchanged mathematics.

Examined direct dependency IDs: `thm-schwartz-zippel-over-finite-fields`, `thm-z-mod-p-is-a-field`.

## Resume instruction

Before further mathematical work, reread this checkpoint, the current simulator item, its exact dependencies, and the relevant complete source passages if genuinely recovered. Do not use the inherited source summaries as proof evidence. Complete the time proof and source owner dispositions before representing this batch as closed. Unchanged completed items need only dependency/receipt refresh when their actual inputs change, not reauthoring.
