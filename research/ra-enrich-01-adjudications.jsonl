{"id": "thm-taylor-series-representation-by-remainder", "model": "gpt-5.6-terra", "item_sha256": "1cab9a6c9ab96da3dc209e31614dddf5188084441d3dc6c5186503507de3f697", "outcome": "confirmed_nonfatal", "defect_type": "dependency_citation", "note": "Correct. Step 1.3 called [a-r,a+r] compact with no fact supplying it: the declared deps carried the extreme value theorem but nothing establishing compactness of a closed bounded interval. Nonfatal under the 30-second rule — thm-heine-borel-r is published and says exactly that. Repaired: added thm-heine-borel-r, thm-algebra-of-continuous-functions and def-higher-derivatives-and-smoothness as deps, added facts [L5]-[L7], and rewrote step 1.3 to derive continuity of f^(n+1) from C-infinity, continuity of |f^(n+1)| from the absolute-value clause, and compactness from Heine-Borel before applying [L2]."}
{"id": "thm-taylor-series-representation-by-remainder", "model": "deepseek-v4-pro", "item_sha256": "1cab9a6c9ab96da3dc209e31614dddf5188084441d3dc6c5186503507de3f697", "outcome": "confirmed_nonfatal", "defect_type": "dependency_citation", "note": "Same defect as the Terra rejection on this id, independently found; both lanes are right. Closed by the same repair to step 1.3. The auditor's Finding 1 (continuity of the absolute-value composite) is the second half of the same gap and is closed by the same edit."}
{"id": "thm-standard-maclaurin-expansions", "model": "gpt-5.6-terra", "item_sha256": "f477c0a3ef09a4d27fcf865dc064369f6daa1cca5582189c1105da1029b4c877", "outcome": "confirmed_nonfatal", "defect_type": "dependency_citation", "note": "Correct. Step 1.4 asserted c_n = binom(m,n) from the recurrence, but [L7] gave only the factorial rule and the finite binomial theorem — no formula or recurrence for the coefficients, so the identification was unlicensed. Nonfatal: thm-binomial-closed-formula is published and supplies binom(n,k) = n!/(k!(n-k)!) as a real number. Repaired: added it as [L12] and wrote out the induction (base binom(m,0)=1; step uses (n+1)!=(n+1)n! and (m-n)!=(m-n)(m-n-1)! for n<m; c_{m+1}=0 kills the tail), then took a=1,b=x in the finite binomial theorem."}
{"id": "thm-standard-maclaurin-expansions", "model": "deepseek-v4-pro", "item_sha256": "f477c0a3ef09a4d27fcf865dc064369f6daa1cca5582189c1105da1029b4c877", "outcome": "confirmed_nonfatal", "defect_type": "dependency_citation", "note": "Correct, and independently found by the auditor as Finding 4. [L6] (thm-real-power-continuity-and-derivatives) gives the derivative of u -> u^beta on (0,infinity) only; step 1.6 differentiated the affine composite x -> (1+x)^(-alpha) with no chain rule cited and thm-chain-rule absent from deps. Nonfatal: the asserted derivative is correct and both the chain rule and the power rule are published. Repaired: added thm-chain-rule as [L13] and lem-derivative-of-a-power as [L14], and rewrote step 1.6 to discharge both limit-point hypotheses (1+x lies in (0,infinity)) before composing."}
{"id": "thm-euler-mascheroni-constant-and-harmonic-asymptotic", "model": "gpt-5.6-terra", "item_sha256": "35efe7c567a13efde1834d3c5fdd0db119f7488e1150fe84639c09beaec4db6d", "outcome": "confirmed_nonfatal", "defect_type": "dependency_citation", "note": "Correct. Step 2.3 cited only [L2] and step 1.5 to get log n >= log(2^m); [L2] compares two integrands on one fixed interval and says nothing about integrals with different upper limits. The needed route is [L1] plus additivity [L3] plus nonnegativity, none of which the step cited — Terra names exactly this. Nonfatal: all three are already declared deps of the item. Repaired: restated [L2] with its nonnegativity and two-sided-bound clauses and [L3] with its integrability clause, then rewrote step 2.3 to split log n = log(2^m) + integral over [2^m,n] with the n = 2^m case handled separately, so no oriented-integral convention is needed. DeepSeek passed this item; on this step Terra is right and DeepSeek's pass was not wrong about the mathematics, only about the licensing."}
{"id": "def-radian-angle-by-unit-circle-arc-length", "model": "deepseek-v4-pro", "item_sha256": "8d3c3b00ad167c2aac0cfbb404d3e98520298ae89694c95d879a0cb26191ba50", "outcome": "confirmed_nonfatal", "defect_type": "other", "note": "Correct, and the one finding here that is not merely a missing citation. def-circular-arcs-circumference-and-diameter admits only parameter intervals [alpha,beta] with alpha < beta, so the singleton restriction at t=0 is not an arc under the cited definition and the draft's phrase 'that unit-circle arc ... this includes t=0' asserted something the citation does not give. Nonfatal, not fatal: the assigned value is correct and independently supported — def-path-polygonal-length-and-rectifiability-in-rn defines L to be 0 on a singleton interval, and thm-c1-paths-have-length-equal-to-the-integral-of-speed separately assigns zero to a singleton. Terra passed this item; DeepSeek's cross-family read caught a real boundary defect Terra missed. Repaired honestly rather than papered over: the arc language is now stated for 0 < t <= 2pi, and t=0 is handled explicitly as the degenerate one-point path, said in as many words not to be a circular arc, with radian measure 0 licensed by the path-length singleton convention. The approved statement (0 <= t <= 2pi, radian measure L = t, full turn 2pi) is unchanged. The auditor's Finding 2 (continuity of gamma and gamma' never discharged before invoking the speed theorem) is closed in the same edit via cor-differentiable-implies-continuous and def-vector-valued-derivative-and-integral, and lem-integral-elementary-bounds now licenses the constant integral."}
{"id": "cex-smooth-function-not-equal-to-its-maclaurin-series", "model": "deepseek-v4-pro", "item_sha256": "76e2bd995e7857848080ead2b2eea9e97ab10319b463a995bba204522b6152e1", "outcome": "confirmed_nonfatal", "defect_type": "dependency_citation", "note": "Correct. Steps 1.1 and 1.2 used q'(x) = 2x and p'(x) for a polynomial p while the declared deps supplied only the chain rule and the algebra of derivatives — [L3] combines differentiable functions but never says the identity or a power is one, so the smoothness induction had no base. Nonfatal: lem-derivative-of-a-power is published and gives the power rule and termwise polynomial differentiation. Repaired: added it as [L5], rewrote steps 1.1 and 1.2 to cite it and to display the product-plus-chain computation that keeps the p(x)phi^(j)(q(x)) form closed, and replaced the hand-rolled continuity argument in step 3.1 with cor-differentiable-implies-continuous plus def-higher-derivatives-and-smoothness, which is what C-infinity means in this library. Terra passed this item; DeepSeek is right."}
{"id": "thm-standard-maclaurin-expansions", "model": "gpt-5.6-terra", "item_sha256": "862dbbc4c3c57117d1f735eb38e4e7ef3bb6a4dac069047269bd3f43d1f4ad22", "outcome": "confirmed_nonfatal", "defect_type": "dependency_citation", "note": "Round 2. Correct. Step 4.1 asserted G'(x)=0 from [L10, step 1.6, step 3.1, algebra]. Cancelling (1+x)^(-alpha-1)(1+x) to (1+x)^(-alpha) for an arbitrary real exponent is the real-power addition law a^(r+s)=a^r a^s, not like-term cancellation, and [L16] carried that law but step 4.1 did not cite it. Nonfatal under the 30-second rule; the law was already a declared dependency (thm-real-power-laws). Repaired: step 4.1 now displays the product-rule derivative, derives (1+x)^1 = exp(log(1+x)) = 1+x and (1+x)^(-alpha-1)(1+x) = (1+x)^(-alpha) from [L16], multiplies by 1+x, and uses (1+x)B'(x)=alpha B(x) from step 3.1 to get (1+x)G'(x)=0, then divides by the nonzero 1+x. Getting (1+x)^1 = 1+x needed the exp(log w) = w direction, which [L16] stated only as log(exp y) = y, so [L16] was extended with the other half — verbatim from the published def-natural-logarithm, which states both. Class sweep for the same move: step 1.4 read '(1+x)^m' from the finite binomial theorem, whose powers are the integer powers of def-integer-power, while the Statement asserts the real power; [L7] now records the natural-power recursion a^0=1, a^(n+1)=a^n a and step 1.4 derives that the real powers (1+x)^n satisfy it, so the two agree. Step 6.1's B(0)=c_0 uses the same 0^0=1 convention and now cites [L7]. Steps 1.6 and 6.1's other real-power moves were already licensed by [L6] and [L16] and were left alone. def-integer-power added as a dep (plan order 16, well before this page at 187)."}
{"id": "def-radian-angle-by-unit-circle-arc-length", "model": "gpt-5.6-terra", "item_sha256": "9b68a41030e301a83e5042a7607de3be3ee2eaae93417dd8a3662977b28f71ae", "outcome": "confirmed_nonfatal", "defect_type": "dependency_citation", "note": "Round 2. Correct. The round-1 repair discharged scalar continuity of sine and cosine via cor-differentiable-implies-continuous and then asserted that gamma restricted to [0,t] is continuous, citing only def-vector-valued-derivative-and-integral, which supplies componentwise DIFFERENTIATION and says nothing about continuity. Nonfatal: thm-componentwise-limits-and-continuity clause 1 is published and says exactly that a map into R^m is continuous at a point iff every component is. Repaired with the full chain rather than the one missing link: -sin is continuous as a scalar multiple (thm-algebra-of-continuous-functions clause 1); continuity passes to a restricted domain (def-continuity-real, 'Continuity passes to subsets of the domain'); the R-native and metric notions of continuity for a real function on a subset of R are the same notion (lem-real-and-metric-notions-agree clause 1), which is the seam clause 1 of the componentwise theorem is stated across; then clause 1 gives continuity of gamma restricted to [0,t] and of v(u)=(-sin u, cos u) on [0,t]. Class sweep for the same move — every other vector-valued step in the paragraph was leaning the same way. Differentiability on (0,t) had the same defect from the other side: it needed the restriction clause of def-derivative (a point of a nondegenerate interval is a limit point of it, and restriction preserves the derivative there) before componentwise differentiation applies, so that is now written out, and v is named explicitly as the continuous extension the C1 hypothesis of thm-c1-paths-have-length-equal-to-the-integral-of-speed asks for. The norm computation was the third instance: ||v(u)||_2 = 1 now unfolds through def-p-norms-on-rn before the Pythagorean identity is applied. Seven deps added, all published and all earlier in plan order (137, 153, 167) than this item's page (183). No approved statement changed; the t=0 clause from round 1 is untouched."}
