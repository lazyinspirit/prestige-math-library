# FA terminal evidence — queue b, item 5

Item: `thm-lusin-theorem-for-radon-measures`.
Decision: independently repaired, restoring the manifest's Radon scope and supplying its missing finite-set argument. No dependency bytes changed.

Read the complete item, all three direct dependencies (`def-radon-measure-on-an-lch-space`, `def-regular-borel-measure-on-an-lch-space`, `lem-finite-lch-partition-of-unity-near-a-compact-set`), the Radon A/B page conventions, batch-3 manifest/coverage/notes, reader/refuter history, both contract entries (no prior item risk_review), Alpha section 4, and both Terra rejections. The partition-of-unity dependency is not invoked by this cell-core proof, so no cutoff or dependent-choice theorem is imported through it.

The rejudge's blanket denial of compact approximation of Borel preimages misses their finite measure. The initial rejection's alleged finite-measure Dieudonne Radon counterexample likewise cannot meet this page's outer-regular/open-inner-regular definition: those properties imply compact approximation for finite-measure Borel sets. Sol's stronger all-Borel regularity assumption makes the current claim true, but unnecessarily narrows the planned theorem. The repair restores Radon and gives the finite-set argument explicitly rather than leaving its applicability implicit.

Verified source: https://math.bme.hu/~pitrik/2023_24_2/Measure_Cohn.pdf — Cohn, Measure Theory, Proposition 7.2.6 and its full proof, printed p.191 / PDF index 208, establish compact inner approximation of finite (indeed sigma-finite) measurable sets under Cohn's regular convention, which the local definition translates to Radon. Theorem 7.4.4, printed pp.208–209 / PDF indices 225–226, and its following complex-valued extension confirm Lusin's conclusion. Both complete relevant proof passages were read from the full PDF, not from search snippets. No global all-Borel inner-regularity hypothesis is needed.

Independent local proof check: for finite Borel B, take a finite-measure open U containing it, compact L in U with small U\L, and open O containing U\B with measure within delta/2 of mu(U\B). Then H=L\O is compact inside B and the excess O intersect B is less than delta/2, proving mu(B\H)<delta. Finiteness of U licenses the subtraction. All subsequent cell preimages are subsets of finite E, so this applies to every one. Truncating the complex value range loses less than epsilon/4. For m>=0, errors epsilon*2^(-m-3), divided across the finite partition, sum to epsilon/4. The intersection of finite compact unions is closed in H_0 and is compact. Its disjoint compact cell pieces are relatively clopen; their diameters tend to zero, proving continuity. Empty preimages and K are harmless. The codomain C already implies pointwise finiteness, so the redundant “finite almost everywhere” phrase was removed without changing the class of functions.

The queued item and its batch-3/merged contracts were updated; proof steps, citations, risk review, and empty boundary now match. The restored title and Radon hypothesis agree with the existing manifest and coverage promise. No direct dependency licence, new judge call, or pass stamp was used.

Focused checks on final bytes: precheck pass; strict proof-contract 1/1 with zero errors/warnings; rendercheck pass. No unresolved mathematical obligation. Next action: record exact bytes and begin the last queue item only after recorder success.
