#!/usr/bin/env node
// Alpha `a`, frontier-15 step 6b — risk_review dispositions.
//
// SCOPE, STATED HONESTLY. risk-report routes 123 high/critical items across
// batches 1-3 (20 + 62 + 41). The brief requires that each be given to an
// additional read-only proof-refuter and then carry my adjudicated disposition.
// The refuter lane is GPT 5.6 Sol on the Codex subscription and is quota-locked
// until 2026-08-20 13:32, so the refuter half was impossible for every one of
// the 123 (see the blocker in the 6b report). I did the adversarial reading
// myself for the items below and write their dispositions here.
//
// I do NOT write a risk_review for an item I did not read. A risk_review is an
// attestation, and one written to clear `--require-reviewed` would turn an
// honest red into a false green. The rest are listed as unreviewed in the report
// and the gate stays red on them, which is the true state.
import fs from 'node:fs';

const REVIEWER = 'Alpha-a frontier-15 (Claude Opus 5, xhigh, 1M) — read by Alpha directly; no independent refuter, lane quota-locked';

const REVIEWS = {
  1: {
    'lem-gauss-lower-half-lattice-count':
      'I re-derived the whole parity computation. Step 1.1: for each x, qx = p t_x + rho_x with 0 < rho_x < p, and the positive y with py < qx are exactly 1..t_x because rho_x/p lies strictly in (0,1), so S = sum t_x. Step 2.1: rho_x = u_x when eps_x = +1 and p - u_x when eps_x = -1, and since every u_x <= (p-1)/2 < p/2, the residues exceeding p/2 are exactly the eps_x = -1 ones, so the sign count N(q,p) is the count of minus signs. Summing qx = p t_x + rho_x and reducing mod 2 with p, q odd gives sum x = S + sum rho_x; sum rho_x = sum u_x + N mod 2; and the permutation clause of [L3] gives sum u_x = sum x. Hence S = N mod 2. Correct, and the claim in the Statement that this avoids floor notation is true of the text. Disposition: no defect.',
    'lem-reciprocity-rectangle-lattice-count':
      'Checked both halves of the partition. No point of the rectangle lies on py = qx, by Euclid [L2] and 1 <= x < p. The points with py < qx are exactly those S_{p,q} counts, because py < qx <= q(p-1)/2 < qp/2 forces y <= (q-1)/2 automatically, so the extra rectangle constraint is not an extra constraint; and (x,y) -> (y,x) is a bijection from the qx < py part onto the set S_{q,p} counts. |R| = ((p-1)/2)((q-1)/2). Disposition: no defect.',
    'thm-jacobi-symbol-multiplicativity':
      'Reader 1 added [L5] (fundamental theorem of arithmetic) for factorisation existence, which R1-2 correctly identified as absent: thm-canonical-prime-factorisation begins from a supplied factor list and does not produce one. I confirmed the repaired step 2.1 uses [L5] for existence and [L3]/[L4] only for canonical exponents and valuation additivity, and that step 1.1 needs no existence input because [L1] is the definition of the symbol, which carries the factorisation with it. The n = 1 empty-product case is stated. Disposition: reader repair confirmed correct.',
    'thm-jacobi-symbol-reciprocity':
      'Same repair class as multiplicativity and same verdict. I additionally checked the sign bookkeeping in step 2.1: (uv-1)/2 = (u-1)/2 + (v-1)/2 mod 2 for odd u, v, iterated over the two factorisations, gives (m-1)/2 = sum alpha_i (p_i - 1)/2 and likewise for n, and their product is congruent to E = sum alpha_i beta_j (p_i - 1)(q_j - 1)/4 mod 2. Coprimality is what makes every p_i distinct from every q_j, so [L1] applies to each pair. Disposition: reader repair confirmed correct.',
    'thm-unit-square-criterion-modulo-n':
      'Highest-scoring item in the batch (13). The CRT decomposition is now licensed for existence by [L5]. I checked the direction that usually fails: from componentwise squares to a global square, the proof chooses one root in each of finitely many nonempty local root sets and applies the inverse isomorphism, which is a finite choice and needs no choice principle. The n = 1 empty product is stated in both the Statement and step 1.1. The explicit two-part conditions (nothing at e <= 1, a = 1 mod 4 at e = 2, a = 1 mod 8 at e >= 3) match [L2]. Disposition: reader repair confirmed correct.',
    'cor-number-of-square-roots-of-a-unit-modulo-n':
      'Checked the count against the local data: 2 per odd prime-power factor by [L1], c_e for the two-part by [L2], giving 2^r c_e, with c_0 = 1 supplied by the empty product rather than by [L2], which starts at e = 1. The CRT restriction to the root set is a bijection because an element solves the global equation exactly when every component solves its own. One gap inside the 30-second rule, recorded not repaired: the root set is taken inside the unit group, which is licensed because u^2 = [a] with [a] a unit forces u to be a unit, and the proof does not say so. Disposition: no fatal defect.',
    'cor-infinitely-many-primes-congruent-to-one-modulo-three':
      'R1-6 was right that permutation invariance alone cannot yield q | P, and the repair is correct: [L4] is clause 3 of thm-generalised-associativity, the commutative permutation law, and moving q to the final position lets the recursion clause of [L3] give P = P\' q. I verified the arithmetic independently: (6P)^2 + 3 = 3(12P^2 + 1) = 3N, so q | N makes 6P a solution of x^2 = -3 mod q; N is odd and N = 1 mod 3, so q is neither 2 nor 3; [L1] then forces q = 1 mod 3. At r = 0 the empty product gives P = 1, N = 13, q = 13, and 13 = 1 mod 3 contradicts S being empty. Disposition: reader repair confirmed correct.',
  },
  2: {
    'thm-sylow-first-theorem':
      'CONFIRMED FATAL and repaired by me; see defect-ledger row f15-a-6b-001. The prior text had "We use strong induction on |G| and Wielandt\'s left action..." as step 1.1 and "We apply induction to that stabilizer." as the whole of step 3.1, with neither of the two load-bearing inferences present. I rewrote the proof: the Wielandt action on the p^a-subsets, p not dividing |Omega| by [L2], an orbit of index prime to p by the partition [L7], p^a | |H| from |G| = |H||Orb| via [L3]/[L4], then the case split — H = G forces A = G and m = 1, while H proper has |H| = p^a m\' by [L5] and the induction hypothesis of [L6] applies. I verified the route against two sources at their stated locators by extracting the PDF text: Idelhaj Section 3 carries exactly this action and the binomial lemma (its Lemma 3.6 is our lem-wielandt-sylow-binomial-valuation), and Conrad, The Sylow Theorems, Section 2 proves the same theorem by the different normalizer-growth route. Idelhaj closes without induction, using that H acts on omega with right cosets as orbits so |H| divides |omega|; I kept the induction because thm-strong-induction is a declared dependency of this item and that extra orbit observation is not. I also corrected the item\'s reference, which pointed at Conrad\'s "Consequences of the Sylow Theorems" — a note about consequences that does not prove Sylow I. Disposition: fatal, repaired, route source-verified.',
    'thm-composition-series-iff-noetherian-and-artinian':
      'CONFIRMED FATAL and repaired by me; defect-ledger row f15-a-6b-002. Statement and title asserted the biconditional unconditionally while step 2.1 read "dependent choice gives a descending chain" and both cited characterization theorems flag dependent choice in their own restatements. I scoped the hypothesis to the converse rather than to the whole biconditional, because the forward direction is genuinely choice-free, and confirmed from disk that both consumers use only the forward direction, so neither inherits it. I also rewrote step 1.1, which had been "A finite composition series gives both chain conditions by induction through its simple factors", into the actual induction along the series via [L4]. Disposition: fatal, narrowed, no blast radius.',
    'thm-finitely-generated-nonzero-modules-have-maximal-proper-submodules':
      'CONFIRMED FATAL and repaired by me; defect-ledger row f15-a-6b-003. Unconditional Statement and title over a proof whose step 3.1 was "Zorn supplies a maximal proper submodule" and whose own [L3] restates thm-zorn beginning "Assume the Axiom of Choice". I added the hypothesis to both, expanded the three announcement steps into the actual Zorn verification, and propagated the hypothesis into the [L2] restatement inside thm-equivalent-characterizations-of-semisimple-modules, which already declared Choice in its own Statement but would otherwise have been citing this one with the hypothesis dropped. Note the empty chain: Zorn needs every chain bounded, and the empty chain is bounded by 0 only because M is nonzero — that is now written down. Disposition: fatal, fixed.',
    'thm-jordan-holder-theorem-for-modules':
      'Reader 2 replaced this proof wholesale under R2-7, so it had had no adversarial reading in its current form. I re-derived the entire induction. The auxiliary claim in step 1.1 is correct: (C ∩ M_i)/(C ∩ M_{i-1}) embeds in M_i/M_{i-1}, hence is zero or simple. The A != B branch is correct: A + B = M by maximality, A/C = M/B and B/C = M/A by the second isomorphism theorem, the induction hypothesis applied to A gives length(C) = n-2, and appending B gives a series of B of length n-1, so the second series has length n and the two factor multisets agree after the two swaps. One nonfatal defect survives and I recorded rather than repaired it (ledger f15-a-6b-006): the closing sentence "This also covers n=1, when C=0" is false about its own branch, since at n=1 the module is simple, A = B = 0 necessarily, and the branch is vacuous — taken literally it would put length(C) at -1. The case is genuinely handled by the A = B branch. Disposition: reader rewrite confirmed correct; one nonfatal closing clause recorded.',
    'cor-length-is-additive-in-short-exact-sequences':
      'R2-8 correctly found that the reverse implication had been replaced by the sentence "Cover ... both directions". I verified the new step 1.2 in full: with N_i = M_i ∩ N and Q_i the image of M_i, the sequence 0 -> N_i/N_{i-1} -> M_i/M_{i-1} -> Q_i/Q_{i-1} -> 0 is exact because M_i ∩ (M_{i-1} + N) = M_{i-1} + N_i, and simplicity of the middle term forces exactly one end to be it and the other zero. Deleting repetitions therefore gives composition series whose factor counts add to n. Disposition: reader repair confirmed correct.',
    'thm-matrix-rings-over-division-rings-are-semisimple':
      'R2-11 was right that def-ring-matrix-product-identity-and-transpose opens "Let R be a commutative ring" and so cannot license matrices over a division ring. The repair moves the construction into this Statement. I checked the noncommutative details, which is where such a repair fails: associativity expands to the same double sum in both bracketings without commuting anything; column ideals M_n(D)e_jj are the matrices supported on column j and intersect the sum of the others trivially; and the simplicity witness has the scalar on the correct side, a_ik = w_i v_k^{-1} giving (Av)_i = w_i v_k^{-1} v_k = w_i. Disposition: reader repair confirmed correct.',
    'thm-wedderburn-artin-theorem':
      'The orientation is the thing that can be silently wrong here, and R2-35 was right that the old transpose mnemonic was invalid with both products read in E. I verified the repaired identity from scratch: writing * for the opposite matrix product and • for the opposite entry product, (A*B)^T = (BA)^T has entries sum_k b_jk a_ki, and (A^T ⊙ B^T) has entries sum_k a_ki • b_jk = sum_k b_jk a_ki. They agree, so transpose is a ring isomorphism M_n(E)^op -> M_n(E^op) and R is recovered rather than R^op. Disposition: reader repair confirmed correct.',
    'thm-uniqueness-of-wedderburn-artin-data':
      'I re-derived step 2.1, which is where the orientation could flip a second time. For f commuting with the matrix action, e_kk annihilating e_1 forces f(e_1) = e_1 d; e_j = e_j1 e_1 gives f(e_j) = e_j d; and for a general column, e_j x_j = A e_j for the diagonal matrix A with entry x_j, so R-linearity (not merely additivity, which is what the text says) gives f(x) = xd. Composition then reverses the scalar order, so End = D^op and D = End^op, which is the orientation the step claims. The multiplicity n_i comes from [L3] and Jordan-Hölder makes it an invariant. Disposition: no fatal defect; the "additivity then gives" phrase understates the step, which needs the matrix action, and that is inside the 30-second rule.',
    'thm-integrality-and-finite-module-equivalences':
      'R2-14 is correct and I confirmed the convention it rests on: def-polynomial-degree-leading-coefficient-and-monic says the zero polynomial has no leading coefficient, so over the zero ring no polynomial is monic and no element is integral, while A[b] = 0 is finitely generated and the zero module over it is vacuously faithful. The third condition therefore holds where the first fails, and A != 0 is a necessary narrowing rather than a cautious one. Step 4.1 now excludes n = 0 by the right argument: an empty generating set forces M = 0, faithfulness forces A[b] = 0, hence A = 0. Disposition: reader repair confirmed correct and minimal.',
    'cor-integral-elements-form-a-subring':
      'Inherits the nonzero-base hypothesis, and its title now names the restriction, which is what R2-16 asked for. I checked the faithfulness claim the proof needs: A[x,y] contains 1, so any r in A[z] annihilating it annihilates 1 and is zero. Closure under subtraction is covered because z ranges over x+y, x-y and xy. Disposition: reader repair confirmed correct.',
    'thm-equivalent-characterizations-of-semisimple-modules':
      'Declares the Axiom of Choice in its own Statement, and after my repair to thm-finitely-generated-nonzero-modules-have-maximal-proper-submodules its [L2] restatement carries the hypothesis too rather than dropping it. R2-9 correctly added the socle dependency, without which step 3.1\'s decomposition M = Soc(M) ⊕ C names an undefined object. R2-42 corrected the maximal-complement sentence, and the corrected version is the right one: a summand not contained in N + C has zero intersection with it. Disposition: reader repairs confirmed correct.',
    'thm-finite-length-semisimple-module-characterizations':
      'This is the one item in the run that has to be choice-free and says so, so I read it against that claim rather than only for validity. Step 1.1 makes at most n selections, bounded by Jordan-Hölder, which is finitely many instantiations and needs no choice axiom. Step 2.1 processes finitely many summands in a fixed order; I checked the invariant persists, since x in (C + S_i) ∩ N gives s in S_i ∩ (N + C) = 0. Step 3.1\'s complement-passes-to-A step is the modular-law argument and is correct. My undeclared-choice detector flagged this item and I adjudicate it a FALSE POSITIVE: the word "choice" appears only in the Statement\'s and the proof\'s assertions that no choice principle is used, which I verified. Disposition: no defect.',
    'thm-submodules-and-quotients-of-semisimple-modules':
      'R2-37 correctly restored the Choice hypothesis to the title and Statement, which had asserted the closure unconditionally while citing a theorem that assumes it. The mathematics is right: for L <= N, intersecting an ambient complement gives L + (N ∩ C) = N by the modular argument, and a quotient is identified with a complement of the kernel. The closing step was "Cover zero and whole submodules." — an announcement, not a disposition — and I replaced it with the actual evaluation of both. Disposition: reader repair confirmed correct; closing step repaired by me.',
    'thm-socle-is-the-largest-semisimple-submodule':
      'R2-38 correctly restored Choice to title and Statement. Step 1.1 is licensed because every simple submodule of M is a simple submodule of Soc(M), so Soc(M) is the sum of its own simple submodules and the sum characterization of [L2] applies to it. Step 2.1 is correct as written. Its closing step was "We include a module with no simple submodule." and I replaced it with the discharge: the defining family is empty, Soc(M) = 0, and 0 is the empty direct sum, hence semisimple. Disposition: reader repair confirmed correct; closing step repaired by me.',
    'cor-semisimple-rings-are-noetherian-and-artinian':
      'Consumes thm-composition-series-iff-noetherian-and-artinian, so I checked which direction it uses before scoping dependent choice there: steps 2.1 and 3.1 use only "has a composition series implies Noetherian and Artinian", the choice-free half. Its [L3] restatement is now narrowed to that clause, which is both faithful and keeps the corollary choice-free. Disposition: no fatal defect; citation narrowed to the used clause.',
    'lem-finite-nilpotence-via-normal-sylow-subgroups':
      'Step 1.1 is terse but licensed: [L2] gives N(N(P)) = N(P) and [L1] says a proper subgroup is properly contained in its normalizer, so N(P) cannot be proper. Step 2.1 asserts that the product of the normal Sylow subgroups has order |G| without an explicit input; that is the coprime-order counting argument and is inside the 30-second rule. The closing step named the trivial group and the one-prime case without checking them, and I replaced it with the discharge using the empty-family and one-factor clauses of [L5] and the class-zero clause of [L7]. Disposition: no fatal defect; closing step repaired by me.',
    'thm-sylow-characterizations-of-finite-nilpotence':
      'R2-2 correctly found that the reverse implication was unlicensed. The repaired step 2.1 is a real argument and I checked each move: the diagonal map is injective because its kernel is the intersection of the maximal subgroups, which is Phi(G); the correspondence theorem makes each G/M have no nontrivial proper subgroup; Cauchy then forces prime order; and the finite product of prime-order groups is nilpotent, as is the subgroup G/Phi(G), so the lifting theorem applies. Step 5.1 handles the empty family of maximal subgroups by an actual argument (finiteness forces G = 1, the diagonal target is the empty product), not an announcement. Disposition: reader repair confirmed correct.',
    'thm-groups-of-order-thirty-have-a-normal-sylow-subgroup':
      'I recomputed the whole chain: left multiplication by an involution on 30 elements is a fixed-point-free involution, hence a product of 15 transpositions with sign -1, so sign composed with the regular representation surjects onto {±1} and its kernel is normal of order 15; a group of order 15 is cyclic since 3 does not divide 4; and the Sylow subgroups of a cyclic group are characteristic, so they are normal in G. One nonfatal citation defect recorded, not repaired (ledger f15-a-6b-005): step 4.1 makes the normal-in-normal-implies-normal inference, which is invalid in general and is licensed here only by [L9], and step 4.1 does not cite [L9] — while step 1.1 cites all ten facts at once, so the contract\'s input map records [L9] as used at a step that does not use it. Disposition: no fatal defect; citation misattribution recorded.',
    'cor-sylow-subgroup-normal-iff-unique':
      'Both directions are one inference each and both are licensed: a normal Sylow subgroup equals all its conjugates and Sylow II makes every Sylow subgroup a conjugate; conversely a conjugate of a Sylow subgroup is a Sylow subgroup, so uniqueness makes it conjugation-invariant. Step 2.1 is terse enough to look like an announcement but does carry that inference. Disposition: no defect.',
    'thm-maximal-subgroups-of-finite-nilpotent-groups':
      'Steps 1.1 and 2.1 are correct: the normalizer condition plus maximality force N(M) = G, and a quotient with no nontrivial proper subgroup has prime order by Cauchy. The closing step was "We check that a maximal subgroup is proper and that the quotient is nontrivial." — naming two checks and performing neither — and I replaced it with the discharge. Disposition: no fatal defect; closing step repaired by me.',
    'cor-rational-algebraic-integers-are-integers':
      'The rational-root argument is correct and the reverse direction is immediate. The closing step named zero and negative integers without evaluating them; I replaced it with the evaluation, noting that X - n is monic over Z for every integer n and that 0 has lowest-terms form 0/1. Disposition: no fatal defect; closing step repaired by me.',
    'cor-finite-direct-sums-preserve-chain-conditions':
      'The induction on summands via the split short exact sequence is correct and the reflection direction is licensed because every summand is both a submodule and a quotient. The closing step named the empty sum and one summand without evaluating them; I replaced it, noting that the empty direct sum is the zero module and makes the "every summand" side vacuous, and that one summand is the induction base. Disposition: no fatal defect; closing step repaired by me.',
    'thm-finitely-generated-modules-over-noetherian-rings-are-noetherian':
      'Correct: an n-generated module is a quotient of R^n, R^n is Noetherian by the direct-sum corollary, and quotients inherit it. R2-31 correctly added the short-exact-sequence citation for that last step. The closing step named the zero-generated zero module without evaluating it; I replaced it. Disposition: reader repair confirmed correct; closing step repaired by me.',
    'thm-second-isomorphism-theorem-modules':
      'R2-20 and R2-26 correctly found that [L2] stopped before the definition of L + N and that [L3] stopped before the quotient-module conclusion; both are load-bearing for the map\'s codomain and surjectivity. The repaired facts carry those clauses. The closing step named zero and equal submodules without evaluating them; I replaced it with the three evaluations. Disposition: reader repairs confirmed correct; closing step repaired by me.',
    'fs-every-module-has-a-composition-series':
      'The refutation is sound: Z is Noetherian and not Artinian, so the equivalence theorem denies it a composition series. Its [L1] now quotes only the choice-free forward direction, which is the one the contraposition uses. The second step read "Also point to the explicit descending chain rather than a scope claim." — a leaked authoring instruction rather than proof text — and I replaced it with the chain itself, Z > 2Z > 4Z > ..., so the refutation rests on a witness rather than on the equivalence alone. Disposition: no fatal defect; leaked instruction replaced.',
    'thm-normalizer-condition-for-finite-nilpotent-groups':
      'The central-series argument in steps 1.1 and 2.1 is correct. The closing step named H = 1 and class zero without evaluating them; I replaced it, noting that class zero means G = 1 and the claim is vacuous, while H = 1 with G nontrivial gives N(H) = G properly containing H. Disposition: no fatal defect; closing step repaired by me.',
    'thm-frattini-subgroup-is-nilpotent':
      'The Frattini-argument route is correct. The closing step was "We include G = 1."; I replaced it with the discharge, and in doing so had to correct my own first wording — the family of maximal proper subgroups is empty, and def-frattini-subgroup-of-a-finite-group states that its intersection inside G is G itself, so Phi(1) = 1. Disposition: no fatal defect; closing step repaired by me.',
  },
};

let n = 0;
for (const [batch, rows] of Object.entries(REVIEWS)) {
  const P = `research/frontier-15-batch-${batch}.proof-contracts.json`;
  const doc = JSON.parse(fs.readFileSync(P, 'utf8'));
  for (const [id, text] of Object.entries(rows)) {
    const k = doc.contracts[id];
    if (!k) { console.log(`NO CONTRACT b${batch} ${id}`); continue; }
    k.risk_review = { status: 'complete', reviewer: REVIEWER, refuter: 'none — GPT 5.6 Sol lane quota-locked until 2026-08-20; read by Alpha directly', disposition: text };
    n++;
  }
  fs.writeFileSync(P, JSON.stringify(doc, null, 2) + '\n');
}
console.log(`${n} risk_review disposition(s) written`);
