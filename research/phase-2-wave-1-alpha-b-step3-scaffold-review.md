# Phase 2 wave 1, Alpha group b, Step 3 scaffold review

Date: 2026-09-08

Assignment: batches 14 and 15. Reviewed A/B pairs:

- `deduction-soundness-completeness-and-compactness` and its examples page
- `set-theoretic-trees-delta-systems-and-diamond` and its examples page
- `dependent-choice-and-the-complete-metric-baire-theorem` and its examples page

## Outcome

The deduction/model-theory pair and the tree/delta-system/diamond pair are `insufficient` because their owned evidence explicitly leaves the complete transitive semantic supplier audit open. The DC/complete-metric-Baire pair is `sufficient`: its A and B inventories, direct published interfaces, ZF proof routes, source dispositions, and axiom-strength boundaries are adequate.

No new prerequisite A/B pair is required by the defects found. Batch 14's owning Beta must repair its scaffold and evidence; this review did not edit either batch manifest, coverage file, notes file, plan, prose scaffold, or published content.

## Inputs and closure review

I read the repository instructions, schema and workflow; the frontier-dependency bookkeeping brief; the current run scope, group assignment, drift record, plan and Foundations prose scaffold; the two manifests, coverage ledgers, notes and dispatch receipts; the local blocker-resolution proof; and the cited source locators relevant to the disputed claims.

Whole-run structural resolution is clean: all 434 manifest items resolve with explicit dependency arrays, the plan is acyclic and consistent, and the Foundations bootstrap gate reports no path from these pages to `deferred-set-theory-beyond-choice` or any item recorded there. Those mechanical results do not discharge batch 14's F4 semantic obligation. Its coverage and notes expressly say that the published transitive proofs have not received the required proof-by-proof adequacy audit.

The exact page-level roots left uncertified for batch 14 are:

- `deduction-soundness-completeness-and-compactness -> formal-set-theoretic-syntax-structures-and-satisfaction`
- `deduction-soundness-completeness-and-compactness -> countability-and-uncountability`
- `deduction-soundness-completeness-and-compactness -> cardinal-arithmetic-and-cofinality`
- `set-theoretic-trees-delta-systems-and-diamond -> club-stationary-sets-and-pressing-down`
- `set-theoretic-trees-delta-systems-and-diamond -> filters-and-ultrafilters`

Every published item reached from the 35+6 and 41+7 local inventories belongs to that unfinished audit. The required repair is evidence, not a new mathematical page: Beta 14 must read each proof on those dependency paths, compare its exact result and assumptions with the consumer use, and add or repair local prerequisites wherever a mismatch is found.

## Pair review: deduction, soundness, completeness and compactness

The local ordering is coherent. The scaffold introduces the Hilbert calculus and syntactic consistency before finite-support, derived-rule, deduction and soundness results; then supplies fresh-constant elimination, Henkin consistency, canonical countable coding, completion, congruence, the term model and truth lemma before completeness and compactness. Elementary embeddings, Tarski-Vaught, witness hulls and the two Loewenheim-Skolem directions occur afterward. The countable-language constructions use least natural codes in ZF, while general hull/cardinality arguments and arbitrary well-orderable languages are explicitly marked ZFC. Arbitrary-language compactness is correctly withheld for the later Boolean-prime-ideal page.

The A-page applications and B-page failure modes match the stated interfaces: unrestricted generalization/discharge is refuted semantically, the empty-signature seed constant is necessary for a closed-term model, the tail inclusion in `(omega,<)` is isomorphic but not elementary, compactness yields a nonstandard element, and the noncategoricity of first-order ZF is conditional on syntactic consistency and uses differently sized models rather than asserting `Con(ZF)`.

Moschovakis sections 1H-1J support the proof-system, Henkin, completeness and compactness spine, with exercise-only arguments correctly identified as local work. Weiss-D'Mello supplies an independent model-theory treatment of elementary chains, Tarski-Vaught, Loewenheim-Skolem and diagrams; the coverage correctly does not pretend that this source proves compactness. No source result is silently dropped. Nevertheless, F4 prevents a sufficient verdict because the scaffold contract requires the entire published supplier closure, not only this direct local spine, to be semantically audited.

## Pair review: trees, delta systems and diamond

The proof order from tree definitions through the normal/splitting and Aronszajn interfaces, the finite delta-system machinery, Knaster products, specialization, diamond constructions and partition relations is mostly coherent. The repaired indexed-delta argument handles repeated supports; the finite-petals argument names its ultrafilter supplier; the specialization-poset proof does not infer a generic filter from ccc; the diamond-to-Suslin route includes antichain reflection/sealing; and the general-cardinal Erdos-Rado proof uses a separate pattern-closure lemma before induction. The B page contains genuine instances and boundary failures and has no outward dependency role.

The square definition was checked separately because its no-thread convention is easy to misstate. Here the indexing is by limits below `kappa^+` and every club has order type at most `kappa`; under this convention a separate no-thread axiom is redundant. If a club `D` threaded the sequence, enumerate `D` continuously and choose a limit index `xi` with `kappa < xi < kappa^+`; at `alpha=D(xi)`, coherence would give `otp(C_alpha)=otp(D cap alpha)=xi>kappa`, contradicting the bound. Thus the scaffold's strategy is sound and no square repair is required.

### Source dispositions

The five active SET-9 source records have exact locators and current fetch receipts. Deferred Kurepa line/tree equivalence, normal reduction, order completion and rational-order equivalence all have the later SET-17 destination; the full Halpern-Lauchli matrix theorem has SET-21; diamond in L has SET-12; and Martin's-axiom specialization has SET-16. These are legitimate scope decisions because no local theorem uses those deferred results. Generalized delta systems and the compact branch-space exercise are genuine out-of-scope strengthenings. None of these dispositions excuses the unfinished F4 supplier audit.

## Pair review: Dependent Choice and complete-metric Baire

This pair is sufficient. The definition fixes a nonempty carrier and the successor direction `a R b`, and distinguishes starting-point-free from prescribed-start DC. The finite-path construction proves their equivalence in ZF: its states are nonempty finite paths beginning at the prescribed point, end-extension is serial using one existential successor at a time, and the nested unbounded union is a function on omega. It does not use the older published DC item or any deferred choice-strength claim.

The Baire definition uses omega-indexed witnesses and the contained-in-a-union convention for meagreness. The equivalence of open-dense, closed-nowhere-dense, nonmeagre-open-set and comeagre-dense forms uses only complements, closure and one already supplied sequence; it does not select decompositions for countably many unrelated meagre sets.

For `DC -> CM-Baire`, the state contains the stage, centre and rational radius. Serial advancement nests a new closed ball inside the previous open ball and the next dense open set. Prescribed-start DC chooses exactly one chain of states; shrinking radii make the centres Cauchy, and the direct epsilon argument puts the complete-space limit in every closed ball, hence in the original open set and every dense open set. No countable-choice-dependent closure-to-sequence or Cantor-intersection theorem is imported.

For the converse, `A^omega` is nonempty using one fixed member of `A`; the first-difference ultrametric is complete in ZF because each limiting coordinate is taken at its least stabilization stage. Each successor-occurrence set is open and dense by one finite-cylinder extension and one successor witness. A point in their intersection yields the definable map `q(i)=min{j:f(i) R f(j)}`; recursion on `q` gives the required chain even though the witness indices need not increase. The B example computes this distinction correctly for natural successor.

Miller, Proposition 5.4, explicitly states the equivalence in ZF and supplies both the nested-neighbourhood and sequence-space routes. Karagila gives an independent full lecture-note treatment and warns against hidden countable choices. All deferred and out-of-scope results have valid destinations or specific reasons; there is no `source_resolution` drop to adjudicate and no DMC comparison in the pair.

## Scope decisions and same-frontier ledger

All 20 group-b source declines were resolved as `stands` with item-specific evidence in `research/phase-2-wave-1-alpha-b-scope-decisions.json`. The group-specific scope-decision check passed. The final whole-run form still failed only because other Alpha groups had 16 unresolved rows; this report does not claim that whole-run scope-decision gate passed.

Neither assigned consumer batch has a page prerequisite, item dependency, well-definedness dependency, or load-bearing forward reference to another batch in this run. The owned inputs are therefore empty arrays:

- `research/phase-2-wave-1-batch-14.cross-batch-dependencies.json`
- `research/phase-2-wave-1-batch-15.cross-batch-dependencies.json`

The unified frontier ledger was refreshed and deduplicated after writing those inputs.

## Checks run

- `node tools/scope-decisions.mjs refresh --run phase-2-wave-1 --group b`: 20 declines, initially 20 pending.
- `node tools/scope-decisions.mjs check --run phase-2-wave-1 --group b`: 20 current declines, 0 errors.
- `node tools/scope-decisions.mjs check --run phase-2-wave-1`: failed with 16 unresolved rows outside group b; group b contributed no error.
- `node tools/scaffold-verdicts.mjs --run phase-2-wave-1`: the whole-run collector found 12/20 A pages reviewed, 9 insufficient and 8 unreviewed; the eight missing verdicts belong to other Alpha groups.
- `node tools/manifest-deps.mjs research/phase-2-wave-1-batch-{1..15}.pages.json`: 434 items, 0 normalized, 0 errors.
- `node tools/validate-plan.mjs research/plan-spec.json`: exit 0; plan acyclic and consistent, with standing redundant-prerequisite advisories.
- `node tools/extcheck.mjs`: exit 0; recorded-not-proved structure valid and no fatal Foundations catalogue path. It reported the repository's existing published warnings outside these proposed dependency paths.
- `node tools/source-fetch-check.mjs --coverage research/phase-2-wave-1-batch-14.coverage.json,research/phase-2-wave-1-batch-15.coverage.json --stamp`: 9/9 active sources fetch-verified and resolved, with 0 newly stamped; the idempotent run made no batch-file edit.
- `node tools/coverage-checklist.mjs` on batches 14 and 15: 115 and 29 harvested results respectively, 0 errors, 0 warnings.
- `node tools/frontier-dependency-ledger.mjs refresh --run phase-2-wave-1`: refreshed and deduplicated.
- JSON parsing of the group scope decisions, verdicts and both owned frontier-dependency inputs: 4/4 passed.
- `git diff --check` on the five owned artifacts: passed.
- Final-newline and trailing-whitespace scan on the five owned artifacts: 5/5 passed.

## Handoff

Route the batch-14 F4 insufficiency to Beta 14. Its repair must be a real proof-by-proof audit, not a rerun of graph validators or a blanket reliance on published status. After that evidence and any resulting mathematical repairs land, rerun this independent review against the changed manifest and evidence. Batch 15 requires no scaffold repair.
