# Normalized torus transfer and final Carleson–Hunt theorem

Both existing Statements are unchanged. The transfer lemma is conditional and now has a complete local argument requiring only the real-line bound on Schwartz input. For a trigonometric polynomial P, the localization phi(epsilon x)P(x) has Fourier support in narrow intervals around the integer modes. Cutoffs at±(N+1/2) select precisely the symmetric modes, producing phi(epsilon x)S_NP(x) exactly. The real-line maximal inequality gives the factor2. A fully proved periodic averaging limit, using uniform shifted Riemann sums for |phi|^p and uniform tail bounds, cancels the nonzero window mass and leaves precisely the normalized integral on[0,1]. Fejer polynomial approximation, coefficient continuity, finite maxima and monotone convergence extend the bound to every complex Lp class.

Fully read the fixed packet/cutoff/period-one definitions, Schwartz inversion, Fejer Lp convergence, Cesaro definition, complex Holder/Minkowski and sigma-finite Tonelli/Fubini/MCT suppliers. The unavailable Fremlin source is not represented as retrieved or read; the proof is a complete local alternative for the transfer claim. The conditional transfer no longer declares the unused Hunt/interpolation/reconstruction edges; it declares the actual window, approximation and integration suppliers.

The final theorem now applies Hunt at(p+1)/2 and2p, restricted weak interpolation at p, explicit averaged reconstruction, and the transfer lemma, for each fixed1<p<infinity. It records normalized coefficients, complex representatives, N=0, countable measurability and inherited AC. No weak-at-p to strong-at-p inference or endpoint claim is made.

Checks after both edits: full batch5 strict contracts27/27; explicit-path precheck on all27 manifest items gives23 proof-bearing passes; renderer on both changed items passes; batch5 content policy27/27 passes. Manifest/contracts are synchronized. No new items, source attempts, source waivers or owner receipts. Local authorship and mechanical checks are distinct from ordinary mathematical review.
