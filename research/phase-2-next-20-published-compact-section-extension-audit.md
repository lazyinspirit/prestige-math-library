# Published compact-section extension audit — 2026-09-11

This bounded audit reconciles the connection author's concern about the exact
compact-support interface used by
`lem-every-vector-in-a-fibre-extends-to-a-compactly-supported-smooth-section`.
It covers that complete published item, its four declared dependencies, the
full current proof of the chart-bump supplier, and the published local-frame
existence interface. It is not a transitive audit of all vector-bundle or bump
mathematics, and no published item was edited.

## Deduplication and fixed artifacts

The target is published, has no aliases and had exactly one canonical U-P row,
from the 2026-09-08 bump-consumer inventory. The whole canonical ledger was
searched by exact ID, title, compact-support mechanism, chart-bump supplier and
local-frame supplier IDs before this disposition. The historical prose labels
the target only as an impact candidate and contains no separate confirmed
defect. This audit moves that one row; it does not add another classification.

Current SHA-256 values at this audit are:

- target `lem-every-vector-in-a-fibre-extends-to-a-compactly-supported-smooth-section`:
  `b592fbbaa5bae277c8fef2fc9fb3edcef85809c23e6ad15ca7d1152dd87fdaff`;
- declared bump supplier `lem-chart-bump-at-a-point-with-prescribed-support`:
  `4d214bd1a388a45efa8b9241b7e08f5678e5e614f6fb5d66070ca8969567f011`;
- declared local-frame definition
  `def-local-frame-and-global-frame-of-a-vector-bundle`:
  `6abfca2874e29ae98a378e3d2e219d1124d97f49f28eb714b520991534e20f46`;
- published local-frame/trivialization equivalence
  `prop-local-frames-and-local-trivializations-are-equivalent-data`:
  `11ac2d7bc9a2de45d15c3ccd5e7fe5d98ea1ea9e7a40954d33fc3f897baab29d`;
- smooth-vector-bundle definition
  `def-smooth-vector-bundle-rank-fibre-and-trivial-bundle`:
  `f6dee641699da132c13e3abd1c2acb177a802bcbf9e7caef4ac6fb6e7795d77f`.

## Exact used clauses

The chart-bump statement gives a smooth real function equal to one at the
chosen point with support contained in the prescribed open set. Its proof is
stronger than that short statement: steps 3.1--5.1 construct
`K=phi^{-1}(closed B_R(a))`, prove `K` compact and closed in the manifold, and
prove `supp rho` is contained in `K`. The zero-dimensional branch has the
compact singleton support. Thus the target's sentence “hence compact” is a
correct use of the complete current published supplier, even though the
supplier's displayed Statement does not repeat compactness.

The rest of target proof 2.1 is valid. Since `supp chi` is closed and contained
in `U`, the open set `V=M minus supp chi` contains `M minus U`; the local
formula `chi tau` and the zero section agree on `U intersect V`. Their pasted
section is nonzero only where `chi` is nonzero, so its support is contained in
`supp chi`, which is compact by the preceding clause.

Target proof 1.1 also has an adequate published implicit existence route. The
smooth-vector-bundle definition supplies a local fibrewise-linear
trivialization around every point, and the local-frame/trivialization
proposition turns it into a local frame. The target's direct `[L1]` label names
only the local-frame definition, so adding those two IDs would improve the
dependency list, but the complete published interface already supplies the
claim. Under the canonical rule, a correct implicit use of an adequate
published supplier is not repair debt.

## Disposition

The target is a bounded no-repair-needed disposition. The connection author's
local proof does not rely on it, and this audit finds no live authoring blocker
or additional Phase-2 pair. The compactness wording and two local-frame IDs are
optional interface exposition only. This disposition is confined to the exact
construction above; it does not certify all direct consumers of the bump
supplier.
