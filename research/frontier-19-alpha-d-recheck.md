`B5-1` — `confirmed`: `research/frontier-19-batch-5.pages.json` still uses `cex-a-product-without-unique-factorisation-does-not-satisfy-the-product-rule`, and `research/frontier-19-batch-5.notes.md` still records the accepted CB-15 design repair replacing the impossible “same OGF, non-isomorphic classes” witness.
`B5-2` — `confirmed`: `research/frontier-19-batch-5.coverage.json` still routes the fixed-cycle-type count to published `cor-symmetric-group-class-equation-by-cycle-type`, while `research/plan-spec.json` still leaves `permutation-statistics-inversions-and-eulerian-numbers` without `the-exponential-function` in its `requires`, so the alternating-permutation branch remains an honest `owner-decision` defer.
`B6-1` — `confirmed`: `research/frontier-19-batch-6.pages.json` still carries the CB-14a scope only, and `research/frontier-19-batch-6.coverage.json` still records the split continuation items such as the general MOLS upper bound `N(n) <= n-1` as `owner-decision` defers; no current plan page or prerequisite change closes that continuation honestly.
`B6-2` — `confirmed`: `research/frontier-19-batch-6.pages.json` still avoids published `cor-a-restricted-set-contains-a-large-stable-set-or-clique`, whose statement provenance on disk remains `ai-generated`, and `research/frontier-19-batch-6.coverage.json` still uses the two narrow quantitative-density remarks plus published non-generated items as the page-403 route.
`B10-1` — `pushback accepted`: the published metadata defect Alpha flagged is still live in `items/def-erdos-hajnal-property-and-constant.md`, `items/prop-erdos-hajnal-property-passes-to-hereditary-subclasses.md`, and `items/lem-erdos-hajnal-constants-are-downward-closed.md`, but `research/frontier-19-batch-10.coverage.json` already carries the corrected Huang-Ju-Zhou title and the finding was an alert on published dependencies rather than a scaffold blocker; remaining block: published-item repair is still needed outside this batch-fix pass.
