# Proposed SYMR item inventory

This is a future-authoring inventory, not live content. Every row gives an
explicit dependency list. Unless a row says otherwise, the statement and proof
provenance are `literature-derived`; definitions have proof provenance
`not-applicable`. Source keys refer to the seven reports in this directory.

## SYMR-1 — Symmetric functions, Hall inner product, and Schur bases

### Symmetric Functions, the Hall Inner Product, and Schur Bases (A page; `symmetric-functions-hall-inner-product-and-schur-bases`)

| proposed id (kind) | exact claim and restrictions | deps | proof route / intermediate joints | source locator and purpose |
|---|---|---|---|---|
| `def-stable-graded-ring-of-symmetric-functions` (definition) | `Lambda^d` is the stable inverse limit of the degree-`d` symmetric-polynomial parts under setting the last variable to zero, and `Lambda=direct-sum_d Lambda^d`; this is not the ungraded inverse-limit ring. | `def-symmetric-polynomial`; `def-partition-young-diagram-and-conjugate-partition` | Define degreewise transition maps and stable multiplication. | Macdonald I §2, pp. 17–20. Genuine stable extension of the published finite-variable definition. |
| `thm-monomial-symmetric-functions-form-the-integral-stable-basis` (theorem) | The stable `m_lambda`, indexed by all partitions of `d`, form a `Z`-basis of `Lambda^d`. | `def-stable-graded-ring-of-symmetric-functions`; `def-monomial-symmetric-polynomials` | Show stabilization is an isomorphism in degree `d` once the number of variables is at least `d`; transport the finite orbit-sum basis. | Macdonald I (2.1), pp. 17–20. Gives integral coefficient control. |
| `thm-elementary-and-complete-families-freely-generate-the-stable-ring` (theorem) | `Lambda=Z[e_1,e_2,...]=Z[h_1,h_2,...]`, both families algebraically independent, and the partition monomials `e_lambda`, `h_lambda` are integral bases. | `thm-monomial-symmetric-functions-form-the-integral-stable-basis`; `def-elementary-symmetric-polynomials`; `def-power-sum-and-complete-homogeneous-symmetric-polynomials` | Use dominance-unitriangular expansion of `e_{lambda'}` in `m_mu`; invert over `Z`; use `H(t)E(-t)=1` for the complete basis. | Macdonald I (2.3)–(2.6), pp. 20–22. Stable theorem, not a repeat of the finite fundamental theorem. |
| `prop-power-sums-form-a-rational-not-integral-stable-basis` (proposition) | `Lambda_Q=Q[p_1,p_2,...]` and the `p_lambda` form a `Q`-basis degreewise; they need not be a `Z`-basis. | `thm-elementary-and-complete-families-freely-generate-the-stable-ring`; `def-power-sum-and-complete-homogeneous-symmetric-polynomials` | Logarithmically differentiate `H(t)` to get triangular Newton relations; identify the nonunit diagonal coefficients. | Macdonald I (2.10)–(2.14), pp. 23–25. Protects every later `z_lambda^{-1}` formula. |
| `def-hall-inner-product-on-symmetric-functions` (definition) | The Hall form is the graded `Z`-bilinear form determined by `<h_lambda,m_mu>=delta_{lambda,mu}` and orthogonality of distinct degrees. | `thm-monomial-symmetric-functions-form-the-integral-stable-basis`; `thm-elementary-and-complete-families-freely-generate-the-stable-ring` | Basis-duality definition; no convergence assertion. | Macdonald I (4.5), p. 63. Supplies adjoints and character isometry. |
| `def-stable-schur-function-by-bialternants` (definition) | For `N>=ell(lambda)`, define `s_lambda(x_1,...,x_N)=a_(lambda+delta)/a_delta`; these compatible polynomials define `s_lambda in Lambda`. | `def-stable-graded-ring-of-symmetric-functions`; `def-partition-young-diagram-and-conjugate-partition` | Divisibility of alternating polynomials by the Vandermonde and stability under a zero last variable. | Macdonald I (3.1)–(3.2), pp. 40–41. Stable symmetric-function object, distinct from RG-9 Specht modules and RL-8 Schur modules. |
| `thm-cauchy-kernel-has-power-complete-and-schur-expansions` (theorem) | Degreewise in the completed bidegree ring, `prod_{i,j}(1-x_i y_j)^{-1}=sum_lambda z_lambda^{-1}p_lambda(x)p_lambda(y)=sum_lambda h_lambda(x)m_lambda(y)=sum_lambda s_lambda(x)s_lambda(y)`. | `def-hall-inner-product-on-symmetric-functions`; `prop-power-sums-form-a-rational-not-integral-stable-basis`; `def-stable-schur-function-by-bialternants` | Expand the product into complete/monomial bases; use logarithms for power sums; use finite alternants and stabilize for the Schur expansion. | Macdonald I (4.1)–(4.3), pp. 62–63. Formal kernel behind all duality arguments. |
| `cor-power-sums-are-orthogonal-for-the-hall-inner-product` (corollary) | Over `Q`, `<p_lambda,p_mu>=delta_{lambda,mu}z_lambda`, where `z_lambda=prod_i i^{m_i(lambda)}m_i(lambda)!`. | `thm-cauchy-kernel-has-power-complete-and-schur-expansions` | Apply the source’s equivalence between a Cauchy expansion and dual bases. | Macdonald I (4.6)–(4.8), pp. 63–65. Matches centralizer orders on SYMR-2. |
| `thm-jacobi-trudi-and-dual-jacobi-trudi-identities` (theorem) | `s_lambda=det(h_{lambda_i-i+j})` and `s_lambda=det(e_{lambda'_i-i+j})`, with `h_0=e_0=1` and negative subscripts zero. | `def-stable-schur-function-by-bialternants`; `thm-elementary-and-complete-families-freely-generate-the-stable-ring` | Factor the alternant matrix to obtain the first determinant; apply complementary minors to `H(t)E(-t)=1` for the dual identity. | Macdonald I (3.4), (3.5), pp. 41–43. Determinantal engine for SYMR-3. |
| `thm-schur-functions-form-an-orthonormal-integral-basis` (theorem) | The `s_lambda` form a `Z`-basis of `Lambda`, and `<s_lambda,s_mu>=delta_{lambda,mu}`. | `thm-jacobi-trudi-and-dual-jacobi-trudi-identities`; `thm-cauchy-kernel-has-power-complete-and-schur-expansions` | Prove unitriangular change from `s` to `m`; then read self-duality from the Schur Cauchy expansion. | Macdonald I §§3–4, pp. 40–65. Basis used by Frobenius characteristic. |
| `def-skew-schur-function-by-hall-adjointness` (definition) | `s_(lambda/mu)` is the unique homogeneous element determined by `<s_(lambda/mu),s_nu>=<s_lambda,s_mu s_nu>` for every partition `nu`. | `def-hall-inner-product-on-symmetric-functions`; `thm-schur-functions-form-an-orthonormal-integral-basis` | Define the adjoint coefficient degreewise. Do not build the later vanishing criterion into the definition. | Macdonald I (5.1), p. 69. Keeps skew objects independent of a particular Specht construction. |
| `thm-skew-jacobi-trudi-and-tableau-expansion` (theorem) | For `mu subseteq lambda`, `s_{lambda/mu}=det(h_{lambda_i-mu_j-i+j})=sum_T x^{wt(T)}` over row-weak/column-strict skew tableaux; otherwise it is zero. | `def-skew-schur-function-by-hall-adjointness`; `thm-jacobi-trudi-and-dual-jacobi-trudi-identities`; `def-semistandard-tableau-and-kostka-number` | Use Cauchy and a zero-block determinant for skew Jacobi–Trudi; split alphabets repeatedly and specialize to one variable for the tableau expansion. | Macdonald I (5.4), (5.7), (5.11)–(5.12), pp. 70–73. Feeds skew characters/LR without duplicating RG tableaux. |
| `lem-kostka-change-of-basis-is-dominance-unitriangular` (lemma) | In every degree, `h_mu=sum_lambda K_(lambda,mu)s_lambda`; moreover `K_(lambda,mu)=0` unless `lambda` dominates `mu` and `K_(mu,mu)=1`, so the finite Kostka matrix is unitriangular and invertible over the integers. | `def-semistandard-tableau-and-kostka-number`; `thm-schur-functions-form-an-orthonormal-integral-basis`; `thm-skew-jacobi-trudi-and-tableau-expansion` | Identify `K=M(s,m)` by the tableau expansion; Hall duality gives `M(h,s)=K^T`, hence the displayed complete-to-Schur formula. For unitriangularity, entries at most `r` in a semistandard tableau force the dominance inequalities, and the unique tableau of shape and weight `mu` has row `i` filled by `i`. | Macdonald I (6.4)–(6.6), pp. 101–102. Exact label-identification joint required by Frobenius characteristic. |
| `prop-omega-conjugates-schur-functions` (proposition) | The stable involution `omega` interchanges `e_r` and `h_r`, sends `p_r` to `(-1)^{r-1}p_r`, and sends `s_lambda` to `s_{lambda'}`. | `thm-elementary-and-complete-families-freely-generate-the-stable-ring`; `thm-jacobi-trudi-and-dual-jacobi-trudi-identities` | Define on free generators; use the dual determinant and Newton relations. | Macdonald I (2.7), (3.8), pp. 22, 44. Later identifies sign twist. |

### Symmetric Functions, the Hall Inner Product, and Schur Bases — Examples (B dependency leaf; `symmetric-functions-hall-inner-product-and-schur-bases-examples`)

| proposed id (kind) | exact computation/boundary | deps | verification route | source locator and purpose |
|---|---|---|---|---|
| `ex-degree-three-stable-symmetric-function-bases` (example) | Write the `m,e,h,p,s` bases of `Lambda^3` and the unitriangular transition matrices. | `thm-schur-functions-form-an-orthonormal-integral-basis`; `prop-power-sums-form-a-rational-not-integral-stable-basis` | Expand each basis in monomials and invert finite matrices. | Macdonald I §§2–3, examples. Checks all normalization conventions. |
| `cex-power-sums-do-not-form-an-integral-basis` (counterexample) | `h_2=(p_1^2+p_2)/2` lies in `Lambda` but is not an integral linear combination of `p_{(1,1)},p_{(2)}`. | `prop-power-sums-form-a-rational-not-integral-stable-basis` | Compare the two degree-two expansions. | Macdonald I §2, pp. 23–25. Prevents later integral overclaim. |
| `ex-cauchy-kernel-through-total-degree-three` (example) | Expand both sides of Cauchy through degree three and pair against `s_(2,1)`. | `thm-cauchy-kernel-has-power-complete-and-schur-expansions` | Finite coefficient comparison plus reproducing property. | Macdonald I §4 Ex. 9, p. 68. Makes completion degreewise. |
| `ex-a-disconnected-skew-schur-function-factors` (example) | Compute a skew diagram with two edge-disconnected components and verify the product factorization. | `thm-skew-jacobi-trudi-and-tableau-expansion` | Factor the determinant/tableau choices by components. | Macdonald I (5.7), pp. 70–71. Concrete skew boundary. |
| `cex-a-nonzero-stable-schur-function-can-vanish-in-too-few-variables` (counterexample) | If `ell(lambda)>N`, then stable `s_lambda` is nonzero in `Lambda` but its specialization to `N` variables is zero. | `def-stable-schur-function-by-bialternants` | Repeated exponent/alternant determinant vanishes at fixed rank. | Macdonald I §3 and §5 (5.8). Separates stable and finite-rank claims. |

## SYMR-2 — Frobenius characteristic and the character dictionary

### Frobenius Characteristic and the Symmetric-Group Character Dictionary (A page; `frobenius-characteristic-and-the-symmetric-group-character-dictionary`)

| proposed id (kind) | exact claim and restrictions | deps | proof route / intermediate joints | source locator and purpose |
|---|---|---|---|---|
| `def-graded-ordinary-representation-ring-of-symmetric-groups` (definition) | `R_S=direct-sum_{n>=0}R_C(S_n)` with degree `n`, including `S_0`; addition is virtual direct sum. | `def-virtual-character-and-character-ring-of-a-finite-group`; `def-partition-young-diagram-and-conjugate-partition` | Assemble the published degreewise Grothendieck groups. | Macdonald I §7, p. 112. Domain of the cross-rank product. |
| `def-outer-induction-product-for-symmetric-group-characters` (definition) | For `f in R(S_m)`, `g in R(S_n)`, set `f circ g=Ind_{S_m x S_n}^{S_{m+n}}(f boxtimes g)`; this is not same-rank tensor product. | `def-graded-ordinary-representation-ring-of-symmetric-groups`; `def-induced-character-of-a-complex-representation` | Use the standard Young-subgroup embedding with the second factor on the last `n` letters. | Macdonald I (7.3), p. 113; James §16, pp. 60–64. Fixes the outer/internal distinction. |
| `lem-centralizer-order-for-a-symmetric-group-cycle-type` (lemma) | For cycle type `rho=(1^{m_1}2^{m_2}...)`, the centralizer order is `z_rho=prod_i i^{m_i}m_i!`, so the class has size `n!/z_rho`. | `def-partition-young-diagram-and-conjugate-partition`; `cor-the-sum-of-squared-irreducible-character-values-at-g-is-the-centralizer-size` | Permute cycles of equal length and rotate each cycle; prove the actions are independent. | Macdonald I §7, p. 112; James §6, pp. 22–26. Normalizes characteristic map. |
| `def-frobenius-characteristic-map` (definition) | For a complex class function `f` on `S_n`, `ch(f)=sum_{rho partition n}f(rho)p_rho/z_rho in Lambda_Q^n`. | `lem-centralizer-order-for-a-symmetric-group-cycle-type`; `prop-power-sums-form-a-rational-not-integral-stable-basis`; `def-class-function-and-the-space-of-complex-class-functions` | Degreewise formula; explicitly evaluate on conjugacy-class representatives. | Macdonald I (7.2), p. 112. Core dictionary. |
| `lem-frobenius-characteristic-is-an-isometry` (lemma) | The ordinary character inner product equals the Hall inner product of characteristics. | `def-frobenius-characteristic-map`; `cor-power-sums-are-orthogonal-for-the-hall-inner-product`; `def-standard-inner-product-on-complex-class-functions` | Substitute class sizes `n!/z_rho` and use power-sum orthogonality. | Macdonald I §7, pp. 112–113. Converts irreducibility to Schur orthogonality. |
| `lem-characteristic-of-a-young-permutation-character-is-complete` (lemma) | `ch(Ind_{S_lambda}^{S_n}1)=h_lambda`. | `def-frobenius-characteristic-map`; `lem-young-permutation-module-is-induced-from-the-trivial-character`; `thm-cauchy-kernel-has-power-complete-and-schur-expansions` | Compute fixed cosets by cycle type or pair with irreducibles via Frobenius reciprocity; identify complete coefficients. | Macdonald I (7.4), pp. 113–114. Matches inherited Young permutation modules. |
| `lem-frobenius-characteristic-preserves-outer-products` (lemma) | `ch(f circ g)=ch(f)ch(g)` for ordinary class functions in different degrees. | `def-outer-induction-product-for-symmetric-group-characters`; `def-frobenius-characteristic-map`; `thm-frobenius-formula-for-induced-characters` | Decompose a permutation’s cycles between the two Young-subgroup blocks; match concatenated cycle types and `z` factors. | Macdonald I (7.3), pp. 112–113. Multiplicative proof joint. |
| `thm-frobenius-characteristic-is-an-isometric-graded-ring-isomorphism` (theorem) | Over ordinary complex character theory, `ch:R_S -> Lambda` is a degree-preserving isometric ring isomorphism from outer induction to multiplication. | `lem-frobenius-characteristic-is-an-isometry`; `lem-characteristic-of-a-young-permutation-character-is-complete`; `lem-frobenius-characteristic-preserves-outer-products`; `thm-elementary-and-complete-families-freely-generate-the-stable-ring` | Multiplicativity plus surjectivity from the `h_lambda` basis; isometry gives injectivity. | Macdonald I (7.3)–(7.5), pp. 112–114. Main ordinary/symmetric-function bridge. |
| `thm-frobenius-characteristic-sends-specht-characters-to-schur-functions` (theorem) | For every `lambda partition n`, `ch(chi^lambda)=s_lambda`, where `chi^lambda` is the complex character of inherited `S^lambda`. | `thm-frobenius-characteristic-is-an-isometric-graded-ring-isomorphism`; `lem-characteristic-of-a-young-permutation-character-is-complete`; `thm-youngs-rule-for-permutation-modules`; `lem-kostka-change-of-basis-is-dominance-unitriangular` | Young's rule gives `[M^mu:S^lambda]=K_(lambda,mu)`, while `ch(M^mu)=h_mu=sum_lambda K_(lambda,mu)s_lambda`. Order partitions by dominance and invert the finite unitriangular Kostka matrix, which identifies the Specht and Schur labels rather than merely matching two orthonormal bases up to permutation. | Macdonald I (7.5)–(7.6), pp. 114–115, reconciled with RG-10 Young's rule. Adds the labelled dictionary without reproving RG-9 classification. |
| `cor-irreducible-symmetric-group-character-values-are-power-sum-coefficients` (corollary) | `chi^lambda(rho)=<s_lambda,p_rho>`, equivalently it is the coefficient of `p_rho/z_rho` in `s_lambda`. | `thm-frobenius-characteristic-sends-specht-characters-to-schur-functions`; `cor-power-sums-are-orthogonal-for-the-hall-inner-product` | Pair the characteristic formula with `p_rho`. | Macdonald I §7, pp. 114–116. Character calculation interface for SYMR-3. |
| `prop-sign-twist-corresponds-to-the-omega-involution` (proposition) | `ch(V tensor sgn)=omega(ch(V))`; hence `S^lambda tensor sgn` has character `chi^{lambda'}` over `C`. | `prop-omega-conjugates-schur-functions`; `thm-frobenius-characteristic-sends-specht-characters-to-schur-functions`; `def-sign-representation-and-restriction-of-a-representation` | Multiply character values by `(-1)^{n-ell(rho)}` and compare `omega(p_rho)`. | Macdonald I §7 Ex. 2, p. 116. Exact ordinary conjugation rule. |
| `prop-regular-character-has-characteristic-p-one-to-the-n` (proposition) | The regular character of `S_n` maps to `p_1^n`, and `p_1^n=sum_lambda f^lambda s_lambda`. | `thm-frobenius-characteristic-is-an-isometric-graded-ring-isomorphism`; `thm-character-of-the-regular-representation`; `thm-standard-polytabloid-basis` | The regular character vanishes off identity and is `n!` at identity; expand in the Schur basis using inherited dimensions. | Macdonald I §7 and RG-9/RG-11. Cross-checks the inherited RSK sum of squares without reminting it. |

### Frobenius Characteristic and the Symmetric-Group Character Dictionary — Examples (B dependency leaf; `frobenius-characteristic-and-the-symmetric-group-character-dictionary-examples`)

| proposed id (kind) | exact computation/boundary | deps | verification route | source locator and purpose |
|---|---|---|---|---|
| `ex-frobenius-characteristic-dictionary-for-s3` (example) | Compute all three irreducible characteristics and recover the `S_3` character table from power-sum coefficients. | `cor-irreducible-symmetric-group-character-values-are-power-sum-coefficients`; `thm-standard-polytabloid-basis` | Expand `s_(3),s_(2,1),s_(1,1,1)` in `p` and read values. | Macdonald I §7 Ex. 1. First full dictionary check. |
| `ex-young-permutation-characteristic-for-shape-two-one` (example) | Verify `ch(M^(2,1))=h_2h_1=s_(3)+s_(2,1)`. | `lem-characteristic-of-a-young-permutation-character-is-complete`; `thm-youngs-rule-for-permutation-modules` | Fixed-coset calculation and Schur expansion. | Macdonald I §§5,7. Checks agreement with inherited Young’s rule. |
| `ex-sign-twist-conjugates-the-s31-character` (example) | In `S_4`, compute that the sign twist of `chi^(3,1)` is `chi^(2,1,1)` on every cycle type. | `prop-sign-twist-corresponds-to-the-omega-involution` | Compare parity `(-1)^{4-ell(rho)}` and omega on power sums. | Macdonald I §7 Ex. 2. Convention check. |
| `cex-outer-induction-is-not-the-kronecker-product` (counterexample) | `chi^(1) circ chi^(1)=chi^(2)+chi^(1,1)` is a character of `S_2`, whereas a same-rank tensor product stays in one degree. | `def-outer-induction-product-for-symmetric-group-characters` | Compute induction from `S_1 x S_1`; compare groups/degrees. | Macdonald I §7. Prevents the central product confusion. |

## SYMR-3 — Outer products, skew Specht modules, and Littlewood–Richardson

The shared LR tableau definition remains owned by RL-8 as the stable item
`def-littlewood-richardson-tableau-and-coefficient`. This page requires RL-8
and reuses that exact convention; no synonym or reverse dependency is proposed.

### Outer Products, Skew Specht Modules, and Littlewood–Richardson Coefficients (A page; `outer-products-skew-specht-modules-and-littlewood-richardson`)

| proposed id (kind) | exact claim and restrictions | deps | proof route / intermediate joints | source locator and purpose |
|---|---|---|---|---|
| `thm-outer-induction-makes-the-graded-representation-group-a-commutative-ring` (theorem) | Outer induction is associative, commutative up to the block-swap conjugation, unital in degree zero, and distributive, so `R_S` is a graded commutative ring. | `def-outer-induction-product-for-symmetric-group-characters`; `thm-transitivity-of-induction-for-finite-groups`; `prop-induction-and-restriction-satisfy-the-projection-formula-on-character-rings` | Use induction transitivity for associativity, conjugacy of the two block embeddings for commutativity, and `S_0` for the unit. | Macdonald I §7, pp. 112–114. Makes the ring structure explicit before `ch` uses it. |
| `def-restriction-coproduct-on-the-graded-symmetric-group-character-ring` (definition) | For `V` of `S_n`, `Delta[V]=sum_(a+b=n)[Res_(S_a x S_b)^(S_n)V]` in the ordinary finite direct sum of bidegrees `(a,b)`. | `def-graded-ordinary-representation-ring-of-symmetric-groups`; `def-sign-representation-and-restriction-of-a-representation` | Use the standard ordered block subgroups, including `a=0,n`; no completion is needed because each homogeneous degree has `n+1` summands. | Macdonald I §7 Ex. 26, pp. 132–134. Distinct cross-rank restriction operation. |
| `def-graded-bialgebra-and-hopf-algebra` (definition) | Define a graded coalgebra, graded bialgebra and Hopf algebra over a commutative ring, including counit, compatibility, convolution, antipode and connectedness `H_0=k`. | `def-algebra-over-a-commutative-ring`; `def-tensor-product-of-modules-by-generators-and-relations` | State all diagrams/equalities on homogeneous elements and fix the tensor-product multiplication; this local definition is needed because no published generic bialgebra page exists. | Aguiar--Bergeron--Sottile S5 §1, pp. 4–6. Minimal generic infrastructure used only by the induction/restriction package. |
| `lem-connected-graded-bialgebra-has-a-recursive-antipode` (lemma) | Every nonnegatively graded bialgebra with degree-zero part equal to the ground ring has a unique antipode, defined on positive degree by the reduced-coproduct recursion. | `def-graded-bialgebra-and-hopf-algebra` | Write `Delta(x)=x tensor 1+1 tensor x+sum x' tensor x''` with strictly smaller positive degrees; define `S(x)=-x-sum S(x')x''` and inductively verify both convolution-inverse identities and uniqueness. | Standard connected-graded-bialgebra recursion; Aguiar--Bergeron--Sottile S5 §1 provides a proof-bearing treatment. |
| `thm-outer-induction-and-restriction-form-a-graded-hopf-algebra` (theorem) | With outer multiplication and restriction coproduct, `R_S` is a connected graded bialgebra and hence a Hopf algebra; Frobenius characteristic identifies it with the usual Hopf algebra `Lambda`. | `thm-outer-induction-makes-the-graded-representation-group-a-commutative-ring`; `def-restriction-coproduct-on-the-graded-symmetric-group-character-ring`; `thm-mackey-double-coset-formula-for-restricting-an-induced-character`; `lem-connected-graded-bialgebra-has-a-recursive-antipode`; `thm-frobenius-characteristic-is-an-isometric-graded-ring-isomorphism` | Apply Mackey to intersections of two block Young subgroups, indexed by 2-by-2 nonnegative integer matrices, to prove bialgebra compatibility; then apply the preceding recursion and compare the transported structure through `ch`. | Macdonald I §7 Ex. 26, pp. 132–134; Aguiar--Bergeron--Sottile S5 §1 checks the connected-Hopf joint. |
| `prop-restriction-coproduct-is-schur-skewing` (proposition) | Under `ch`, `Delta(s_lambda)=sum_mu s_mu tensor s_{lambda/mu}`; equivalently the `S_m x S_{n-m}` restriction multiplicity is the skew-Schur coefficient. | `def-skew-schur-function-by-hall-adjointness`; `def-restriction-coproduct-on-the-graded-symmetric-group-character-ring`; `cor-frobenius-reciprocity-for-complex-characters`; `thm-frobenius-characteristic-sends-specht-characters-to-schur-functions` | Pair both tensor factors with Schur functions; Frobenius reciprocity turns restriction into outer multiplication and Hall adjointness defines skewing. | Macdonald I §7 Ex. 3 and Ex. 26. Exact skew-character bridge. |
| `thm-outer-littlewood-richardson-rule` (theorem) | Over `C`, `Ind_{S_m x S_n}^{S_{m+n}}(S^mu boxtimes S^nu)` decomposes as `direct-sum_lambda (S^lambda)^{c_{mu,nu}^lambda}`, where the coefficient is the reconciled LR tableau count; this is not a Kronecker product. | `thm-frobenius-characteristic-sends-specht-characters-to-schur-functions`; `lem-frobenius-characteristic-preserves-outer-products`; `def-littlewood-richardson-tableau-and-coefficient` | Translate the Schur product through `ch`. For the coefficient proof use Macdonald’s letter-replacement/recording bijection: repair the first maximal bad index, prove reversibility, tableau preservation, and the decreasing-row lemma. | Macdonald I §9 (9.1)–(9.7), pp. 142–148; James Theorem 16.4, pp. 60–64 independently checks the induced-product conclusion. Main LR statement in symmetric-group form. |
| `cor-outer-pieri-rules-for-trivial-and-sign-factors` (corollary) | Outer induction with `S^(r)` adds a horizontal `r`-strip and with `S^(1^r)` adds a vertical `r`-strip, each with multiplicity one. | `thm-outer-littlewood-richardson-rule`; `prop-sign-twist-corresponds-to-the-omega-involution`; `thm-skew-jacobi-trudi-and-tableau-expansion` | In content `(r)` the tableau conditions force a horizontal strip; apply omega for the vertical case. | Macdonald I (5.16)–(5.17), pp. 73–74; James §14. Genuine multi-box extension of RG-10 one-box branching. |
| `def-skew-multiplicity-module-over-c` (definition) | For `mu subseteq lambda`, put `m=|mu|`, `r=|lambda|-m`, and define the skew multiplicity module `K^(lambda/mu)=Hom_(C[S_m])(S^mu,Res_(S_m x S_r)^(S_(m+r))S^lambda)`. The commuting `S_r` action is by postcomposition on the restricted target. | `def-restriction-coproduct-on-the-graded-symmetric-group-character-ring`; `thm-complex-specht-modules-are-irreducible`; `prop-intertwiner-space-is-a-vector-space-and-endomorphisms-form-a-k-algebra` | Verify the two Young-subgroup factors commute, so postcomposition preserves `S_m`-equivariance and defines an `S_r` module. This gives a proof-closed characteristic-zero module realization without asserting an unsourced classical skew-polytabloid filtration. | Macdonald I §7 Ex. 3, pp. 116–117, interpreted by Frobenius reciprocity; James–Peel's arbitrary-field skew-polytabloid theorem is deliberately not claimed without its complete proof text. |
| `thm-skew-multiplicity-module-has-littlewood-richardson-specht-decomposition-over-c` (theorem) | As a complex `S_r`-module, `K^(lambda/mu) isomorphic to direct-sum_(nu partition r)(S^nu)^(c_(mu,nu)^lambda)`; equivalently its Frobenius characteristic is `s_(lambda/mu)`. | `def-skew-multiplicity-module-over-c`; `prop-restriction-coproduct-is-schur-skewing`; `thm-outer-littlewood-richardson-rule`; `thm-maschkes-theorem-for-finite-groups-over-fields-whose-characteristic-does-not-divide-the-group-order` | Frobenius reciprocity identifies the multiplicity of `S^nu` in `K^(lambda/mu)` with the multiplicity of `S^mu boxtimes S^nu` in the restricted `S^lambda`, hence with the outer LR coefficient. Complex Maschke semisimplicity upgrades the character/multiplicity equality to the displayed direct-sum isomorphism. | Macdonald I §§5,7 and Ex. 3, pp. 69–74, 112–117; Macdonald I §9 supplies the LR coefficient proof. Module-level result with exact characteristic-zero boundary. |
| `cor-littlewood-richardson-coefficients-have-conjugation-symmetry` (corollary) | `c_{mu,nu}^lambda=c_{mu',nu'}^{lambda'}` and `c_{mu,nu}^lambda=c_{nu,mu}^lambda`. | `thm-outer-littlewood-richardson-rule`; `prop-sign-twist-corresponds-to-the-omega-involution`; `thm-outer-induction-makes-the-graded-representation-group-a-commutative-ring` | Apply omega to a Schur product and use commutativity. | Macdonald I §§5,9. Useful exact symmetries, not a general LR reproof. |

### Outer Products, Skew Specht Modules, and Littlewood–Richardson Coefficients — Examples (B dependency leaf; `outer-products-skew-specht-modules-and-littlewood-richardson-examples`)

| proposed id (kind) | exact computation/boundary | deps | verification route | source locator and purpose |
|---|---|---|---|---|
| `ex-outer-product-s32-with-s2` (example) | Decompose James’s `[3,2][2]` into its five irreducibles by horizontal strips. | `cor-outer-pieri-rules-for-trivial-and-sign-factors` | Enumerate all legal size-two horizontal strips. | James Example 14.5, pp. 52–53. Nontrivial repeated-row check. |
| `ex-littlewood-richardson-coefficient-greater-than-one-for-outer-induction` (example) | Exhibit two distinct lattice tableaux contributing to the same outer-induction constituent. | `thm-outer-littlewood-richardson-rule` | Draw both tableaux and check every prefix inequality. | Macdonald I §9; exact choice may reuse RL-8’s proposed example after ownership reconciliation, not duplicate it. |
| `cex-a-semistandard-skew-tableau-with-a-nonlattice-word-contributes-zero` (counterexample) | Give a row-weak/column-strict skew tableau whose reading word fails one lattice-prefix inequality and hence is not counted. | `thm-outer-littlewood-richardson-rule` | Compute the first failing prefix and run the first repair step. | Macdonald I (9.2)–(9.7), pp. 142–148. Shows semistandardness is insufficient. |
| `ex-restriction-coproduct-for-s-three-one` (example) | Compute every bidegree component of `Delta[S^(3,1)]` and match the skew Schur expansion. | `prop-restriction-coproduct-is-schur-skewing` | Remove all subshapes, compute small skew characters, compare dimensions. | Macdonald I §§5,7. Checks endpoint terms and middle restrictions. |
| `cex-an-lr-coefficient-is-not-a-kronecker-coefficient` (counterexample) | The nonzero outer coefficient `c^((2))_((1),(1))=1` compares representations of `S_1 x S_1` and `S_2`, whereas a Kronecker coefficient requires all three partitions to have the same size; it is therefore not a Kronecker coefficient at all. | `thm-outer-littlewood-richardson-rule`; `def-outer-induction-product-for-symmetric-group-characters` | Compute `Ind_(S_1 x S_1)^(S_2)(1 box 1)=S^(2) direct-sum S^(1,1)` and compare the input ranks with the same-rank domain of an internal tensor product. | Macdonald I §7 internal/outer products. Exact degree mismatch protecting SYMR-13’s boundary without a B-page dependency. |

## SYMR-4 — Rim hooks and Murnaghan–Nakayama

### Rim Hooks and the Murnaghan–Nakayama Rule (A page; `rim-hooks-and-the-murnaghan-nakayama-rule`)

| proposed id (kind) | exact claim and restrictions | deps | proof route / intermediate joints | source locator and purpose |
|---|---|---|---|---|
| `def-removable-border-strip-and-height` (definition) | A removable border strip of size `r` is a connected skew diagram with `r` nodes, no `2x2` block, whose removal leaves a partition; height is its number of occupied rows minus one. | `def-partition-young-diagram-and-conjugate-partition`; `def-removable-and-addable-nodes-of-a-partition` | Match James’s removable skew hook and leg convention. | James §§18,21, pp. 73–86; Macdonald I §7 Ex. 5. Fixes sign and connectivity. |
| `lem-hooks-correspond-to-removable-border-strips` (lemma) | Nodes/hooks of `lambda` correspond bijectively to removable border strips of `lambda`, preserving hook length and leg/height. | `def-removable-border-strip-and-height`; `def-hook-arm-leg-and-hook-length` | Slide the hook to the rim along its northeast/southwest boundary and give the inverse anchor. | James Lemma 18.4, p. 74. Links RG-11 hook data to MN. |
| `thm-determinantal-form-of-a-specht-character` (theorem) | In the ordinary character ring, `chi^lambda=det([lambda_i-i+j])`, where `[r]` is the trivial character of `S_r`, products mean outer induction, `[0]=1`, and `[r]=0` for `r<0`. | `thm-jacobi-trudi-and-dual-jacobi-trudi-identities`; `thm-frobenius-characteristic-sends-specht-characters-to-schur-functions`; `def-outer-induction-product-for-symmetric-group-characters` | Transport Jacobi–Trudi through `ch`; independently, James expands the determinant and cancels non-lambda diagrams using horizontal-strip induction. | James Theorem 19.2, pp. 74–76; Macdonald I (3.4),(7.5). Character-side determinant. |
| `lem-character-value-on-a-full-cycle-is-supported-on-hooks` (lemma) | For an `n`-cycle, `chi^lambda` is zero unless `lambda=(n-r,1^r)`, when its value is `(-1)^r`. | `thm-determinantal-form-of-a-specht-character`; `thm-first-orthogonality-relation-for-irreducible-complex-characters` | Construct the alternating hook-character column and show it is orthogonal to all non-full-cycle columns; normalize at the full cycle. | James Theorem 21.4, pp. 78–80. Base case for recursive removal. |
| `lem-alternating-hook-induction-cancels-disconnected-rim-removals` (lemma) | Suppose `lambda/nu` is a union of `m` disconnected skew hooks containing `r` nodes and occupying `c` columns. In the alternating hook induction of size `r`, its coefficient is `sum_(x=1)^r (-1)^(r-x) binom(m-1,c-x)`, with out-of-range binomials zero: this equals `(-1)^(r-c)` for `m=1` and is zero for `m>1`. Thus exactly one connected border strip survives, with sign `(-1)^height`. | `def-removable-border-strip-and-height`; `thm-outer-littlewood-richardson-rule`; `lem-character-value-on-a-full-cycle-is-supported-on-hooks` | James Lemma 21.5 identifies the LR multiplicity for the hook parameter `x` as `binom(m-1,c-x)`. Sum over the alternating hook characters and evaluate the finite sum as the corresponding coefficient of `(1-1)^(m-1)`; in the connected case `r-c` is the height. | James Lemma 21.5 and proof of Theorem 21.1, pp. 80–82. Exact binomial cancellation joint, not a slogan. |
| `thm-murnaghan-nakayama-character-rule` (theorem) | If `sigma` acts on `n-r` letters and `c` is a disjoint `r`-cycle, then `chi^lambda(sigma c)=sum_nu (-1)^{height(lambda/nu)}chi^nu(sigma)` over removable size-`r` border strips. | `lem-alternating-hook-induction-cancels-disconnected-rim-removals`; `cor-frobenius-reciprocity-for-complex-characters`; `lem-character-value-on-a-full-cycle-is-supported-on-hooks` | Express the full-cycle class functional by the alternating sum of hook characters, pair outer induction with restriction via Frobenius reciprocity, expand each product by LR, and apply the exact cancellation lemma. The symmetric-function check is multiplication by `p_r`. | James Theorem 21.1, pp. 77–82; Macdonald I §7 Ex. 5, p. 117 (independent statement check). Main recursive computation. |
| `cor-no-removable-cycle-length-strip-forces-a-character-zero` (corollary) | If `lambda` has no removable border strip of size equal to a cycle length `r` occurring in a class, then `chi^lambda` vanishes on that class. | `thm-murnaghan-nakayama-character-rule` | The defining sum is empty at the first removal. | James Example 21.2, pp. 77–78. Fast zero test. |
| `cor-hook-divisibility-defect-zero-vanishing-criterion` (corollary) | If no hook length of `lambda` is divisible by prime `p`, then ordinary `chi^lambda` vanishes on every p-singular permutation. No converse is claimed. | `lem-hooks-correspond-to-removable-border-strips`; `thm-murnaghan-nakayama-character-rule`; `thm-hook-length-formula` | A p-singular permutation has a cycle of length divisible by p; the hook hypothesis excludes that strip size. | James Cor. 21.6, p. 83. Ordinary vanishing criterion, not modular block classification. |
| `prop-r-cycle-virtual-character-filter` (proposition) | For `nu partition n-r`, the signed sum over all partitions obtained from `nu` by an `r`-border strip vanishes on classes having no `r`-cycle. | `thm-murnaghan-nakayama-character-rule`; `def-virtual-character-and-character-ring-of-a-finite-group` | Express as outer induction with the alternating hook character and evaluate by the induction formula. | James Theorem 21.7, pp. 83–84. Useful class-support relation with exact boundary. |
| `thm-triangular-fixed-tabloid-reconstruction-of-the-character-table` (theorem) | With one fixed order of partitions, Young-subgroup/class intersection matrix `A` and the nonnegative upper-triangular scaled multiplicity matrix `B` with prescribed diagonal satisfy James’s Gram equation and uniquely recover the ordinary character table as `C=B(A^T)^{-1}`. | `def-young-subgroup-tabloid-and-permutation-module`; `thm-youngs-rule-for-permutation-modules`; `thm-irreducible-complex-characters-form-an-orthonormal-basis-of-the-class-functions`; `lem-centralizer-order-for-a-symmetric-group-cycle-type` | Count tabloid fixed points to form `A`; compute pairwise permutation-character inner products two ways; solve `B` recursively using triangularity/nonnegativity; use `B=CA^T`. | James §6, Theorem 6.2, pp. 22–26. Independent all-table algorithm; second proof treatment remains a reported gap. |

### Rim Hooks and the Murnaghan–Nakayama Rule — Examples (B dependency leaf; `rim-hooks-and-the-murnaghan-nakayama-rule-examples`)

| proposed id (kind) | exact computation/boundary | deps | verification route | source locator and purpose |
|---|---|---|---|---|
| `ex-murnaghan-nakayama-for-shape-five-four-four` (example) | Compute `chi^(5,4,4)` on `(5,4,3,1)` as `-1`, on `(7,3,3)` as `2`, and explain specified forced-zero cycle lengths. | `thm-murnaghan-nakayama-character-rule` | Draw every legal successive border-strip removal with signs. | James Example 21.2, pp. 77–78. Nontrivial branching/cancellation check. |
| `ex-the-s4-character-table-by-rim-hook-recursion` (example) | Reconstruct all ordinary `S_4` irreducible values via MN and check row orthogonality. | `thm-murnaghan-nakayama-character-rule`; `thm-first-orthogonality-relation-for-irreducible-complex-characters` | Recurse over each cycle type, then take inner products. | James §21 plus Macdonald I §7. Small complete table. |
| `cex-a-disconnected-rim-set-is-not-a-border-strip` (counterexample) | Exhibit a size-`r` removable node set with two components; show including it spoils the MN value because it should cancel in the LR alternating sum. | `lem-alternating-hook-induction-cancels-disconnected-rim-removals` | Draw the set, identify `m>1`, and compute the alternating binomial cancellation. | James Lemma 21.5. Enforces connectivity. |
| `ex-r-cycle-filter-for-nu-three-two-and-r-three` (example) | Write James’s four-term virtual character of `S_8` and verify it vanishes on classes without a 3-cycle. | `prop-r-cycle-virtual-character-filter` | Apply MN or direct table values on representative classes. | James Example 21.8, p. 84. Tests “has an r-cycle,” not “order divisible by r.” |
| `ex-s5-character-table-by-fixed-tabloid-reconstruction` (example) | Compute the `A,B,C` matrices for `S_5`, declaring the nonstandard column order, and check the determinant. | `thm-triangular-fixed-tabloid-reconstruction-of-the-character-table` | Count subgroup/class intersections, triangular solve, matrix multiplication. | James Example 6.3 and Cor. 6.5, pp. 23–26. Second character algorithm. |

## SYMR-5 — Jucys–Murphy elements and seminormal forms

### Jucys–Murphy Elements and Seminormal Forms (A page; `jucys-murphy-elements-and-seminormal-forms`)

| proposed id (kind) | exact claim and restrictions | deps | proof route / intermediate joints | source locator and purpose |
|---|---|---|---|---|
| `def-jucys-murphy-elements-of-the-symmetric-group-algebra` (definition) | In `Z[S_n]`, set `X_1=0` and `X_k=sum_(j<k)(j k)` for `2<=k<=n`, and define the same elements over any commutative coefficient ring by base change. | `def-group-ring`; `def-finite-symmetric-group-and-permutation-notation`; `def-restriction-and-extension-of-scalars` | The finite integral group-algebra sum is compatible under `S_(n-1) subset S_n`; only later spectral conclusions specialize to characteristic zero. | Agent 4 S1 §3 pp. 18–24; S2 §2 pp. 8–13. Integral normalization needed by modular residue functors. |
| `lem-jucys-murphy-local-relations` (lemma) | For `s_i=(i i+1)`, `s_iX_j=X_js_i` for `j notin {i,i+1}` and `s_iX_i+1=X_{i+1}s_i` (equivalently the conjugation form in S1). | `def-jucys-murphy-elements-of-the-symmetric-group-algebra`; `thm-the-symmetric-group-has-the-coxeter-presentation` | Separate the two transpositions involving `i,i+1`; conjugate all remote summands and isolate `(i i+1)`. | Agent 4 S1 Thm 3.1; S2 (3.2)–(3.3), pp. 14–16. Local algebra for seminormal blocks. |
| `cor-jucys-murphy-elements-commute-pairwise` (corollary) | `[X_i,X_j]=0` for all `i,j`. | `lem-jucys-murphy-local-relations` | For `i<j`, group summands of `X_j` and cancel commutators in triples, or induct through the local relations. | Agent 4 S1 Thm 3.1 and S2 Cor. 2.6. Enables joint spectral calculus. |
| `def-gelfand-tsetlin-algebra-for-the-symmetric-group-chain` (definition) | `GZ(n)` is the subalgebra of `C[S_n]` generated by `Z(C[S_1]),...,Z(C[S_n])`. | `cor-group-algebra-is-semisimple-when-char-k-does-not-divide-group-order`; `def-center-of-the-group-algebra` | Use the standard subgroup chain and embedded centers. | Agent 4 S2 §1, pp. 7–8. |
| `lem-relative-centralizer-for-sn-minus-one-in-sn-is-commutative` (lemma) | The centralizer of `C[S_{n-1}]` in `C[S_n]` is commutative. | `def-gelfand-tsetlin-algebra-for-the-symmetric-group-chain`; `thm-maschkes-theorem-for-finite-groups-over-fields-whose-characteristic-does-not-divide-the-group-order` | Prove every permutation is conjugate to its inverse by an element of `S_{n-1}`; the induced involution fixes real centralizer elements, forcing commutativity. | Agent 4 S2 Thm 2.1, Lemmas 2.2–2.3, pp. 8–10. Independent structural explanation of inherited multiplicity-free branching. |
| `thm-relative-centralizer-is-generated-by-the-previous-center-and-last-jucys-murphy-element` (theorem) | `Z(C[S_n],C[S_(n-1)])=<Z(C[S_(n-1)]),X_n>`. | `lem-relative-centralizer-for-sn-minus-one-in-sn-is-commutative`; `def-jucys-murphy-elements-of-the-symmetric-group-algebra`; `cor-complex-specht-restriction-branching-rule` | Generate one-cycle class sums inductively and compare the relative-centralizer dimension with the multiplicity-free branching graph. | Agent 4 S2 Thms. 2.5, 2.8–2.9, pp. 10–13. Proof joint for full GZ generation. |
| `thm-jucys-murphy-elements-generate-the-gelfand-tsetlin-algebra` (theorem) | `GZ(n)=C[X_1,...,X_n]`; it is the algebra diagonal in the path basis and is maximal commutative. | `thm-relative-centralizer-is-generated-by-the-previous-center-and-last-jucys-murphy-element`; `cor-jucys-murphy-elements-commute-pairwise`; `cor-complex-specht-restriction-branching-rule` | Induct on `n`; multiply central idempotents along a branching path to obtain rank-one projections, then identify the whole diagonal algebra. | Agent 4 S2 Prop. 1.1, Cor. 2.6. Genuine strengthening of RG-10 branching. |
| `def-content-vector-of-a-standard-tableau` (definition) | For standard `T`, `Cont(T)=(c_T(1),...,c_T(n))`, where an entry in node `(r,c)` has content `c-r`. | `def-young-tableau-standard-tableau-and-shape` | Read the content in entry order. | Agent 4 S2 §§3–5. Spectral label. |
| `thm-jucys-murphy-joint-spectrum-is-the-set-of-tableau-content-vectors` (theorem) | On the GZ line indexed by `T`, `X_k` acts by `c_T(k)`; all and only standard-tableau content vectors occur, with one-dimensional joint eigenspaces. | `thm-jucys-murphy-elements-generate-the-gelfand-tsetlin-algebra`; `def-content-vector-of-a-standard-tableau`; `cor-complex-specht-restriction-branching-rule` | Classify admissible integer weights using the predecessor and collision conditions; biject them with tableaux and use GZ maximality for one-dimensionality. Independently derive eigenvalues from the transposition-class scalar and induction. | Agent 4 S2 Prop. 5.3, Thm 5.8; S1 Thms 3.2–3.3. Two independent proof routes. |
| `thm-primitive-tableau-idempotents-by-jucys-murphy-interpolation` (theorem) | Let `mu` be the shape of `T` with its `n`-node removed and let `A(mu)` be the set of contents of addable nodes. With `P_[1]=1`, the rank-one path idempotent is `P_T=P_(T downarrow [n-1]) product_(c in A(mu)\{c_T(n)}) (X_n-c)/(c_T(n)-c)` in `C[S_n]`; the displayed denominators are nonzero. | `thm-jucys-murphy-joint-spectrum-is-the-set-of-tableau-content-vectors`; `def-removable-and-addable-nodes-of-a-partition` | Addable nodes lie on distinct diagonals, so their integer contents are distinct. The Lagrange factors kill every competing branch out of `mu`, fix the selected eigenline, and induction gives mutually orthogonal primitive idempotents summing to one. | Agent 4 S1 Thms. 3.4–3.5, pp. 21–24; S3 Thm. 3.16 independently checks separation/projectors. Exact spectral construction and coefficient-ring boundary. |
| `thm-young-seminormal-form-from-jucys-murphy-eigenlines` (theorem) | Put `r=c_T(i+1)-c_T(i)`. If the entries share a row or column, the left action is `s_i v_T=+v_T` or `-v_T`, respectively. Otherwise `T'=s_iT` is standard and, in the declared left-action triangular phasing, `s_i v_T=r^(-1)v_T+v_(T')` and `s_i v_(T')=(1-r^(-2))v_T-r^(-1)v_(T')`. | `lem-jucys-murphy-local-relations`; `thm-jucys-murphy-joint-spectrum-is-the-set-of-tableau-content-vectors`; `thm-primitive-tableau-idempotents-by-jucys-murphy-interpolation` | The local relations show only the swapped content vector can mix; solve the first diagonal coefficient as `1/r`, use `s_i^2=1` for its companion, and fix the coefficient one by the source's tableau order/phasing. A right-action version would be the transpose and is not mixed into this row. | Agent 4 S2 Props. 6.1–6.2, pp. 22–23; S1 §4 Thms. 4.2–4.4 independently, after its `pi=-r` translation. |
| `thm-young-orthogonal-form-from-seminormal-rescaling` (theorem) | Over `R` equal to the real numbers, rescale to unit vectors so that on the ordered basis `(u_T,u_(T'))` the nontrivial block is `[[r^(-1),sqrt(1-r^(-2))],[sqrt(1-r^(-2)),-r^(-1)]]`, taking the positive square root; common-row/common-column cases are the scalars `+1,-1`. | `thm-young-seminormal-form-from-jucys-murphy-eigenlines`; `def-invariant-inner-product-on-a-tabloid-module` | Solve the positive norm ratio along admissible tableau swaps, verify path-independence and Coxeter relations, and choose phases giving positive off-diagonal entries. | Agent 4 S2 §6 and S1 Thm. 4.4; James §25 provides an independent Gram--Schmidt/branching proof. |
| `lem-a-partition-is-determined-by-its-multiset-of-node-contents` (lemma) | Two Young diagrams with the same multiset of integers `c-r` have the same partition shape. | `def-partition-young-diagram-and-conjugate-partition`; `def-content-vector-of-a-standard-tableau` | Recover the Frobenius diagonal length and arm/leg lengths from the multiplicities on positive and negative diagonals. | Agent 4 S1 §5 and S3 Prop. 4.13. Exact separation joint for central interpolation. |
| `thm-symmetric-polynomials-in-jucys-murphy-elements-give-the-center` (theorem) | Evaluating symmetric polynomials in `X_1,...,X_n` gives central elements, and every central element of `C[S_n]` is obtained this way. | `thm-jucys-murphy-elements-generate-the-gelfand-tsetlin-algebra`; `thm-jucys-murphy-joint-spectrum-is-the-set-of-tableau-content-vectors`; `lem-a-partition-is-determined-by-its-multiset-of-node-contents`; `thm-class-sums-form-a-basis-of-the-center-of-k-g` | Symmetric evaluations depend only on the content multiset and hence only on shape, so they are scalar on each simple; multivariate interpolation on the finitely many distinct shape spectra gives the reverse containment. | Agent 4 S1 Thms. 5.1–5.2, pp. 33–36; S3 Prop. 4.13 checks the content-multiset hypothesis. |
| `thm-elementary-symmetric-jucys-evaluation-is-a-cycle-count-class-sum` (theorem) | `e_s(X_2,...,X_n)` equals the sum of all permutations with exactly `n-s` cycles, grouped as their class sums. | `thm-symmetric-polynomials-in-jucys-murphy-elements-give-the-center`; `def-elementary-symmetric-polynomials` | Induct on `n`: choosing a transposition `(j n)` inserts `n` into an existing cycle, while omitting it leaves `n` fixed; track degree and cycle loss. | Agent 4 S1 Thm 5.10, pp. 51–52. Concrete central class formula. |

### Jucys–Murphy Elements and Seminormal Forms — Examples (B dependency leaf; `jucys-murphy-elements-and-seminormal-forms-examples`)

| proposed id (kind) | exact computation/boundary | deps | verification route | source locator and purpose |
|---|---|---|---|---|
| `ex-jucys-murphy-spectrum-and-projectors-for-s3` (example) | List content vectors for every standard tableau of size three and compute the corresponding interpolation projectors in `C[S_3]`. | `thm-primitive-tableau-idempotents-by-jucys-murphy-interpolation` | Evaluate the recursive products and check orthogonal idempotence/sum to one. | Agent 4 S1 after Thm 3.5. Tests all projector denominators. |
| `ex-seminormal-and-orthogonal-block-for-shape-two-one` (example) | For the two standard `(2,1)` tableaux, compute the `s_2` seminormal matrix and the orthogonal block with entries `±1/2,sqrt(3)/2`; compute row/column scalar cases for `s_1`. | `thm-young-seminormal-form-from-jucys-murphy-eigenlines`; `thm-young-orthogonal-form-from-seminormal-rescaling` | Use content difference `r=±2`, square the matrices, and verify the braid relation. | Agent 4 S2 §6; S1 §4. Sign/phasing audit. |
| `cex-ordinary-jucys-murphy-projection-formulas-do-not-survive-content-collision` (counterexample) | For shape `(2,1)`, the two standard tableaux have content vectors `(0,1,-1)` and `(0,-1,1)`, which coincide modulo `2`; already over the predecessor shape `(1)`, the addable contents `1` and `-1` give Lagrange denominator `2=0`. Thus the characteristic-zero primitive-projector formula fails after reduction, without implying that the module fails to exist. | `thm-primitive-tableau-idempotents-by-jucys-murphy-interpolation` | Draw both tableaux, reduce the content vectors modulo two, and display the vanishing denominator. | Agent 4 S3 separation condition, §§2–3. Exact modular collision boundary. |
| `ex-elementary-jucys-murphy-class-sums-through-s4` (example) | Expand `e_1,e_2,e_3` in the `X_i` and verify they sum permutations with 3,2,1 cycles respectively. | `thm-elementary-symmetric-jucys-evaluation-is-a-cycle-count-class-sum` | Direct multiplication/class grouping in `S_4`. | Agent 4 S1 Thm 5.10. Character-class application. |

## SYMR-6 — Integral Specht modules and modular simple modules

Use James’s p-regular convention: `D^lambda` is the radical quotient of the
Specht module indexed by `lambda`. Kleshchev/Brundan p-restricted labels are
translated only in SYMR-9, with transpose and sign twist visible.

### Integral Specht Modules and Modular Simple Modules (A page; `integral-specht-modules-and-modular-simple-modules`)

| proposed id (kind) | exact claim and restrictions | deps | proof route / intermediate joints | source locator and purpose |
|---|---|---|---|---|
| `def-integral-specht-lattice-and-base-change` (definition) | The polytabloid span over `Z` is a free `Z[S_n]`-lattice with standard basis, and `S_R^lambda=Z S^lambda tensor_Z R` for a commutative ring `R`. | `def-column-antisymmetrizer-polytabloid-and-specht-module`; `thm-standard-polytabloid-basis`; `def-free-module-on-a-set-and-standard-basis` | Repeat no ordinary classification: transport the integral polytabloid relations and standard basis by base change. | Agent 5 James §§9–10 and Tomczak §2.2. Exact integral bridge missing from RG-9’s complex conclusion. |
| `def-modular-specht-form-and-radical-quotient` (definition) | Over the fixed splitting field `k` of characteristic prime `p`, restrict the invariant tabloid form to `S_k^lambda` and define `D^lambda=S_k^lambda/(S_k^lambda cap (S_k^lambda)^perp)`. | `def-integral-specht-lattice-and-base-change`; `def-invariant-inner-product-on-a-tabloid-module`; `def-module-radical-socle-head-and-loewy-series`; `def-p-modular-system` | Define the form after reduction; distinguish its form radical from the ambient module radical until equality is proved. | Agent 5 James §11.1, p. 39; Craven §2.3; Tomczak §2.2. Candidate simple. |
| `thm-james-submodule-theorem-over-an-arbitrary-field` (theorem) | For any field, every submodule of the Young permutation module either contains `S^lambda` or lies in its orthogonal complement; consequently `D^lambda` is zero or absolutely irreducible. | `def-modular-specht-form-and-radical-quotient`; `lem-column-antisymmetrizer-detects-dominance`; `lem-antisymmetrizer-image-on-its-tabloid-module-is-one-dimensional` | Apply every column antisymmetrizer to the submodule; a nonzero image is a scalar polytabloid, while all zero images give orthogonality. Then any nonzero quotient submodule contains the image of `S^lambda`. | Agent 5 Tomczak §2.2 Thms 2.5,2.7 and James §§10–11. Genuine characteristic-free extension of RG-9’s char-zero statement. |
| `def-p-regular-and-p-restricted-partitions` (definition) | A partition is p-regular when no positive part occurs p times, and p-restricted when every adjacent difference is less than p; `lambda` is p-restricted iff `lambda'` is p-regular. | `def-partition-young-diagram-and-conjugate-partition` | Direct multiplicity/difference comparison under conjugation. | Agent 5 boundary audit; Kleshchev Remark 5.5. Prevents convention collapse. |
| `lem-specht-gram-gcd-detects-p-regularity` (lemma) | The Gram-gcd integer attached to `S_Z^lambda` is nonzero modulo `p` exactly when `lambda` is p-regular. | `def-integral-specht-lattice-and-base-change`; `def-p-regular-and-p-restricted-partitions` | Bound the gcd between factorial products from equal row lengths; show divisibility by p exactly when p equal rows occur, and construct a coefficient not divisible by p otherwise. | Agent 5 James §10 Lemma 10.4, Cor. 10.5. Arithmetic nonvanishing joint. |
| `thm-specht-radical-quotient-is-nonzero-exactly-for-p-regular-partitions` (theorem) | Over the fixed splitting field, `D^lambda` is nonzero iff `lambda` is p-regular. | `def-modular-specht-form-and-radical-quotient`; `lem-specht-gram-gcd-detects-p-regularity`; `thm-james-submodule-theorem-over-an-arbitrary-field` | Nonzero Gram coefficient makes the quotient nonzero; p-divisibility annihilates the whole form. | Agent 5 James Thm 11.1; Craven Thm 2.12 agrees. Exact label criterion. |
| `lem-nonzero-maps-between-specht-quotients-force-dominance` (lemma) | A nonzero map from the appropriate Specht/permutation quotient indexed by `lambda` to one indexed by `mu` forces the exact James dominance direction; equality gives scalar endomorphisms. | `thm-james-submodule-theorem-over-an-arbitrary-field`; `def-dominance-order-on-partitions`; `lem-column-antisymmetrizer-detects-dominance` | Use the reversed-row tableau and a nonzero antisymmetrizer scalar to invoke the basic tableau lemma; apply in both directions for inequivalence. | Agent 5 James Lemma 11.3 and Tomczak §2.2. Separates simple labels. |
| `thm-modular-simple-modules-of-sn-are-the-p-regular-specht-heads` (theorem) | For any splitting field `k` of characteristic `p`, the `D^lambda` with p-regular `lambda partition n` are absolutely irreducible, pairwise nonisomorphic, and exhaust all simple `k[S_n]`-modules. | `thm-specht-radical-quotient-is-nonzero-exactly-for-p-regular-partitions`; `lem-nonzero-maps-between-specht-quotients-force-dominance`; `cor-number-of-simple-kg-modules-equals-number-of-p-regular-conjugacy-classes` | Irreducibility is James submodule theorem; dominance gives inequivalence; p-regular partitions count p-regular conjugacy classes, matching the published Brauer-simple count. | Agent 5 James Thm 11.5, Craven Thm 2.12/Cor. 2.14. Central modular classification. |
| `thm-symmetric-group-decomposition-matrix-is-dominance-unitriangular` (theorem) | With rows all partitions and columns p-regular partitions, `d_{lambda,mu}=[S_k^lambda:D^mu]` is zero unless `mu` dominates `lambda`, and `d_{lambda,lambda}=1` for p-regular `lambda`; matrix orientation is declared explicitly. | `thm-modular-simple-modules-of-sn-are-the-p-regular-specht-heads`; `def-decomposition-numbers-and-decomposition-matrix`; `def-dominance-order-on-partitions` | Filter `M^lambda/S^lambda` by the orthogonal Specht complement; apply the dominance-map lemma to every factor and insert the unique head. | Agent 5 James §§12.1–12.3; Craven Cor. 2.11 independently checks. Constraint, not formula. |
| `prop-p-regular-and-p-restricted-simple-labels-are-related-by-conjugate-sign-duality` (proposition) | In characteristic `p`, if `lambda` is p-regular and `D^lambda` is the James radical-quotient simple, then the p-restricted cellular label satisfies `D(lambda') tensor sgn isomorphic to D^lambda`; equivalently `D(mu) isomorphic to D^(mu') tensor sgn` for p-restricted `mu`. State the corresponding dual-Specht convention separately. | `def-p-regular-and-p-restricted-partitions`; `thm-modular-simple-modules-of-sn-are-the-p-regular-specht-heads`; `def-sign-representation-and-restriction-of-a-representation` | Compare row/column polytabloids under transpose, vector-space duality and the sign twist at `q=1`; then pass to the unique simple head/socle. This is a group-side convention theorem and does not depend on future Hecke specialization. | Agent 5 Kleshchev Remark 5.5; Agent 3 p. 25 warning. Mandatory acyclic seam for SYMR-9. |
| `rem-general-modular-decomposition-numbers-are-not-determined-by-triangularity` (remark) | The classification and unitriangularity do not compute the remaining decomposition entries; arbitrary defining-characteristic decomposition numbers remain a genuine open program. | `thm-symmetric-group-decomposition-matrix-is-dominance-unitriangular` | No proof section; compare the exact triangular constraints with the source's remaining decomposition problem. Provenance `literature-derived/not-applicable`. | James §24; Kleshchev §§9.4–10. Explicit non-overclaim without a forward page edge. |

### Integral Specht Modules and Modular Simple Modules — Examples (B dependency leaf; `integral-specht-modules-and-modular-simple-modules-examples`)

| proposed id (kind) | exact computation/boundary | deps | verification route | source locator and purpose |
|---|---|---|---|---|
| `ex-specht-form-rank-for-shape-two-two-in-small-characteristics` (example) | The form quotient for `(2,2)` has rank 0 in characteristic 2, rank 1 in characteristic 3, and rank 2 for primes above 3. | `def-modular-specht-form-and-radical-quotient` | Write the standard-polytabloid Gram matrix and reduce it modulo each prime. | Agent 5 James Example 11.7. Sharp characteristic dependence. |
| `ex-decomposition-matrices-of-s3-in-characteristics-two-and-three` (example) | Compute all Specht composition factors for `S_3` at p=2 and p=3 and exhibit the declared unitriangular orientation. | `thm-symmetric-group-decomposition-matrix-is-dominance-unitriangular` | Reduce explicit Specht matrices/forms and match Brauer simples. | Agent 5 James Example 12.4. Small full modular table. |
| `cex-p-regular-and-p-restricted-are-not-the-same-label` (counterexample) | Give a partition that is p-regular but not p-restricted and show its conjugate has the opposite property. | `def-p-regular-and-p-restricted-partitions` | Compare multiplicities and adjacent differences. | Agent 5 convention audit. Prevents importing a good-node theorem under the wrong label. |
| `cex-a-modular-specht-module-need-not-be-simple-or-have-nonzero-form-head` (counterexample) | Use the p-singular `(2,2)` characteristic-two case to refute the ordinary claim that every Specht is irreducible. | `thm-specht-radical-quotient-is-nonzero-exactly-for-p-regular-partitions`; `def-modular-specht-form-and-radical-quotient` | Compute the polytabloid Gram matrix modulo two: the whole invariant form is radical although the Specht module itself is nonzero. | James Example 11.7. Distinguishes module from radical quotient without depending on another B item. |

## SYMR-7 — Type-A Hecke Specht modules and cellular bases

This page inherits RG-13’s generic type-A Hecke presentation, standard basis,
specializations, and Tits deformation. It adds the non-semisimple module theory.
The scaffold uses the Dipper--James Specht convention, with simples indexed by
e-regular partitions; Mathas’s reported dual-Specht/e-restricted statements are
translated by conjugating partitions and dualizing.

### Type-A Hecke Specht Modules and Cellular Bases (A page; `type-a-hecke-specht-modules-and-cellular-bases`)

| proposed id (kind) | exact claim and restrictions | deps | proof route / intermediate joints | source locator and purpose |
|---|---|---|---|---|
| `def-quantum-characteristic-of-an-invertible-hecke-parameter` (definition) | For a field `F` and `q in F^times`, `e` is the least `e>=2` with `1+q+...+q^{e-1}=0`, or infinity; for `q=1` this is `char(F)` when positive and infinity in characteristic zero. | `def-generic-type-a-hecke-algebra`; `prop-group-algebra-and-finite-field-specializations-of-the-generic-hecke-algebra` | State the sum convention separately from multiplicative order so `q=1` is correct. | Agent 3 Ch. 1 and conventions; Agent 4 S3 §2.15. Parameter used by every later simple-label theorem. |
| `def-cellular-algebra-basis-and-cell-module` (definition) | A finite-rank algebra with a poset-indexed basis, anti-involution, and triangular multiplication modulo higher cells has cell modules and their canonical cell forms. | `def-algebra-over-a-commutative-ring`; `def-left-and-right-modules` | Record the precise Graham–Lehrer axioms, including coefficient independence from the right index. | Agent 3 Ch. 2 Def. 2.1, pp. 9–11. Missing infrastructure required by Murphy theory. |
| `lem-cell-form-is-associative-and-its-radical-is-a-submodule` (lemma) | Over a field, the cellular multiplication defines a symmetric associative bilinear form on each cell module, and its radical is a submodule. | `def-cellular-algebra-basis-and-cell-module` | Multiply two basis elements modulo higher ideals in two associative orders; apply the anti-involution for symmetry. | Agent 3 Props. 2.7, 2.9, pp. 11–14. Proof joint for simple heads. |
| `thm-nonzero-cell-heads-classify-the-simple-modules-of-a-cellular-algebra` (theorem) | For a finite-dimensional cellular algebra over a field, every nonzero `D^lambda=C^lambda/rad C^lambda` is absolutely simple, these are pairwise nonisomorphic, and they exhaust all simples. | `lem-cell-form-is-associative-and-its-radical-is-a-submodule`; `thm-jordan-holder-theorem-for-modules` | A vector pairing to one can be sent to every cell basis vector, proving simplicity; filter the regular algebra by cell ideals and use order-sensitive Hom vanishing for exhaustion. | Agent 3 Thm 2.13, pp. 14–16. General theorem instantiated below. |
| `cor-cellular-decomposition-matrices-are-dominance-unitriangular` (corollary) | In the cell poset, `[C^lambda:D^lambda]=1` when nonzero, and an off-diagonal factor can occur only in the allowed order direction; this constrains but does not compute entries. | `thm-nonzero-cell-heads-classify-the-simple-modules-of-a-cellular-algebra` | Track the first cell ideal in which a simple appears and apply the cell-form map. | Agent 3 Cor. 2.14. Protects against calling triangularity an algorithm. |
| `def-murphy-elements-and-murphy-tableau-elements` (definition) | For the inherited Hecke algebra in the chosen convention, define row symmetrizer `m_lambda` and `m_st=T_{d(s)}^*m_lambda T_{d(t)}` for standard tableaux. | `def-generic-type-a-hecke-algebra`; `def-row-and-column-stabilizers-of-a-tableau`; `thm-standard-basis-of-the-generic-type-a-hecke-algebra` | Fix right/left module and generator normalizations; choose minimal row-standard permutations. | Agent 3 Ch. 3 §§1–2, pp. 17–25. Input to the cellular basis. |
| `thm-murphy-tableau-elements-form-a-cellular-basis` (theorem) | Over any commutative coefficient ring with invertible `q`, the `m_st` over all partitions and standard tableaux form a dominance-cellular basis of the type-A Hecke algebra. | `def-murphy-elements-and-murphy-tableau-elements`; `thm-standard-basis-of-the-generic-type-a-hecke-algebra`; `def-cellular-algebra-basis-and-cell-module`; `thm-standard-polytabloid-basis` | Straighten row-standard terms; prove the critical column exclusion; obtain dominance triangularity and compare the total tableau-square count with `n!`; verify the anti-involution. | Agent 3 Thm 3.18 with Prop. 3.16, pp. 17–25. Main new structure beyond RG-13. |
| `def-type-a-hecke-specht-module-and-cell-form` (definition) | The Specht module is the Murphy cell module (in the fixed Dipper--James convention), with basis indexed by standard tableaux and radical quotient `D^lambda`. | `thm-murphy-tableau-elements-form-a-cellular-basis`; `lem-cell-form-is-associative-and-its-radical-is-a-submodule` | Instantiate cell ideals/forms and explicitly record the conjugate-dual translation from Mathas’s convention. | Agent 3 Prop. 3.20 and p. 25 warning. |
| `thm-simple-type-a-hecke-modules-are-indexed-by-e-regular-partitions` (theorem) | Over a splitting field `F`, the nonzero heads `D^lambda`, for e-regular `lambda`, give all simple `H_{F,q}(S_n)`-modules; no simple is attached to a non-e-regular label in this convention. | `def-type-a-hecke-specht-module-and-cell-form`; `thm-nonzero-cell-heads-classify-the-simple-modules-of-a-cellular-algebra`; `def-quantum-characteristic-of-an-invertible-hecke-parameter` | Use the cell-head classification, then the source’s nonvanishing criterion translated from e-restricted dual labels. | Agent 3 Ch. 3 §4, Thm 3.21 and classification discussion. Exact root-of-unity replacement for ordinary Specht classification. |
| `cor-type-a-hecke-decomposition-matrix-is-dominance-unitriangular` (corollary) | With e-regular columns, `d_{lambda,lambda}=1`, and `d_{lambda,mu}` can be nonzero only in the stated dominance direction. | `thm-simple-type-a-hecke-modules-are-indexed-by-e-regular-partitions`; `cor-cellular-decomposition-matrices-are-dominance-unitriangular` | Apply cellular order and translate conjugate dominance carefully. | Agent 3 Chs. 2–3. Boundary: the remaining entries are not generally known. |
| `def-hecke-jucys-murphy-elements-and-q-contents` (definition) | Define Mathas-normalized commuting `L_k` and tableau diagonal value `[c-r]_q`, explicitly relating the `q=1` limit to ordinary `X_k`. | `def-jucys-murphy-elements-of-the-symmetric-group-algebra`; `def-generic-type-a-hecke-algebra`; `def-quantum-characteristic-of-an-invertible-hecke-parameter` | Write the weighted transposition-word sum and quantum integer convention. | Agent 3 Example 3.23; Agent 4 S3 §2.15. Connects ordinary and Hecke spectra without identifying them at roots of unity. |
| `thm-hecke-jucys-murphy-action-is-dominance-triangular` (theorem) | On the Murphy basis, `L_k` acts triangularly with diagonal `[content_t(k)]_q` over every allowed coefficient ring. | `def-hecke-jucys-murphy-elements-and-q-contents`; `thm-murphy-tableau-elements-form-a-cellular-basis` | Induct under tableau deletion; transport diagonal terms through adjacent swaps using JM–generator relations; handle the first-column case by a Garnir relation. | Agent 3 Thm 3.30, pp. 26–29. Survives beyond semisimplicity. |
| `thm-separated-hecke-residues-give-seminormal-idempotents-and-bases` (theorem) | Let the coefficient ring be a field, `q!=0`, and quantum characteristic `e>n` (including `e=infinity`). Standard tableau q-content sequences separate. On a fixed Specht module set `F_t=product_(k=1)^n product_(s: [content_s(k)]_q != [content_t(k)]_q) (L_k-[content_s(k)]_q)/([content_t(k)]_q-[content_s(k)]_q)` and `f_t=m_tF_t`; these are well-defined spectral projectors/vectors and the `f_t` form an orthogonal seminormal basis. For the right action, if `u=s(i,i+1)` and `rho=content_s(i)-content_s(i+1)`, then `f_sT_i=qf_s` in one row, `-f_s` in one column, `-[rho]_q^(-1)f_s+f_u` when `s` dominates `u`, and `q^rho[rho]_q^(-1)f_s+([rho+1]_q[rho-1]_q/[rho]_q^2)f_u` when `u` dominates `s`. | `thm-hecke-jucys-murphy-action-is-dominance-triangular`; `def-quantum-characteristic-of-an-invertible-hecke-parameter`; `thm-murphy-tableau-elements-form-a-cellular-basis` | Lemma 3.32 proves q-content separation; the displayed q-Lagrange product, rather than the ordinary `X_k` projector, isolates each triangular joint eigenspace. Unitriangular transition and the cellular form give orthogonality; the JM-generator relations and quadratic relation give the four exact right-action cases. | Agent 3 Lemma 3.32, Proposition 3.33, Theorem 3.34, printed pp. 29–30; Agent 4 S3 independently checks the abstract separated-cellular construction. Exact q-projector and generator formula. |
| `prop-hecke-specht-theory-specializes-to-group-specht-theory-at-q-one` (proposition) | Under `q=1`, the algebra, Murphy cells/forms, and simple-label statement specialize to symmetric-group Specht theory; in characteristic `p`, quantum characteristic becomes `p`, so not every Specht remains simple. | `def-type-a-hecke-specht-module-and-cell-form`; `prop-group-algebra-and-finite-field-specializations-of-the-generic-hecke-algebra`; `def-quantum-characteristic-of-an-invertible-hecke-parameter`; `thm-simple-type-a-hecke-modules-are-indexed-by-e-regular-partitions`; `prop-p-regular-and-p-restricted-simple-labels-are-related-by-conjugate-sign-duality` | Base-change the cellular basis/form and use the already established group-side conjugate/sign label translation to identify the specialized cell module. This direction is SYMR-6 to SYMR-7 only. | Agent 3 Chs. 1,3 and Agent 5 Kleshchev Remark 5.5. Seam from group Spechts without a page cycle. |
| `def-type-a-q-schur-algebra-and-weyl-modules` (definition) | For `d>=n`, let `M` be the direct sum of the type-A Hecke permutation modules indexed by compositions of `n` with at most `d` parts and define `S_{F,q}(d,n)=End_H(M)`. Its cellular standard modules are the Weyl modules `Delta(lambda)` for partitions with at most `d` parts. | `def-murphy-elements-and-murphy-tableau-elements`; `def-cellular-algebra-basis-and-cell-module`; `def-young-subgroup-tabloid-and-permutation-module` | Construct the q-permutation summands and endomorphism basis from semistandard homomorphisms, verify dominance triangularity and identify the resulting cell modules. Keep the endomorphism-side convention fixed throughout. | Mathas notes Ch. 4 §§1–2 and Ch. 5 opening, printed pp. 34–46, 53–54. Required q-Schur object for the full block proof. |
| `def-blocks-of-a-finite-dimensional-algebra-by-primitive-central-idempotents` (definition) | For a finite-dimensional algebra over a field, a block is the two-sided ideal cut out by one primitive central idempotent; the identity is the unique sum of mutually orthogonal primitive central idempotents, and every finite-dimensional module is the direct sum of its block-idempotent images. | `lem-artinian-product-idempotents`; `cor-artinian-local-ring-only-idempotents-are-zero-and-one` | The center is finite-dimensional, hence Artinian by descending-chain stabilization of vector subspaces. Apply its unique Artinian local-product decomposition and lift the coordinate units; orthogonality gives the algebra and module decompositions. State separately that this does not assert semisimplicity of a block. | Mathas notes Ch. 5 block conventions, printed pp. 53–54; the finite-dimensional algebra proof is supplied locally from the inherited Artinian-ring decomposition. Generic block language needed before q-Schur specialization. |
| `def-schur-idempotent-and-schur-functor-for-type-a-q-schur-algebras` (definition) | The summand of `M` indexed by `(1^n)` determines an idempotent `e` with `eSe isomorphic to H`; for the fixed right-module convention the Schur functor is the exact functor `F(X)=Xe`, a right `eSe`-module. | `def-type-a-q-schur-algebra-and-weyl-modules`; `def-blocks-of-a-finite-dimensional-algebra-by-primitive-central-idempotents` | Identify the corner by its action on the regular Hecke summand. Right multiplication by `e` is a direct-summand projection on underlying vector spaces and commutes with kernels and cokernels, proving exactness directly. The left-module formula `eX` is not mixed into this convention. | Mathas notes Ch. 4 §3, printed pp. 47–52. Exact bridge used in Corollaries 5.32 and 5.37. |
| `thm-schur-functor-sends-weyl-and-simple-modules-to-specht-and-hecke-simple-modules` (theorem) | In the fixed right-module convention, `F(Delta(lambda))` is the selected Hecke Specht module `S^lambda`, while `F(L(lambda))` is its simple head for an allowed restricted/regular label and is zero otherwise, after the declared conjugate-dual translation. | `def-schur-idempotent-and-schur-functor-for-type-a-q-schur-algebras`; `def-type-a-hecke-specht-module-and-cell-form`; `thm-simple-type-a-hecke-modules-are-indexed-by-e-regular-partitions`; `prop-p-regular-and-p-restricted-simple-labels-are-related-by-conjugate-sign-duality` | Apply the distinguished tableau idempotent to the semistandard Weyl basis, obtaining exactly the standard-tableau Specht basis; then pass the cell form to the head and use the nonvanishing label criterion. Print the conjugate-dual dictionary because Mathas states the result with e-restricted labels. | Mathas notes Proposition 4.19, printed pp. 51–52. Exact Schur-functor object dictionary used in the block transfer. |
| `def-alternating-weyl-and-specht-symbols-from-beta-sequences` (definition) | Fix length `d`. For an integral beta sequence `b=(b_1,...,b_d)`, set the virtual Weyl/Specht symbol to zero if two entries repeat or the sorted sequence does not recover a partition; otherwise let `w` sort `b` strictly decreasing, recover `alpha_i=b_(w(i))-d+i`, and set `[Delta^b]=(-1)^(ell(w))[Delta^alpha]` and `[S^b]=(-1)^(ell(w))[S^alpha]`. Define `d_mu(b)` by the same signed semistandard weight multiplicity. | `def-type-a-q-schur-algebra-and-weyl-modules`; `def-semistandard-tableau-and-kostka-number` | Define beta numbers here by `b_i=alpha_i+d-i`; verify adjacent interchange changes the determinant sign and repeated beta entries annihilate the alternant. For a strictly decreasing beta sequence the inverse formula recovers the unique partition. | Mathas notes Lemma 5.26 and conventions preceding Theorem 5.27, printed pp. 55–57. Exact notation needed by the determinant and q-Schaper formulas without a reverse page edge to the later abacus treatment. |
| `thm-q-schur-weyl-weight-space-gram-determinant-hook-product` (theorem) | Let `R` be an integral domain, `lambda` a partition with length at most `d`, `beta_i=lambda_i+d-i`, and `mu` a weight. Up to the explicit unit `q^N`, the Gram determinant on the `mu` weight space of `Delta(lambda)` is `product_((a,b),(a,c) in [lambda], b<c) ([h_(a,b)^lambda]_q/[h_(a,c)^lambda]_q)^(d_mu(beta+h_(a,b)^lambda epsilon_b-h_(a,c)^lambda epsilon_c))`, interpreted in `R` after cancellation. | `def-alternating-weyl-and-specht-symbols-from-beta-sequences`; `def-type-a-q-schur-algebra-and-weyl-modules`; `def-hook-arm-leg-and-hook-length` | Start from the seminormal weight basis and its norm ratios, group factors by restriction-tableau paths, telescope the branching products, then use Lemma 5.26 to convert each beta displacement to the signed semistandard exponent. Pair numerator/denominator factors before specialization so the displayed ratio represents an element of `R`. | Mathas notes Theorem 5.27 with Lemmas 5.22–5.26, printed pp. 52–57. Missing Gram determinant engine for q-Schaper. |
| `lem-semistandard-weight-dimensions-detect-virtual-weyl-classes` (lemma) | If a finite q-Schur module `M` and integers `a_lambda` satisfy `dim M e_mu=sum_lambda a_lambda dim Delta(lambda)e_mu` for every composition weight `mu`, then `[M]=sum_lambda a_lambda[Delta(lambda)]` in `G_0`. | `def-type-a-q-schur-algebra-and-weyl-modules`; `def-grothendieck-group-of-an-essentially-small-abelian-category`; `lem-kostka-change-of-basis-is-dominance-unitriangular` | Expand both sides in simple classes. The semistandard weight-dimension matrix is dominance unitriangular, so equality in every weight forces equality of all simple multiplicities by triangular inversion. | Mathas notes Lemma 5.30, printed p. 58. Exact passage from Gram valuations by weights to a virtual module identity. |
| `def-jantzen-filtration-of-a-lifted-cell-module` (definition) | Let `R` be a PID, `pi` a prime element, `F=R/pi R`, and `M` a finite free `R`-module with symmetric form nondegenerate over `Frac(R)`. Put `M(i)={x in M: <x,M> subset pi^iR}` and `M_F(i)=(M(i)+pi M)/pi M`; then `M_F=M_F(0) superset M_F(1) superset ...`, with first layer the radical. | `def-type-a-hecke-specht-module-and-cell-form`; `def-principal-ideal-domain` | Verify each `M(i)` is a submodule, reduction gives a descending finite filtration, and associativity of the cell form makes every reduced layer a module subquotient. Distinguish the prime element from its ideal and the generic fraction field from `F`; no valuation-ring hypothesis is silently substituted for the stated PID hypothesis. | Mathas notes Ch. 5 §2, printed pp. 54–55. Exact arithmetic setup for Schaper. |
| `lem-jantzen-layer-dimensions-equal-the-gram-determinant-valuation` (lemma) | Under the PID/free/nondegenerate hypotheses, the valuation of the Gram determinant equals the sum of the positive Jantzen-layer dimensions. | `def-jantzen-filtration-of-a-lifted-cell-module`; `thm-smith-normal-form-existence-over-a-pid` | Diagonalize the form by Smith normal form and count, basis vector by basis vector, the layers in which its valuation survives. | Agent 3 Lemma 5.29. Proof-sized arithmetic joint. |
| `thm-q-schaper-virtual-radical-layer-formula` (theorem) | Let `lambda` partition `n` with at most `d` parts, `beta_i=lambda_i+d-i`, and use the preceding PID setup with `q=hat q+pi R` and `[n]!_(hat q)!=0` in `R`. In `G_0`, `sum_(i>0)[S_F^lambda(i)]` equals `sum_((a,b),(a,c) in [lambda], b<c) (nu_pi([h^lambda_(a,b)]_(hat q))-nu_pi([h^lambda_(a,c)]_(hat q))) [S_F^(beta+h^lambda_(a,b)epsilon_b-h^lambda_(a,c)epsilon_c)]`, where every beta-sequence Specht symbol has the exact sorting-sign/repeated-entry interpretation above. | `lem-jantzen-layer-dimensions-equal-the-gram-determinant-valuation`; `thm-q-schur-weyl-weight-space-gram-determinant-hook-product`; `lem-semistandard-weight-dimensions-detect-virtual-weyl-classes`; `def-alternating-weyl-and-specht-symbols-from-beta-sequences`; `def-schur-idempotent-and-schur-functor-for-type-a-q-schur-algebras` | Smith normal form turns each Weyl weight-space Gram determinant valuation into the sum of positive Jantzen-layer dimensions. Insert the exact hook product, collect the beta-displacement terms, and use semistandard triangular detection to obtain Theorem 5.31 as a virtual Weyl identity. Apply the exact Schur idempotent to the filtration and identity for Corollary 5.32. | Mathas notes Theorem 5.31 and Corollary 5.32, printed pp. 58–60. Exact beta-number formula; it supplies constraints/upper bounds, not a general decomposition algorithm. |
| `rem-general-type-a-decomposition-numbers-remain-unsolved` (remark) | Dominance, Jantzen/Schaper constraints, and canonical-basis results in special coefficient regimes do not solve all symmetric-group decomposition numbers in arbitrary positive characteristic. | `cor-type-a-hecke-decomposition-matrix-is-dominance-unitriangular`; `thm-q-schaper-virtual-radical-layer-formula` | Compare exactly what each preceding theorem determines; record no proof claim. Provenance: statement `literature-derived`, proof `not-applicable`. | Agent 3 pre-Thm 5.40 discussion and closing audit. Required truth boundary. |

### Type-A Hecke Specht Modules and Cellular Bases — Examples (B dependency leaf; `type-a-hecke-specht-modules-and-cellular-bases-examples`)

| proposed id (kind) | exact computation/boundary | deps | verification route | source locator and purpose |
|---|---|---|---|---|
| `ex-murphy-cellular-basis-for-h3` (example) | List the cellular basis pieces for `(3),(2,1),(1^3)` and verify the two-tableau cell has four matrix-like basis elements. | `thm-murphy-tableau-elements-form-a-cellular-basis` | Write reduced words and multiply modulo higher cells. | Agent 3 Example 2.2(ii). Small cellular check. |
| `ex-hecke-jucys-murphy-elements-through-rank-four` (example) | Expand `L_1,...,L_4` in weighted transposition words and compute q-contents on one standard tableau. | `def-hecke-jucys-murphy-elements-and-q-contents`; `thm-hecke-jucys-murphy-action-is-dominance-triangular` | Direct formula and diagonal coefficient comparison. | Agent 3 Examples 3.23, 3.29. Normalization check. |
| `cex-residue-sequences-need-not-separate-when-e-is-at-most-n` (counterexample) | Exhibit distinct standard tableaux with the same residue sequence at finite `e<=n`, so a seminormal interpolation denominator vanishes. | `thm-separated-hecke-residues-give-seminormal-idempotents-and-bases` | Compute all residues modulo `e` and identify the collision. | Agent 3 Lemma 3.32 boundary; Agent 4 S3 separation condition. |
| `ex-q-schaper-for-shape-three-two-at-e-two` (example) | Work the source’s Jantzen calculation for `(3,2)` at `q=-1`, obtaining the eventual decomposition entry only after the extra dimension/filtration argument. | `thm-q-schaper-virtual-radical-layer-formula`; `cor-type-a-hecke-decomposition-matrix-is-dominance-unitriangular` | Compute quantum hook valuations, virtual layer sum, and the final composition-factor constraint. | Agent 3 Example 5.33. Shows formula alone first gives only a bound. |
| `cex-cellularity-does-not-make-every-cell-module-simple` (counterexample) | Give a degenerate cell form whose radical is nonzero or whole, showing the cell module can be reducible and its labelled head can vanish. | `thm-nonzero-cell-heads-classify-the-simple-modules-of-a-cellular-algebra` | Compute the Gram matrix at a small root of unity. | Agent 3 Chs. 2–3. Prevents ordinary-semisimple extrapolation. |

## SYMR-8 — Cores, quotients, and blocks of symmetric groups

### Cores, Quotients, and Blocks of Symmetric Groups (A page; `cores-quotients-and-blocks-of-symmetric-groups`)

| proposed id (kind) | exact claim and restrictions | deps | proof route / intermediate joints | source locator and purpose |
|---|---|---|---|---|
| `def-beta-set-and-e-runner-abacus-of-a-partition` (definition) | A sufficiently long beta-set for `lambda` places beads at the beta numbers on e runners; changing the auxiliary length applies the standard uniform shift and does not change the represented partition. | `def-partition-young-diagram-and-conjugate-partition` | Give the partition–beta inverse formulas and runner/residue convention. | Agent 5 Tomczak §4.1, pp. 53–64. Computational model. |
| `lem-hooks-are-bead-gap-pairs-on-an-abacus` (lemma) | Hooks of `lambda` correspond to bead-gap pairs `(b,g)` with hook length `b-g`; an e-divisible hook has bead and gap on the same runner. | `def-beta-set-and-e-runner-abacus-of-a-partition`; `def-hook-arm-leg-and-hook-length` | Trace the southeast boundary word, where beads/gaps encode vertical/horizontal steps; the interval length is hook length. | Agent 5 Tomczak Lemmas 4.1–4.9. Key diagram/abacus bridge. |
| `def-e-core-e-quotient-and-e-weight` (definition) | Sliding every bead to the top of its runner defines the e-core; normalized bead positions on each runner define the e-quotient `(lambda^(0),...,lambda^(e-1))`; weight is the total quotient size. | `def-beta-set-and-e-runner-abacus-of-a-partition`; `lem-hooks-are-bead-gap-pairs-on-an-abacus` | State normalization independence and reserve proof for following theorems. | Agent 5 Tomczak §4.1. Combinatorics valid for every `e>=2`. |
| `thm-e-core-is-independent-of-the-rim-hook-removal-order` (theorem) | Repeated removal of rim e-hooks terminates at a unique e-core, equal to the top-packed abacus, independent of choices. | `def-e-core-e-quotient-and-e-weight`; `def-removable-border-strip-and-height` | Rim e-hook removal is one upward bead move; the terminal top-packed position on each runner is unique. | Agent 5 Tomczak Thm 4.10; Agent 3 Lemma 5.34 independently checks. |
| `thm-e-quotient-controls-hooks-divisible-by-e` (theorem) | Hooks of `lambda` with e-divisible length correspond to hooks in its e-quotient, dividing lengths by e and respecting the quotient after removal. | `def-e-core-e-quotient-and-e-weight`; `lem-hooks-are-bead-gap-pairs-on-an-abacus` | Same-runner bead-gap pairs rescale to bead-gap pairs of the runner partition. | Agent 5 Tomczak Thm 4.11. Supplies quotient recursion. |
| `prop-core-quotient-size-and-weight-formula` (proposition) | `|lambda|=|core_e(lambda)|+e sum_i|lambda^(i)|`, so the weight equals the number of successive e-hook removals. | `thm-e-core-is-independent-of-the-rim-hook-removal-order`; `thm-e-quotient-controls-hooks-divisible-by-e` | Each upward bead step removes e nodes; induct to the core and compare runner quotient sizes. | Agent 5 Tomczak Prop. 4.12. Exact numerical invariant. |
| `lem-virtual-weyl-combinations-detect-q-schur-block-separation` (lemma) | Let all composition factors of an `S_{F,q}(d,n)`-module `M` belong to one block and suppose `[M]=sum_lambda a_lambda[Delta(lambda)]` in `G_0`. Then every `Delta(lambda)` with `a_lambda!=0` belongs to the block of `M`. | `def-type-a-q-schur-algebra-and-weyl-modules`; `def-blocks-of-a-finite-dimensional-algebra-by-primitive-central-idempotents`; `def-grothendieck-group-of-an-essentially-small-abelian-category` | Project the equality by the sum of all block idempotents other than the block of `M`. Linear independence of Weyl classes—proved from their dominance-unitriangular simple multiplicities—forces every surviving coefficient to vanish. | Mathas notes Lemmas 5.30 and 5.35, printed pp. 57, 60. The exact block-idempotent joint used with each Jantzen sum. |
| `thm-q-schur-weyl-modules-are-in-the-same-block-iff-their-labels-have-the-same-e-core` (theorem) | Let `F` be a field, `d>=n`, and `e` the quantum characteristic in the q-Schaper modular-system setup. Two q-Schur Weyl modules `Delta(lambda),Delta(mu)` lie in the same block iff `lambda` and `mu` have the same e-core. | `lem-virtual-weyl-combinations-detect-q-schur-block-separation`; `thm-e-core-is-independent-of-the-rim-hook-removal-order`; `thm-q-schaper-virtual-radical-layer-formula` | Necessity: every non-head composition factor exposed by the Jantzen formula comes from unwrapping a rim hook and rewrapping it lower; a nonzero quantum-hook valuation forces an e-divisible moved hook, so the core is preserved, and block linkage is transitive. Sufficiency: fix the partition obtained from the common core by adjoining all `we` boxes to the first row; repeatedly move an e-rim hook from a lower row to the first row. The target Weyl module occurs with nonzero coefficient in the source Jantzen sum, so the block lemma links each step; induct to the fixed partition. | Mathas notes Lemmas 5.34–5.35 and Theorem 5.36 with its full proof, printed pp. 60–62. Exact q-Schur block proof, with both directions exposed. |
| `cor-type-a-hecke-specht-modules-are-in-the-same-block-iff-their-labels-have-the-same-e-core` (corollary) | Under the same field/parameter hypotheses, two type-A Hecke Specht modules belong to the same block iff their partitions have the same e-core. | `thm-q-schur-weyl-modules-are-in-the-same-block-iff-their-labels-have-the-same-e-core`; `thm-schur-functor-sends-weyl-and-simple-modules-to-specht-and-hecke-simple-modules` | The Schur functor and q-Schur theorem give the only-if direction. For the converse decompose the Hecke algebra into blocks `H=direct-sum B_i`; because the defining q-permutation sum `M` contains the regular module, every `MB_i` is nonzero, distinct summands have zero cross-Hom, and `End_H(M)=product_i End_{B_i}(MB_i)`. Hence q-Schur has at least as many blocks as Hecke, so labels in one q-Schur/e-core block cannot split across Hecke blocks. | Mathas notes Corollary 5.37 and full proof, printed pp. 62–63. Exact endomorphism-algebra block-count argument. |
| `thm-nakayama-block-classification-for-symmetric-groups` (theorem) | Over a splitting field `k` of characteristic prime `p`, two Specht modules for `k[S_n]` belong to the same block iff their partitions have the same p-core. Hence blocks are indexed by pairs `(gamma,w)` with `gamma` a p-core and `n=|gamma|+pw`. | `def-e-core-e-quotient-and-e-weight`; `def-p-blocks-by-primitive-central-idempotents`; `prop-hecke-specht-theory-specializes-to-group-specht-theory-at-q-one`; `cor-type-a-hecke-specht-modules-are-in-the-same-block-iff-their-labels-have-the-same-e-core` | Specialize the integral Hecke/cellular/q-Schur construction at `q=1`; its quantum characteristic is exactly `p`, and the specialized Murphy cell is the group Specht module. Apply the proved Hecke e-core block theorem, then use the core/weight size formula for the block index. This q-Schur/Jantzen route avoids importing the separate Young-module/Brauer-quotient proof. | Mathas notes Corollary 5.37, printed p. 61, specialized using Chs. 1, 3–5. O’Donovan Thm. 1 independently confirms the result by the Young–Brauer route. |
| `prop-weight-zero-symmetric-group-blocks-are-defect-zero` (proposition) | If the block core has weight zero, that block contains one ordinary Specht/irreducible label, its decomposition matrix and Cartan matrix are `(1)`, its unique simple module is projective, and the block is defect zero under the fixed splitting modular system. | `thm-nakayama-block-classification-for-symmetric-groups`; `thm-symmetric-group-decomposition-matrix-is-dominance-unitriangular`; `thm-cartan-matrix-is-d-transpose-d`; `thm-defect-zero-blocks-are-simple-algebras` | Nakayama and the size formula leave only the core partition in the block. Unitriangularity makes its decomposition row `(1)`; `C=D^T D` makes the unique projective indecomposable have that simple as its sole factor, so the block is simple/projective, and the inherited defect-zero equivalence gives the conclusion. | Mathas notes after Cor. 5.37, printed p. 61; GT-19 Cartan identity and RG-14 defect-zero equivalence close the two non-combinatorial joints. |
| `thm-weight-one-block-decomposition-pattern` (theorem) | Let `gamma` be an e-core of `m` and `n=m+e`. Exactly `e` partitions of `n` have core `gamma`; order them `lambda_1 strictly dominates ... strictly dominates lambda_e` in Mathas's e-restricted q-Schur convention. Then `lambda_1` is not e-restricted, `lambda_2,...,lambda_e` are, `Delta(lambda_e)=L(lambda_e)`, and `[Delta(lambda_j)]=[L(lambda_j)]+[L(lambda_(j+1))]` for `1<=j<e`. Under the Schur functor, `[S^(lambda_1)]=[D^(lambda_2)]`, `[S^(lambda_j)]=[D^(lambda_j)]+[D^(lambda_(j+1))]` for `2<=j<e`, and `[S^(lambda_e)]=[D^(lambda_e)]`; at `q=1,e=p`, conjugate/sign translate this displayed matrix to the p-regular James convention. | `def-e-core-e-quotient-and-e-weight`; `thm-q-schaper-virtual-radical-layer-formula`; `thm-schur-functor-sends-weyl-and-simple-modules-to-specht-and-hecke-simple-modules`; `prop-p-regular-and-p-restricted-simple-labels-are-related-by-conjugate-sign-duality` | List the one-bead moves in dominance order. Exact q-hook valuations in the Jantzen sum force the adjacent two-factor Weyl pattern by downward induction; Proposition 4.19 sends Weyls and allowed simples to the displayed Hecke classes and kills the non-e-restricted simple `L(lambda_1)`. | Mathas notes Proposition 5.38 and proof, printed pp. 61–63. A solved weight-one island with row/column conventions explicit, not a general algorithm. |
| `rem-block-weight-is-not-the-same-as-numerical-defect` (remark) | Weight counts removed p-hooks; numerical defect is a p-adic group/order invariant and must not be identified with weight without a theorem and range. | `prop-core-quotient-size-and-weight-formula`; `def-defect-group-and-numerical-defect-of-a-block` | No proof section; record the distinction and the reported Sylow-`S_{pw}` theorem only after a counted authoritative proof source is added. Provenance `literature-derived/not-applicable`. | Agent 5 defect cross-check and boundary audit. Prevents a common false equality. |

### Cores, Quotients, and Blocks of Symmetric Groups — Examples (B dependency leaf; `cores-quotients-and-blocks-of-symmetric-groups-examples`)

| proposed id (kind) | exact computation/boundary | deps | verification route | source locator and purpose |
|---|---|---|---|---|
| `ex-three-core-and-three-quotient-by-abacus` (example) | Compute the source’s e=3 core and full three-component quotient for a nontrivial partition, verifying the size formula. | `thm-e-core-is-independent-of-the-rim-hook-removal-order`; `thm-e-quotient-controls-hooks-divisible-by-e`; `prop-core-quotient-size-and-weight-formula` | Draw all runners, top-pack beads, normalize each runner, and total quotient sizes. | Agent 5 Tomczak §4.1 full example; PDF layout must be followed. |
| `ex-three-core-of-four-three-two-one` (example) | Reduce `(4,3,2,1)` to 3-core `(1)` by bead moves and by rim-hook removals. | `thm-e-core-is-independent-of-the-rim-hook-removal-order` | Exhibit two removal orders with the same terminal core. | Agent 3 p. 61 example. Choice-independence check. |
| `ex-a-weight-one-block-decomposition-matrix` (example) | Write the complete adjacent decomposition matrix for one small weight-one p-block. | `thm-weight-one-block-decomposition-pattern` | List its bead moves in dominance order and apply the bidiagonal formula. | Agent 3 Prop. 5.38. Concrete solved case. |
| `cex-the-same-number-of-removable-p-hooks-does-not-replace-the-core-test` (counterexample) | Give partitions with misleading local removable-hook counts but different top-packed p-cores. | `thm-e-core-is-independent-of-the-rim-hook-removal-order` | Compute both full abaci; compare terminal configurations. | Derived finite check from Tomczak §4.1; statement provenance `ai-generated`, proof `ai-generated`, generation role `counterexample`, and it may not be a dependency target. |

## SYMR-9 — Modular branching and the good-node crystal

The normal-node multiplicities use Kleshchev's full first-level branching
calculation together with Brundan's explicit q-Schur weight-space comparison.
The affine-crystal identification itself remains in SYMR-10, so the modular
branching proof does not depend circularly on Ariki categorification.

### Modular Branching and the Good-Node Crystal (A page; `modular-branching-and-the-good-node-crystal`)

| proposed id (kind) | exact claim and restrictions | deps | proof route / intermediate joints | source locator and purpose |
|---|---|---|---|---|
| `def-residue-signature-normal-good-conormal-and-cogood-nodes` (definition) | For a p-restricted partition and residue `i`, list addable/removable i-nodes top to bottom as `+/-`, cancel each `-+` pair repeatedly; remaining `-` nodes are normal and the leftmost is good, remaining `+` nodes are conormal and the rightmost is cogood. | `def-p-regular-and-p-restricted-partitions`; `def-removable-and-addable-nodes-of-a-partition` | Fix residue `c-r mod p`, order, cancellation, and empty-signature conventions; explicitly note that this residue is the negative of Brundan's convention. | Kleshchev §§3.4,11.4 and Brundan §2; local researcher 5 convention audit. Definitions driving exact branching. |
| `def-generalized-jm-primary-idempotents-and-residue-summand-functors` (definition) | Over a field of characteristic `p>0`, the commuting integral JM elements have simultaneous generalized eigenvalues in `F_p`. Iterated Chinese-remainder projections give orthogonal idempotents `e(i_1,...,i_n)` summing to one; sums by content are block idempotents, and sums with fixed last residue define exact functors `E_i,F_i` with `Res=direct-sum_i E_i` and `Ind=direct-sum_i F_i`. | `def-jucys-murphy-elements-of-the-symmetric-group-algebra`; `def-restriction-induction-and-coinduction-for-group-modules`; `def-quantum-characteristic-of-an-invertible-hecke-parameter`; `thm-chinese-remainder-theorem-for-comaximal-ideals` | In the finite commutative algebra generated by the JM elements, factor each minimal polynomial into powers of `X-i`; Bézout/CRT produces the functorial primary idempotents. Direct-summand projection proves exactness; the tower embedding identifies the fixed-last-residue restriction and induction summands. | Kleshchev, *Representation Theory of Symmetric Groups and Related Hecke Algebras*, §§2.3–2.4, (2.6)–(2.8), and §2.9, (2.41), complete arXiv:0909.4844; local researcher 5 CRT audit. Valid also at `p=2`. |
| `thm-kleshchev-first-level-branching-multiplicity` (theorem) | For a restricted highest weight, the multiplicity of the first-level simple obtained by removing row-node `A` is zero unless `A` is normal and otherwise is one plus the number of normal same-residue removable nodes above `A`, in Kleshchev's p-regular/source convention. | `def-residue-signature-normal-good-conormal-and-cogood-nodes`; `def-highest-weight-vector-and-cyclic-highest-weight-module`; `def-weight-and-weight-space-of-a-lie-algebra-representation` | Construct the lowering-operator/PBW bases in §§§2–8, prove the two spanning/independence theorems, and apply Theorem 9.3's explicit `sigma(i,l)` count. Keep the p-regular row order until the later transpose/sign conversion. | Kleshchev, *On decomposition numbers and branching coefficients for symmetric and special linear groups* (1997), §§§2–9, Thms. 8.6,8.8,9.3 and Thm. 1.4, complete author manuscript. Full primary proof route, not a citation to Kleshchev I. |
| `prop-schur-functor-compares-first-level-branching-with-hecke-restriction` (proposition) | For stable rank `d>=n`, the `(1^n)` q-Schur weight idempotent identifies the first-level summand of a polynomial simple with restriction of its Hecke simple, and exactness preserves the selected composition multiplicity. | `thm-kleshchev-first-level-branching-multiplicity`; `def-type-a-q-schur-algebra-and-weyl-modules`; `def-schur-idempotent-and-schur-functor-for-type-a-q-schur-algebras`; `thm-schur-functor-sends-weyl-and-simple-modules-to-specht-and-hecke-simple-modules` | In Brundan's left convention, delete the final coordinate from the `(1^n)` weight inside the level-one summand and check that `T_1,...,T_(n-2)` act unchanged; pass to the fixed right-module convention by opposites. Lemmas 5.6–5.7 identify surviving simples, and exactness transfers composition multiplicities. | Brundan (1998) §5.5–5.7, pp. 31–32, including the displayed first-level equality; local researcher 5 supplies the explicit weight-space proof. Complete replacement for Kleshchev I §2.12(v). |
| `thm-normal-node-composition-multiplicities-under-residue-restriction` (theorem) | For p-restricted `mu` and `nu=mu-A`, `[E_iD(mu):D(nu)]` is nonzero exactly when `A` is normal, and then equals the number of normal i-nodes weakly below `A`; the corresponding Specht-to-restriction Hom criterion remains a distinct statement. | `def-generalized-jm-primary-idempotents-and-residue-summand-functors`; `prop-schur-functor-compares-first-level-branching-with-hecke-restriction`; `prop-p-regular-and-p-restricted-simple-labels-are-related-by-conjugate-sign-duality` | Transfer Kleshchev's first-level count through the exact Schur functor, project to the residue-primary summand, and transpose/sign-twist: above becomes below and residues negate. The construction and conversion remain valid at `p=2`. | Kleshchev 1997 Thm. 1.4 with §§§2–9 and §11 Prop. 11.2 replaced by Brundan's proof-bearing comparison; Kleshchev II Thm. 0.4 separately supplies the Hom criterion. |
| `lem-finite-group-induction-is-coinduction-and-thus-biadjoint` (lemma) | For every finite inclusion `H<=G` over every field, `Ind_H^G` is naturally isomorphic to `Coind_H^G`; hence induction is both left and right adjoint to restriction and is exact. | `def-restriction-induction-and-coinduction-for-group-modules`; `lem-the-group-ring-is-free-over-a-subgroup-ring`; `thm-induction-is-left-adjoint-and-coinduction-right-adjoint-to-restriction` | Define `Phi(g tensor m)(x)=(xg)m` when `xg in H` and zero otherwise; verify balancing and `G`-linearity. A finite left-coset transversal gives inverse `phi maps to sum_r r^(-1) tensor phi(r)`. Freeness proves exactness. | Standard relative Frobenius isomorphism, written out completely; local researcher 5 audited the handedness against the published induction/coinduction definitions. |
| `thm-conormal-node-composition-multiplicities-under-residue-induction` (theorem) | For p-restricted `mu` and `nu=mu+B`, `[F_iD(mu):D(nu)]` is nonzero exactly when `B` is conormal, with multiplicity the number of conormal i-nodes weakly above `B`. | `def-generalized-jm-primary-idempotents-and-residue-summand-functors`; `thm-normal-node-composition-multiplicities-under-residue-restriction`; `lem-finite-group-induction-is-coinduction-and-thus-biadjoint`; `prop-p-regular-and-p-restricted-simple-labels-are-related-by-conjugate-sign-duality` | Biadjunction identifies the relevant induction multiplicity with the restriction multiplicity after contragredient duality. Apply the normal formula, then transpose/sign/residue conversion turns normal-below into conormal-above. | Kleshchev 1997 Thm. 1.4 plus the explicit finite-group biadjunction. No complex-character reciprocity is used. |
| `thm-good-node-gives-the-simple-socle-and-head-of-residue-restriction` (theorem) | If `epsilon_i(mu)>0`, `E_iD(mu)` has simple socle and head labelled by deleting the good i-node; this simple occurs with total ungraded multiplicity `epsilon_i(mu)`. | `thm-normal-node-composition-multiplicities-under-residue-restriction`; `def-generalized-jm-primary-idempotents-and-residue-summand-functors`; `def-module-radical-socle-head-and-loewy-series` | Kleshchev II Thm. 0.5 gives the multiplicity-free uncoloured socle and equal head. Residue-primary projection retains the unique good node of residue `i`; the normal multiplicity theorem supplies the total `epsilon_i`. | Kleshchev II Thm. 0.5, pp. 165–166, full GDZ scan; Kleshchev 1997 Thm. 1.4 for total multiplicity. Distinguishes Loewy endpoints from the full composition series. |
| `thm-cogood-node-gives-the-simple-socle-and-head-of-residue-induction` (theorem) | If `phi_i(mu)>0`, `F_iD(mu)` has simple socle and head labelled by adding the cogood i-node, with total ungraded multiplicity `phi_i(mu)`. | `thm-conormal-node-composition-multiplicities-under-residue-induction`; `thm-good-node-gives-the-simple-socle-and-head-of-residue-restriction`; `lem-finite-group-induction-is-coinduction-and-thus-biadjoint` | Apply the same biadjunction and contragredient-duality argument to the good-node head/socle theorem and use the conormal multiplicity formula. | Dual of the fully proved restriction theorem; Kleshchev's induction formulation checks the endpoint convention. |
| `cor-socle-of-uncoloured-modular-restriction-is-the-sum-over-good-residues` (corollary) | The socle of `Res D(mu)` is the multiplicity-free direct sum over the distinct good-residue deletions, not over all normal nodes. | `def-generalized-jm-primary-idempotents-and-residue-summand-functors`; `thm-good-node-gives-the-simple-socle-and-head-of-residue-restriction` | Sum the exact residue-primary summands; Kleshchev II Thm. 0.5 says precisely that their simple socles form the full uncoloured socle. | Kleshchev II Thm. 0.5, p. 166. Sharp contrast with ordinary branching. |
| `def-good-node-and-cogood-node-crystal-operators` (definition) | Define partial operators `tilde e_i,tilde f_i` on p-restricted partitions by good deletion and cogood addition, with `epsilon_i,phi_i` the reduced-signature lengths. | `def-residue-signature-normal-good-conormal-and-cogood-nodes` | Pure combinatorial definition; no quantum theorem yet. | Agent 5 Kleshchev §§3.4,11.4. Interface to SYMR-10. |
| `prop-every-p-restricted-partition-is-reached-by-cogood-node-additions` (proposition) | Starting from the empty partition, successive cogood additions reach every p-restricted partition; reversing gives good deletions to empty. | `def-good-node-and-cogood-node-crystal-operators`; `thm-modular-simple-modules-of-sn-are-the-p-regular-specht-heads`; `prop-p-regular-and-p-restricted-simple-labels-are-related-by-conjugate-sign-duality` | Induct via nonzero restrictions of nontrivial simples and the simple-socle theorem; translate the label convention. | Agent 5 Kleshchev Thm 11.2/§11.4 (crystal-level statement); local proof can use branching plus induction. Connectivity of the modular branching graph. |
| `rem-the-crystal-identification-does-not-elementarily-identify-cellular-simple-labels` (remark) | The inductive crystal labels and cellular radical-quotient labels agree by nontrivial Ariki/graded theory; the agreement is not assumed in the proof of the elementary radical quotient classification. | `def-good-node-and-cogood-node-crystal-operators`; `prop-p-regular-and-p-restricted-simple-labels-are-related-by-conjugate-sign-duality` | No proof section; point forward to SYMR-10/SYMR-12. Provenance `literature-derived/not-applicable`. | Agent 5 Kleshchev §11.2 boundary. Prevents a circular classification proof. |

### Modular Branching and the Good-Node Crystal — Examples (B dependency leaf; `modular-branching-and-the-good-node-crystal-examples`)

| proposed id (kind) | exact computation/boundary | deps | verification route | source locator and purpose |
|---|---|---|---|---|
| `ex-normal-and-good-nodes-for-a-large-partition` (example) | Work Brundan’s `lambda=(6,4,4,3,2)` signature for the stated quantum characteristics and identify all normal/good nodes. | `def-residue-signature-normal-good-conormal-and-cogood-nodes` | Draw residues, write and cancel each signature in the declared order. | Agent 5 Brundan Example 2.8. Full convention stress test. |
| `ex-good-node-path-from-empty-to-a-p-restricted-partition` (example) | Label every edge in one full cogood-addition path and reverse it by good deletions. | `prop-every-p-restricted-partition-is-reached-by-cogood-node-additions` | Compute residues/reduced signatures at every rank. | Agent 5 Kleshchev §3.4. Prepares the Fock crystal. |

## SYMR-10 — Level-one Fock space and residue crystals

The quantum-affine action, semi-infinite wedge, crystal and global-basis proofs
belong to the prerequisite page `quantum-affine-type-a-and-level-one-fock-space`.
This page performs only the symmetric-group convention reconciliation. The
parameter `v` is the quantum/grading variable; the Hecke parameter remains
`xi` and is never conflated with it.

### Level-One Fock Space and Residue Crystals (A page; `level-one-fock-space-and-residue-crystals`)

| proposed id (kind) | exact claim and restrictions | deps | proof route / intermediate joints | source locator and purpose |
|---|---|---|---|---|
| `def-symmetric-group-level-one-residue-and-partition-label-convention` (definition) | Specialize the QG-6 level-one charge to `Lambda_0`, use English nodes with residue `c-r mod e`, and record the transpose/sign conversion between e-restricted and e-regular simple labels, including residue negation. | `def-level-one-partition-fock-space-and-node-residues`; `def-residue-signature-normal-good-conormal-and-cogood-nodes`; `prop-p-regular-and-p-restricted-simple-labels-are-related-by-conjugate-sign-duality` | Compare the two node orders and cancellation directions term by term. Under transpose, above and below reverse and `c-r` changes sign; sign twist gives the declared simple-label convention. | QG-6 convention supplier; local researchers 5 and 6 convention audits. Prevents a silent e-regular/e-restricted mismatch. |
| `prop-qg-fock-crystal-arrows-agree-with-the-modular-good-node-operators` (proposition) | In the preceding convention, the Kashiwara arrows on the level-one partition Fock crystal are exactly SYMR-9 good-node deletion and cogood-node addition. | `def-symmetric-group-level-one-residue-and-partition-label-convention`; `def-good-node-and-cogood-node-crystal-operators`; `thm-the-crystal-of-level-one-fock-space-is-the-good-node-partition-graph` | Match the reduced signature used for the leading `v=0` term with the modular residue signature, including cancellation direction and charge zero. | QG-6 crystal theorem and Kleshchev signature convention. Exact quantum/modular seam. |
| `thm-restricted-partition-component-is-the-basic-crystal-in-the-symmetric-group-convention` (theorem) | For finite `e`, the empty component consists exactly of e-restricted partitions and is `B(Lambda_0)`; after transpose/sign it labels the e-regular modular simples. For `e=0`, all partitions occur. | `prop-qg-fock-crystal-arrows-agree-with-the-modular-good-node-operators`; `thm-the-empty-component-is-the-highest-weight-crystal-b-lambda-zero`; `prop-every-p-restricted-partition-is-reached-by-cogood-node-additions` | QG-6 proves the highest-weight component theorem; the local convention bridge identifies its vertices and arrows with the independently constructed modular branching graph. | QG-6 empty-component theorem; local researcher 6 §§ on Fock/crystal conventions. |
| `def-level-one-global-basis-transition-polynomials-in-the-symmetric-group-convention` (definition) | Let `d_(lambda,mu)(v)` be the coefficient of the standard partition vector `M_mu` in the lower global-basis vector indexed by the e-restricted crystal label `lambda`, using QG-6's bar, dominance and unitriangular normalization; define the dual coefficients by the perfect pairing, not by reversing names. | `def-symmetric-group-level-one-residue-and-partition-label-convention`; `thm-the-basic-representation-global-basis-is-unitriangular-in-the-partition-basis`; `thm-restricted-partition-component-is-the-basic-crystal-in-the-symmetric-group-convention` | Restrict the QG-6 global basis to the charge-zero empty component and rewrite its labels in the fixed restricted convention. The existence/uniqueness proof remains QG-owned. | QG-6 global-basis theorem; local researcher 6 basis-orientation audit. Exact coefficient convention consumed by SYMR-12. |

### Level-One Fock Space and Residue Crystals — Examples (B dependency leaf; `level-one-fock-space-and-residue-crystals-examples`)

| proposed id (kind) | exact computation/boundary | deps | verification route | source locator and purpose |
|---|---|---|---|---|
| `ex-level-one-charge-zero-crystal-through-rank-four-at-e-three` (example) | Draw every e=3 charge-zero crystal vertex/arrow in the empty component through size four. | `thm-restricted-partition-component-is-the-basic-crystal-in-the-symmetric-group-convention` | Compute residues and reduced signatures at each vertex. | QG-6 plus local researchers 5–6. Full convention check. |
| `ex-fock-e-and-f-actions-on-shape-two-one-at-e-three` (example) | Compute every coefficient/exponent in `E_iM_(2,1)` and `F_iM_(2,1)` for all residues. | `thm-the-fock-space-node-formulas-satisfy-the-quantum-affine-relations`; `def-symmetric-group-level-one-residue-and-partition-label-convention` | List nodes above/below and evaluate v powers. | QG-6 node formulas. Verifies sign/exponent conventions without duplicating their proof. |
| `cex-the-full-fock-space-is-not-automatically-the-irreducible-basic-module` (counterexample) | At finite e, exhibit a partition outside the empty component, showing the whole Fock representation need not equal its highest-weight basic submodule. | `thm-the-empty-component-is-the-highest-weight-crystal-b-lambda-zero` | Compute its signature component and show no path from empty under the source crystal convention. | QG-6 boundary. Prevents whole-Fock/basic-module confusion. |

## SYMR-11 — Cyclotomic Hecke and quiver Hecke algebras

### Cyclotomic Hecke and Quiver Hecke Algebras (A page; `cyclotomic-hecke-and-quiver-hecke-algebras`)

| proposed id (kind) | exact claim and restrictions | deps | proof route / intermediate joints | source locator and purpose |
|---|---|---|---|---|
| `def-affine-and-cyclotomic-type-a-hecke-algebras` (definition) | Define separate degenerate (`xi=1`) and nondegenerate (`xi!=1`) affine Hecke algebras and the cyclotomic quotient `H_d^Lambda` by the exact polynomial in the first polynomial/JM generator. | `def-generic-type-a-hecke-algebra`; `def-quantum-characteristic-of-an-invertible-hecke-parameter` | State level, dominant weight, residue set and quotient polynomial; at `Lambda_0`, identify the symmetric-group/finite-Hecke specialization. | Agent 6 S1 intro/§2.2 and S2 §4.1. Exact object before KLR bridge. |
| `def-residue-sequence-idempotents-and-cyclotomic-hecke-blocks` (definition) | Generalized eigenvalue spaces of the commuting polynomial/JM generators define orthogonal idempotents `e(i)`; summing over sequences of content `alpha` gives the nonzero block idempotent `e_alpha`. | `def-affine-and-cyclotomic-type-a-hecke-algebras`; `thm-hecke-jucys-murphy-action-is-dominance-triangular`; `def-p-blocks-by-primitive-central-idempotents` | Use simultaneous primary decomposition over the algebraically closed field and symmetric central polynomials to show block centrality. | Agent 6 S1 pp. 1–7 and S2 §4.1. Residue-block refinement. |
| `def-type-a-cyclotomic-specialization-of-the-qg-klr-algebras` (definition) | Specialize QG-7's symmetrizable KLR datum to the cyclic type-A residue quiver fixed by the Hecke quantum characteristic, including the double-edge `e=2` polynomials, and specialize QG-8's cyclotomic quotient to the dominant weight `Lambda`; write the resulting summands as `R_alpha` and `R_alpha^Lambda`. | `def-quiver-hecke-klr-algebra-for-a-symmetrizable-cartan-datum`; `def-cyclotomic-klr-quotient-at-a-dominant-weight`; `def-cyclic-affine-type-a-cartan-datum`; `def-quantum-characteristic-of-an-invertible-hecke-parameter` | Substitute the type-A Cartan entries and the fixed `Q_ij`; display the resulting quadratic/corrected-braid cases and the exponent `(Lambda,alpha_(i_1))`. The PBW, grading and finite-dimensionality proofs remain QG-owned. | QG-7/QG-8 specialization seam; Brundan--Kleshchev §§3.2,4.2 fixes the exact type-A normalization. |
| `lem-hecke-intertwiners-produce-local-klr-generators` (lemma) | Inside each Hecke block, explicit intertwiners and local power-series coordinates satisfy the specialized KLR mixed, quadratic and corrected-braid relations, separately in degenerate and nondegenerate cases. | `def-affine-and-cyclotomic-type-a-hecke-algebras`; `def-residue-sequence-idempotents-and-cyclotomic-hecke-blocks`; `def-type-a-cyclotomic-specialization-of-the-qg-klr-algebras` | Build `y` from the polynomial/JM generator near its generalized eigenvalue; normalize intertwiners by invertible power series; use local nilpotence to interpret them finitely and check rank-two identities case by case. | Brundan--Kleshchev §§3.1–3.5,4.1–4.5. Hard Hecke/KLR bridge joint. |
| `thm-brundan-kleshchev-blockwise-hecke-klr-isomorphism` (theorem) | Over an algebraically closed field, with the source's parameter and quiver conventions, each nonzero cyclotomic Hecke block `H_alpha^Lambda` is isomorphic to the specialized QG-8 algebra `R_alpha^Lambda`; the degenerate and nondegenerate formulas are stated separately. | `lem-hecke-intertwiners-produce-local-klr-generators`; `thm-klr-pbw-basis-theorem`; `def-residue-sequence-idempotents-and-cyclotomic-hecke-blocks` | Construct Hecke-to-KLR generators and inverse Hecke generators within the finite block; local nilpotence makes every power series finite; verify all mutual-inverse generator identities. QG-7's PBW theorem supplies the KLR structural basis used in the comparison. | Brundan--Kleshchev Main Theorem, complete pp. 1–29; local researcher 6 independently fixed normalized formulas. Landmark bridge, with generic KLR structure inherited from QG-7. |
| `cor-cyclotomic-hecke-blocks-inherit-a-nontrivial-z-grading` (corollary) | Transporting QG-7/QG-8's KLR grading makes cyclotomic Hecke blocks graded; this grading is not visible from the ungraded RG-13 presentation and depends on the chosen bridge conventions up to graded isomorphism. | `thm-brundan-kleshchev-blockwise-hecke-klr-isomorphism`; `def-klr-grading-and-diagrammatic-composition`; `def-cyclotomic-klr-quotient-at-a-dominant-weight` | Pull back homogeneous pieces and multiplication across the block isomorphism. | Brundan--Kleshchev Main Theorem plus QG-7/QG-8 grading suppliers. Enables graded decomposition numbers. |

### Cyclotomic Hecke and Quiver Hecke Algebras — Examples (B dependency leaf; `cyclotomic-hecke-and-quiver-hecke-algebras-examples`)

| proposed id (kind) | exact computation/boundary | deps | verification route | source locator and purpose |
|---|---|---|---|---|
| `ex-level-one-rank-two-cyclotomic-klr-algebra-at-e-three` (example) | List surviving residue sequences, cyclotomic `y_1` relations, and every `psi_1^2e(i)` case for `d=2,e=3,Lambda_0`. | `def-type-a-cyclotomic-specialization-of-the-qg-klr-algebras` | Enumerate sequences and apply the specialized quiver cases directly. | Brundan--Kleshchev relations and QG-8 quotient. Small full presentation check. |
| `ex-degenerate-level-one-hecke-klr-map-for-s3` (example) | At `xi=1`, write the block idempotents and the images of `y_r,psi_r` for one `S_3` block, then verify one mixed and one quadratic relation. | `thm-brundan-kleshchev-blockwise-hecke-klr-isomorphism` | Evaluate finite local power series on the nilpotent block. | Agent 6 S1 §3 and §5. Makes “blockwise” concrete. |
| `cex-the-whole-affine-hecke-algebra-is-not-one-cyclotomic-klr-block` (counterexample) | Show why omitting both cyclotomic quotient and residue-content idempotent gives objects larger than the theorem’s isomorphic pair. | `thm-brundan-kleshchev-blockwise-hecke-klr-isomorphism` | Compare central characters/blocks and quotient relations. | Agent 6 S1 Main Theorem boundary. Prevents overstatement. |
| `ex-homogeneous-degree-check-for-the-corrected-klr-braid-relation` (example) | Verify both sides and correction term have the same degree in each adjacent-residue case, including e=2. | `def-type-a-cyclotomic-specialization-of-the-qg-klr-algebras`; `def-klr-grading-and-diagrammatic-composition` | Substitute the Cartan entries and polynomial degrees. | QG-7 grading plus Brundan--Kleshchev specialization. Grading sanity check. |

## SYMR-12 — Ariki categorification and graded decomposition numbers

The nondegenerate characteristic-zero theorem is proved through the modern
direct route: QG-8 identifies self-dual cyclotomic KLR projectives and simples
with the lower and upper global bases, and SYMR-11 transports that statement
across the blockwise Hecke--KLR isomorphism. This avoids importing Ariki's
original geometric proof as an unstated black box. The distinct degenerate
category-O route belongs to `the-degenerate-ariki-categorification-theorem` and
does not license the nondegenerate parameter regime.

### Ariki Categorification and Graded Decomposition Numbers (A page; `ariki-categorification-and-graded-decomposition-numbers`)

| proposed id (kind) | exact claim and restrictions | deps | proof route / intermediate joints | source locator and purpose |
|---|---|---|---|---|
| `def-graded-module-and-projective-grothendieck-groups-for-klr-towers` (definition) | Take the direct sums over all relevant blocks/ranks of HA-19's short-exact finite-module `G_0` and split finitely-generated-projective `K_0`, with `[M<r>]=v^r[M]`; do not identify the two groups. | `def-graded-grothendieck-group-shift-module-and-cartan-map`; `thm-graded-projective-and-simple-classes-have-shift-orbit-bases`; `cor-cyclotomic-hecke-blocks-inherit-a-nontrivial-z-grading` | Specialize the two general HA constructions to the cyclotomic finite-module/projective categories and separately to noncyclotomic projectives; state exactly where the algebra is finite-dimensional. | Agent 6 S2 §§2.3–2.10,4.18. Exact decategorification objects without reminting generic K-groups. |
| `def-graded-cartan-pairing-between-projectives-and-modules` (definition) | Specialize HA-19's Laurent-valued homogeneous-Hom pairing to each finite-dimensional cyclotomic KLR block and take the orthogonal direct sum over ranks; fix the bar/duality convention. | `def-graded-module-and-projective-grothendieck-groups-for-klr-towers`; `def-projective-simple-hom-pairing-on-grothendieck-groups`; `thm-projective-hom-pairing-is-additive-and-graded-sesquilinear` | HA-19 gives descent and shift sesquilinearity; this row checks block finiteness and the KLR duality convention. Perfectness/nondegeneracy is asserted only after the split-simple basis theorem applies. | Agent 6 S2 Thm 4.18. Identifies Shapovalov/Cartan pairing later. |
| `def-nondegenerate-root-of-unity-level-one-hecke-regime` (definition) | Work over an algebraically closed field of characteristic zero with nondegenerate Hecke parameter `xi!=1` of finite order `e>=2`, level-one weight `Lambda_0`, and Ariki's normalization `xi=q^2`; state separately that the degenerate parameter `xi=1` is not covered by this row. | `def-affine-and-cyclotomic-type-a-hecke-algebras`; `def-quantum-characteristic-of-an-invertible-hecke-parameter`; `def-symmetric-group-level-one-residue-and-partition-label-convention` | Match the affine-Hecke quadratic relation, residue eigenvalues and QG-8 cyclic Cartan datum under `xi=q^2`. | Ariki §1 and §4.6, pp. 789,806; local researcher 6 exact parameter audit. |
| `prop-qg8-categorification-transports-across-the-hecke-klr-bridge` (proposition) | In the preceding regime, the direct sum of cyclotomic Hecke projective Grothendieck groups is the lower integral basic module and the finite-module Grothendieck group is its dual; uniquely shifted self-dual indecomposable projectives and simples transport to the lower and upper global bases respectively. | `def-nondegenerate-root-of-unity-level-one-hecke-regime`; `thm-brundan-kleshchev-blockwise-hecke-klr-isomorphism`; `thm-cyclotomic-klr-projective-k-zero-is-the-integral-highest-weight-module`; `thm-cyclotomic-klr-module-g-zero-is-the-dual-integral-highest-weight-module`; `thm-self-dual-indecomposable-cyclotomic-klr-projectives-give-the-lower-global-basis`; `thm-self-dual-simple-cyclotomic-klr-modules-give-the-upper-global-basis`; `def-graded-cartan-pairing-between-projectives-and-modules` | Apply the blockwise graded equivalence induced by the algebra isomorphism; it preserves projectives, simples, graded duality and Hom pairings. Then transport QG-8's two Grothendieck identifications and keep the lower/projective versus upper/simple sides distinct. | QG-8 categorification/global-basis suppliers plus Brundan--Kleshchev Main Theorem. Direct nongeometric proof seam. |
| `def-graded-specht-module-by-homogeneous-standard-tableaux` (definition) | For every partition `mu`, define the canonical graded lift `S(mu)` of the cyclotomic Hecke cell/Specht module with homogeneous basis `v_T` indexed by standard tableaux, residue idempotent `e(i_T)v_T=v_T`, and recursive degree `deg(T)` obtained by removing the node containing the largest entry. | `def-type-a-hecke-specht-module-and-cell-form`; `def-type-a-cyclotomic-specialization-of-the-qg-klr-algebras`; `cor-cyclotomic-hecke-blocks-inherit-a-nontrivial-z-grading` | Present the homogeneous KLR generators on the tableau basis, verify the defining relations/cell quotient, and check that forgetting the grading recovers the chosen Hecke Specht module. | Brundan--Kleshchev--Wang construction as summarized with exact basis/character in Brundan--Kleshchev §5.2, (5.6)–(5.7), pp. 46–47. |
| `thm-graded-specht-restriction-has-the-removable-node-filtration` (theorem) | If `A_1,...,A_c` are the removable `i`-nodes of `mu` from bottom to top, then `E_iS(mu)` has a filtration whose `m`th subquotient is `S(mu-A_m)<d_(A_m)(mu)>`, with the exact source degree shift. | `def-graded-specht-module-by-homogeneous-standard-tableaux`; `def-cyclotomic-klr-induction-and-restriction-functors`; `def-generalized-jm-primary-idempotents-and-residue-summand-functors` | Filter the standard-tableau basis by the row occupied by the maximal entry. Generator triangularity preserves the filtration; delete the maximal node to identify each homogeneous quotient and compute its degree shift from the recursive tableau degree. | Brundan--Kleshchev--Wang Thm. 4.11, restated and derived as Brundan--Kleshchev Prop. 5.5, pp. 47–48. Exact recursive proof joint. |
| `thm-graded-specht-classes-realize-standard-fock-vectors` (theorem) | The canonical graded lift `S(mu)` has a homogeneous standard-tableau basis and, under the QG-8 module-Grothendieck identification, its class is the standard Fock vector `M_mu` in the fixed level-one convention. | `prop-qg8-categorification-transports-across-the-hecke-klr-bridge`; `def-level-one-global-basis-transition-polynomials-in-the-symmetric-group-convention`; `cor-cyclotomic-hecke-blocks-inherit-a-nontrivial-z-grading`; `thm-graded-specht-restriction-has-the-removable-node-filtration` | Use the homogeneous tableau basis for the graded lift. Induct on rank: the graded removable-node filtration gives the same `E_i` recursion as the standard Fock vectors; the difference is killed by every `E_i`, hence vanishes in the irreducible highest-weight module. | Brundan--Kleshchev, *Graded decomposition numbers for cyclotomic Hecke algebras*, §5.2, Prop. 5.5 and Thm. 5.6, pp. 46–48. Complete standard-basis comparison. |
| `thm-ariki-canonical-basis-theorem-in-characteristic-zero` (theorem) | In the stated nondegenerate level-one characteristic-zero regime, lower global-basis elements correspond to uniquely shifted self-dual indecomposable projective cyclotomic Hecke modules and upper global-basis elements correspond to self-dual simple modules, with the declared restricted-partition labels. | `prop-qg8-categorification-transports-across-the-hecke-klr-bridge`; `thm-graded-specht-classes-realize-standard-fock-vectors` | The QG-8 global-basis identification is preserved by the graded Hecke--KLR equivalence. The standard-Specht/Fock comparison fixes the label bijection rather than leaving it up to permutation; specialization `v=1` gives Ariki's original ungraded statement. | QG-8 direct categorification; Brundan--Kleshchev Thms. 5.3,5.6,5.14; Ariki Thm. 4.4 independently identifies the ungraded endpoint. |
| `thm-graded-decomposition-numbers-equal-canonical-basis-coefficients-in-characteristic-zero` (theorem) | In the same regime, the graded multiplicity `[S(mu):D(lambda)]_v` equals the global-basis transition coefficient `d_(lambda,mu)(v)`, with the stated dominance, grading and self-dual normalization. | `thm-ariki-canonical-basis-theorem-in-characteristic-zero`; `thm-graded-specht-classes-realize-standard-fock-vectors`; `def-level-one-global-basis-transition-polynomials-in-the-symmetric-group-convention` | Expand the standard Fock vector in the upper global basis. Transport the equality to module `G_0`: the standard vector is `[S(mu)]`, upper global vectors are self-dual `[D(lambda)]`, so their Laurent coefficients are exactly graded composition multiplicities. | Brundan--Kleshchev §5.5, Thm. 5.14 and its combination with Thms. 5.6 and 3.9; direct QG-8 proof replaces the old geometric black box. |
| `def-graded-adjustment-polynomials-in-positive-characteristic` (definition) | Reduction of characteristic-zero simple lattices defines bar-invariant nonnegative graded adjustment coefficients `a_{lambda,nu}(v)`; at level one, `xi=1,v=1` recovers James’s adjustment matrix. | `def-graded-module-and-projective-grothendieck-groups-for-klr-towers`; `thm-symmetric-group-decomposition-matrix-is-dominance-unitriangular`; `thm-graded-decomposition-numbers-equal-canonical-basis-coefficients-in-characteristic-zero` | Define through the expansion of reduced characteristic-zero simple classes in the positive-characteristic simple basis. | Agent 6 S2 §5.6. Names the missing layer. |
| `thm-positive-characteristic-decomposition-polynomials-factor-through-adjustment` (theorem) | The positive-characteristic graded decomposition polynomial is `sum_nu a_{lambda,nu}(v)d_{nu,mu}(v)` in the source orientation; no general identity adjustment matrix is asserted. | `def-graded-adjustment-polynomials-in-positive-characteristic`; `cor-type-a-hecke-decomposition-matrix-is-dominance-unitriangular` | Reduce lattices, use transitivity of composition multiplicities, and preserve grading/self-duality. | Agent 6 S2 §5.6 and Agent 5 Kleshchev §10. Exact reason canonical coefficients alone do not solve the modular problem. |
| `rem-canonical-bases-do-not-solve-arbitrary-positive-characteristic-decomposition-numbers` (remark) | The characteristic-zero Ariki/graded theorem and positive-characteristic adjustment factorization leave nontrivial adjustment entries undetermined in general. | `thm-positive-characteristic-decomposition-polynomials-factor-through-adjustment`; `rem-general-modular-decomposition-numbers-are-not-determined-by-triangularity` | No proof section; compare regimes and exact factorization. Provenance `literature-derived/not-applicable`. | Agent 6 S2 §5.6, S4 §9; Agent 5 §2 boundary. Required frontier truth. |

### Ariki Categorification and Graded Decomposition Numbers — Examples (B dependency leaf; `ariki-categorification-and-graded-decomposition-numbers-examples`)

| proposed id (kind) | exact computation/boundary | deps | verification route | source locator and purpose |
|---|---|---|---|---|
| `ex-projective-versus-simple-grothendieck-classes-in-a-small-block` (example) | For a small nonsemisimple block, write the Cartan/decomposition pairing and show a projective class is not a simple class although they become dual bases. | `prop-qg8-categorification-transports-across-the-hecke-klr-bridge` | Compute projective covers and graded Hom dimensions, then identify the two dual basis vectors under the transported pairing. | QG-8 plus Brundan--Kleshchev §4.18. Makes the two-group distinction visible. |
| `cex-specializing-v-to-one-forgets-graded-decomposition-information` (counterexample) | Exhibit two distinct Laurent polynomials with the same value at one, and a small graded module where shifts distinguish them before specialization. | `def-graded-module-and-projective-grothendieck-groups-for-klr-towers` | Compare `[M]` and `[M<2>]` and their v=1 images. | Direct graded-category check; `ai-generated/ai-generated`, generation role `counterexample`, non-dependency leaf. |

## SYMR-13 — Kronecker coefficients and internal products

### Kronecker Coefficients and Internal Products (A page; `kronecker-coefficients-and-internal-products`)

| proposed id (kind) | exact claim and restrictions | deps | proof route / intermediate joints | source locator and purpose |
|---|---|---|---|---|
| `def-kronecker-coefficient-and-internal-tensor-product` (definition) | For `lambda,mu,nu partition n`, `g(lambda,mu,nu)` is the multiplicity of `S^nu` in the diagonal `S_n`-module `S^lambda tensor S^mu`; this internal product stays in degree n. | `thm-complex-specht-modules-are-irreducible`; `def-tensor-product-of-complex-representations`; `thm-maschkes-theorem-for-finite-groups-over-fields-whose-characteristic-does-not-divide-the-group-order` | Apply complex Maschke to the same-group tensor product and define the multiplicity of each classified Specht simple. Contrast this fixed-rank product with outer induction in prose, without making the contrast a logical prerequisite. | Agent 7 product table and §B; Macdonald I §7 Ex. 20. Main distinction from LR. |
| `lem-complex-specht-modules-are-self-dual` (lemma) | Every complex Specht module carries a nondegenerate symmetric invariant bilinear form, hence `S^lambda isomorphic to (S^lambda)^*`; its irreducible character is real-valued. | `def-invariant-inner-product-on-a-tabloid-module`; `lem-specht-module-has-nondegenerate-self-pairing-in-characteristic-zero`; `thm-complex-specht-modules-are-irreducible` | Restrict the invariant tabloid form to the Specht submodule. Nondegeneracy makes `v maps to <v,->` an equivariant isomorphism to the contragredient dual; the character identity `chi(g^-1)=conj(chi(g))` and self-duality give real values. | James §§7,11 and §25; this is an explicit supplier for the three-character formula rather than an unstated folklore fact. |
| `prop-kronecker-coefficient-is-a-three-character-inner-product` (proposition) | `g(lambda,mu,nu)=<chi^lambda chi^mu,chi^nu>=(1/n!)sum_{sigma}chi^lambda(sigma)chi^mu(sigma)chi^nu(sigma)` because symmetric-group irreducibles are self-dual. | `def-kronecker-coefficient-and-internal-tensor-product`; `thm-characters-of-direct-sums-tensor-products-and-duals`; `thm-character-inner-product-computes-intertwiner-dimension`; `lem-complex-specht-modules-are-self-dual` | Character of tensor product is pointwise product; apply the multiplicity inner product, then use real-valued self-dual Specht characters to remove conjugation from the third factor. | Agent 7 §B; Macdonald I §7 Ex. 20. Exact computational formula. |
| `cor-kronecker-coefficients-are-nonnegative-integers-and-s3-symmetric` (corollary) | Kronecker coefficients are nonnegative integers and invariant under all permutations of `(lambda,mu,nu)`; conjugating exactly two partitions also preserves them. | `prop-kronecker-coefficient-is-a-three-character-inner-product`; `prop-sign-twist-corresponds-to-the-omega-involution` | Multiplicity gives integrality/positivity; the triple product is symmetric; two sign twists cancel. | Agent 7 §B. Basic symmetries only, not a positive rule. |
| `def-internal-product-on-symmetric-functions` (definition) | Transport same-rank tensor product through Frobenius characteristic to a degree-preserving bilinear product `*` on `Lambda`. | `def-kronecker-coefficient-and-internal-tensor-product`; `thm-frobenius-characteristic-is-an-isometric-graded-ring-isomorphism` | Define degreewise and set products of unequal degrees to zero if a global convention is needed. | Macdonald I §7 Ex. 20; Agent 7 product table. |
| `prop-power-sums-diagonalize-the-internal-product` (proposition) | `p_rho*p_sigma=delta_{rho,sigma}z_rho p_rho`; hence `s_lambda*s_mu=sum_nu g(lambda,mu,nu)s_nu`. | `def-internal-product-on-symmetric-functions`; `cor-power-sums-are-orthogonal-for-the-hall-inner-product`; `def-frobenius-characteristic-map` | Class functions multiply pointwise; conjugacy-class indicator basis is diagonal and maps to scaled power sums. | Macdonald I (7.12); Agent 7. Fast exact formula without combinatorial positivity. |
| `def-first-row-padding-of-a-partition` (definition) | For a partition `alpha`, define `alpha[n]=(n-|alpha|,alpha_1,alpha_2,...)` only when `n>=|alpha|+alpha_1`. | `def-partition-young-diagram-and-conjugate-partition` | Verify that the displayed sequence is weakly decreasing exactly at the stated threshold. This neutral notation is shared with FI-modules and does not presuppose Kronecker stability. | Agent 7 S1 padding convention and S2 pp. 3–5. |
| `def-padded-kronecker-coefficient-sequence` (definition) | For fixed partitions `alpha,beta,gamma`, define `g_n(alpha,beta,gamma)=g(alpha[n],beta[n],gamma[n])` whenever all three padded sequences are partitions. This notation asserts no eventual constancy. | `def-first-row-padding-of-a-partition`; `def-kronecker-coefficient-and-internal-tensor-product` | Take the maximum of the three padding thresholds as the initial domain and evaluate the already defined Kronecker coefficient. | Agent 7 S2 pp. 3–5. Noncircular input to Murnaghan stability. |
| `thm-row-gap-reduction-and-conditional-k-stability` (theorem) | Under Pak–Panova’s exact row-separation hypothesis with `s=n-nu_1`, simultaneous addition of `(t^k)` to all three partitions preserves the Kronecker coefficient for every `t>=0`; without it only eventual k-stability is claimed. | `def-kronecker-coefficient-and-internal-tensor-product`; `def-first-row-padding-of-a-partition` | Apply the reduction lemma: an excessive row difference forces zero; otherwise map to a bounded-size triple. Under separation, original and padded reductions coincide; monotonicity/boundedness gives general eventual k-stability. | Agent 7 S3 Thm 4.1, Cor. 4.2, Reduction Lemma 4.3 and Thm 1.1. Independent stability route. |
| `rem-no-general-positive-combinatorial-rule-for-kronecker-coefficients-is-known` (remark) | The character formula, reductions and stability theorems do not supply a general positive combinatorial rule for all Kronecker coefficients. | `prop-kronecker-coefficient-is-a-three-character-inner-product`; `thm-row-gap-reduction-and-conditional-k-stability` | No proof section; state the open boundary exactly. Provenance `literature-derived/not-applicable`. | Agent 7 S3 Introduction and closing audit. Required non-overclaim. |

### Kronecker Coefficients and Internal Products — Examples (B dependency leaf; `kronecker-coefficients-and-internal-products-examples`)

| proposed id (kind) | exact computation/boundary | deps | verification route | source locator and purpose |
|---|---|---|---|---|
| `ex-kronecker-products-for-s3` (example) | Compute every internal product of the three `S_3` irreducibles and verify all triple symmetries. | `prop-kronecker-coefficient-is-a-three-character-inner-product` | Multiply the known character-table rows and decompose by inner products. | Agent 7 §B; uses SYMR-2’s `S_3` table. Small complete internal ring. |
| `cex-first-row-padding-before-the-partition-threshold-is-undefined` (counterexample) | For `alpha=(2,1)`, `alpha[3]=(0,2,1)` is not a partition; the stable family begins only at n=5. | `def-first-row-padding-of-a-partition` | Check the first part is at least `alpha_1`. | Agent 7 S1 §C. Prevents malformed labels. |
| `cex-row-padding-does-not-preserve-a-kronecker-coefficient-before-stability` (counterexample) | Pak–Panova’s `(2,2),(2,2),(3,1)` coefficient is 0 before padding and 1 after every positive first-row padding because the separation hypothesis fails. | `thm-row-gap-reduction-and-conditional-k-stability` | Compute the initial coefficient and apply the source’s Example 4.4 equality for `t>=1`. | Agent 7 S3 Example 4.4. Shows eventual is not immediate. |

## SYMR-14 — Character polynomials, FI-modules, and representation stability

All representation-stability equivalences below are over a characteristic-zero
field and assume finite-dimensional values. The Noetherian theorem is a real
proof component, not an implicit property of arbitrary functor categories.

### Character Polynomials, FI-Modules, and Representation Stability (A page; `character-polynomials-fi-modules-and-representation-stability`)

| proposed id (kind) | exact claim and restrictions | deps | proof route / intermediate joints | source locator and purpose |
|---|---|---|---|---|
| `def-fi-category-and-fi-module` (definition) | `FI` has finite sets and injections; an FI-module over `k` is a covariant functor to `k`-modules, so evaluation at `[n]` is naturally an `S_n`-module with compatible transition maps. | `def-category`; `def-functor-and-contravariant-functor`; `def-finite-symmetric-group-and-permutation-notation`; `def-left-and-right-modules` | Spell out automorphism and functor actions. Natural transformations are not needed for this definition. | Agent 7 S1 Defs. 1.1–1.4. Organizes sequences across rank. |
| `def-finite-generation-and-generation-degree-of-an-fi-module` (definition) | An FI-module `V` is generated by elements `v_j in V(S_j)` when the smallest subfunctor containing them is `V`; it is generated in degree at most `d` when the union of `V(S)` for `|S|<=d` generates. | `def-fi-category-and-fi-module` | Define the span objectwise by all injection images and verify it is a subfunctor. Keep finite generation separate from pointwise finite dimensionality. | Agent 7 S1 Def. 2.3.4 and Lemma 2.3.2. |
| `def-fi-coinvariant-functors-and-transition-maps` (definition) | For `a,n>=0`, define `Phi_a(V)_n=(V_(a disjoint-union n))_(S_n)` and let `T:Phi_a(V)_n to Phi_a(V)_(n+1)` be induced by `id_a disjoint-union f` for any injection `f:n to n+1`; postcomposition by `S_(n+1)` makes `T` independent of `f`. | `def-fi-category-and-fi-module`; `def-coinvariants-functor-of-a-group-module` | Prove choice-independence and functoriality. State that each `Phi_a` is right exact, and is exact when all `n!` are invertible in `k`, by averaging. | Church--Ellenberg--Farb S1 Defs. 3.1.1–3.1.2, p. 16. |
| `def-injectivity-surjectivity-and-stability-degree-of-an-fi-module` (definition) | `inj-deg(V)` (respectively `surj-deg(V)`) is the least `s` for which every `T:Phi_a(V)_n to Phi_a(V)_(n+1)` is injective (respectively surjective) for all `a` and `n>=s`; `stab-deg(V)=max(inj-deg(V),surj-deg(V))`. | `def-fi-coinvariant-functors-and-transition-maps` | Keep these three bounds distinct and allow infinity. | Church--Ellenberg--Farb S1 Defs. 3.1.3 and 3.1.5, pp. 16–17. |
| `def-weight-of-a-characteristic-zero-fi-module` (definition) | Over a characteristic-zero field, `weight(V)` is the supremum of `|lambda|` over padded irreducibles `V(lambda)_n=V_(lambda[n])` occurring in all `V_n`, with weight zero for the zero module. | `def-fi-category-and-fi-module`; `def-first-row-padding-of-a-partition`; `thm-complex-specht-modules-are-irreducible` | Use scalar extension if the characteristic-zero field is not algebraically closed and state the constituent convention. | Church--Ellenberg--Farb S1 Def. 3.2.1, p. 18. |
| `def-free-fi-module-generated-by-an-sm-module` (definition) | For an `S_m`-module `W`, the free FI-module `M(W)_n` is the induced module from injections/Young subgroup, with its universal mapping property. | `def-fi-category-and-fi-module`; `def-induced-r-linear-g-module-by-h-covariant-functions`; `def-outer-induction-product-for-symmetric-group-characters` | Linearize the representable functor and tensor over `k[S_m]`; prove adjunction by evaluation. | Agent 7 S1 §2.3. Generator building block. |
| `lem-free-fi-modules-have-explicit-weight-and-stability-bounds` (lemma) | If an FB-module `W` vanishes above degree `m`, then `M(W)` has injectivity degree zero and surjectivity degree at most `m`; in characteristic zero, `M(V_lambda)` has weight `|lambda|` and stability degree exactly `lambda_1`. | `def-free-fi-module-generated-by-an-sm-module`; `def-injectivity-surjectivity-and-stability-degree-of-an-fi-module`; `def-weight-of-a-characteristic-zero-fi-module`; `thm-outer-littlewood-richardson-rule`; `cor-outer-pieri-rules-for-trivial-and-sign-factors` | Compute `Phi_a M(W)_n` as the direct sum indexed by subsets of the fixed `a`-set and intersection cardinalities; then apply the branching/Pieri rule to `M(V_lambda)`. | Church--Ellenberg--Farb S1 Props. 3.1.7 and 3.2.4–3.2.6, pp. 17–19. Quantitative seed for presentations. |
| `lem-fi-coinvariant-functors-detect-vanishing-in-bounded-weight` (lemma) | Over a characteristic-zero field, if `V_n` has weight at most `a`, then an `S_n`-subquotient `W_n` is zero iff `(W_n)_(S_(n-a))=0`; moreover coinvariants of a fixed padded irreducible stabilize in the exact range used by the source. | `def-weight-of-a-characteristic-zero-fi-module`; `def-fi-coinvariant-functors-and-transition-maps`; `cor-complex-specht-restriction-branching-rule` | Remove boxes in different columns via branching; at the boundary recover the unpadded irreducible, and prove the stable induction formula for the coinvariants. | Church--Ellenberg--Farb S1 Lemma 3.2.7, pp. 18–19. Decisive faithfulness joint in Noetherianity and multiplicity stability. |
| `thm-fi-modules-over-a-noetherian-q-algebra-form-a-noetherian-category` (theorem) | If `k` is a commutative Noetherian ring containing `Q`, every sub-FI-module of a finitely generated FI-module over `k` is finitely generated. | `def-finite-generation-and-generation-degree-of-an-fi-module`; `def-fi-coinvariant-functors-and-transition-maps`; `def-free-fi-module-generated-by-an-sm-module`; `lem-fi-coinvariant-functors-detect-vanishing-in-bounded-weight`; `thm-hilbert-basis-theorem` | For `V` generated in degree at most `a` and `W subset V`, prove `Phi_a(V)` is a finite graded `k[T]`-module; Hilbert basis makes `Phi_a(W)` finite. Lift its generators plus generators of `W_0,...,W_(a-1)` to a finite submodule `W'`; exactness gives `Phi_a(W/W')=0`, while bounded weight and the preceding detection lemma force `W/W'=0`. | Church--Ellenberg--Farb S1 Thm. 1.3 and complete proof, pp. 20–21. This is the actual Noetherian proof, not an unspecified shift induction. |
| `def-uniform-representation-stability-for-consistent-sn-sequences` (definition) | A consistent characteristic-zero sequence is uniformly representation stable after N when maps are injective, their `S_{n+1}`-spans are surjective, and each padded irreducible multiplicity is constant. | `def-fi-category-and-fi-module`; `def-first-row-padding-of-a-partition`; `thm-complex-specht-modules-are-irreducible` | State all three conditions and padding threshold. | Agent 7 S1 Def. 3.3.2. Stable conclusion must not omit spanning. |
| `prop-weight-and-stability-degree-give-uniform-representation-stability-range` (proposition) | If a characteristic-zero FI-module with finite-dimensional values has weight at most `d` and stability degree at most `s`, then its evaluated sequence is uniformly representation stable for every `n>=d+s`. | `def-uniform-representation-stability-for-consistent-sn-sequences`; `def-weight-of-a-characteristic-zero-fi-module`; `def-injectivity-surjectivity-and-stability-degree-of-an-fi-module`; `lem-fi-coinvariant-functors-detect-vanishing-in-bounded-weight` | Use stable coinvariants to detect the kernel and cokernel of each transition map in bounded weight, then induct on padded irreducible size in the stable coinvariant decomposition to freeze every multiplicity. | Church--Ellenberg--Farb Prop. 3.3.3, pp. 21–22. Exact missing bridge from numerical FI bounds to all three representation-stability clauses. |
| `thm-finite-generation-is-equivalent-to-uniform-representation-stability` (theorem) | For an FI-module with finite-dimensional values over a characteristic-zero field, finite generation is equivalent to uniform representation stability of the evaluated sequence. | `thm-fi-modules-over-a-noetherian-q-algebra-form-a-noetherian-category`; `def-uniform-representation-stability-for-consistent-sn-sequences`; `lem-free-fi-modules-have-explicit-weight-and-stability-bounds`; `prop-weight-and-stability-degree-give-uniform-representation-stability-range`; `lem-fi-coinvariant-functors-detect-vanishing-in-bounded-weight` | Present `V` as a quotient of finite free `M`, use Noetherianity to finitely generate the kernel `K`, and use exact `Phi_a` plus the free bounds to obtain finite weight and stability degree; the preceding proposition gives uniform stability. Conversely, eventual `S_(n+1)`-spanning makes finitely many initial ranks generate. | Church--Ellenberg--Farb Thm. 1.13 and complete proof, pp. 21–23. Landmark categorical stability theorem. |
| `prop-tensor-products-of-finitely-generated-fi-modules-are-finitely-generated` (proposition) | Over a Noetherian ring, the pointwise tensor product of two finitely generated FI-modules is finitely generated. | `def-finite-generation-and-generation-degree-of-an-fi-module`; `def-free-fi-module-generated-by-an-sm-module` | Reduce to `M(m_1) tensor M(m_2)`. Every basis pair of injections is the FI-image of one supported on the union of the two images, whose size is at most `m_1+m_2`; finite presentations then give the general case. | Church--Ellenberg--Farb Prop. 2.3.6, p. 14. Exact tensor-closure joint in the Murnaghan proof. |
| `def-character-polynomial-in-cycle-counts` (definition) | A character polynomial is `P in Q[X_1,X_2,...]` with weighted degree `deg X_i=i`, evaluated by `X_i(sigma)=` number of i-cycles. | `def-permutation-support-disjoint-cycles-and-cycle-type`; `def-class-function-and-the-space-of-complex-class-functions` | Finite polynomial evaluation on cycle data. | Agent 7 S1 §3.3. Stable character language. |
| `thm-finitely-generated-fi-modules-have-eventual-character-polynomials` (theorem) | If `V` has weight at most d and stability degree at most s, there is a unique character polynomial of weighted degree at most d equal to `chi_{V_n}` for every `n>=s+d`. | `thm-finite-generation-is-equivalent-to-uniform-representation-stability`; `def-character-polynomial-in-cycle-counts`; `def-first-row-padding-of-a-partition`; `def-weight-of-a-characteristic-zero-fi-module`; `def-injectivity-surjectivity-and-stability-degree-of-an-fi-module` | Stable decomposition has only `V(lambda)_n` with `|lambda|<=d` and bounded first row; prove each padded irreducible has a character polynomial and sum with stable multiplicities; use linear independence for uniqueness. | Agent 7 S1 Thm 3.3.4. Exact range and degree. |
| `prop-fi-module-realizing-one-padded-irreducible-family` (proposition) | For fixed `lambda`, the source constructs `V(lambda)` with `V(lambda)_n=V_(lambda[n])` for `n>=|lambda|+lambda_1` and zero below, generated at the threshold with weight `|lambda|` and stability degree `lambda_1`. | `def-free-fi-module-generated-by-an-sm-module`; `def-first-row-padding-of-a-partition`; `def-weight-of-a-characteristic-zero-fi-module`; `def-injectivity-surjectivity-and-stability-degree-of-an-fi-module`; `thm-outer-littlewood-richardson-rule` | Form the quotient of the relevant free FI-module singled out by Pieri and verify the transition maps. | Agent 7 S1 Prop. 3.4.1. Concrete irreducible stable family. |
| `cor-dimensions-of-finitely-generated-fi-modules-are-eventually-polynomial` (corollary) | Under the preceding characteristic-zero finite-generation hypotheses, `dim V_n=P_V(n,0,0,...)` for all sufficiently large n. | `thm-finitely-generated-fi-modules-have-eventual-character-polynomials` | Evaluate the identity permutation’s cycle counts. | Agent 7 S1 Thm 3.3.4. Numerical consequence. |

### Character Polynomials, FI-Modules, and Representation Stability — Examples (B dependency leaf; `character-polynomials-fi-modules-and-representation-stability-examples`)

| proposed id (kind) | exact computation/boundary | deps | verification route | source locator and purpose |
|---|---|---|---|---|
| `ex-fi-permutation-module-has-character-x-one` (example) | The permutation representation on `[n]` is an FI-module with character polynomial `X_1`. | `def-fi-category-and-fi-module`; `def-character-polynomial-in-cycle-counts` | A permutation matrix trace counts fixed points. | Agent 7 S1 Introduction. First character polynomial. |
| `ex-exterior-square-of-the-fi-permutation-module` (example) | The exterior square of the FI permutation module has character polynomial `binom(X_1,2)-X_2`. | `def-fi-category-and-fi-module`; `def-character-polynomial-in-cycle-counts`; `thm-characters-of-direct-sums-tensor-products-and-duals` | Define the exterior-square transition maps and count invariant oriented two-subsets, separating fixed-point pairs from two-cycles. | Church--Ellenberg--Farb introductory example. Shows higher cycle counts without a B-to-B edge. |
| `ex-the-padded-family-for-lambda-two-one-begins-at-rank-five` (example) | Construct `V((2,1))_n` from n=5 onward and compute its weight/stability degree. | `prop-fi-module-realizing-one-padded-irreducible-family` | Verify partition threshold and stable transition conditions. | Agent 7 S1 Prop. 3.4.1. Padding boundary. |
| `cex-pointwise-finite-dimensionality-does-not-imply-fi-finite-generation` (counterexample) | Let `V_n=k e_n`, let every bijection act trivially, and send every non-bijection injection to zero. This is a pointwise one-dimensional FI-module that is not finitely generated. | `def-finite-generation-and-generation-degree-of-an-fi-module` | Identity and bijection maps compose correctly, while any composite involving a non-bijection is again a non-bijection; no finite collection of ranks can generate a later `e_n`. | Direct functor check; `ai-generated/ai-generated`, generation role `counterexample`, non-dependency leaf. |

## SYMR-15 — Murnaghan stability via FI-modules

### Murnaghan Stability via FI-Modules (A page; `murnaghan-stability-via-fi-modules`)

| proposed id (kind) | exact claim and restrictions | deps | proof route / intermediate joints | source locator and purpose |
|---|---|---|---|---|
| `thm-murnaghan-stability-for-kronecker-coefficients` (theorem) | For fixed partitions `alpha,beta,gamma`, the sequence `g_n(alpha,beta,gamma)` is eventually constant at a finite nonnegative integer. | `def-padded-kronecker-coefficient-sequence`; `prop-fi-module-realizing-one-padded-irreducible-family`; `prop-tensor-products-of-finitely-generated-fi-modules-are-finitely-generated`; `thm-finite-generation-is-equivalent-to-uniform-representation-stability` | Form the finitely generated FI-module `V(alpha) tensor V(beta)`. Uniform representation stability makes the multiplicity of each padded irreducible `V(gamma)_n` constant in a common stable range; that multiplicity is exactly `g_n(alpha,beta,gamma)`. | Church--Ellenberg--Farb Prop. 3.4.1 and Thm. 3.4.2, pp. 23–24, with Prop. 2.3.6, Thm. 1.3 and Prop. 3.3.3 as the complete inspected proof chain. This is the existence proof; Briand--Orellana--Rosas only supplies later bounds. |
| `def-reduced-kronecker-coefficient` (definition) | The eventual value furnished by Murnaghan stability is the reduced Kronecker coefficient `bar g(alpha,beta,gamma)`. | `thm-murnaghan-stability-for-kronecker-coefficients` | Well-definedness is exactly the preceding theorem; no circular appeal to the name occurs. | Murnaghan stability convention; Briand--Orellana--Rosas pp. 3–5. |
| `lem-bor-nonnegative-expansion-for-reduced-kronecker-coefficients` (lemma) | The reduced coefficient has Briand--Orellana--Rosas's finite nonnegative expansion as a sum of an ordinary Kronecker coefficient times three iterated Littlewood--Richardson coefficients; consequently every nonzero summand satisfies the source's degree-matching equations. | `def-reduced-kronecker-coefficient`; `def-kronecker-coefficient-and-internal-tensor-product`; `thm-outer-littlewood-richardson-rule` | Apply the source's vertex-operator reduction formula, expand each skew Schur function by LR coefficients, and compare homogeneous degrees. Finiteness follows because all six auxiliary partitions have bounded sizes. | Briand--Orellana--Rosas Lemma 2.1, pp. 7–8. Exact positivity/support supplier for the next three rows. |
| `lem-murnaghan-littlewood-top-degree-reduced-coefficients-are-lr-coefficients` (lemma) | If `|gamma|=|alpha|+|beta|`, then `bar g(alpha,beta,gamma)=c_(alpha,beta)^gamma`; in particular `bar g(alpha,beta,alpha+beta)=1`. | `lem-bor-nonnegative-expansion-for-reduced-kronecker-coefficients`; `thm-outer-littlewood-richardson-rule` | In the finite nonnegative expansion, the top-degree equations force every auxiliary partition except the LR term to be empty. The unique LR tableau adding the `alpha_i` boxes in row `i` with entry `i` gives coefficient one for `gamma=alpha+beta`. | Murnaghan--Littlewood theorem; Briand--Orellana--Rosas Lemma 3.1 and proof of Thm. 3.2, pp. 8–9. Exact attainment joint. |
| `prop-reduced-kronecker-support-satisfies-size-triangle-inequalities` (proposition) | Nonzero `bar g(alpha,beta,gamma)` forces the three triangle inequalities among `|alpha|,|beta|,|gamma|`. | `lem-bor-nonnegative-expansion-for-reduced-kronecker-coefficients` | Read the three degree equations in any nonzero summand and add them pairwise. | Briand--Orellana--Rosas Lemma 2.1, pp. 7–8. Useful exact zero test. |
| `thm-exact-global-stabilization-threshold-for-a-fixed-kronecker-product` (theorem) | The entire Schur internal product `s_(alpha[n])*s_(beta[n])` reaches its stable reduced expansion for every `n>=|alpha|+|beta|+alpha_1+beta_1`, and this is the exact global threshold in the source convention; no claim is made that this bound is sharp for each fixed coefficient. | `def-reduced-kronecker-coefficient`; `lem-bor-nonnegative-expansion-for-reduced-kronecker-coefficients`; `lem-murnaghan-littlewood-top-degree-reduced-coefficients-are-lr-coefficients` | Briand--Orellana--Rosas identify the product threshold with the maximum `|gamma|+gamma_1` over reduced support. Their support expansion and LR row bounds give the upper bound; the nonzero top-degree term `gamma=alpha+beta` attains it. | Briand--Orellana--Rosas Lemma 3.1 and Thm. 3.2, pp. 8–9. Exact global-product range. |

### Murnaghan Stability via FI-Modules — Examples (B dependency leaf; `murnaghan-stability-via-fi-modules-examples`)

| proposed id (kind) | exact computation/boundary | deps | verification route | source locator and purpose |
|---|---|---|---|---|
| `ex-murnaghan-stability-for-alpha-beta-two` (example) | For `alpha=beta=(2)`, calculate the products through the exact global threshold `n=8` and display the stable expansion from `(6,2)*(6,2)`. | `thm-exact-global-stabilization-threshold-for-a-fixed-kronecker-product` | Use the source's reduced expansion/recovery formula and small character computations. | Briand--Orellana--Rosas Thm. 3.2; exact threshold check. |
| `cex-a-global-product-threshold-is-not-a-per-triple-sharpness-claim` (counterexample) | A particular reduced coefficient may stabilize before the maximum support threshold; the theorem identifies when the whole product has stabilized, not the least stable rank of every triple. | `thm-exact-global-stabilization-threshold-for-a-fixed-kronecker-product` | Compare the maximum-over-support characterization with any coefficient whose padding is already defined and constant earlier. | Briand--Orellana--Rosas Lemma 3.1; conceptual boundary preventing a quantified overclaim. |

## SYMR-16 — Plancherel measure and asymptotic Young diagrams

The limit-shape and central-limit theorems remain central. Their exact
shifted-observable, tightness, topology and moment-method joints are listed
below. Ivanov--Kerov supplies the finite support-union factorization count and
the algebra isomorphism imported by Ivanov--Olshanski, so the filtered
multiplication proof is closed rather than citation-only. The RSK bound below
replaces their separate citation of Hammersley's tightness theorem and makes
the profile-support step local.

### Plancherel Measure and Asymptotic Young Diagrams (A page; `plancherel-measure-and-asymptotic-young-diagrams`)

| proposed id (kind) | exact claim and restrictions | deps | proof route / intermediate joints | source locator and purpose |
|---|---|---|---|---|
| `def-plancherel-measure-on-partitions` (definition) | For `lambda partition n`, `P_n(lambda)=(f^lambda)^2/n!`, where `f^lambda=dim_C S^lambda`. | `thm-standard-polytabloid-basis`; `thm-hook-length-formula` | Define the finite measure candidate. | Agent 7 S4 §5; ordinary dimensions inherited from RG-9/RG-11. |
| `prop-plancherel-weights-sum-to-one` (proposition) | `sum_{lambda partition n}(f^lambda)^2=n!`, so the Plancherel weights form a probability distribution. | `def-plancherel-measure-on-partitions`; `thm-finitely-many-irreducibles-occur-in-the-regular-representation-with-multiplicity-equal-to-their-degree`; `cor-sum-of-squares-of-standard-tableau-numbers` | Decompose the regular representation; RG-11’s RSK identity provides an independent combinatorial check. | Agent 7 S4 definition/interface. Establishes normalization without duplicating RG-11. |
| `thm-rsk-shape-of-a-uniform-random-permutation-has-plancherel-law` (theorem) | For uniform `sigma in S_n`, the RSK shape is distributed by `P_n`. | `thm-robinson-schensted-correspondence`; `def-plancherel-measure-on-partitions` | RSK bijects permutations of shape `lambda` with pairs of standard tableaux, exactly `(f^lambda)^2` choices. | RG-11 supplier plus Agent 7 boundary. Probabilistic representation/combinatorics bridge. |
| `def-russian-profile-and-sqrt-n-scaling-of-a-young-diagram` (definition) | Rotate the boundary to Russian coordinates, scale both axes by `sqrt(n)`, and define the 1-Lipschitz profile `bar lambda`. | `def-partition-young-diagram-and-conjugate-partition`; `def-plancherel-measure-on-partitions` | Give exact rotation/normalization matching the source’s curve. | Agent 7 S4 §5, pp. 25–29. Needed to state a limit shape without ambiguity. |
| `def-logan-shepp-vershik-kerov-limit-profile` (definition) | Define `Omega(x)=(2/pi)(x arcsin(x/2)+sqrt(4-x^2))` for `|x|<=2`, and `Omega(x)=|x|` for `|x|>=2`. | `def-russian-profile-and-sqrt-n-scaling-of-a-young-diagram` | Check agreement, differentiability and the 1-Lipschitz condition at `x=±2`. | Ivanov--Olshanski S4 §5, p. 25. Exact convention used in Theorems 5.4–5.5. |
| `def-shifted-character-observables-and-profile-moments` (definition) | For `rho partition r` and `lambda partition n`, set `p_rho^#(lambda)=n^(downarrow r) chi^lambda_(rho union 1^(n-r))/dim(lambda)` when `n>=r`, and zero otherwise. For a Russian profile `omega`, define the source moments `tilde p_k[omega]=-k integral_R x^(k-1)((omega(x)-|x|)/2)' dx` for `k>=2`. | `cor-irreducible-symmetric-group-character-values-are-power-sum-coefficients`; `def-russian-profile-and-sqrt-n-scaling-of-a-young-diagram`; `def-plancherel-measure-on-partitions` | State falling-factorial, repeated-one and distributional-derivative conventions, then verify the scaling `tilde p_k[bar lambda]=n^(-k/2)tilde p_k(lambda)`. | Ivanov--Olshanski S4 Def. 4.1, Def. 2.1, Prop. 2.2 and Def. 2.10, pp. 7–13,19,26. |
| `thm-shifted-character-basis-and-weight-filtration` (theorem) | The functions `p_rho^#` form a basis of the shifted-symmetric observable algebra `A`; with `deg_N(p_rho^#)=|rho|+ell(rho)`, multiplication is filtered and `p_sigma^#p_tau^#=p_(sigma union tau)^#+` lower-weight terms. This filtration agrees with the profile-moment weight filtration generated by products of `tilde p_k` of total weight at most the bound. | `def-shifted-character-observables-and-profile-moments` | In the Ivanov--Kerov partial-permutation algebra, prove that the coefficient of an orbit sum counts factorization pairs whose supports have the prescribed union (Props. 6.1–6.3); disjoint support gives the top term (Remark 6.4). Their Thm. 9.1 proves `A_rho maps to p_rho^#/z_rho` is an algebra isomorphism by evaluating central-character eigenvalues. Transport multiplication, apply Ivanov--Olshanski's cycle/support inequalities, and use both triangular generator changes to identify the profile-moment filtration. | Ivanov--Kerov §§2–6 and §9, Props. 6.1–6.3, Remark 6.4, Thm. 9.1; Ivanov--Olshanski Props. 4.2, 4.5, 4.7, 4.9–4.10, pp. 19–23. Complete imported proof chain audited by local researcher 7. |
| `prop-plancherel-expectations-of-shifted-character-observables` (proposition) | If `rho partition r`, then `E_(P_n)[p_rho^#]=n^(downarrow r)` for `rho=(1^r)`, and is zero otherwise, including the case `n<r`. | `def-shifted-character-observables-and-profile-moments`; `thm-character-of-the-regular-representation`; `prop-plancherel-weights-sum-to-one` | Expand by `(dim lambda)^2/n!`; the character sum is the regular character at cycle type `rho union 1^(n-r)`. | Ivanov--Olshanski S4 Prop. 5.1, pp. 24–25. Main exact moment calculation. |
| `prop-limit-profile-moments-are-central-binomial-coefficients` (proposition) | The profile `Omega` has `tilde p_(2m)[Omega]=(2m)!/(m!m!)` for `m>=1` and `tilde p_k[Omega]=0` for odd `k`. | `def-logan-shepp-vershik-kerov-limit-profile`; `def-shifted-character-observables-and-profile-moments` | Odd moments vanish by symmetry; for even moments substitute `x=2 sin(theta)` in the defining integral and integrate by parts. | Ivanov--Olshanski S4 Prop. 5.3, p. 26. Identifies the deterministic limit. |
| `prop-scaled-plancherel-profile-moments-converge-in-probability` (proposition) | For every `k>=0`, `integral_R (bar lambda(x)-Omega(x))x^k dx` converges to zero in probability under `P_n`. | `thm-shifted-character-basis-and-weight-filtration`; `prop-plancherel-expectations-of-shifted-character-observables`; `prop-limit-profile-moments-are-central-binomial-coefficients`; `def-convergence-in-probability`; `cor-chebyshev-inequality-for-random-variables` | For homogeneous `f in A`, filtered multiplication and the exact expectation formula give `E[f(bar lambda)] to f[Omega]`; apply this to the first and second moments of each `tilde p_k` and use Chebyshev, then translate by integration by parts. | Ivanov--Olshanski Thm. 5.4 and proof, pp. 26–28. |
| `lem-rsk-union-bound-localizes-plancherel-profiles` (lemma) | For uniform `sigma in S_n` and every integer `L`, `P(LIS(sigma)>=L)<=binom(n,L)/L!` and the same bound holds for the longest decreasing subsequence. Hence for every fixed `C>e`, with probability tending to one both the first row and first column of the Plancherel RSK shape are at most `C sqrt(n)`, so `bar lambda-|x|` has support in one fixed compact interval. | `thm-rsk-shape-of-a-uniform-random-permutation-has-plancherel-law`; `thm-schensted-longest-increasing-and-decreasing-subsequence-theorem`; `lem-probability-measure-basic-identities` | Choose the `L` positions and `L` values of a candidate increasing subsequence; the remaining values have `(n-L)!` orders. Divide by `n!`, union-bound, and use `binom(n,L)<= (en/L)^L` and `L! >= (L/e)^L`; apply the same argument after reversing values. | Local proof replacing Ivanov--Olshanski Lemma 5.6's citation of Hammersley; exact Schensted supplier is requested from RG-11 rather than duplicated here. |
| `lem-bounded-lipschitz-profile-moments-control-uniform-distance` (lemma) | On the set of real 1-Lipschitz functions supported in a fixed compact interval, convergence of all polynomial moments implies convergence in the supremum norm. | `cor-weierstrass-approximation-on-a-closed-interval`; `def-pointwise-uniform-and-uniformly-cauchy-convergence` | Uniform boundedness lets polynomial test functions approximate arbitrary continuous tests. A finite mesh plus the Lipschitz condition reduces the supremum norm to point evaluations; a nonnegative bump of total integral one turns each point evaluation bound into a weak-neighborhood bound. Define the supremum distance inline for these bounded real functions and identify convergence in it with the inherited epsilon definition. No norm over a general absolutely valued field is used. | Ivanov--Olshanski S4 Lemma 5.7 and complete proof, pp. 28–29. |
| `thm-plancherel-young-diagrams-converge-to-the-limit-shape` (theorem) | Under `P_n`, `sup_(x in R)|bar lambda(x)-Omega(x)|` converges to zero in probability. | `prop-scaled-plancherel-profile-moments-converge-in-probability`; `lem-rsk-union-bound-localizes-plancherel-profiles`; `lem-bounded-lipschitz-profile-moments-control-uniform-distance` | On the high-probability compact-support event, apply the topology lemma to finitely many moment neighborhoods controlling a prescribed sup-norm ball; the exceptional support event has probability tending to zero. | Ivanov--Olshanski Thm. 5.5, pp. 28–29, with the local RSK tightness replacement above. |
| `def-joint-convergence-and-normalized-cycle-character-observables` (definition) | For fixed `N`, joint convergence in distribution of vectors means weak convergence of their laws against every bounded continuous function on `R^(N-1)`. Define `eta_k^(n)=p_k^#/(sqrt(k)n^(k/2))` for `2<=k<=N`. | `def-shifted-character-observables-and-profile-moments`; `def-law-or-distribution-of-a-random-element`; `def-convergence-in-distribution-for-real-random-variables`; `def-multivariate-normal-law` | At `lambda partition n`, write the exact identity `eta_k^(n)(lambda)=(n downarrow k)chi^lambda_(k,1^(n-k))/(sqrt(k)n^(k/2)dim(lambda))`; the falling-factorial ratio tends to one after the equivalent unnormalized scaling. State that the target is the standard Gaussian product law. | Ivanov--Olshanski Thm. 6.1, p. 29; multivariate probability convention supplied by PT-7/PT-9. |
| `thm-multivariate-method-of-moments-for-a-determinate-limit` (theorem) | If every mixed moment of `R^d`-valued random vectors converges to the corresponding moment of a law determined by all its mixed moments, then their laws converge weakly to that law; a multivariate Gaussian is moment-determinate. | `thm-prokhorov-tightness-theorem-on-polish-spaces`; `def-multivariate-normal-law`; `lem-characteristic-function-of-a-multivariate-normal-law`; `cor-markov-inequality-for-random-variables` | Bounded coordinate second moments give tightness by Markov and a union bound. Prokhorov gives subsequential weak limits; higher even-moment bounds give uniform integrability of each monomial, so truncation passes all mixed moments to a limit. Moment determinacy identifies every subsequential limit; analyticity of the Gaussian characteristic function proves the final clause. | Ivanov--Olshanski S4 Prop. 6.4 gives the finite-vector argument, p. 31; generic compactness and Gaussian joints are exact PT-7/PT-9 suppliers. |
| `def-normalized-shifted-character-basis-elements` (definition) | In the localization adjoining `(p_1^#)^(1/2)`, for `rho=(1^(m_1)2^(m_2)...)` define `eta_rho=p_rho^#/((p_1^#)^(m_1/2) product_(k>=2)(k(p_1^#)^k)^(m_k/2))`; in particular `eta_(k)=p_k^#/(sqrt(k)(p_1^#)^(k/2))`. | `thm-shifted-character-basis-and-weight-filtration`; `def-joint-convergence-and-normalized-cycle-character-observables` | Check homogeneity in the Kerov filtration and specialize `p_1^#(lambda)=|lambda|=n` to recover each random observable `eta_k^(n)`. | Ivanov--Olshanski §6 preceding Prop. 6.3, pp. 30–31. Exact normalization needed for the Hermite recurrence. |
| `lem-hermite-leading-terms-for-normalized-shifted-characters` (lemma) | The normalized observable `eta_rho` has Kerov degree zero and equals `product_(k>=2) H_(m_k(rho))(eta_k)` plus terms of strictly negative Kerov degree, for the monic probabilists' Hermite polynomials `xH_m=H_(m+1)+mH_(m-1)`. | `def-normalized-shifted-character-basis-elements` | Use the exact top-degree product `p_(k^m)^#p_k^#=p_(k^(m+1))^#+km p_(k^(m-1),1^k)^#+lower`; division by the definition's `sqrt(k)` and `p_1^#` factors gives the Hermite recurrence, and top-degree disjoint union multiplies different part sizes. | Ivanov--Olshanski Props. 4.11–4.13 and 6.3, pp. 23–24,30–31. |
| `thm-kerov-central-limit-theorem-for-normalized-cycle-characters` (theorem) | For every fixed `N>=2`, `(p_k^#/(sqrt(k)n^(k/2)))_(2<=k<=N)` converges jointly to `N-1` independent standard Gaussians; equivalently `(p_k^#/n^(k/2))` has independent centered Gaussian limits of variances `k`. | `def-joint-convergence-and-normalized-cycle-character-observables`; `lem-hermite-leading-terms-for-normalized-shifted-characters`; `prop-plancherel-expectations-of-shifted-character-observables`; `thm-multivariate-method-of-moments-for-a-determinate-limit` | Expand arbitrary mixed monomials in the Hermite basis. Every nonconstant leading term is a normalized `p_rho^#` with `m_1(rho)=0`, hence has expectation zero; negative-degree remainders have expectation `O(n^(-1/2))`. Gaussian Hermite orthogonality gives the same mixed moments, then apply the multivariate moment theorem, whose tightness and uniform-integrability joints pass moments to subsequential limits. | Ivanov--Olshanski Thm. 6.1 and Props. 6.2–6.4, pp. 29–32. Complete finite-dimensional joint-convergence proof. |
| `rem-rsk-longest-increasing-subsequence-consequences-remain-rg11-owned` (remark) | The asymptotic page consumes RSK shape distribution but does not re-mint RSK/LIS identities or Tracy–Widom theory; those need their own analytic/integrable-probability suppliers. | `thm-rsk-shape-of-a-uniform-random-permutation-has-plancherel-law`; `thm-plancherel-young-diagrams-converge-to-the-limit-shape` | No proof section; ownership/boundary record. | Agent 7 §D and cross-source audit. |

### Plancherel Measure and Asymptotic Young Diagrams — Examples (B dependency leaf; `plancherel-measure-and-asymptotic-young-diagrams-examples`)

| proposed id (kind) | exact computation/boundary | deps | verification route | source locator and purpose |
|---|---|---|---|---|
| `ex-plancherel-measure-on-partitions-of-three` (example) | The partitions `(3),(2,1),(1^3)` have probabilities `1/6,4/6,1/6`. | `def-plancherel-measure-on-partitions`; `thm-standard-polytabloid-basis` | Square dimensions `1,2,1` and divide by six. | Agent 7 §D recommendation. Basic probability check. |
| `ex-rsk-shapes-of-all-six-permutations-in-s3` (example) | List all six RSK shape pairs and recover the Plancherel frequencies. | `thm-rsk-shape-of-a-uniform-random-permutation-has-plancherel-law`; `thm-robinson-schensted-correspondence` | Run insertion on each permutation and count shapes. | RG-11 RSK plus Agent 7 interface. Concrete bridge. |
| `cex-plancherel-measure-is-not-uniform-on-partitions` (counterexample) | At n=3 the middle shape has probability four times either extreme shape. | `def-plancherel-measure-on-partitions`; `thm-standard-polytabloid-basis` | Square the dimensions `1,2,1`, divide by six, and compare `4/6` with `1/6`. | Derived from the sourced finite calculation; `ai-generated/ai-generated`, generation role `counterexample`, non-dependency leaf. |
