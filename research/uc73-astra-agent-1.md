# UC-73 Astra agent 1 audit receipt

Date: 2026-09-08. Scope: assigned published items, read individually, with direct prerequisite contracts and targeted proof inspection; not an independent judge or whole-closure certification. Completed: two published items locally repaired, eleven deferred.

Common supplier home S: `classical-affine-varieties-coordinate-rings-morphisms-and-rational-maps-interface`, A order 366.0403, selected in `phase-2-wave-1` by the canonical Phase-2 manifest. The replacement item files inspected are draft, so no release is inferred from page state. Exact replacement contracts are in `research/plan-spec.json` and the AG prose scaffold; canonical old/new map is ledger §AG-P2-1. Mathematical source: Milne, *Algebraic Geometry* v6.10, full PDF https://www.jmilne.org/math/CourseNotes/AG.pdf (accessible full text). Checks for deferred items are read-only contract/proof/status comparisons; precheck/rendercheck are not repair checks and are not claimed.

## 1. cor-dimension-birational-invariant — A-P

Original concern: draft `thm-birational-equivalence-function-fields`. Read entire consumer, that theorem, birational definition, published open-dimension proof and chart-field proof. The conclusion and restriction argument are mathematically sound conditional on those interfaces. Local simplification can remove the affine field theorem by defining birationality through inverse maps and shrinking to inverse isomorphic opens; it does not close the unpublished rational-map interface or the open-dimension supplier's dependency on draft field identifications. Thus defer the prerequisite repair, without claiming the theorem false.

Exact S suppliers: `def-classical-birational-equivalence`, `thm-classical-birational-equivalence-iff-function-fields-isomorphic`; indirect chart repair needs `thm-classical-function-field-independent-of-affine-open` and `lem-classical-dominant-map-pulls-back-function-fields`. Existing replacement birational files are draft. Recommended Phase-3 proof: explicitly shrink inverse rational representatives to common isomorphic opens, then apply repaired open invariance. Source: Milne §5l, Proposition 5.39 (printed p.117). AC is inherited through the field/dimension interfaces; consumer asserts AC but lacks `def-axiom-of-choice`, so Phase 3 must declare it and identify inherited use rather than invent a new choice step. Files changed: this receipt only. No new missing Phase-2 scope identified. Downstream claims are impact candidates, not separately audited defects.

## 2. cor-module-finite-affine-map-quasi-finite — A-P

Read complete consumer and all four direct contracts, including the Nullstellensatz proof. The finite-dimensional algebra argument is valid: any finite collection of distinct maximal ideals yields that many independent CRT residue vectors, bounding the collection size. Empty fibres are handled. Crucially, surjectivity of the point/maximal-ideal correspondence is unnecessary: distinct classical points already yield distinct evaluation kernels, so the direct Nullstellensatz call can be removed locally in a later rewrite. This does not close the draft morphism/coordinate-ring interface behind the module-finite and fibre definitions. No false theorem found.

S suppliers: `thm-classical-affine-nullstellensatz-correspondence` (current direct F4/reduced-fibre interface; draft), `thm-classical-affine-algebraic-sets-reduced-algebras-antiequivalence` (reducible/empty module-finite source and target), `def-classical-affine-variety-morphism`, `def-classical-regular-map-image-and-set-theoretic-fibre` (all S drafts). Repair strategy: retain finite-dimensional CRT proof, use point evaluation injections directly, and bind definitions to the new algebraic-set/morphism interface after publication. This elementary finite CRT argument uses no arbitrary choice; the current blanket AC clause is inherited, and its dependency/use must be made explicit only if the repaired interface still uses AC. Source research was unnecessary for the elementary algebra; the full local CRT proof was checked, not treated as an external black box. Files changed: receipt only; no precheck/rendercheck required or claimed; no new Phase-2 scope. Audit does not certify suppliers' whole closures.

## 3. def-fibre-dimension-at-point-classical — A-P

Read entire definition and its four contracts. The reduced fibre formula is correct, including empty fibre (unit radical, zero quotient). The classical point is closed, continuity makes its inverse image closed, and Nullstellensatz identifies the reduced coordinate ring with the radical quotient. The source/target chart must be affine as stated. The later general-variety definition already addresses the affine/general distinction but itself retains a draft affine-set edge. A local formula proof from pullbacks of coordinate differences is possible; establishing its radical equality still uses the absent affine Nullstellensatz interface, so there is no closed local repair.

S replacements: `def-classical-affine-variety-morphism`, `thm-classical-affine-nullstellensatz-correspondence`; for the general-variety definition's boundary, `def-classical-affine-algebraic-set-with-empty-boundaries`, `def-classical-regular-function-on-open-set`. Replacement morphism contract explicitly allows reducible/empty affine algebraic sets and open sources. All are currently draft in S. The set-theoretic fibre supplier does NOT itself prove the quotient formula; Phase 3 must include the short coordinate-difference plus radical-correspondence derivation. Dimension supplier additionally awaits repaired `lem-classical-variety-noetherian-components` and its exact S finite-components supplier. No independent AC step in taking a fibre; retain/depend on AC if imported Nullstellensatz uses it, identifying that inherited invocation. No unfamiliar mathematics left unresolved; source evidence here is the full local contracts and proof already read for item 2, with Milne PDF available. Files changed: receipt only. No new Phase-2 scope or mathematical counterexample found; local checks read-only, no validation claim.

## 4. def-module-finite-affine-classical-map — A-P

Read the complete definition, old draft morphism dictionary and the replacement coordinate-ring definition and both replacement antiequivalence proofs. The finite-module definition is correct including empty affine sets, but its only declared supplier is draft and the old irreducible-variety language does not by itself provide the advertised reducible/empty interface. Local definition of pullback alone still needs the identification of global regular functions with coordinate-ring elements; importing an unproved dictionary is not local closure.

The precise sufficient S supplier is `thm-classical-affine-morphisms-coordinate-ring-antiequivalence`: despite its title, its displayed bijection explicitly covers ALL affine algebraic sets, including empty ones. `def-classical-affine-coordinate-ring` supplies the zero-ring convention and `def-finite-type-and-module-finite-algebras` the module-finite language. The stronger `thm-classical-affine-algebraic-sets-reduced-algebras-antiequivalence` is available in S but object realization is not needed here. This sharpens item 2's supplier list: that stronger result is optional, not a blocker; use the all-algebraic-set hom-set clause of the morphism dictionary instead. S definitions/theorems are draft. Phase-3 repair: rebind to the sufficient dictionary clause, explain induced scalar multiplication, keep the empty convention. AC is inherited from the chosen global-regular/Nullstellensatz route (no AC needed just to define finite generation); the definition currently asserts it without a dependency. No further mathematical defect found in scope. Files changed: receipt only; no tests or independent review claimed. No new scope.

## 5. def-morphism-to-projective-space-homogeneous-coordinates — A-P

Read entire consumer, all four direct contracts, affine regular-function definition, and the immediate coordinate-morphism lemma. Same-degree tuples, absence of base points and the cross-product overlap criterion are correct. The equality criterion works even when the two local tuples have different degrees: each cross product has the same total degree. Multiplication by a locally nonzero regular function preserves the projective point, though the resulting tuple need not itself be a tuple of global homogeneous polynomials; the sentence should be read as equality of local representatives. No tuple/gluing counterexample was found.

Full-text source consulted: Milne §6h, Remark 6.19 and Proposition 6.20 (printed p.141), and §6g Proposition 6.16 (pp.139–140). The local tuple characterization agrees with this source. The direct old affine morphism definition is draft; the projective regular-function supplier also depends on draft affine regular/function-field definitions. Exact S replacements: `def-classical-affine-variety-morphism`, `def-classical-regular-function-on-open-set`, `def-classical-variety-function-field`; the chart comparison uses `thm-classical-function-field-independent-of-affine-open`. Phase-3 strategy: rebind to those interfaces and justify equivalence with chartwise regular maps by clearing finitely many local denominators and homogenizing to one degree. The projective chart and regular-function lemmas must be repaired in their existing homes, not assumed resolved by the S page release. A purely formal tuple definition would avoid some interfaces but would fail to establish its advertised morphism interpretation, so it is not a sufficient local closure. No independent AC use in finite denominator clearing or tuple compatibility; propagate only AC genuinely used by imported field/regular interfaces. Files changed: receipt only; no precheck/rendercheck or independent review claimed. No additional Phase-2 scope identified.

## Checkpoint requested by parent

Items 1–5 above have completed defect-focused dispositions, all A-P. No published files edited. This is NOT completion of the assignment. Resume with item 6, then proceed one item at a time:

- `def-product-varieties-universal-property`
- `def-projective-closure-affine-set`
- `def-projective-morphism-classical`
- `def-regular-function-projective-variety`
- `lem-classical-variety-noetherian-components`
- `lem-dimension-local-ring-codimension-closure`
- `lem-dominant-affine-image-contains-principal-open`
- `lem-dominant-affine-map-normalization-over-open`

Before resuming, recheck draft/publication states because active workflow authors may have advanced them. The failed screenshot and absent `pdftotext` command did not prevent full-text research: the web PDF text returned the actual cited propositions and surrounding arguments. Do not mistake those failed alternate retrieval mechanisms for an unresolved source blocker.

## 6. def-product-varieties-universal-property — A-P

Resumed after checking this receipt and current files. Read entire item, categorical product and affine/projective-set contracts, and the affine product consumer. The restricted category is legitimate; it deliberately does not assert existence for mixed factors, so the familiar nonprojective/nonaffine mixed-product obstruction is not a defect. The universal property is the correct one for all test objects in that category. Empty and point cases are sound. However regular maps, including projective test objects, are used without a declared adequate general regular-map interface; the listed affine-set definition is still draft. The affine product theorem proves only the affine-test-object version and cannot be used to close the larger category's product construction without a chartwise gluing argument.

Exact S draft suppliers: `def-classical-affine-algebraic-set-with-empty-boundaries`, `def-classical-affine-variety-morphism`, `lem-classical-morphisms-glue-on-open-cover`; general-map meaning comes from repaired existing `def-classical-algebraic-prevariety-regular-maps-and-varieties`. Local set-equation definition can replace the first edge but not adequately establish the regular-map category and its transported structures without duplicating the missing interface. Phase 3: rebind the category definition and prove constructed products' universal properties on source charts, then glue. This definition has no new AC use. Source: categorical contract read in full; Milne §5g product construction full text previously retrieved around Propositions 5.20–5.24. No published edit, no new scope, no test/independent-review claim. The affine product theorem's larger-category compatibility is an impact-review candidate (its own stated affine-category theorem is narrower, not contradicted).

## 7. def-projective-closure-affine-set — A-R

Local repair completed. The old draft affine-set dependency was unnecessary because the definition applies to arbitrary subsets of affine space. Read the full old item, homogenization lemma, projective-set definition, homogeneous-polynomial definition and representative-independence proof. Rewrote with $k^n$, explicit chart injection/inverse, intersection-of-homogeneous-zero-loci closure, and the short closed-set verification. This supplies the missing projective-topology meaning locally, handles empty subset and $n=0$, and removes the unused homogenization edge. The sole dependency is earlier published `def-projective-algebraic-set`; its immediate homogeneous-coordinate prerequisites were inspected. No Phase-2 supplier is necessary for this repaired definition. It is choice-free. No new uncertain mathematics required external research; the intersection/product-equation verification is complete locally.

Changed `items/def-projective-closure-affine-set.md` and this receipt only. Targeted precheck exited 0 with **0 proof-bearing items checked** (definition); rendercheck passed 1 file against real YAML/KaTeX. These are local checks, not independent review. Replaced obsolete 2026-09-06 audited/judge records with a truthful owner-delegated local verification record, per parent instruction. No theorem about its defining saturated ideal is certified by this definition audit.

## 8. def-projective-morphism-classical — A-P

Read full definition and all direct contracts, including relative homogeneous-equations proof. Its factorization definition and reduced closed-immersion meaning are correct for the stated classical category, including empty source/base. The direct draft affine-morphism edge is redundant with a repaired general locally ringed-space definition, but simply removing it leaves the actual affine/general map and mixed product construction debts. The homogeneous-equations supplier's Nullstellensatz clause is unrelated to the product-existence clause used here and is NOT an additional blocker for this definition.

Exact S suppliers for needed interfaces: `def-classical-affine-variety-morphism`, `def-classical-affine-algebraic-set-with-empty-boundaries`, `def-classical-regular-function-on-open-set`, `lem-classical-morphisms-glue-on-open-cover`; the existing general prevariety definition and product construction must consume these in their own Phase-3 repairs. The relative product is supplied by the existing `lem-relative-projective-homogeneous-equations` product clause through `thm-dimension-product-varieties`, not by the restricted mixed-category definition. Local closure would require rebuilding that chart/gluing interface, not merely deleting a citation. Phase 3 retain definition, rebind and make product/closed-subvariety structures explicit. No new AC step; any inherited AC must be documented rather than attributed to the factorization itself. No new source uncertainty or Phase-2 scope. Files changed: receipt only; no validation or independent review claimed.

## 9. def-regular-function-projective-variety — A-R

Read full definition and four original contracts, fraction-field existence proof, and the newly locally repaired earlier projective irreducibility lemma. The two draft affine interfaces are avoidable: the degree-zero fraction construction is intrinsic to the homogeneous coordinate ring. Added its elementary field-law and evaluation checks, using only the choice-free irreducibility clause of the earlier lemma. Also fixed an actual empty-open defect: the old wording identified functions on the empty open with all rational functions vacuously regular there. Now sections are actual locally represented maps to $k$, giving the zero ring on the empty open. For nonempty opens the rational-function identification is proved by irreducibility and vanishing of cross differences on a dense open.

Exact adequate earlier published suppliers: `def-projective-variety-classical`, `def-homogeneous-coordinate-ring`, `lem-projective-irreducibility-homogeneous-prime` (only its choice-free clause), `def-field-of-fractions`, `thm-field-of-fractions-is-a-field-and-the-domain-embeds`. All precede this item in home/order; no Phase-2 supplier or new AC assumption is needed. Source full text: Milne §§6f–g, Propositions 6.15–6.16, read earlier in this audit (printed pp.139–140); the empty-open correction explicitly repairs the usual nonempty-domain convention. Files changed: this item and receipt. Precheck exit0, 0 proof-bearing items checked; rendercheck 1 file passed. Obsolete audited/judge stamps replaced by scoped local verified record. Immediate chart-compatibility consumers need impact review for empty-open language, not automatic defect classification. Item 5's regular-function dependency is now locally discharged, but its affine morphism interpretation remains pending.

## 10. lem-classical-variety-noetherian-components — A-P

Read entire consumer, direct contracts and the draft affine-components proof. The finite-chart chain-stabilization argument is sound. The direct draft finite-components edge can be avoided by Noetherian induction: a minimal closed set without a finite irreducible decomposition splits into two smaller closed sets, contradiction. This would use AC (or DC) to obtain a descending chain if a minimal counterexample did not exist under the sequential definition. More visibly, the current step 2.1 successively selects a new cover member to form a strict ascending chain: that is a real dependent-choice use, covered by authorized AC but currently missing `def-axiom-of-choice` and exact use text. The principal-open affine identification also needs a regular-map/structure-sheaf argument, beyond an equality of underlying sets. These geometric interfaces still have draft prerequisites through the general prevariety definition, so the local topological replacement alone cannot close this item.

Exact S draft suppliers: `lem-classical-affine-algebraic-set-finite-irreducible-components` (optional if replaced by local Noetherian induction), `lem-classical-principal-opens-form-affine-basis`, `thm-classical-principal-open-is-affine-variety`; the general prevariety definition needs `def-classical-affine-algebraic-set-with-empty-boundaries` and `def-classical-regular-function-on-open-set`. The published Hilbert basis theorem suffices for algebraic chain stabilization; no Nullstellensatz is inherently necessary for that part. Phase 3: retain that proof, add the AC declaration and exact countable recursive selection use, and bind finite affine covers to proved principal-open chart isomorphisms. Consumers importing its AC-based quasi-compactness conclusion require AC propagation; mere suppliers sharing a page do not. No new Phase-2 scope or unresolved mathematical uncertainty; no published changes/checks claimed. Scope is the displayed proof and direct interfaces, not whole closure.

The currently authored S principal-open-is-affine theorem covers irreducible varieties and nonzero functions only. For this consumer's reducible/empty affine algebraic sets, Phase 3 must give the graph $D(f)\cong\{tf=1\}$ argument for all sets using the all-algebraic-set local regular-map contract. That extension is elementary and does not require a new Phase-2 supplier.

## 11. lem-dimension-local-ring-codimension-closure — A-P

Read full consumer, all direct contracts and the local-ring and transcendence-degree supplier proofs. The statement is correct because the point is classical closed and ambient variety irreducible. The algebraic height formula is adequate and earlier published. The proof omits an explicit bridge between $\dim A$ and $\dim U$: use already published `thm-affine-variety-dimension-coordinate-ring`, or the algebraic transcendence-degree theorem together with F1. F1 alone is geometric and cannot silently supply the ring-dimension equality. Germ restriction to an open neighborhood is justified by the germ equivalence relation.

Exact S draft replacement of direct debt: `thm-classical-affine-local-ring-is-localization`, supported by `def-classical-germ-and-local-ring` and `def-classical-regular-function-on-open-set`. Its elementary germ/fraction proof can be given inline for an irreducible chart but would still require the unbuilt affine regular/germ and chart-field interface. For nonempty-open dimension invariance the existing F1/F2 path needs `thm-classical-function-field-independent-of-affine-open` (S draft), after repairs to `lem-general-variety-function-field-charts`. Phase 3 retain height argument, explicitly add the ring/geometric bridge, rebind the local-ring supplier, and declare AC inherited from the selected dimension/local interface route with `def-axiom-of-choice`. No independent choice in choosing one neighborhood or evaluating a fixed point; do not misidentify those as AC. No new Phase-2 scope. Files changed: receipt only; no tests or independent certification claimed.

## 12. lem-dominant-affine-image-contains-principal-open — A-P

Read full consumer, normalization supplier and lying-over proof; affine Nullstellensatz correspondence had already been read in full. The argument is sound conditional on normalization and the coordinate dictionary. The direct draft Nullstellensatz edge is gratuitous: a nonzero element of a coordinate ring is, by its definition as polynomial functions, nonzero at some point. The constructed residue homomorphism to $k$ gives a point by evaluating finitely many coordinate classes and checking defining equations; no surjectivity theorem for all maximal ideals is needed. The integral/finite residue field argument is correct, as is $r=0$.

Remaining real prerequisite is normalization's affine-map/function-field interface. Exact S draft suppliers: `thm-classical-affine-morphisms-coordinate-ring-antiequivalence`, `thm-classical-polynomial-functions-equal-coordinate-ring`, `def-classical-variety-function-field`, `lem-classical-dominant-map-pulls-back-function-fields`; these support the needed affine clauses, without requiring unrelated general-variety results. Old direct `thm-affine-nullstellensatz-correspondence` need not map to a new Nullstellensatz blocker for this proof. Phase 3 remove that call and retain direct coordinate evaluation, after normalization is repaired. Add `def-axiom-of-choice` with exact use: lying over invokes existence of a maximal ideal in a nonzero localized ring, proved using AC; no simultaneous choice of lifts for all $y$ is required. No new scope. Files changed: receipt only, no checks claimed. This is a prerequisite defect, not a false image theorem.

## 13. lem-dominant-affine-map-normalization-over-open — A-P

Read complete consumer, all direct supplier contracts, normalization module-finiteness proof and its induction supplier. Full-text authoritative comparison: Stacks §10.115, Lemmas 10.115.4 and 10.115.7, https://stacks.math.columbia.edu/tag/00OW (including its denominator-clearing proof). The algebra argument is valid: finitely many normalization elements and monic-equation coefficients have finitely many denominators in the base domain. After inverting their nonzero product, bounded monomials in the original generators span. Independence descends from the fraction field, and finite domain extension gives algebraicity of fraction fields, identifying the number of variables with relative transcendence degree. No missing generic-freeness hypothesis is required.

The direct draft principal-open-coordinate-ring theorem is unnecessary for the displayed **algebraic** conclusion about $B_a$; it is used only in the final extra geometric identification. Remove that line/edge if keeping the current statement. For its title's geometric factorization interpretation, use S `thm-classical-principal-open-coordinate-ring-localization` and `thm-classical-principal-open-is-affine-variety` (draft). Necessary affine-map/function-field prerequisites remain S drafts `thm-classical-affine-morphisms-coordinate-ring-antiequivalence`, `thm-classical-polynomial-functions-equal-coordinate-ring`, `def-classical-variety-function-field`, `lem-classical-dominant-map-pulls-back-function-fields`. The stronger general chart-field theorem is not needed for this affine-only assertion; its unrelated general-variety components must not delay this supplier once the affine interface is published. A purely algebraic strengthening for an injective finite-type domain map would close the algebra part now, but replacing the geometric map-to-ring premise without proving its meaning would not repair the advertised result.

Phase 3: use that affine-only interface, explicitly cite already published `thm-integrality-and-finite-module-equivalences` for module-finite implies integral (or give determinant trick locally), clear denominators, and use the displayed bounded-monomial proof. The finite choices here need no arbitrary AC; any inherited AC comes from the chosen geometric dictionary and must be declared accurately. No new Phase-2 supplier or unknown mathematics remains. Files changed: receipt only; no validation or independent review claimed.

## Completed assignment disposition

All 13 assigned items have now been individually audited. A-R: items 7 and 9. A-P: items 1–6, 8, and 10–13 (11 items). No reviewed/no-repair verdicts. Two published files changed, each with local rendering verification; no engine/shared-plan/ledger edits. Parent owns canonical integration, including exact two new dependency arrays and later consumer AC propagation. No additional Phase-2 scope is required by this audit; the reducible principal-open graph extension is a precise local Phase-3 obligation. These findings certify neither the full dependency closures nor all downstream consumers.
