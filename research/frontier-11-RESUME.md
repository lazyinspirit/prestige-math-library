# frontier-11 — RESUME

**Verify disk state before trusting any line here.** Run prefix is `frontier-11`,
never a level-numbered prefix: this run's pairs span dependency levels 18–22.

## Objective

Owner, 2026-08-11: **build the next set of non-topology A/B pairs.** Orchestrate
steps 0 → 10, stop at the step-10 owner pause. Nothing here publishes. Owner also
asked for a stuck-check about every 15 minutes.

## Current step

**Steps 0–4 COMPLETE and committed. Step 5 (authoring) is the next action.**

Alpha's step-4 prose propagation was dispatched at ~10:12 on 2026-08-12 and was
still running when the session ended. **It was probably killed on session exit**
(dispatched agents are child processes of the session on this machine; there is
no systemd here). It is cheap and safe to re-run — it only writes
`research/plan-*.md` and was explicitly forbidden to touch `items/`, `library/`,
`plan-spec.json` or any batch artifact. **Check for
`research/frontier-11-dispatch/alpha-step4.result.json`; if absent, re-dispatch
(command below).**

## Permissions — read this before anything else

`.claude/settings.json` was written with blanket `Bash`/`Read`/`Edit`/`Write`/
`WebSearch`/`WebFetch` allow rules and `defaultMode: "bypassPermissions"`. Project
settings load at startup, so a **restarted session should no longer prompt**.
`.claude/settings.local.json` is regenerated by the runtime and CANNOT be used for
this — writing blanket rules there gets discarded. `dangerouslyDisableSandbox`
suppresses sandboxing, not the permission check.

Dispatched agents never prompt: Codex lanes get `--ask-for-approval never`, the
claude lane `--permission-mode bypassPermissions`.

## What is done

| step | state |
|---|---|
| 0 | 9 non-topology pairs, 5 batches, frontier computed from disk. Page-level cross-batch seams **0**; item-level correctly reported as not-yet-computable, not 0. |
| 1–2 | All 5 Betas scaffolded. Gates green. |
| 3 | Alpha review → **5 of 9 pairs `insufficient`** → all Betas fixed → Alpha re-check → **all 9 `sufficient`**, no locator fails. |
| 4 | Splice applied, D1 executed in plan-spec, both gates pass (`validate-plan` OK; `content-policy` 239 items / 0 errors). |

**239 items across 9 pairs; 508 harvested source headings.**

| pair (order) | A+B |
|---|---|
| field-extensions-and-the-complex-numbers (54) | 21+7 |
| conjugacy-and-simplicity-in-the-symmetric-groups (64) | 18+8 |
| the-determinant-of-a-linear-operator (84) | 20+8 |
| the-integral-logarithm-and-its-characterisations (178.1) | 19+5 |
| arc-length-and-rectifiable-curves (181) | 15+7 |
| formal-power-series (193) | 22+9 |
| extremal-graph-theory (219) | 20+9 |
| the-fundamental-theorems-of-calculus (239) | 10+6 |
| universal-properties-and-the-yoneda-lemma (361) | 21+14 |

## Exact next actions

1. If `alpha-step4.result.json` is missing, re-dispatch:
   `node tools/dispatch.mjs --role alpha --brief research/frontier-11-brief-alpha.md --label step4 --run frontier-11 --task research/frontier-11-alpha-step4.task.md --timeout 10800 --json`
2. **Step 5 authoring.** Brief is written and ready:
   `research/frontier-11-brief-authoring.md`. Dispatch one Beta per batch, same
   Beta that scaffolded it (ownership rule, owner 2026-07-31):
   `node tools/dispatch.mjs --role beta --brief research/frontier-11-brief-authoring.md --label batch-<i>-author --run frontier-11 --var i=<i> --task research/frontier-11-beta-<i>.task.md --timeout 10800 --json`
   Beta lane cap is 5, so all five run in parallel.
3. Gate step 5: `node tools/gates.mjs --step 5 --run frontier-11 --json`.
   Note the contract trio runs with `reviewed:false` at step 5 — a `risk_review`
   is Alpha's record, written at step 6. Do not demand it at step 5.
4. Step 6: independent readers (never a batch's own author) + Alpha refuters.
5. Step 7 judge sweep — supply **every A page in the run** to `--pages`; the
   sweep adds B companions itself. `preflight --judges` = READY, both lanes
   validated with a real call.
6. Steps 8 (fatal-only, R1, `touchlog` baseline then `step8-guard`), 9, 10.

## Owner decisions on record — `research/frontier-11-step3-decisions.md`

- **D1/D12 (OWNER, re-decided 2026-08-12): deliberate material rewrite** of four
  published ℂ items, moving them from `the-complex-exponential-and-eulers-formula`
  (189) to `field-extensions-and-the-complex-numbers` (54), ids unchanged.
  My first framing was **wrong** — I told the owner it was citation-neutral;
  Alpha proved it changes titles, dependency sets and the construction of ℂ
  (plane ℝ² → stem field ℝ[x]/(x²+1)). A pure relocation is **impossible**:
  published `lem-complex-conjugation-and-modulus-laws` takes the triangle
  inequality from order-167 p-norm machinery that order 54 cannot cite; the
  rewrite derives it from Lagrange's identity with inputs at order ≤ 9.
  Receipt `research/frontier-11-rehomed.json`; baseline snapshot
  `pre-d1-rewrite` in `research/frontier-11-touches.json`.
  **At step 5 the four are rewritten in place with `verification.audited` and any
  `verification.judge` DELETED, precheck re-run, `status: draft`.**
  **Do not edit `library/real-analysis/the-complex-exponential-and-eulers-formula.md`
  until the step-10 publish commit** — the split state (plan says 54, disk says
  189) is intended and receipted.
- **D2** `roots-and-rational-powers` added to order 54's requires. Applied.
- **D4** batch 2's Lebesgue-FTC / Banach–Zarecki / Henstock–Kurzweil declines
  approved — each needs a whole absent integration theory.
- **D6** records an orchestrator error: my batch-4 brief wrongly said order 82
  owns cofactors/adjugate. It does not; the Beta verified per item and built them.
- **D8** RA-27b B-page citation replaced by its A-page source. **A deviation from
  the letter of the owner's design — must appear in the step-10 report.**
- **D9** batch 5 prose amendments, incl. replacing a CT-2 clause false as written.
- **D10 FOR THE OWNER, not this run:** no field-of-fractions construction exists
  anywhere in library or plan, yet published `cex-ordered-field-not-archimedean`
  and `ex-rational-function-field-order` both open "Given: … the field of
  fractions of ℝ[t]". Natural home is orders 46–50.
- **D11** `dispatch.mjs` claude lane `acceptEdits` → `bypassPermissions`.
  `acceptEdits` still prompts for `WebFetch`, which silently cost Alpha the entire
  source-faithfulness criterion at step 3.

## Standing scope obligation — NOT yet dischargeable

`the-integral-logarithm-and-its-characterisations` (178.1/178.2), batch 4.
`LEVELS.md` §0.5 requires **all three** RA-27b gaps; the scaffold has all three
(integral-first `L(x) := ∫₁ˣ dt/t` with no `exp` in its proofs and a single
bridge citation of `thm-exponential-ivp-uniqueness`; the functional-equation
characterisation; the equivalence theorem + roadmap remark). **Two of three does
not discharge it.** Strike the obligation from `LEVELS.md` in the same commit that
publishes.

## Open risks

- **B3, unresolved:** Apostol's internal numbering (Definition 6.16, Theorems
  6.17–6.20) could not be verified — full-text sources returned 403/401. Batch 2
  harvest rows citing those numbers are unconfirmed at item granularity.
- Batch 3's Flajolet–Sedgewick leg is **under-enumerated** (not false).
- `run-wave.test.mjs` A10 failure is **pre-existing** on a clean tree.
- 308 `redundant-prereq` from `validate-plan` are **non-fatal and pre-existing**
  (304 before this run's splice); the tool exits 0 on them.

## Fixed — do not re-diagnose

- `dispatch.mjs` claude lane: `bypassPermissions` (D11); and `OPUS_MODEL` is
  `claude-opus-5[1m]` — the CLI has no window flag, the `[1m]` id IS the 1M window.
- App repo `web/node_modules` installed. **Without `katex`/`yaml`, `rendercheck`
  SKIPS SILENTLY** rather than failing.
- `research/frontier-11-splice.mjs` is idempotent: its re-home guard asserts the
  END STATE, not the delta. Re-run it freely after any batch repair.
- `touchlog.mjs snap <ledger> <label>` takes the label **positionally**; a
  `--label` flag is recorded as the literal snapshot name.

## Roles

Betas = GPT 5.6 Sol, xhigh, 1M, workspace-write, web on, cap 5. Alpha = Claude
Opus 5 `claude-opus-5[1m]`, xhigh, cap 1. Step-6 readers + Alpha's refuters = Sol
(refuters read-only). Judges = DeepSeek V4 Pro + GPT 5.6 Terra,
`JUDGE_LINEUP=deepseek+terra`, pools 16 each / 32 combined. `DEEPSEEK_API_KEY` is
in `/Users/ianx/Projects/prestige-intelligence/.env`.

A run brief must not contain a literal `<n>` or `<k>` — `dispatch.mjs` refuses it
and exits 2 having written only a one-line `.out`. The proof a dispatch really
started is its `<role>-<label>.prompt.md` file.
