# frontier-25 step 9 lead review

Resolved the 18 pending rows from `research/frontier-25-step9-scope-delta.json`
without a coverage, manifest, page, item, contract, splice, or impact edit.

- Group `a`: all eight pending NT-9 rows now `stand`. The current authored page
  still closes at the finite Dirichlet-convolution package plus the divisor,
  Liouville, von Mangoldt, and Mertens functions, so Shoup's two extra
  squarefree identities, Crisman's omega-function definition, Crisman's
  ring-theoretic forward remark, and the already-published Möbius special case
  remain honest exclusions. Sanders `Propositions 1.3-1.5` also still belong to
  the exact next page `average-orders-divisor-sums-and-representation-counts`
  at order `348.003`, where NT-10 begins the harmonic-sum, hyperbola-method,
  and average-order asymptotic work.
- Group `d`: all ten pending TC-9 rows now `stand`. The current page still owns
  only the base one-tape semantics, computation histories for a fixed machine
  and fixed input, configuration-graph reachability, and the two local false
  statements. Savage `Theorems 5.2.1-5.2.2`, Savage `Definition 5.3.2`, and
  Open Logic `14.7 Disciplined Machines` remain exact deferments to
  `robust-machine-models-and-universal-computation` at order `599`, while
  Sipser's PCP and ALLCFG undecidability theorems remain exact deferments to
  `post-correspondence-and-language-undecidability` at order `607`. The unary
  encoding convention, Savage's acyclicity hypothesis, and the two ELBA
  theorems remain honest out-of-scope exclusions on the August 31, 2026 disk.

No decline was overturned, so no in-scope result was added and no authored
content changed after `post-step8`.

Terminal defect sweep: `research/defect-ledger.jsonl` currently has zero open
rows for `frontier-25`, so no ledger row required closing or deferring and no
ledger render was triggered.
