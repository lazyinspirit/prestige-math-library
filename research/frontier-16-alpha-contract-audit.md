# Frontier-16 Alpha contract audit — `contract-audit-2`

## Recovery and scope

I listed `research/frontier-16-dispatch/` before re-deriving the failure. There is no earlier `contract-audit` result or log on disk; `alpha-contract-audit-2.prompt.md` is the first artifact for this lane. I therefore recovered the predecessor state from `.autopilot/events.jsonl`: the engine's join found `finite-smoke` vacuous at 0 checks while proof-contract, coverage-checklist, and precheck had live scopes. No prior mathematical disposition was available to inherit.

The audit read the seven owning batch contract files, the merged contract, the relevant frontier item and its cited inputs, `QUALITY-CONTROLS.md`, and the finite-smoke registry implementation. Item text is unfrozen. This pass changed no item or page text.

## Boundary detector

`boundary-audit.mjs --fail-on-contradicted --fail-on-template` examined 2,168 boundary rows in the seven batch contracts. It emitted no template-reuse candidate and no contradicted-disposition candidate. There was therefore no detector-emitted row to uphold or annotate with `reviewed`.

The required independent read nevertheless found two false worksheet rows that the regex detector missed in `ex-deck-groups-of-connected-circle-coverings`. Alpha verified both against the current item:

- The `one` row cited step 2.1, but that step treats only $n=0$; step 1.1 treats $n=1$ and identifies the trivial deck group. I corrected the anchor to step 1.1.
- The `endpoints` row called the fixed base circle an endpoint discharge. The Example and Verification quantify no interval, path endpoint, or endpoint parameter, so the truthful disposition is a specific `not_applicable` reason. I replaced the false checked row.

These contract-only corrections are recorded separately as `f16-contract-audit-002` and `f16-contract-audit-003`. The final boundary audit still has 2,168 rows, now with 838 `not_applicable`, and emits no candidate.

## Citation-fidelity detector

`citation-fidelity.mjs --fail-on-missing-quote` examined 1,223 citations over 271 authored items. Every recorded quote occurs in its cited item and no widening candidate was emitted. There was therefore no citation row to uphold, repair, or annotate with `reviewed`.

## Finite-smoke liveness

The zero-check result was real under-selection, not registry inapplicability. The level contains finite claims about polygon triangulations, finite sigma-algebras, and finite-sheeted coverings. The graph, poset, recurrence, polynomial, and matrix registry checks do not directly probe the first two classes, so I did not attach an unrelated toy check to them.

`ex-circle-covering-towers-follow-divisibility` has a direct match. Its Example asserts:

> If $m=nq$, then this morphism has $q$ sheets; in particular, $q=1$ gives a based covering isomorphism.

Step 3.1 identifies the relevant fibre size with $[n\mathbb Z:m\mathbb Z]=q$. For every bounded divisor pair $n\mid m$, `cyclic-subgroup-lagrange` independently computes the subgroup generated by $n$ in $\mathbb Z/m\mathbb Z$; that subgroup is $n\mathbb Z/m\mathbb Z$ and has $m/n=q$ elements. Thus the check probes exactly the finite-index calculation used for the asserted sheet count. I added that obligation to the item's entry in `research/frontier-16-batch-6.proof-contracts.json`, with the exact item excerpt and the step-3.1 connection, then regenerated `research/frontier-16-proof-contracts.json` from all seven owning files.

The resulting bounded check passes through modulus 24:

```text
PASS [ex-circle-covering-towers-follow-divisibility] cyclic-subgroup-lagrange: checked cyclic subgroups of Z/nZ through n = 24
finite-smoke: 0 error(s), 1 check(s) over 1/271 item(s) carrying obligations
```

This is bounded falsification evidence for the index calculation, not a proof of the covering-space theorem.

The under-selection is recorded as `f16-contract-audit-001` in `research/defect-ledger.jsonl` (`gate-vacuous`, nonfatal, fixed); `research/DEFECT-LEDGER.md` was regenerated by the append action.

## Risk review

`risk-report.mjs research/frontier-16-proof-contracts.json --require-reviewed` reports 0 errors over 271 routed items. It named no `risk-review-missing` item, so this round had no missing Alpha disposition to write.

## Rechecks

- Merge: 271 scoped items from seven batch contracts.
- `proof-contract --strict`: 271/271 checked, 0 errors, one existing `shotgun-bracket` warning on `thm-weierstrass-convergence-holomorphic-functions`.
- Boundary audit: 2,168 rows, 838 `not_applicable`, no candidate after the two independently found row repairs.
- Citation fidelity: 1,223 citations, no candidate.
- Finite smoke: one selected check, pass.
- Coverage checklist: 579 harvested results, 0 errors and four existing low-yield warnings.
- Precheck: 4,540 checked, 0 failing.
- Defect-ledger validation for `frontier-16`: 173 rows, 0 errors.

## Local gate-liveness limitation

The `gate-liveness.mjs` wrapper itself cannot complete in this Codex workspace sandbox: each of its internal `spawnSync('node', ...)` calls is refused with `EPERM`, so it reports all four probes as `unparsed` even though the same four commands run directly and produce the live counts above. No escalation or permission prompt was attempted. The remaining action is for the engine's normal stage process—whose earlier run successfully parsed these probes—to rerun `gate-liveness` against the merged contract and confirm the repaired count of one. No content, contract, or mathematical blocker remains.

## Refuter

The read-only refuter opened `ex-deck-groups-of-connected-circle-coverings`, all ten declared dependencies, its contract entry, and the cyclic-subgroup registry check. It accepted the Example, Facts, Verification, and citation fidelity: no concrete false claim, unlicensed inference, missing hypothesis, scope/quantifier error, or inaccurate citation was found. It reported the two false boundary rows adjudicated above. It also confirmed that `cyclic-subgroup-lagrange` is a genuine bounded test of finite cyclic order, while correctly limiting its force: it does not establish the covering-space/deck-group isomorphism. The refuter made no edit; Alpha alone applied and gated the worksheet repairs.
