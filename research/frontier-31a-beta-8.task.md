# Batch 8 of run `frontier-31a` — Ext and Balanced Resolutions · Yoneda Extensions and Homological Dimension

2 A/B pairs, homological-algebra. You own every page here and you author them all at step 5.

## `ext-and-balanced-resolutions`

| | |
|---|---|
| A page | `ext-and-balanced-resolutions` · order **365.051** · `homological-algebra` |
| B page | `ext-and-balanced-resolutions-examples` |
| title | Ext and Balanced Resolutions |
| requires | `delta-functors-and-universality-examples` |
| design | `research/plan-homological-algebra-track.md` L1661<br>`research/plan-homological-algebra-track.md` L1780 |

**More than one design location matched.** Open each before scaffolding: a track document and a completion/expansion document often both speak about this page, and where they differ the later amendment usually controls. Record in your notes which one you treated as controlling and why.

**Open the design section and hold yourself to what it states.** The traps, the decided conventions and the chosen route for this page are written there, by whoever designed it, with the sources to hand. They are not repeated here: this file is generated and has read no mathematics. A scaffold that contradicts a decision the design records is wrong however rich it looks.

Check the design's stated `order` and prerequisites against `research/plan-spec.json` above. **If they disagree, do not pick one** — record it in your notes and carry on with the spec. Stage 1's `drift` unit adjudicates design-vs-spec conflicts for the whole run, and settling one inside a batch is how two batches end up built against two different plans.

## `yoneda-extensions-and-homological-dimension`

| | |
|---|---|
| A page | `yoneda-extensions-and-homological-dimension` · order **365.053** · `homological-algebra` |
| B page | `yoneda-extensions-and-homological-dimension-examples` |
| title | Yoneda Extensions and Homological Dimension |
| requires | `ext-and-balanced-resolutions-examples` |
| design | `research/plan-homological-algebra-track.md` L1814<br>`research/plan-homological-algebra-track.md` L1957 |

**More than one design location matched.** Open each before scaffolding: a track document and a completion/expansion document often both speak about this page, and where they differ the later amendment usually controls. Record in your notes which one you treated as controlling and why.

**Open the design section and hold yourself to what it states.** The traps, the decided conventions and the chosen route for this page are written there, by whoever designed it, with the sources to hand. They are not repeated here: this file is generated and has read no mathematics. A scaffold that contradicts a decision the design records is wrong however rich it looks.

Check the design's stated `order` and prerequisites against `research/plan-spec.json` above. **If they disagree, do not pick one** — record it in your notes and carry on with the spec. Stage 1's `drift` unit adjudicates design-vs-spec conflicts for the whole run, and settling one inside a batch is how two batches end up built against two different plans.

## Standing, for every pair here

- **Two independent treatments**, at least one a textbook, monograph or full lecture-note set with a harvestable table of contents. Wikipedia is a convention tiebreaker, never a pair's backing.
- **Enumerate each source's own section and named-result headings** over the exact range you read, and give every harvested heading a disposition: `included` (naming the item id), `inline`, `already-published`, `deferred` or `out-of-scope` — the last two with a written reason about that specific result.
- **Self-contained scope.** No item may rest on a result the library has not established. If a needed result is missing, build the prerequisite definitions and theorems; dropping it for want of a lemma you could have written is not a permitted disposition.
- **An A page over 60 items is SPLIT**, decided now. `validate-plan.mjs` enforces it, and after authoring a split is a rewrite.
- **Never pad.** Decompose long proofs into focused lemmas and make a pass for cheaply proved corollaries; never mint a statement to enrich a page.

**No permission prompts of any kind**, including inside an `&&` chain.

## Owner-authorized recovery continuation

The original author left placeholder scaffold prose. The first recovery
repaired many core Ext and homological-dimension carriers but explicitly
reported itself incomplete; 28 item files still contain the generic template
statement/proof phrases. Continue from the current files and replace every
remaining placeholder with a precise mathematical statement and proof. Review
the whole batch, regenerate any proof-contract rows made stale by the repairs,
and do not return a completion receipt until focused precheck, rendercheck,
content policy, and strict proof-contract checks pass for the full batch.

### Remaining leaf recovery

The second recovery removed the original statement/proof templates but left 27
duplicate non-proofs: 13 `ex-` items all say only to construct and substitute,
12 `fs-` items all say only that definitions distinguish the claim, and the two
`cex-` items name no actual counterexample. Replace each of those 27 bodies with
its own explicit objects, maps, calculation, or witness. They are precisely the
three duplicate-body groups reported in the current
`research/frontier-31a-alpha-b-6b.md`; do not preserve a shared generic proof
body merely because phase-format checks accept it. Regenerate the full contract
afterward and report incomplete if any of those 27 carriers remains generic.
