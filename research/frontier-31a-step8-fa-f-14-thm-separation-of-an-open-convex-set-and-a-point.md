# Final adjudication — queue position 14

Disposition: repaired. Source status: familiar.

Read the full item, all six complete direct dependencies, batch-3 A/B and manifest/coverage conventions, proof/boundary contract, Alpha repair and original/final Terra rejections. The contract's exact F2 source quotation explicitly begins Assume the Axiom of Choice, while the item omitted that hypothesis. The final rejection is justified. Added AC to Statement, Given and F2, and recorded it in the nonempty-choice boundary. No dependency edit is necessary.

Independent argument: translate u0 in U to obtain an open convex zero-neighbourhood V and nonzero y outside it. Its finite nonnegative gauge p is sublinear, with p(y)>=1 and p(v)<1 on V. On the real line through y, g(ty)=t is dominated: positive t uses homogeneity and negative t uses nonnegativity. The assumed AC licenses the supplied dominated Hahn-Banach extension h with h(y)=1. If the radius-r ball is inside V then p(plus or minus z)<=norm(z)/r, giving |h(z)|<=norm(z)/r. I spelled out this infimum estimate. In the complex case f(z)=h(z)-i h(iz) is complex linear by the exact reconstruction lemma, has real part h, and is bounded because norm(iz)=norm(z). h(y)=1 makes f nonzero, now explicitly stated. For u=u0+v, Re f(u)-Re f(u0)=h(v)<1=h(y), which proves the stated pointwise strict separation. It does not assert a uniform positive gap at boundary points.

The scalar and gauge calculations and the use of the explicitly conditional library theorem are familiar; no external source verification was needed. No claim that full AC is logically necessary for every formulation is made: it is the sufficient hypothesis required by this chosen local dependency. Updated this item's local/aggregate contract inputs and choice boundary. No separate risk_review exists for this contract. Earlier batch-3 decisions remain valid with this sibling's explicit assumption; refresh receipts.

Focused precheck, strict proof-contract and rendercheck pass. No prerequisite licence, judge stamp or unresolved obligation. Next: record, then position 15.
