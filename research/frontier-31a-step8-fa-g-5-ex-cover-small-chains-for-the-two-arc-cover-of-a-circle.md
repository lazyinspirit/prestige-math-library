# FA terminal evidence — queue g/5

Item: ex-cover-small-chains-for-the-two-arc-cover-of-a-circle. Decision: repaired.

Read the item and its three direct dependencies (def-cover-small-singular-chain-subcomplex, lem-every-finite-singular-chain-becomes-cover-small-after-enough-subdivision, def-mayer-vietoris-connecting-class), batch-12 A/B context, manifest and coverage, batch/merged contracts, reader/refute-12, group-g conventions, Alpha's adjudication/repair, and Terra's initial/final rejection rows (2026-09-05T18:50:48.491Z and 2026-09-05T21:37:05.173Z).

Alpha correctly replaced cuts at excluded arc endpoints by points inside the two overlap components. With these points, the two complementary closed subarcs are carried by U and V respectively; their endpoints belong to both. The final Terra objection correctly notes that an arbitrary abelian group has no chosen element 1. The page fixes G but cannot implicitly select its coefficient. I preserved arbitrary G and supplied g in G explicitly. The integral paths are tensored with g; their boundaries are opposite signed endpoint differences. The connector is the component pair (-g,g), not an unspecified generator of G. The zero coefficient and sign reversal are explicit. The integral two-edge loop traverses the circle once and is the oriented cycle from which these coefficient chains are obtained. No claim that v=-u as singular chains is made; the old contract's erroneous shorthand was removed.

Verified authoritative Hatcher Chapter 2, Mayer–Vietoris Sequences, printed pp.149–150 and coefficient conventions p.153:
https://pi.math.cornell.edu/~hatcher/AT/ATch2.pdf
The source supports the connector computed from the U-boundary and arbitrary abelian coefficients. The open-arc geometry and tensor boundary calculation are independently checked above, without attributing the chosen explicit paths to a named source example.

Changed only the queued item and its batch/merged proof-contract entries. Existing dependencies and manifest scope remain correct; no dependency edit or prerequisite licence. Focused precheck and both strict contract checks each pass (1/1, zero errors/warnings). The clarified example is consistent with the cone and MV conventions and with sphere/suspension results; positions 1–4 are resealed solely for shared A/B context currency. No unresolved obligation, judge call, or pass stamp. Next action after recording: position 6.
