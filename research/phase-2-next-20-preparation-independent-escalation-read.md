# Preparation: independent escalation read

2026-09-11. Second Astra medium reader, owner-authorized local review. This is not an acceptance receipt or a certification of all transitive suppliers.

Read the complete current preparation target, complete `phase-2-next-20-preparation-owner-candidate.md`, complete current Laver-function theorem, and complete Boolean generic-truth and ZFC/ordinal-preservation lemmas. Read Hamkins, *The Lottery Preparation*, the full support definition and Lemmas 3.1–3.2 on printed pages 24–26 and full Corollary 4.6 on printed page 40 (PDF page 40, zero-based index 39), at https://arxiv.org/pdf/math/9808012. The existing local extraction was checked against the accessible primary PDF. No new recovery attempt for the exhausted original Laver source was made, and no full-paper reading is claimed.

The candidate's directed-closed Laver iteration, long gap, two lifts, master condition and bounded measure descent form a viable conditional route. This read found no counterexample to those arguments. It does not close the canonical target: the common forcing foundation remains an explicit mathematical premise.

## Required completion and precise checks

1. Current `lem-lc-boolean-generic-truth` and `lem-lc-boolean-generic-zfc-and-ordinals` explicitly concern supplied transitive **set** models. Their conclusions do not by themselves supply the fixed-formula forcing/truth schema used for the universe and definable transitive class ultrapower target. Author that extension, or a direct formula-by-formula Boolean forcing proof, before using elementarity to transfer forcing assertions. The argument must not infer a consistency theorem from the existence of a supplied transitive set model.

2. Author the Boolean mixing/maximum principle with a set of witnesses obtained by Collection before using AC. For an existential join, take a maximal antichain refining nonzero attained matrix values below the join, choose one witness name for each antichain member from a bounded set of witnesses, and mix those names. State and prove the mixing equality on each antichain coefficient. This is an additional algebraic interface; semantic axiom preservation alone does not establish it.

3. Give dense-presentation/regular-open invariance and two-step name translation explicitly. At the small prefix used in supplier A, M and V must have the **same** dense preorder S, not merely similarly named versions of P*qdot. A presentation by pairs (p,alpha) from the common ordinal enumeration name is suitable once the membership and order assertions for this presentation are shown to agree. Do not claim arbitrary forcing statements are absolute between M and V.

4. Resolved bounded-name repair for supplier A: replace the unbounded phrase 'a condition forcing that f is a sequence of M[K]-elements' by a bounded-name argument. First justify a single ordinal rank bound and the ground set T of eligible M-names; then use the ordinary forcing assertion that every value is the valuation of a member of T. Explain the rank bound, for example via rank-bounded name normalization. This avoids silently treating M[K] as an available predicate in the extension, or using Collection on a class of names without a definability argument. The same correction applies to its common closed-forcing clause.

5. Supplier B's uniform support argument addresses the actual direct-limit difficulty: at an inaccessible delta strictly above the prefix size bound, an antichain of at most that size cannot make possible support bounds cofinal in delta. Retain this check when transposing names. At or below the cut, the relevant tail has no coordinates; after the anticipated stage the marker makes every coordinate through chi trivial. The directed-family lower-bound recursion must keep a common stronger prefix so that the original directedness witnesses remain forced valid. The candidate does this.

6. Keep the explicit directed-closure proof, rather than importing directed closure from Hamkins's strategic-closure setup. His lottery version and the candidate's stage definition differ. The candidate's actual directed-closed stages and bounded support unions provide its needed closure independently.

7. The master set has to be an internal set before internal directed closure is applied. The candidate's enumeration formula using e, j(e), H and j restricted to nu supplies it and bounds its internal size by nu<j(kappa). This is stronger evidence than saying merely that an image of a generic filter is directed.

8. Retain the double name-count bound for the power set of the **new** P_kappa(lambda), and descend the measure as a subset of that bounded ground-of-the-temporary-forcing set. It is insufficient to bound only old subsets of lambda, or to leave the derived embedding in an outer extension and assert that this alone witnesses supercompactness in W. The candidate explicitly avoids both errors.

The remaining task is to author and check these common forcing interfaces and integrate the candidate's local lemmas as actual content and contracts. No acceptance, owner-hold replacement, controller command or canonical mathematical edit was made by this reader.

## Exact replacement for A's bounded-name step

This argument is conditional only on FT and ordinal preservation already requested. For any common preorder S with a top, recursively in M define T_0=empty, T_(alpha+1)=P^M(T_alpha×S), regarded as coefficient names, and T_delta=union_(alpha<delta) T_alpha at limits. For every supplied generic K, the valuations of T_alpha are exactly V_alpha of M[K]. Prove this by induction. At a successor, all displayed names evaluate to subsets of the previous rank. Conversely, for any such subset x in M[K], take one M-name sigma for x and form the name consisting of (u,p) with u in T_alpha and p forcing u in sigma internally in M. This is one of the displayed subset names; internal truth shows its valuation is x. Limit ranks use unions. This requires no selection of a name for every x simultaneously.

The name dotT_alpha={(tau,1):tau in T_alpha} belongs to M and names V_alpha of M[K]. Given the actual sequence f in W[K], choose a ground ordinal alpha greater than its rank; ordinal preservation supplies the ordinal, and transitivity makes ranks of its entries agree in M[K] and W[K]. All its entries lie in the valuation of dotT_alpha. Truth in W now gives p in K forcing this ordinary set-coded assertion. There is no M[K] predicate in that assertion.

For small S, take antichains below p deciding each f(i) as the valuation of some tau in T_alpha. Membership in dotT_alpha makes these decisions dense by FT. Complete antichains away from p with a fixed empty-name fallback if desired. There are at most chi selected pairs across chi indices, all elements of M; ambient ground chi-closure puts their indexed family into M. Mix there and form the sequence name. The resulting name evaluates to f below p.

For common externally at-most-chi-closed R, perform the same decisions successively below any extension of p, using lower bounds at limits and once after chi coordinates. All selected names lie in T_alpha, so their chi-sequence belongs to M by ground closure. Its canonical sequence name is in M, and the final condition forces equality to f. Such final conditions are dense below p, hence the common generic meets them. This proves the second closure assertion without defining ground M in the forcing extension. The external closure of R before forcing follows because every descending chi-sequence of its conditions is a ground W-sequence of M-elements, hence belongs to M and has an internal lower bound.
