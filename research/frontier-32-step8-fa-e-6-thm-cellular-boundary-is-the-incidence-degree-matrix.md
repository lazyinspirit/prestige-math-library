# FA terminal evidence — queue e, position 6

Decision: repaired. Independently read the current proof and all three dependencies, the CW A/B context and batch-10 conventions/manifest/coverage, its proof contract and generic risk boundaries, Alpha's degree-zero adjudication/repair, and both Terra rejections. The first repair correctly excludes n=0 from the incidence formula. The final objection concerns an ambiguous shorthand that is best made explicit: the projection to one summand takes place after collapsing the lower skeleton.

The revised proof spells out X^(n-1) -> X^(n-1)/X^(n-2) followed by collapse of every other summand sphere. Equivalently the full complement of the selected open (n-1)-cell is collapsed, exactly as the cited incidence definition requires. Naturality for the characteristic disk sends its oriented relative fundamental class through the connecting map to the oriented boundary-sphere class. The resulting map to the chosen quotient sphere is exactly the integer multiplication defining its incidence number. Thus every coefficient of the three-skeleton differential is the stated degree. This uses the quotient's sphere identification, not a claim that every closed CW cell is an embedded ball.

For n=1 the ordinary relative connecting map is terminal endpoint minus initial endpoint; a loop has zero coefficient at its repeated vertex. No S^0 degree convention is imported from a later page. For n=0 the differential is separately zero and no negative-dimensional incidence number is written. The statement explicitly says integral chains, where the e_alpha really are oriented generators. Since the target is a direct sum, the boundary has finite support even for infinite CW complexes, and equality of component projections proves the sum formula.

This mathematics is familiar: naturality of a pair connecting homomorphism, oriented disk/sphere generators, and direct-sum coordinates are standard cellular homology constructions that I could verify from the given interfaces and chain boundary formula. No external verification was needed or claimed.

Validation: focused precheck and strict batch proof-contract check passed with no failures/errors/warnings. Both contract copies now record the actual proof and specific low-degree, loop and quotient risks. No dependency edit, judge call or pass stamp was made.

Obligations: none for position 6. Next: refresh the cellular definition receipt for the clarified sibling scope, record position 6, then begin position 7.

Context refresh at position 7: the comparison sibling now explicitly states n>=0, consistent with the separately treated d0=0 and positive-degree incidence formula. Its proof expansion leaves this item's statement and all three dependency bytes unchanged.
