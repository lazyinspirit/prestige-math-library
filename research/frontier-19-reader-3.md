# Frontier-19 Reader-3 Report

## Confirmed defects

Fatal findings, repaired in place:

1. Subject: `thm-additive-hilberts-theorem-90`
   Location: `items/thm-additive-hilberts-theorem-90.md`, Proof step `2.1`
   Defect: false computation
   Evidence: the original computation applied `\sigma` to `\alpha` without applying `\sigma` to the coefficients `s_i`, so the displayed equality for `\sigma(\alpha)` was false.
   Severity: fatal
   Repair: rewrote step `2.1` so `\sigma` acts on the coefficients and the telescoping argument now yields `\alpha-\sigma(\alpha)=b` correctly.

2. Subject: `thm-artin-schreier-characterization-of-cyclic-degree-p-extensions`
   Location: `items/thm-artin-schreier-characterization-of-cyclic-degree-p-extensions.md`, Proof step `2.1`
   Defect: unlicensed inference
   Evidence: the original forward implication treated “no root in `F`” as enough for irreducibility of `x^p-x-a`; that does not follow from degree primality alone.
   Severity: fatal
   Repair: replaced that step with the prime-degree argument `F \subseteq F(\alpha) \subseteq K`, so `[K:F]=p` forces `F(\alpha)=K`, and the degree-`p` polynomial with root `\alpha` is the minimal polynomial.

3. Subject: `thm-kummer-pairing-is-perfect`
   Location: `items/thm-kummer-pairing-is-perfect.md`, Proof step `2.3`
   Defect: unlicensed inference
   Evidence: the original right-kernel argument claimed that the order of `\bar b` forces a smaller-power obstruction for `\gamma=\beta^{n/d}`; that implication is not justified in `F^\times/(F^\times)^n`.
   Severity: fatal
   Repair: replaced the step with the direct fixed-field argument: a nontrivial class gives `\beta\notin F`, so some Galois automorphism moves `\beta`, making the pairing nontrivial.

4. Subject: `thm-kummer-correspondence`
   Location: `items/thm-kummer-correspondence.md`, Proof steps `1.3`, `1.4`, `2.2`
   Defect: citation-inaccurate and unlicensed inference
   Evidence: the original `B(K)` finiteness step cited the perfect-pairing theorem outside its hypotheses, the recovery step used the unsupported phrase “generated by its cyclic quotients”, and the inverse-on-subgroups step did not justify `B(K)=B`.
   Severity: fatal
   Repair: rewrote the proof to use a direct `B(K)/(F^\times)^n -> Hom(G,\mu_n)` injection, a maximal-subgroup / cyclic-subextension argument for `K=F(B(K)^{1/n})`, and a character-comparison argument for `B(K)=B`.

5. Subject: `lem-normal-closure-of-a-radical-extension-is-radical`
   Location: `items/lem-normal-closure-of-a-radical-extension-is-radical.md`, Proof steps `1.2` to `3.1`
   Defect: ill-formed and unlicensed inference
   Evidence: the original proof used `N`, `a`, and `\alpha` before introducing them, and then concluded that a normal subfield of a radical extension is radical without justification.
   Severity: fatal
   Repair: rewrote the induction so the data are introduced before use, constructed the normal closure explicitly as `N(\alpha_1,\dots,\alpha_s,\mu_m)`, and built a radical tower for that field directly.

6. Subject: `thm-solvable-by-radicals-implies-a-solvable-galois-group`
   Location: `items/thm-solvable-by-radicals-implies-a-solvable-galois-group.md`, Proof step `3.1`
   Defect: citation-inaccurate
   Evidence: the original step cited the cyclic Kummer theorem for radical adjunctions without checking its irreducibility / exact-degree hypotheses.
   Severity: fatal
   Repair: replaced the citation with a direct argument that, after adjoining the relevant roots of unity, each step is a splitting field whose automorphism group embeds in a cyclic roots-of-unity group.

7. Subject: `cor-abel-ruffini-for-the-general-polynomial`
   Location: `items/cor-abel-ruffini-for-the-general-polynomial.md`, title and Statement
   Defect: overstrong title-or-statement
   Evidence: the corollary invoked `thm-solvable-by-radicals-implies-a-solvable-galois-group`, which is stated only in characteristic `0`, but the corollary itself omitted that hypothesis.
   Severity: fatal
   Repair: added the characteristic-`0` hypothesis to the title, Statement, and Given block.

8. Subject: `thm-a-transitive-subgroup-of-s-p-containing-a-transposition-is-s-p`
   Location: `items/thm-a-transitive-subgroup-of-s-p-containing-a-transposition-is-s-p.md`, Statement and Proof steps `1.1` to `3.1`
   Defect: ill-formed and false intermediate claim
   Evidence: the original proof mixed the repo’s `S_p = Sym({0,\dots,p-1})` convention with `\{1,\dots,p\}` notation, wrote the non-element `(1\,2\,\dots\,p)`, and then identified all `d`-step transpositions with adjacent transpositions even though one wraparound edge is extra.
   Severity: fatal
   Repair: converted the theorem to the `\{0,\dots,p-1\}` convention and restricted the generation step to the `p-1` consecutive transpositions in the induced linear ordering.

## Edits applied

- Edited `items/thm-additive-hilberts-theorem-90.md` to fix the additive Hilbert 90 computation.
- Edited `items/thm-artin-schreier-characterization-of-cyclic-degree-p-extensions.md` to justify irreducibility in the forward implication.
- Edited `items/thm-kummer-pairing-is-perfect.md` to replace the invalid right-kernel argument.
- Edited `items/thm-kummer-correspondence.md` to remove the circular `B(K)` argument and prove both inverse directions cleanly.
- Edited `items/lem-normal-closure-of-a-radical-extension-is-radical.md` to replace the ill-formed induction with an explicit conjugate-root construction.
- Edited `items/thm-solvable-by-radicals-implies-a-solvable-galois-group.md` to replace the inaccurate Kummer citation with a direct cyclic-Galois argument.
- Edited `items/cor-abel-ruffini-for-the-general-polynomial.md` to add the missing characteristic-`0` hypothesis.
- Edited `items/thm-a-transitive-subgroup-of-s-p-containing-a-transposition-is-s-p.md` to restore the repo’s `S_p` indexing convention and fix the generating-set argument.
- Edited `research/frontier-19-batch-3.proof-contracts.json` to regenerate the changed citation / derivation entries and to anchor the boundary evidence for the repaired items.

## Coverage

- Assigned page files opened: `2`
- Unique item files opened: `111`
  - Assigned batch items: `42`
  - Dependency items opened for verification: `69`
- Blocker: none

Page verdicts:

- `library/abstract-algebra/solvability-by-radicals-and-kummer-theory.md`: pass after repaired in-flight item defects; no A-page prose edit needed.
- `library/abstract-algebra/solvability-by-radicals-and-kummer-theory-examples.md`: pass unchanged; no B-page prose defect found.

Verification run:

- `tools/reflow.mts` run on all edited items.
- `tools/precheck.mts` run on all edited items; all passed after adopting the required canonical numbering in the repaired proofs.
- `tools/proof-contract.mjs research/frontier-19-batch-3.proof-contracts.json --items ... --strict` now passes for all eight repaired items.
