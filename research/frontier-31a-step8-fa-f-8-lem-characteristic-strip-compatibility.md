# Final adjudication — queue position 8

Disposition: accepted-after-review. Source status: familiar.

Independently read the current statement and two-step proof, both complete direct dependencies (the Cauchy-problem definition and total-derivative chain rule), batch-16 A/B context, manifest and coverage/convention notes, Reader-16, this item's proof/boundary contract, Alpha adjudication and both Terra rejections. No independent high-risk entry appears in this contract.

The final rejection says the statement assumes only a C1 function attaining the trace. The actual statement says a C1 solution, and the Given repeats a C1 solution attaining the data. In this page's fixed context, the directly supplied definition explicitly defines a classical local solution by BOTH the PDE and local initial trace. The word solution therefore supplies precisely a(x,u(x)) dot Du(x)=b(x,u(x)); it is not synonymous with arbitrary function. The rejection drops a material word from the hypothesis. No repair is needed.

Independently: on the local parameter neighbourhood where gamma lands in the solution domain, the total chain rule gives D(u composed with gamma)=Du(gamma)Dgamma=Dphi. Evaluation of the defining PDE there and substitution u(gamma)=phi gives the second identity. p0 is a row covector of length n and Dgamma is n by n-1, so the first product is correctly typed. When n=1 the tangential identity is an equality of empty covectors; the PDE identity remains. Neither calculation asserts the full-rank noncharacteristic condition; that remains separate as the text says.

Alpha's replacement of the scalar-only chain-rule citation is correct and sufficient. These are familiar elementary differentiation and evaluation arguments, so no external verification was needed. The existing routine/boundary contract is consistent with the two steps. Focused precheck, strict proof-contract check, and rendercheck pass. No item or dependency edit, no prerequisite licence, and no judge stamp. No open obligation. Next: record acceptance, then position 9.
