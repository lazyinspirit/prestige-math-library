# Restricted weak interpolation: complete conditional proof

The existing interpolation Statement is unchanged. Its proof now starts from characteristic-function restricted weak estimates at1<r<p<s and proves the uniform strong-Lp estimate for every finite model. The Hunt lemma was removed as an unused dependency: the statement is conditional and its proof does not need the separate endpoint estimates to have already been established.

Fully read the layer-cake theorem, weak/strong-type definition, packet definition, complex Holder/Minkowski theorem and dominated-convergence theorem used directly. The proof defines the testing functional N_q(h)=sup_B |B|^(−1+1/q) integral_B|h| and proves both comparisons with weak Lq for q>1. A finite level decomposition extends characteristic estimates to arbitrary bounded complex simple pieces. Splitting dyadic input amplitudes at each output threshold produces two discrete convolution sums with geometric weights2^(−(p−r)j/r) and2^(−(s−p)j/s). Finite Holder and nonnegative summation bound both, and layer cake gives strong Lp. Explicit simple approximants and Holder identify the extension with the actual finite packet formula for every Lp input.

No general Marcinkiewicz theorem, arbitrary-measure Tonelli exchange, or endpoint weak-to-strong inference is used. Existing packet, Holder and dominated-convergence suppliers were added; manifest and exact strict contract were synchronized. No new items, statement changes, source attempts, source waivers or owner receipts. Hunt remains an independent open analytic obligation.

Checks: explicit-path canonical precheck, strict selected-item contract, renderer and batch5 content-policy27/27 pass. This is local proof authorship and mechanical validation, not independent mathematical review.
