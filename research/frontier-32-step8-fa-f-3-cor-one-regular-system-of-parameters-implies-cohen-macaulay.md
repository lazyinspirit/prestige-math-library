# FA terminal evidence — item 3

Disposition: repaired. Source status: familiar.

Read current statement/proof, the full three dependency bodies, depth definition, depth A/B pages, batch-9 manifest, aggregate contract and boundary/risk entries, group-f module-parameter warning, initial rejection, Alpha adjudication, and Terra rejudge. The library excludes zero modules from local Cohen--Macaulayness and requires a nonzero terminal quotient for regular sequences.

The module-parameter dependency's proof 1.4 expressly introduces the term for d-tuples whose images form parameters in R/Ann(M); it is not intended as a one-way theorem about some separately defined larger class of tuples. Nevertheless the rejudge correctly exposes avoidable ambiguity in the corollary's reliance on that wording. I made the exact module-parameter convention explicit in the statement and Given, without editing the dependency. This preserves exactly the intended claim.

A regular tuple of length d gives depth at least d by its supremum definition. The cited general inequality gives depth at most support dimension d. Equality is precisely the cited local Cohen--Macaulay definition. For d=0 the empty tuple has nonzero terminal quotient M, and the inequalities still give depth zero. These are familiar definitions and an immediate squeeze argument, independently checked on disk; external verification was unnecessary.

Aligned the stale batch-manifest edge and both proof-contract copies, replacing inaccurate empty/zero boundary exclusions with explicit checks. No dependency was edited and no prerequisite licence is needed. Focused precheck passed; strict batch-9 contract check passed, 67/67, no errors or warnings. No pass stamp was created. No unresolved obligation remains. Next: record, then item 4.
