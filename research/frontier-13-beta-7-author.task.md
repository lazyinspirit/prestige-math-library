## Batch 7 — step 5 authoring

Splice receipt: `research/frontier-13-splice-7.json`. **Read it first** — its
`summary_corrections` map is binding, and the lead Alpha has already decided each
A page summary.

| page | kind | order | items |
|---|---|---|---|
| `linear-recurrences-and-rational-generating-functions` | A | 195 | 32 |
| `linear-recurrences-and-rational-generating-functions-examples` | B | 196 | 15 |

**47 items to author.**

### Specific to this batch

`cor-eigenvalue-form-of-the-transfer-matrix-trace` now takes the split factorisation as its hypothesis and `ex-closed-walks-in-the-complete-graph` is over $\\mathbb Q$ — Alpha checked the basis argument and notes it works because $p\\neq 0$ in $\\mathbb Q$ and would fail over $\\mathbf F_p$. Two carry-forwards: `ex-closed-walks-in-the-complete-graph` **declares one dependency and uses three** (it was re-based from ℂ to ℚ and left its walk-counting obligation behind, while every other walk example on that B page declares the machinery), and the Stanley locator is still narrower than its enumeration.
