# Deferred Step-0 drift finding: the Riemann zeta function

The original Alpha judgment was:

> VERDICT: drift-rescoped — build schwartz-space-and-the-plancherel-theorem
> (order 288.091), tempered-distributions-and-the-fourier-transform
> (order 288.095) instead

The controller failed to materialize that decision before scaffolding because
the Step-0 gate did not directly verify that rescope targets entered the scope
ledger. A later frontier recomputation established that both named targets are
themselves blocked behind the unpublished functional-analysis chain, so they
cannot replace Zeta in this run. Zeta and its companion were removed from the
active `frontier-25` manifest, their complete scaffold was preserved, and the
two mandatory prerequisites were added to Zeta's canonical `requires` list.
