# Hunt major subset and distribution: local proof

The existing Hunt lemma now states its full quantifiers: E,F of finite positive measure; one major subset F' depending only on E,F; all bounded inputs |f|<=1_E and |g|<=1_F'; all finite tile families and measurable selectors. It proves the original min(e,h)(1+|log(e/h)|) bound and explicitly derives restricted weak(q,q) for every1<q<infinity. Root was notified before authoring and refreshed the scope after reading the clarified statement.

Read all of Lacey section7.1–7.2, including7.11–7.15, and all direct local supplier bodies. The source's randomization and subtree bootstrap are supplied explicitly. Since psi is even/nonnegative, its exact plateau/support give a strictly positive lower bound for phi on[-1/2,1/2]. Finite signs have second moment B and fourth moment at most3B², giving probability at least1/12 above sqrt(B/2). The signed-tree weighted bound consequently controls the dyadic square function. Singleton bounds control its increments; first-crossing intervals and bounded overshoot yield a geometric distribution recursion over every subtree. This proves the required size bound proportional to the local maximal threshold, not its square root.

For h>e the exceptional set is {M1_E>20e/h}, with measure<=h/4. Outside spatial tiles have size<=Ce/h. For inside tiles, depth-k tail decay is2^(-18k); maximal dyadic containers give total interval length<=C(k+1)|Omega| at that depth. The resulting sum is O(e). Capped joint stopping with size targets min(B,max(B,sqrt(e/h))*2^-n) and density targets(2/19)*4^-n gives top count O(h4^n). Summing the resulting min terms produces the logarithm in both ratio cases. Finite coefficient minima prove actual stopping. The final level-set test uses the phase of the output and the major subset to derive the restricted weak bound.

The full proof uses the already authored density/size/tree/signed-tree suppliers and the elementary maximal, layer-cake, pairing and monotone-convergence interfaces. It does not use the forest lemma as an unexplained decomposition interface; the modified stopping is proved locally. Manifest and exact contracts synchronized. No new item IDs, source attempts or waivers, shared plan edits, or owner receipts. Continuous model reconstruction and torus transfer remain separate open obligations.

Checks: explicit-path canonical precheck, strict selected-item contract, renderer and batch5 content-policy27/27 pass. These checks do not constitute independent mathematical review.
